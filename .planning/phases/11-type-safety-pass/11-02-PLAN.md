---
phase: 11-type-safety-pass
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - mix.exs
  - .dialyzer_ignore.exs
  - .github/workflows/ci.yml
  - lib/paddle/client.ex
  - lib/paddle/error.ex
  - lib/paddle/page.ex
  - lib/paddle/customers.ex
  - lib/paddle/customers/addresses.ex
  - lib/paddle/transactions.ex
  - lib/paddle/subscriptions.ex
  - lib/paddle/webhooks.ex
  - lib/paddle/http.ex
  - lib/paddle/http/telemetry.ex
  - lib/paddle/internal/attrs.ex
  - lib/paddle/internal/pagination.ex
  - lib/paddle/application.ex
  - lib/mix/tasks/typecheck.specs.ex
autonomous: false
requirements: [TYPES-01, TYPES-02]
must_haves:
  truths:
    - "`mix dialyzer` exits 0 with `.dialyzer_ignore.exs` containing `[]`, so the Phase 11 baseline is genuinely clean per TYPES-02, D-11, D-12, and D-17."
    - "Dialyxir uses stable PLT paths under `priv/plts` and CI caches them with OS + OTP + Elixir + `mix.lock`, keeping the static-analysis job isolated from normal compile/test cache churn per D-13, D-15, and D-16."
    - "CI has a dedicated required static-analysis job that runs `mix typecheck.specs` and `mix dialyzer` separately from `mix test`, so spec drift and Dialyzer drift fail pre-merge with clear attribution per D-14 and D-17."
    - "Any extra type refinements made after the first `mix dialyzer` run stay additive and internal-focused; they do not widen the public seam or suppress real warnings per D-09, D-12, and D-23."
  artifacts:
    - path: "mix.exs"
      provides: "Dialyxir dependency and dialyzer project configuration pointing at `priv/plts` and `.dialyzer_ignore.exs`."
      contains: "dialyzer:"
    - path: ".dialyzer_ignore.exs"
      provides: "Empty baseline warning ignore file."
      contains: "[]"
    - path: ".github/workflows/ci.yml"
      provides: "Dedicated `dialyzer` CI job with PLT restore/build/save and required gates."
      contains: "mix dialyzer"
    - path: "lib/mix/tasks/typecheck.specs.ex"
      provides: "CI-invoked public-spec gate from Plan 11-01."
      contains: "typecheck.specs"
  key_links:
    - from: "mix.exs"
      to: ".github/workflows/ci.yml"
      via: "CI job installs deps and runs the exact Dialyxir and spec-check commands configured in the project."
      pattern: "mix dialyzer"
    - from: ".github/workflows/ci.yml"
      to: "priv/plts"
      via: "Dedicated restore/build/save steps use the configured PLT path instead of `_build`."
      pattern: "priv/plts"
    - from: "mix dialyzer"
      to: "lib/paddle/**/*.ex and sealed internals"
      via: "First clean baseline may require targeted spec refinements in whichever modules Dialyzer reports, but no ignore suppressions."
      pattern: "@spec"
---

<objective>
Wire the Phase 11 static-analysis baseline end-to-end: Dialyxir dependency/config, empty ignore file, stable PLT cache path, and a dedicated CI gate that runs both `mix typecheck.specs` and `mix dialyzer`.

Purpose: Deliver TYPES-02 and the enforcement half of the phase so type drift fails in CI on the current Elixir `1.19.5` / OTP `28.1` baseline, not in production or milestone-close cleanup.

Output: `mix.exs` Dialyxir config, `.dialyzer_ignore.exs`, CI workflow updates, and any additive type refinements needed to make the first clean Dialyzer run pass without suppressions.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-type-safety-pass/11-CONTEXT.md
@.planning/phases/11-type-safety-pass/11-RESEARCH.md
@.planning/phases/11-type-safety-pass/11-PATTERNS.md
@.planning/phases/11-type-safety-pass/11-01-PLAN.md
@.tool-versions
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify the approved Dialyxir package identity before installation</name>
  <files>mix.exs, .planning/phases/11-type-safety-pass/11-RESEARCH.md</files>
  <read_first>
    - .planning/phases/11-type-safety-pass/11-RESEARCH.md
    - mix.exs
    - .planning/phases/11-type-safety-pass/11-CONTEXT.md
  </read_first>
  <action>
    Because `dialyxir` is marked `[ASSUMED]` in the Phase 11 package legitimacy audit, perform the required package-verification checkpoint before adding the dependency. Confirm the package identity against Hex and the official docs named in research: package name `dialyxir`, Hex package metadata, and the official docs/source lineage referenced in `11-RESEARCH.md`. This checkpoint implements D-11 while satisfying the package-legitimacy gate for an Elixir package without slopcheck coverage.
  </action>
  <how-to-verify>
    1. Open the package metadata already cited in `11-RESEARCH.md` (`mix hex.info dialyxir` output / official docs references).
    2. Confirm the dependency being added is exactly `dialyxir` and that its docs/source match the cited ecosystem package.
    3. Resume only after the package identity is accepted.
  </how-to-verify>
  <resume-signal>Type "approved" after confirming the package identity, or describe the concern.</resume-signal>
  <verify>
    <automated>MISSING — this is the required human legitimacy checkpoint before package installation</automated>
  </verify>
  <acceptance_criteria>
    - The user explicitly approves installing `dialyxir` after reviewing the package identity.
    - No dependency is added before this checkpoint is approved.
  </acceptance_criteria>
  <done>
    The required package-legitimacy gate for `dialyxir` is satisfied and implementation may proceed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire Dialyxir, empty baseline ignore, and a clean local dialyzer run</name>
  <files>mix.exs, .dialyzer_ignore.exs, lib/paddle/client.ex, lib/paddle/error.ex, lib/paddle/page.ex, lib/paddle/customers.ex, lib/paddle/customers/addresses.ex, lib/paddle/transactions.ex, lib/paddle/subscriptions.ex, lib/paddle/webhooks.ex, lib/paddle/http.ex, lib/paddle/http/telemetry.ex, lib/paddle/internal/attrs.ex, lib/paddle/internal/pagination.ex, lib/paddle/application.ex, lib/mix/tasks/typecheck.specs.ex</files>
  <read_first>
    - mix.exs
    - lib/mix/tasks/typecheck.specs.ex
    - lib/paddle/client.ex
    - lib/paddle/error.ex
    - lib/paddle/page.ex
    - lib/paddle/customers.ex
    - lib/paddle/customers/addresses.ex
    - lib/paddle/transactions.ex
    - lib/paddle/subscriptions.ex
    - lib/paddle/webhooks.ex
    - lib/paddle/http.ex
    - lib/paddle/http/telemetry.ex
    - lib/paddle/internal/attrs.ex
    - lib/paddle/internal/pagination.ex
    - lib/paddle/application.ex
    - .planning/phases/11-type-safety-pass/11-CONTEXT.md
    - .planning/phases/11-type-safety-pass/11-RESEARCH.md
    - .planning/phases/11-type-safety-pass/11-PATTERNS.md
    - .tool-versions
  </read_first>
  <action>
    After the package-legitimacy checkpoint passes, add `{:dialyxir, "~> 1.4.7", only: [:dev, :test], runtime: false}` to `mix.exs` per D-11 and the researched version pin. Add `dialyzer:` project config that places the project/core PLTs under `priv/plts`, points `ignore_warnings` at `.dialyzer_ignore.exs`, and keeps the config stable for CI caching per D-12 and D-13. Create `.dialyzer_ignore.exs` with exactly `[]`.

    Run the first real `mix dialyzer` cycle against the Phase 11 typed surface from Plan 11-01. If Dialyzer reports missing or imprecise specs in public modules or sealed/internal helpers, patch the smallest concrete set of reported modules in this task using additive `@spec` / `@typep` refinements only. Favor sealed/internal modules (`Paddle.Http`, `Paddle.Http.Telemetry`, `Paddle.Internal.Attrs`, `Paddle.Internal.Pagination`, `Paddle.Application`) for cleanup when the warnings originate there, per D-09 and D-20. Do not add suppressions, `@dialyzer` escapes, `--ignore-exit-status`, or non-empty ignore entries.

    Keep the public seam unchanged. This task may refine types in Plan 11-01 files if Dialyzer proves a spec is too loose or contradictory, but it must not introduce new APIs, new option keys, or behavior changes.
  </action>
  <verify>
    <automated>mix deps.get</automated>
    <automated>mix typecheck.specs</automated>
    <automated>mix dialyzer</automated>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F '{:dialyxir, "~> 1.4.7", only: [:dev, :test], runtime: false}' mix.exs` returns a hit.
    - `grep -F 'plt_file' mix.exs` returns a hit that points at `priv/plts`.
    - `grep -F 'ignore_warnings: ".dialyzer_ignore.exs"' mix.exs` returns a hit.
    - `test -f .dialyzer_ignore.exs` succeeds and `tr -d '[:space:]' < .dialyzer_ignore.exs` outputs `[]`.
    - `mix typecheck.specs` exits 0.
    - `mix dialyzer` exits 0.
    - `mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>
    Dialyxir is wired with a clean, unsuppressed local baseline and stable PLT paths under `priv/plts`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add the dedicated CI dialyzer and spec gate</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml
    - mix.exs
    - .tool-versions
    - lib/mix/tasks/typecheck.specs.ex
    - .planning/phases/11-type-safety-pass/11-CONTEXT.md
    - .planning/phases/11-type-safety-pass/11-RESEARCH.md
    - .planning/phases/11-type-safety-pass/11-PATTERNS.md
  </read_first>
  <action>
    Extend `.github/workflows/ci.yml` with a dedicated `dialyzer` job per D-14 through D-17. Keep the existing pinned-action style, `erlef/setup-beam` usage, and `.tool-versions` baseline. Add restore/build/save steps for the `priv/plts` cache using a key that includes `runner.os`, the resolved OTP version, the resolved Elixir version, and `hashFiles('**/mix.lock')` per D-15. Run `mix deps.get`, `mix typecheck.specs`, and `mix dialyzer` in this job. Keep the normal `mix test` job separate; do not fold Dialyzer into it.

    The CI job must be blocking and must not use advisory flags. If the workflow needs a PLT build step before `mix dialyzer`, make it explicit and cache-aware. Preserve the repo’s current compile/test bar; this task adds a required static-analysis lane rather than replacing any existing verification.
  </action>
  <verify>
    <automated>rg -n 'dialyzer|typecheck\\.specs|priv/plts|actions/cache/(restore|save)|setup-beam' .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'dialyzer:' .github/workflows/ci.yml` returns a hit for a dedicated job name or identifier.
    - `grep -F 'mix typecheck.specs' .github/workflows/ci.yml` returns a hit.
    - `grep -F 'mix dialyzer' .github/workflows/ci.yml` returns a hit.
    - `grep -F 'priv/plts' .github/workflows/ci.yml` returns a hit.
    - `grep -F "hashFiles('**/mix.lock')" .github/workflows/ci.yml` returns a hit in the PLT cache key.
    - `rg -n -- '--ignore-exit-status' .github/workflows/ci.yml` returns no matches.
  </acceptance_criteria>
  <done>
    CI now blocks merges on both public-spec coverage drift and Dialyzer drift using a dedicated PLT-cached static-analysis job.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Third-party package metadata -> project dependency graph | Adding Dialyxir changes the dev/test toolchain and must pass the package-legitimacy gate first. |
| Local static-analysis config -> CI enforcement | PLT paths, ignore files, and CI job wiring decide whether type drift is actually blocked before merge. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | Tampering | Package install (`dialyxir`) | mitigate | Enforce the blocking human package-legitimacy checkpoint before adding the dependency because the research audit marks it `[ASSUMED]` for Hex-only verification. |
| T-11-05 | Tampering | Warning suppression baseline | mitigate | Commit `.dialyzer_ignore.exs` as exactly `[]` and forbid non-empty suppressions or advisory flags per D-12 and D-17. |
| T-11-06 | Denial of Service | CI static-analysis runtime cost | mitigate | Store PLTs under `priv/plts` and cache them with OS + OTP + Elixir + `mix.lock` so the dedicated job stays predictable per D-13 and D-15. |
| T-11-07 | Repudiation | CI attribution of type failures | mitigate | Run `mix typecheck.specs` and `mix dialyzer` in a dedicated `dialyzer` job separate from `mix test` per D-14, making drift failures explicit and reviewable. |
| T-11-SC | Tampering | npm/pip/cargo installs | mitigate | No npm, pip, or cargo installs are involved; the only new package is the human-verified Hex dependency `dialyxir`. |
</threat_model>

<verification>
- `mix deps.get`
- `mix typecheck.specs`
- `mix dialyzer`
- `mix compile --warnings-as-errors`
- `mix format --check-formatted`
- `rg -n 'dialyzer|typecheck\\.specs|priv/plts' .github/workflows/ci.yml`
</verification>

<success_criteria>
- TYPES-02 is complete with a clean empty-ignore Dialyzer baseline.
- Dialyzer PLTs live under `priv/plts` and are CI-cacheable by OS/runtime/mix.lock.
- CI has a dedicated blocking job that runs `mix typecheck.specs` and `mix dialyzer`.
</success_criteria>

<output>
Create `.planning/phases/11-type-safety-pass/11-02-SUMMARY.md` when done
</output>
