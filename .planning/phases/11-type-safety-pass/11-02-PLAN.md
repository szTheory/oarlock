---
phase: 11-type-safety-pass
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - lib/paddle/customers.ex
  - lib/paddle/customers/addresses.ex
  - lib/paddle/transactions.ex
  - lib/paddle/subscriptions.ex
  - lib/paddle/webhooks.ex
  - test/paddle/customers_test.exs
  - test/paddle/customers/addresses_test.exs
  - test/paddle/transactions_test.exs
  - test/paddle/subscriptions_test.exs
  - test/paddle/webhooks_test.exs
autonomous: true
requirements: [TYPES-01]
must_haves:
  truths:
    - "Every public resource function in the Phase 11 seam carries an explicit `@spec`, per TYPES-01 and D-18."
    - "Option vocabularies remain locked: create/start flows accept only `idempotency_key` and `retry`, while pause/resume accept lifecycle opts plus `retry` only, per D-05."
    - "Lazy pagination contracts stay `Enumerable.t()` and eager calls keep tagged results with known local validation atoms, per D-06 and D-08."
  artifacts:
    - path: "lib/paddle/transactions.ex"
      provides: "Typed transaction create/get contracts and request opts."
      contains: "@type request_opt"
    - path: "lib/paddle/subscriptions.ex"
      provides: "Typed lifecycle, pagination, and validation contracts."
      contains: "@type pause_opt"
    - path: "lib/paddle/webhooks.ex"
      provides: "Typed webhook verify/parse public seam."
      contains: "@spec"
  key_links:
    - from: "lib/paddle/subscriptions.ex"
      to: "test/paddle/subscriptions_test.exs"
      via: "pause/resume/list/get contracts stay aligned with locked behavior"
      pattern: "invalid_(subscription_id|resume_at|effective_from|on_resume|params)"
    - from: "lib/paddle/transactions.ex"
      to: "test/paddle/transactions_test.exs"
      via: "typed request options preserve the idempotency boundary"
      pattern: "idempotency_key"
---

<objective>
Add explicit public function specs across the resource modules that make up the active SDK seam.

Purpose: Complete the broad public-function spec sweep for TYPES-01 while preserving the locked request-option vocabulary, pagination semantics, webhook behavior, and validation atoms defined in D-03 through D-10, while following the least-surprise mature-library stance from D-22 and preserving D-23.

Output: resource-module `@type` aliases and `@spec` coverage across customers, addresses, transactions, subscriptions, and webhooks.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-type-safety-pass/11-CONTEXT.md
@.planning/phases/11-type-safety-pass/11-RESEARCH.md
@.planning/phases/11-type-safety-pass/11-PATTERNS.md
@.planning/phases/08-reliability-primitives/08-03-SUMMARY.md
@.planning/phases/09-pagination-ergonomics-0-plans/09-01-SUMMARY.md
@.planning/phases/10-subscriptions-surface-completion/10-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Spec customer, address, and transaction resource functions</name>
  <files>lib/paddle/customers.ex, lib/paddle/customers/addresses.ex, lib/paddle/transactions.ex, test/paddle/customers_test.exs, test/paddle/customers/addresses_test.exs, test/paddle/transactions_test.exs</files>
  <read_first>
    - lib/paddle/customers.ex
    - lib/paddle/customers/addresses.ex
    - lib/paddle/transactions.ex
    - test/paddle/customers_test.exs
    - test/paddle/customers/addresses_test.exs
    - test/paddle/transactions_test.exs
    - .planning/phases/11-type-safety-pass/11-CONTEXT.md
  </read_first>
  <action>
    Add local aliases and public specs for every public `def` in `Paddle.Customers`, `Paddle.Customers.Addresses`, and `Paddle.Transactions`. Encode the locked create/start request vocabulary exactly per D-05: only `{:idempotency_key, String.t()}` and `{:retry, boolean()}` belong in request opts. Keep return contracts explicit per D-06, keep provider-owned payload shapes intentionally broad per D-07, keep local validation atoms stable without inventing a global registry per D-10, and prefer the mature-library explicitness described in D-22 instead of over-abstracting the seam.
  </action>
  <verify>
    <automated>mix test test/paddle/customers_test.exs test/paddle/customers/addresses_test.exs test/paddle/transactions_test.exs --color</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F '@type request_opt' lib/paddle/transactions.ex` returns a hit.
    - `rg -n '^\\s*@spec ' lib/paddle/customers.ex lib/paddle/customers/addresses.ex lib/paddle/transactions.ex` returns coverage for every public `def`.
    - `mix test test/paddle/customers_test.exs test/paddle/customers/addresses_test.exs test/paddle/transactions_test.exs --color` exits 0.
  </acceptance_criteria>
  <done>
    Customer, address, and transaction resource functions are fully specced with the locked request-option boundary intact.
  </done>
</task>

<task type="auto">
  <name>Task 2: Spec subscription and webhook resource functions</name>
  <files>lib/paddle/subscriptions.ex, lib/paddle/webhooks.ex, test/paddle/subscriptions_test.exs, test/paddle/webhooks_test.exs</files>
  <read_first>
    - lib/paddle/subscriptions.ex
    - lib/paddle/webhooks.ex
    - test/paddle/subscriptions_test.exs
    - test/paddle/webhooks_test.exs
    - .planning/phases/11-type-safety-pass/11-CONTEXT.md
    - .planning/phases/10-subscriptions-surface-completion/10-03-SUMMARY.md
  </read_first>
  <action>
    Add local aliases and public specs for every public `def` in `Paddle.Subscriptions` and `Paddle.Webhooks`. Implement D-05 and D-08 precisely: `pause_opt()` and `resume_opt()` include lifecycle opts plus `{:retry, boolean()}` only, never `idempotency_key`, and streaming helpers must return `Enumerable.t()`. Keep explicit local validation atoms in result specs per D-06 and D-10, preserve the mature-library explicitness from D-22, and preserve current webhook success/error tuples without runtime changes.
  </action>
  <verify>
    <automated>mix test test/paddle/subscriptions_test.exs test/paddle/webhooks_test.exs --color</automated>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F '@type pause_opt' lib/paddle/subscriptions.ex` returns a hit.
    - `grep -F '@type resume_opt' lib/paddle/subscriptions.ex` returns a hit.
    - `rg -n '@type (pause_opt|resume_opt).*(idempotency_key)' lib/paddle/subscriptions.ex` returns no matches.
    - `grep -F 'Enumerable.t()' lib/paddle/subscriptions.ex` returns hits for the stream helpers.
    - `mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>
    Subscription and webhook public contracts are fully specced and still reflect the locked lifecycle and streaming behavior.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public resource modules -> SDK consumers | These specs define the supported request vocabulary and observable result contracts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Tampering | Request option contracts | mitigate | Encode `request_opt`, `pause_opt`, and `resume_opt` exactly per D-05 so unsupported option keys cannot be normalized as supported API surface. |
| T-11-04 | Repudiation | Tagged result contracts | mitigate | Spell out `%Paddle.Error{}` and local validation atoms in public specs per D-06 so consumer expectations remain review-visible. |
| T-11-05 | Information Disclosure | Provider-owned payload typing | mitigate | Keep external payload maps broad per D-07 rather than claiming false schema certainty in the public seam. |
| T-11-SC | Tampering | Dependency/install surface | accept | No third-party dependency changes occur in this plan. |
</threat_model>

<verification>
- `mix test test/paddle/customers_test.exs test/paddle/customers/addresses_test.exs test/paddle/transactions_test.exs test/paddle/subscriptions_test.exs test/paddle/webhooks_test.exs --color`
- `mix compile --warnings-as-errors`
</verification>

<success_criteria>
- Every active public resource function under `lib/paddle/` has an explicit spec.
- Locked option vocabularies and validation atoms are reflected honestly in the types.
- Stream helpers remain typed as `Enumerable.t()` and eager helpers keep tagged result contracts.
</success_criteria>

<output>
Create `.planning/phases/11-type-safety-pass/11-02-SUMMARY.md` when done
</output>
