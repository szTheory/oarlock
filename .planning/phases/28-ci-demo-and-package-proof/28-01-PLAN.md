---
phase: 28-ci-demo-and-package-proof
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/paddle/mock_server.ex
  - test/paddle/mock_server_test.exs
  - README.md
  - demo/README.md
autonomous: true
requirements:
  - PROOF-04
must_haves:
  truths:
    - "Per D-13, `Paddle.MockServer` works when optional `plug` and `bandit` dependencies are available."
    - "Per D-13 and D-14, core SDK consumers can compile without optional `plug` and `bandit` dependencies."
    - "Per D-17, public docs state that `Paddle.MockServer` is an optional development/test fixture requiring `plug` and `bandit`, while core SDK modules do not require Phoenix, Ecto, Plug, or Bandit."
  artifacts:
    - path: "lib/paddle/mock_server.ex"
      provides: "Compile-safe optional dependency boundary for Paddle.MockServer"
      contains: "Paddle.MockServer"
    - path: "test/paddle/mock_server_test.exs"
      provides: "Positive MockServer proof with plug and bandit available"
      contains: "Paddle.MockServer.start_link"
    - path: "README.md"
      provides: "Root optional dependency boundary documentation"
      contains: "plug"
    - path: "demo/README.md"
      provides: "Demo MockServer optional dependency documentation"
      contains: "bandit"
  key_links:
    - from: "lib/paddle/mock_server.ex"
      to: "mix.exs"
      via: "Paddle.MockServer compiles safely when optional dependencies are not present downstream"
      pattern: "optional"
    - from: "test/paddle/mock_server_test.exs"
      to: "lib/paddle/mock_server.ex"
      via: "positive MockServer start_link/request-response test"
      pattern: "MockServer\\.start_link"
---

<objective>
Fix the compile-time optional dependency boundary around `Paddle.MockServer` and document it plainly.

Purpose: PROOF-04 currently has a real adopter failure mode because `lib/paddle/mock_server.ex` expands `Plug.Router` at compile time even for consumers that do not depend on `plug` or `bandit`. This plan makes the core package compile without those optional dependencies while preserving the MockServer fixture when they are present, per D-13 through D-17.
Output: Compile-safe MockServer source, positive MockServer test coverage, and public docs that state the optional dependency boundary.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/28-ci-demo-and-package-proof/28-CONTEXT.md
@.planning/phases/28-ci-demo-and-package-proof/28-RESEARCH.md
@.planning/phases/28-ci-demo-and-package-proof/28-PATTERNS.md
@.planning/phases/28-ci-demo-and-package-proof/28-VALIDATION.md
@mix.exs
@lib/paddle/mock_server.ex
@test/paddle/mock_server_test.exs
@README.md
@demo/README.md
</context>

## Artifacts this phase produces

- `Paddle.MockServer.start_link/1` behavior remains available with `plug` and `bandit` present.
- `Paddle.MockServer.start_link/1` becomes compile-safe for consumers without optional `plug` and `bandit`.
- No new public module namespace is introduced.
- New private helper functions may be added inside `Paddle.MockServer` only if needed to keep the dependency check readable.
- Updated file path: `lib/paddle/mock_server.ex`.
- Updated file path: `test/paddle/mock_server_test.exs`.
- Updated file path: `README.md`.
- Updated file path: `demo/README.md`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make Paddle.MockServer compile-safe without optional deps</name>
  <files>lib/paddle/mock_server.ex, test/paddle/mock_server_test.exs</files>
  <read_first>
    - `.planning/phases/28-ci-demo-and-package-proof/28-CONTEXT.md` decisions D-13 through D-17.
    - `.planning/phases/28-ci-demo-and-package-proof/28-RESEARCH.md` optional-deps pitfall and validation map.
    - `.planning/phases/28-ci-demo-and-package-proof/28-PATTERNS.md` optional dependency boundary and MockServer response shape.
    - `mix.exs` optional `plug` and `bandit` dependency declarations.
    - `lib/paddle/mock_server.ex` current `use Plug.Router` and `Bandit.start_link/1` implementation.
    - `test/paddle/mock_server_test.exs` existing positive MockServer behavior proof.
  </read_first>
  <behavior>
    - Positive case: with root test dependencies available, `Paddle.MockServer.start_link(port: port)` starts and existing MockServer request-response tests pass.
    - Negative compile case: a package consumer that does not include optional `plug` or `bandit` can compile the core SDK without loading `Plug.Router`.
    - Boundary case: code that tries to start MockServer without optional dependencies receives a clear missing-optional-dependency failure instead of a compile failure.
  </behavior>
  <action>Implement the PROOF-04 compile-boundary fix per D-13, D-14, and D-16 in `lib/paddle/mock_server.ex`. Keep `Paddle.MockServer` as the public fixture module and preserve the existing route semantics, response envelopes, fallback response, and `start_link/1` entrypoint when `plug` and `bandit` are available. Arrange the module so `Plug.Router`, `Plug.Parsers`, and `Bandit.start_link/1` are not expanded or compiled in a downstream package consumer that lacks those optional dependencies. Add or adjust focused tests in `test/paddle/mock_server_test.exs` only for the positive MockServer lane that can run in the root project; leave the negative fresh-consumer proof to the package smoke plan so it exercises the real Hex artifact boundary.</action>
  <acceptance_criteria>
    - Source assertion: `lib/paddle/mock_server.ex` no longer unconditionally expands `Plug.Router` for every compile path.
    - Behavior assertion: `MIX_ENV=test mix test test/paddle/mock_server_test.exs` passes and still exercises customer, transaction, portal-session, subscription, and fallback routes.
    - Compile assertion: `mix compile --warnings-as-errors` passes in the root project.
    - Boundary assertion: the implementation has an explicit no-optional-deps behavior for `Paddle.MockServer.start_link/1` rather than crashing at compile time.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/paddle/mock_server_test.exs</automated>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <done>`Paddle.MockServer` remains executable with optional deps present and no longer blocks package consumers that do not install `plug` or `bandit`.</done>
</task>

<task type="auto">
  <name>Task 2: Document the MockServer optional dependency boundary</name>
  <files>README.md, demo/README.md</files>
  <read_first>
    - `.planning/phases/28-ci-demo-and-package-proof/28-CONTEXT.md` decisions D-17, D-18, D-20.
    - `.planning/phases/28-ci-demo-and-package-proof/28-PATTERNS.md` public docs proof boundary wording.
    - `README.md` proof boundary and core SDK boundary sections.
    - `demo/README.md` Offline Paddle Mode section.
    - `prompts/oarlock-brand-book.md` if additional wording guidance is needed.
  </read_first>
  <action>Update public documentation per D-17 without expanding Phase 28 scope. In `README.md`, state that `Paddle.MockServer` is an offline development/test fixture requiring optional `plug` and `bandit`, and that the core SDK, webhook verification, typed resources, and Req-based HTTP client do not require Phoenix, Ecto, Plug, or Bandit. In `demo/README.md`, state that the demo includes the dependencies needed to run MockServer-backed tests. Preserve the existing proof ladder language per D-20 and do not imply live Paddle sandbox/provider-state CI per D-18.</action>
  <acceptance_criteria>
    - Source assertion: `README.md` mentions the optional `plug` and `bandit` boundary for `Paddle.MockServer`.
    - Source assertion: `demo/README.md` explains that demo tests run MockServer with those dependencies available.
    - Prohibition assertion: docs do not claim local MockServer proof is live Paddle provider-state verification.
    - Test assertion: existing public-doc guard tests continue to pass.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/paddle/seam_test.exs</automated>
  </verify>
  <done>Docs tell adopters exactly when `plug` and `bandit` are needed and preserve the honest proof boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Downstream Mix consumer to packaged SDK | A fresh consumer compiles the Hex artifact without optional `plug` and `bandit`. |
| Local test process to MockServer HTTP fixture | Tests send HTTP requests to a local Plug/Bandit fixture with no live Paddle credentials. |
| Public docs to adopter expectations | Documentation tells adopters which dependencies and proof levels are real. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Denial of Service | `lib/paddle/mock_server.ex` | mitigate | Prevent optional `plug`/`bandit` absence from causing downstream compile failure; prove positive MockServer behavior with `mix test test/paddle/mock_server_test.exs`. |
| T-28-02 | Spoofing | `README.md`, `demo/README.md` | mitigate | State that MockServer is a deterministic local fixture and not live Paddle provider-state verification. |
| T-28-03 | Information Disclosure | MockServer/test docs | accept | Phase adds no live credentials and keeps provider-state CI out of scope per D-18. |
| T-28-SC | Tampering | npm/pip/cargo installs | accept | No npm, pip, or cargo packages are installed by this plan; existing Hex dependencies are already declared. |
</threat_model>

<verification>
Run the focused MockServer and docs commands:
- `MIX_ENV=test mix test test/paddle/mock_server_test.exs`
- `mix test test/paddle/seam_test.exs`
- `mix compile --warnings-as-errors`
</verification>

<success_criteria>
PROOF-04 is satisfied at the source boundary: MockServer remains executable with optional deps, core consumers are not forced to compile Plug/Bandit code paths, and docs state the boundary plainly.
</success_criteria>

<output>
Create `.planning/phases/28-ci-demo-and-package-proof/28-01-SUMMARY.md` when done.
</output>
