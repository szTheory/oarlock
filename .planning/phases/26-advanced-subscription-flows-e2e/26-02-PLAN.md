---
phase: "26"
plan: "02"
type: execute
wave: 2
depends_on:
  - 26-01
files_modified:
  - test/paddle/subscription_flows_test.exs
autonomous: true
requirements:
  - ADV-02
must_haves:
  truths:
    - E2E tests exercise update/3 for upgrade and downgrade flows.
  artifacts:
    - path: test/paddle/subscription_flows_test.exs
      provides: integration test cases
  key_links:
    - from: test/paddle/subscription_flows_test.exs
      to: lib/paddle/subscriptions.ex
      via: test asserts
      pattern: Paddle.Subscriptions.update
---

<objective>
Verify complex subscription upgrade and downgrade scenarios via comprehensive E2E tests.

Purpose: Guarantee the SDK accurately deserializes `scheduled_change` and behaves deterministically during subscription mutations.
Output: Complete E2E flow tests for ADV-02.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-advanced-subscription-flows-e2e/26-CONTEXT.md
@.planning/phases/26-advanced-subscription-flows-e2e/26-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add E2E tests for subscription flows</name>
  <files>test/paddle/subscription_flows_test.exs</files>
  <read_first>
    - test/paddle/subscriptions_test.exs
    - .planning/phases/26-advanced-subscription-flows-e2e/26-RESEARCH.md
  </read_first>
  <behavior>
    - `setup_all` toggles cleanly between `Paddle.MockServer` and sandbox based on `PADDLE_API_KEY` and `INTEGRATION_TESTS`.
    - Immediate upgrade via `proration_billing_mode: "prorated_immediately"` succeeds.
    - Scheduled downgrade via `proration_billing_mode: "next_billing_period"` returns a Subscription with a populated `scheduled_change` struct.
  </behavior>
  <action>
    Create `test/paddle/subscription_flows_test.exs`.
    Implement `setup_all` using `Paddle.MockServer.start_link(port: 4448)` as the default test target.
    Add test "immediate upgrade": calls `Paddle.Subscriptions.update(client, id, items: [...], proration_billing_mode: "prorated_immediately")` and asserts `{:ok, %Paddle.Subscription{}}` returned.
    Add test "scheduled downgrade": calls `Paddle.Subscriptions.update(client, id, items: [...], proration_billing_mode: "next_billing_period")` and asserts the returned `subscription.scheduled_change` is parsed as a `%Paddle.Subscription.ScheduledChange{}` struct.
  </action>
  <acceptance_criteria>
    - `test/paddle/subscription_flows_test.exs` exists.
    - E2E tests pass reliably using MockServer.
    - Test asserts correct parsing of `scheduled_change` struct for downgrade.
    - `mix test test/paddle/subscription_flows_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/paddle/subscription_flows_test.exs</automated>
  </verify>
  <done>E2E tests for advanced flows successfully completed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SDK Client -> API | Client sends authenticated request to Paddle API (or Mock Server). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-03 | Information Disclosure | Tests | mitigate | Ensure test setup correctly defaults to MockServer to avoid sandbox API key leakage or unexpected calls in CI. |
| T-26-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies installed in this phase. |
</threat_model>

<verification>
mix test test/paddle/subscription_flows_test.exs
</verification>

<success_criteria>
The complete E2E test suite asserts complex subscription upgrade/downgrade logic properly against Paddle mock state.
</success_criteria>

<output>
Create `.planning/phases/26-advanced-subscription-flows-e2e/26-02-SUMMARY.md` when done
</output>
