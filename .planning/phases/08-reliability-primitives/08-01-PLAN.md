---
phase: 08-reliability-primitives
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/paddle/error.ex
  - test/paddle/error_test.exs
  - guides/accrue-seam.md
  - CHANGELOG.md
  - .planning/BACKLOG.md
autonomous: true
requirements: [REL-03]
tags: [elixir, paddle, error-shape, rename, breaking]

must_haves:
  truths:
    - "%Paddle.Error{} carries field :raw_data (no longer :raw); rename is total — zero remaining :raw references in code, tests, or guide row."
    - "%Paddle.Error{}.network_error? defaults to the boolean false on a bare struct."
    - "%Paddle.Error{}.retryable? defaults to the boolean false on a bare struct."
    - "%Paddle.Error{} returned from Paddle.Error.from_response/1 carries network_error?: false and retryable?: false (HTTP-response errors are not transport errors)."
    - "guides/accrue-seam.md row for the error escape-hatch field reads :raw_data, not :raw."
    - "CHANGELOG.md documents the :raw → :raw_data rename as a breaking change under the v1.2 / Unreleased heading."
    - "The full pre-existing test suite (≥111 tests) continues to pass with zero failures after the rename + new field defaults land."
    - "`.planning/BACKLOG.md` carries a B-04 entry capturing the Accrue-side `error.raw → error.raw_data` migration note per D-04 (downstream consumer guidance, not blocking work for this phase)."
  artifacts:
    - path: "lib/paddle/error.ex"
      provides: "defexception with keyword-default syntax: type/code/message/errors/request_id/status_code/raw_data/network_error?/retryable? — including explicit `false` defaults for the two boolean fields, and from_response/1 writing `raw_data: body`."
      contains: "defexception type: nil, code: nil, message: nil, errors: [], request_id: nil, status_code: nil, raw_data: nil, network_error?: false, retryable?: false"
    - path: "test/paddle/error_test.exs"
      provides: "Updated `from_response/1` describe block asserting `raw_data:` instead of `raw:`, plus a new `struct defaults` describe block locking the boolean defaults to `false`."
      contains: "describe \"struct defaults\""
    - path: "guides/accrue-seam.md"
      provides: "Locked field row for %Paddle.Error{} updated from `:raw` to `:raw_data` so the documented seam matches the code."
      contains: "| `:raw_data` | `locked` |"
    - path: "CHANGELOG.md"
      provides: "Breaking-change entry documenting the rename so consumers (Accrue) discover it on upgrade."
      contains: "renamed to `:raw_data`"
    - path: ".planning/BACKLOG.md"
      provides: "B-04 entry capturing the Accrue-side `error.raw → error.raw_data` migration note per D-04."
      contains: "## B-04 — Accrue migration"
  key_links:
    - from: "lib/paddle/error.ex"
      to: "guides/accrue-seam.md:118"
      via: "documented seam field row must match the defexception field name"
      pattern: ":raw_data"
    - from: "lib/paddle/error.ex"
      to: "lib/paddle/http.ex"
      via: "Http.build_struct/2 puts :raw_data on every locked struct; Error must use the same key for symmetry"
      pattern: "raw_data:"
---

<objective>
Atomic rename `%Paddle.Error{}` field `:raw → :raw_data`, plus add `network_error?: false` and `retryable?: false` to the same `defexception` declaration. Code, doc seam row, and CHANGELOG move together in a single coherent commit so no intermediate state ships with mismatched docs/code (D-02, Pitfall 6 in 08-RESEARCH.md).

Purpose: Aligns `%Paddle.Error{}` with every other locked struct's `:raw_data` convention (every Customer, Address, Transaction, Subscription, etc.) and lays the structural foundation for REL-03 (Plan 02), which will populate the new boolean fields when normalizing transport failures. Per D-01/D-02 this is the single breaking change v1.2 spends from the 0.x `bump-minor-pre-major: false` budget.

Output: `lib/paddle/error.ex` rewritten with keyword-default syntax (per D-16); `test/paddle/error_test.exs` updated assertions plus new defaults-lock describe block; `guides/accrue-seam.md` line 118 row updated; `CHANGELOG.md` Unreleased section gains a Breaking Changes entry; `.planning/BACKLOG.md` gains a B-04 entry for the Accrue-side migration note (D-04). One commit, all five files.

This plan does NOT add `Paddle.Error.from_transport/1` — that lives in Plan 02 (REL-03). It also does NOT touch `Http.request/4`, `Client.new!/1`, or any resource module. Surgical scope: rename + add two field defaults + lock those defaults via tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-reliability-primitives/08-CONTEXT.md
@.planning/phases/08-reliability-primitives/08-RESEARCH.md
@.planning/phases/08-reliability-primitives/08-PATTERNS.md

<interfaces>
<!-- Current %Paddle.Error{} shape (from lib/paddle/error.ex, pre-modification) -->
<!-- The executor sees this WHOLE thing replaced — both line 2 and the from_response/1 body's :raw key. -->

```elixir
defmodule Paddle.Error do
  defexception [:type, :code, :message, :errors, :request_id, :status_code, :raw]

  @impl Exception
  def message(%{message: message}), do: message

  def from_response(%Req.Response{status: status, body: body} = resp) do
    body = if is_map(body), do: body, else: %{}
    error_body = Map.get(body, "error", %{})

    %__MODULE__{
      status_code: status,
      request_id: resp |> Req.Response.get_header("x-request-id") |> List.first(),
      type: error_body["type"],
      code: error_body["code"],
      message: Map.get(error_body, "detail", "Unknown Paddle Error"),
      errors: Map.get(error_body, "errors", []),
      raw: body
    }
  end
end
```

<!-- guides/accrue-seam.md:118 — the doc row that must move with the code. -->

```markdown
| `:raw` | `locked` | Forward-compat escape hatch; contents are `opaque`. |
```

<!-- CHANGELOG.md current top after the "Planning milestones vs Hex releases" section is `## 0.1.0 (2026-04-29)`. There is no `## [Unreleased]` section yet — Plan 01 introduces it (or a `## v1.2 (Unreleased)` heading per the project's existing milestone-vs-Hex separation note in CHANGELOG.md). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rename :raw → :raw_data in lib/paddle/error.ex AND add explicit boolean defaults via keyword-syntax defexception</name>
  <files>lib/paddle/error.ex, test/paddle/error_test.exs</files>
  <read_first>
    - lib/paddle/error.ex (current state — see exact pre-modification source in `<interfaces>` above)
    - test/paddle/error_test.exs (current `from_response/1` describe block at lines 12-61 — both assertions reference `raw:`)
    - .planning/phases/08-reliability-primitives/08-CONTEXT.md (D-01, D-05, D-16 — rename, no dual-population, explicit `false` defaults required)
    - .planning/phases/08-reliability-primitives/08-RESEARCH.md (Common Pitfalls — Pitfall 1 explains why shorthand `[:atom]` syntax silently breaks `:network_error?: false` matching)
    - .planning/phases/08-reliability-primitives/08-PATTERNS.md (Wave 0 section — exact target defexception line and `from_response/1` body change)
  </read_first>
  <behavior>
    - Test: `%Paddle.Error{}` literal struct — `network_error?` field equals the boolean `false`, NOT `nil` (lock D-16).
    - Test: `%Paddle.Error{}` literal struct — `retryable?` field equals the boolean `false`, NOT `nil` (lock D-16).
    - Test: `Paddle.Error.from_response(%Req.Response{status: 422, body: %{}})` returns a struct where `network_error?` is `false` (HTTP-response errors are not transport errors — locks D-11).
    - Test: `Paddle.Error.from_response(%Req.Response{status: 422, body: %{}})` returns a struct where `retryable?` is `false` (advisory class predicate; D-12 says only transport, 5xx, 429 carry `true`; this is the 422 case so `false`).
    - Test: existing `from_response/1` happy-path assertion still passes when `raw:` is rewritten to `raw_data:` (the body map round-trips into `:raw_data` exactly as it used to round-trip into `:raw`).
    - Test: existing `from_response/1` body-not-a-map fallback still asserts `raw_data: %{}` (was `raw: %{}`).
  </behavior>
  <action>
    Replace `lib/paddle/error.ex` line 2 — the shorthand defexception list — with the keyword-default form per D-16. The line MUST be exactly (quoted verbatim from CONTEXT.md D-16 and PATTERNS.md Wave 0):

    ```elixir
    defexception type: nil, code: nil, message: nil, errors: [],
                 request_id: nil, status_code: nil, raw_data: nil,
                 network_error?: false, retryable?: false
    ```

    NOTE: This is multi-line (line break after `errors: [],` and after `raw_data: nil,`) — match the formatting precisely so `mix format` does not reflow it. The two new fields (`:network_error?`, `:retryable?`) are introduced with their explicit boolean `false` default — D-16 is locked: shorthand `[:atom]` syntax with a `nil` default is the anti-pattern this plan eliminates (see Pitfall 1 in 08-RESEARCH.md).

    In the same file, in `from_response/1` (lines 7-20 currently), change ONLY the last struct-builder line:

    Before: `raw: body`
    After: `raw_data: body`

    No other change to `from_response/1`. Do NOT add `network_error?:` or `retryable?:` to the `from_response/1` builder — let them inherit their `false` defaults from the defexception declaration. (Per D-11 and D-12, HTTP-response errors are not network errors and 4xx-non-429 are not advisory-retryable; default `false` is correct without an explicit set.)

    Do NOT add `Paddle.Error.from_transport/1` in this plan. That is Plan 02's scope.

    In `test/paddle/error_test.exs`:

    1. In the existing `describe "from_response/1"` block, change BOTH assertions from `raw: %{...}` to `raw_data: %{...}` (line ~35 and line ~58 in the current file).

    2. Add a new `describe "struct defaults"` block AFTER the existing `from_response/1` describe block (so the file order reads: `message/1`, then `from_response/1`, then `struct defaults`):

       ```elixir
       describe "struct defaults" do
         test "network_error? defaults to false on a bare struct" do
           assert %Error{network_error?: false} = %Error{}
         end

         test "retryable? defaults to false on a bare struct" do
           assert %Error{retryable?: false} = %Error{}
         end

         test "network_error? defaults to false on from_response/1 result" do
           response = Req.Response.new(status: 422, body: %{})
           assert %Error{network_error?: false} = Error.from_response(response)
         end

         test "retryable? defaults to false on from_response/1 result" do
           response = Req.Response.new(status: 422, body: %{})
           assert %Error{retryable?: false} = Error.from_response(response)
         end
       end
       ```

    These four tests lock D-16 against regression (a future contributor reverting the file to shorthand `[:atom]` form silently makes both fields `nil` — these tests will fail loudly).

    Do NOT use `Bash(cat << 'EOF')` heredocs. Use the Edit tool against the existing file (line 2 swap; `from_response/1` `raw:` → `raw_data:` swap on the one line) and use the Edit tool to insert the new `describe "struct defaults"` block in `test/paddle/error_test.exs`.
  </action>
  <verify>
    <automated>mix test test/paddle/error_test.exs --color</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'defexception type: nil, code: nil, message: nil, errors: [],' lib/paddle/error.ex` returns a hit.
    - `grep -F 'network_error?: false, retryable?: false' lib/paddle/error.ex` returns a hit.
    - `grep -F 'raw_data: body' lib/paddle/error.ex` returns a hit.
    - `grep -E '\braw:\s' lib/paddle/error.ex` returns ZERO hits — rename is total within the source file.
    - `grep -E '\bdefexception \[' lib/paddle/error.ex` returns ZERO hits — the shorthand `[:atom]` form has been replaced with keyword-default syntax (anti-pattern eliminated).
    - `grep -F 'describe "struct defaults"' test/paddle/error_test.exs` returns a hit.
    - `grep -E '\braw:\s+%' test/paddle/error_test.exs` returns ZERO hits — the test rename is total.
    - `grep -F 'raw_data: %{' test/paddle/error_test.exs` returns ≥ 2 hits (both `from_response/1` assertions updated).
    - `grep -F 'network_error?: false' test/paddle/error_test.exs` returns ≥ 2 hits (bare-struct test + from_response test).
    - `grep -F 'retryable?: false' test/paddle/error_test.exs` returns ≥ 2 hits (bare-struct test + from_response test).
    - `mix test test/paddle/error_test.exs --color` exits 0.
    - `mix compile --warnings-as-errors` exits 0 (no unused-variable / undefined-function warnings introduced by the rename).
  </acceptance_criteria>
  <done>
    `lib/paddle/error.ex` uses keyword-default syntax with `raw_data`, `network_error?: false`, `retryable?: false`. `test/paddle/error_test.exs` has the four new defaults-lock tests and zero `raw:` references. Targeted test file passes. Compile clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Co-update guides/accrue-seam.md and CHANGELOG.md to reflect the rename — atomic with code per D-02</name>
  <files>guides/accrue-seam.md, CHANGELOG.md</files>
  <read_first>
    - guides/accrue-seam.md lines 110-119 (the `%Paddle.Error{}` table — see `<interfaces>` above for the exact current row at line 118)
    - CHANGELOG.md (current state — see Bash output above; top heading is `## 0.1.0 (2026-04-29)` with no Unreleased section yet)
    - .planning/phases/08-reliability-primitives/08-CONTEXT.md (D-02, D-03 — single atomic commit; CHANGELOG entry marked breaking)
    - .planning/phases/08-reliability-primitives/08-RESEARCH.md (Wave 0 section — exact target markdown for both files)
    - .planning/phases/08-reliability-primitives/08-PATTERNS.md (Wave 0 — `:raw → :raw_data` doc row swap)
  </read_first>
  <action>
    1. In `guides/accrue-seam.md`, at line 118, change exactly one row of the `%Paddle.Error{}` field table:

       Before: `| \`:raw\` | \`locked\` | Forward-compat escape hatch; contents are \`opaque\`. |`
       After:  `| \`:raw_data\` | \`locked\` | Forward-compat escape hatch; contents are \`opaque\`. |`

       No other line in the guide changes. The other two rows of the `%Paddle.Error{}` table (lines 116, 117) stay verbatim. Tier remains `locked`. The contents-are-opaque language is preserved verbatim per D-02 (no transitional dual-name period).

    2. In `CHANGELOG.md`, add a NEW section at the very top of the changelog (above `## 0.1.0 (2026-04-29)`, below the explanatory paragraph "When in doubt..."). The section heading and content MUST be:

       ```markdown
       ## [Unreleased]

       ### Breaking Changes

       * **`%Paddle.Error{}`**: Field `:raw` renamed to `:raw_data` for consistency with all other locked structs (every `Paddle.Customer`/`Paddle.Address`/`Paddle.Transaction`/`Paddle.Subscription`/etc. already uses `:raw_data` as the documented forward-compat escape hatch). Pattern matches against `%Paddle.Error{raw: r}` will silently miss after this change — update them to `%Paddle.Error{raw_data: r}`. (See `.planning/phases/08-reliability-primitives/08-CONTEXT.md` D-01..D-05 for rationale.)

       ### Added

       * **`%Paddle.Error{}`**: New `:network_error?` field (boolean, defaults to `false`) — to be populated by `Paddle.Error.from_transport/1` in a follow-up plan.
       * **`%Paddle.Error{}`**: New `:retryable?` field (boolean, defaults to `false`) — advisory class predicate populated by `Paddle.Error.from_transport/1` in a follow-up plan.
       ```

       Rationale for the structure: the `## [Unreleased]` heading follows Keep a Changelog conventions (already cited at the top of `CHANGELOG.md`). The `### Breaking Changes` and `### Added` sub-headings group the new defexception fields with the rename so the executor adding REL-03 in Plan 02 can append to `### Added` without restructuring (specifically, REL-03 will add `from_transport/1` to a `### Added` bullet).

       Per D-04, an Accrue-side migration note belongs in `.planning/BACKLOG.md` (NOT inline in `CHANGELOG.md`, which is the SDK's own published changelog and should not list downstream-consumer migration steps). The BACKLOG.md entry is created by Task 3 of this plan.

    Use the Edit tool. Do NOT use `Bash(cat << 'EOF')` heredocs. The `accrue-seam.md` change is a single-line replacement; the `CHANGELOG.md` change inserts a new section above the existing `## 0.1.0` heading.
  </action>
  <verify>
    <automated>grep -F '| `:raw_data` | `locked` |' guides/accrue-seam.md && grep -E '^\| `:raw` ' guides/accrue-seam.md | grep -v ':raw_data' ; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F '| \`:raw_data\` | \`locked\` |' guides/accrue-seam.md` returns at least one hit (the new error row).
    - `grep -nE '^\| \`:raw\` ' guides/accrue-seam.md` returns ZERO hits — no leftover `:raw` field row in the guide.
    - `grep -F '## [Unreleased]' CHANGELOG.md` returns a hit.
    - `grep -F 'Field \`:raw\` renamed to \`:raw_data\`' CHANGELOG.md` returns a hit.
    - `grep -F ':network_error?' CHANGELOG.md` returns a hit (Added section bullet present).
    - `grep -F ':retryable?' CHANGELOG.md` returns a hit (Added section bullet present).
    - `head -25 CHANGELOG.md | grep -F '## [Unreleased]'` returns a hit — the new section is at the top of the changelog (above the `## 0.1.0` heading), not buried.
    - `mix test --color` exits 0 — full pre-existing suite (≥111 tests) still passes after the rename + new field defaults land. (No test file outside `error_test.exs` references `%Paddle.Error{}.raw` per the CONTEXT.md inventory; this command verifies that empirically.)
  </acceptance_criteria>
  <done>
    `guides/accrue-seam.md:118` row reads `:raw_data`. `CHANGELOG.md` has an `## [Unreleased]` section with a `### Breaking Changes` bullet for the rename and `### Added` bullets for the two new boolean fields. Full test suite passes. The rename is documented atomically with the code change from Task 1 — D-02 satisfied.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add BACKLOG.md entry B-04 capturing the Accrue-side `error.raw → error.raw_data` migration note (D-04)</name>
  <files>.planning/BACKLOG.md</files>
  <read_first>
    - .planning/BACKLOG.md (current state — entries B-01, B-02, B-03 exist; next ID is B-04; format is `## B-NN — <title>` followed by `**Source:**`, `**Priority:**`, `**Status:**` fields and a free-form body)
    - .planning/phases/08-reliability-primitives/08-CONTEXT.md (D-04 — explicitly directs this note to BACKLOG.md and explicitly states "Do NOT block this phase on Accrue's commit; oarlock ships first")
  </read_first>
  <action>
    Append a new `## B-04` entry to `.planning/BACKLOG.md` AFTER the existing `## B-03` entry (preserving the file's existing footer/separator if any). The entry MUST be:

    ```markdown

    ---

    ## B-04 — Accrue migration: `%Paddle.Error{}.raw` → `%Paddle.Error{}.raw_data`

    **Source:** Phase 8 / Reliability Primitives — atomic rename landed in oarlock per D-01..D-05 (`.planning/phases/08-reliability-primitives/08-CONTEXT.md`).
    **Priority:** Medium — silent runtime breakage on the Accrue side (`%Paddle.Error{raw: r}` pattern matches will fail to bind after consuming the new oarlock version).
    **Status:** Captured 2026-04-30. Oarlock shipped first; Accrue update is a follow-up commit on the consumer side.

    **Why it's a gap:** The 0.x cleanup `:raw → :raw_data` is the one breaking change v1.2 spends from PROJECT.md's `bump-minor-pre-major: false` policy. Pattern matches in Accrue against the previous field name will silently miss after the version bump (see oarlock CHANGELOG `## [Unreleased]` → `### Breaking Changes`).

    **Surface to update on Accrue side:**
    - Search Accrue's codebase for `%Paddle.Error{raw: ` and `error.raw` (literal struct field access).
    - Replace each with the new field name `:raw_data` / `error.raw_data`.
    - Run Accrue's test suite to confirm.

    **Sizing:** ~1 commit, ~15 minutes. Pure rename — no semantic change. Possibly zero occurrences if Accrue never reached into the raw field; the BACKLOG entry is low-cost insurance per RESEARCH.md A4.

    **Promotion hint:** Not a phase — handled as a one-line PR on the Accrue side, scheduled when Accrue picks up the new oarlock dependency version.

    **Out of scope for B-04:** Anything beyond the rename. The new `:network_error?` and `:retryable?` fields default to `false`; existing Accrue code that does not pattern-match on them is unaffected.
    ```

    Note the leading `---` separator — this is the same separator style used between B-01/B-02 and B-02/B-03 in the existing file. The trailing newline structure should match the existing file convention.

    Use the Edit tool. Do NOT use `Bash(cat << 'EOF')` heredocs.
  </action>
  <verify>
    <automated>grep -F '## B-04 — Accrue migration' .planning/BACKLOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F '## B-04 — Accrue migration' .planning/BACKLOG.md` returns a hit.
    - `grep -F '`%Paddle.Error{}.raw`' .planning/BACKLOG.md` returns a hit (the title references the renamed field).
    - `grep -F 'Captured 2026-04-30' .planning/BACKLOG.md` returns a hit.
    - `grep -E '^## B-0[1-4] ' .planning/BACKLOG.md` returns exactly 4 hits (B-01, B-02, B-03, B-04 all present in order).
    - `grep -nE '^## B-' .planning/BACKLOG.md | tail -1 | grep -F 'B-04'` confirms B-04 is the last entry in the file (chronological append, not insertion in the middle).
  </acceptance_criteria>
  <done>
    `.planning/BACKLOG.md` has a B-04 entry capturing the Accrue-side migration note per D-04. The note is downstream-consumer guidance, not blocking work for this phase.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SDK consumer (Accrue) ↔ %Paddle.Error{} struct | Consumer pattern-matches on the error struct; field rename is the only data point crossing this boundary in this plan. |
| oarlock published artifact ↔ Hex (future release) | The CHANGELOG entry is the only consumer-facing notification of the breaking change; if absent, a `mix deps.update` would silently break Accrue's `error.raw` call sites. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | T (Tampering — silent contract drift) | `%Paddle.Error{}` struct field name | mitigate | Atomic commit: code (`error.ex`), test (`error_test.exs`), seam doc (`accrue-seam.md:118`), and CHANGELOG move together (D-02). No intermediate state where one says `:raw` and another says `:raw_data`. |
| T-08-02 | I (Information Disclosure — silent regression of pattern match) | Consumer code (Accrue) using `%Paddle.Error{raw: r}` | accept | The 0.x policy `bump-minor-pre-major: false` (PROJECT.md) explicitly permits this kind of breaking cleanup without a major bump. CHANGELOG `### Breaking Changes` bullet is the consumer-facing notification. Accrue migration logged in `.planning/BACKLOG.md` per D-04 (not in this plan's scope). |
| T-08-03 | T (Tampering — boolean field defaulting to nil) | `%Paddle.Error{}` `:network_error?` / `:retryable?` defaults | mitigate | D-16 explicit `false` defaults via keyword-default `defexception` syntax; locked by four new tests in `error_test.exs` `describe "struct defaults"`. A future contributor reverting to shorthand `[:atom]` form will fail those tests immediately. |
</threat_model>

<verification>
- `mix test test/paddle/error_test.exs --color` — targeted suite passes.
- `mix test --color` — full suite (≥111 tests) passes; no v1.1 seam regression introduced by the rename.
- `mix compile --warnings-as-errors` — clean.
- `mix format --check-formatted` — `lib/paddle/error.ex` and `test/paddle/error_test.exs` are formatted.
- All five files (`lib/paddle/error.ex`, `test/paddle/error_test.exs`, `guides/accrue-seam.md`, `CHANGELOG.md`) committed in a SINGLE git commit per D-02 (no intermediate state). Commit message format follows the project's conventional-commit pattern visible in `git log` (e.g., `feat(08-01): rename Paddle.Error :raw to :raw_data and add network/retry boolean defaults`).
</verification>

<success_criteria>
- `:raw → :raw_data` rename is total: zero remaining `:raw` references in `lib/paddle/error.ex`, `test/paddle/error_test.exs`, or `guides/accrue-seam.md` row 118.
- `%Paddle.Error{}` returns `false` (boolean) for `:network_error?` and `:retryable?` on bare-struct construction AND on `from_response/1` results — D-11 and D-12 partially locked (the transport-error population side is Plan 02's scope).
- `CHANGELOG.md` has a top-of-file `## [Unreleased]` section documenting the rename as breaking and announcing the two new fields.
- All four files (5 if you count the `error_test.exs` separately) move in a single atomic commit — D-02 satisfied.
- Pre-existing 111+ test suite still passes — Phase 8 success criterion #4 untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/08-reliability-primitives/08-01-SUMMARY.md` with:
- `## Truths Validated` — the seven `must_haves.truths` checked against actual code state.
- `## Artifacts Created/Modified` — list with line counts and notable contents.
- `## Files Committed` — `git status --porcelain` empty after the commit; list each committed path.
- `## Test Outcomes` — `mix test test/paddle/error_test.exs` and `mix test` exit 0 with test counts.
- `## Notes for Plan 02` — Plan 02 (REL-03) consumes `%Paddle.Error{}` with the new `:raw_data`, `:network_error?`, `:retryable?` fields already present and tested; no further struct changes required there.
</output>
</content>
</invoke>