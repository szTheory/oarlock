---
phase: 12-documentation-pass
plan: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/paddle/client.ex
  - lib/paddle/error.ex
  - lib/paddle/page.ex
  - lib/paddle/event.ex
  - lib/paddle/customer.ex
  - lib/paddle/address.ex
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02

must_haves:
  truths:
    - "Core SDK structs are documented with their fields and purposes"
    - "Client setup shows explicit construction"
  artifacts:
    - path: "lib/paddle/client.ex"
      provides: "Client structure docs"
    - path: "lib/paddle/error.ex"
      provides: "Error structure docs"
    - path: "lib/paddle/page.ex"
      provides: "Pagination structure docs"
  key_links: []
---

<objective>
Document the core SDK models: Client, Error, Page, Event, Customer, and Address.

Purpose: Provide structural reference and explain utility functions like `Client.new!/1` (DOCS-01, DOCS-02).
Output: Core structs documented with `@moduledoc` and `@doc`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-documentation-pass/12-CONTEXT.md
@.planning/phases/12-documentation-pass/12-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document Client, Error, Page, Event</name>
  <files>lib/paddle/client.ex, lib/paddle/error.ex, lib/paddle/page.ex, lib/paddle/event.ex</files>
  <read_first>lib/paddle/client.ex, lib/paddle/error.ex, lib/paddle/page.ex, lib/paddle/event.ex</read_first>
  <action>
    Add `@moduledoc` to `Paddle.Client`, `Paddle.Error`, `Paddle.Page`, `Paddle.Event` explaining their purpose.
    For `Paddle.Client.new!/1`, add `@doc` showing instantiation.
    For `Paddle.Error`, document how network and API errors are normalized (D-12).
    For `Paddle.Page.next_cursor/1`, add `@doc` with an example showing usage.
    Ensure `@doc` examples use the `{:ok, _}` and `{:error, _}` pattern match if applicable (though `Client.new!/1` returns a struct directly, and `next_cursor/1` returns a string or nil). For purely pure functions that don't return tuples, adapt the example to show the correct return.
  </action>
  <verify>
    <automated>grep -c "@moduledoc" lib/paddle/client.ex > /dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `lib/paddle/client.ex` contains `@moduledoc` and `@doc` for `new!/1`
    - `lib/paddle/error.ex` contains `@moduledoc` explaining error normalization
    - `lib/paddle/page.ex` contains `@moduledoc` and `@doc` for `next_cursor/1`
    - `lib/paddle/event.ex` contains `@moduledoc`
  </acceptance_criteria>
  <done>Core utility structs are documented.</done>
</task>

<task type="auto">
  <name>Task 2: Document Customer and Address structs</name>
  <files>lib/paddle/customer.ex, lib/paddle/address.ex</files>
  <read_first>lib/paddle/customer.ex, lib/paddle/address.ex</read_first>
  <action>
    Add `@moduledoc` to `Paddle.Customer` and `Paddle.Address`.
    Explain the fields concisely. As these are just data structs, they don't need pipeline examples (which belong in the controller modules). Just document the struct purpose.
    Link to Paddle Billing documentation for the domain rules (D-07).
  </action>
  <verify>
    <automated>grep -c "@moduledoc" lib/paddle/customer.ex > /dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `lib/paddle/customer.ex` contains `@moduledoc`
    - `lib/paddle/address.ex` contains `@moduledoc`
  </acceptance_criteria>
  <done>Customer and Address structs are documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation | No runtime trust boundaries affected. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02 | Information Disclosure | Docs | mitigate | No real API keys in examples; use placeholders like `sk_test_123` |
</threat_model>

<verification>
No warnings in `mix docs`.
</verification>

<success_criteria>
Core structs are well-documented.
</success_criteria>

<output>
Create `.planning/phases/12-documentation-pass/12-02-SUMMARY.md` when done
</output>
