---
phase: 12-documentation-pass
plan: 6
type: execute
wave: 2
depends_on:
  - 12-01
  - 12-02
  - 12-03
  - 12-04
  - 12-05
files_modified:
  - test/paddle/seam_test.exs
  - lib/paddle.ex
  - lib/paddle/http.ex
  - lib/paddle/http/telemetry.ex
  - lib/paddle/application.ex
  - lib/paddle/internal/attrs.ex
  - lib/paddle/internal/pagination.ex
autonomous: false
requirements:
  - DOCS-02

must_haves:
  truths:
    - "Internal sealed modules are confirmed to be hidden from documentation"
    - "Documentation compilation passes with no warnings"
  artifacts:
    - path: "test/paddle/seam_test.exs"
      provides: "Test asserting modules are sealed"
  key_links:
    - from: "test/paddle/seam_test.exs"
      to: "Code.fetch_docs/1"
      via: "test assertion"
      pattern: "Code\\.fetch_docs"
---

<objective>
Ensure internal modules remain sealed (hidden from documentation) and finalize documentation compilation (DOCS-02).

Purpose: Prevent regression where internal API modules leak into public Hex docs.
Output: Updates to sealed modules and a test asserting they remain undocumented.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-documentation-pass/12-CONTEXT.md
@.planning/phases/12-documentation-pass/12-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce @moduledoc false on sealed modules</name>
  <files>lib/paddle.ex, lib/paddle/http.ex, lib/paddle/http/telemetry.ex, lib/paddle/application.ex, lib/paddle/internal/attrs.ex, lib/paddle/internal/pagination.ex</files>
  <read_first>lib/paddle.ex, lib/paddle/http.ex, lib/paddle/http/telemetry.ex, lib/paddle/application.ex, lib/paddle/internal/attrs.ex, lib/paddle/internal/pagination.ex</read_first>
  <action>
    Review the 6 modules and ensure they explicitly contain `@moduledoc false`.
    Do not add or modify any functions, just ensure the module doc is false.
  </action>
  <verify>
    <automated>grep -c "@moduledoc false" lib/paddle.ex > /dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `lib/paddle.ex` contains `@moduledoc false`
    - `lib/paddle/http.ex` contains `@moduledoc false`
    - `lib/paddle/http/telemetry.ex` contains `@moduledoc false`
    - `lib/paddle/application.ex` contains `@moduledoc false`
    - `lib/paddle/internal/attrs.ex` contains `@moduledoc false`
    - `lib/paddle/internal/pagination.ex` contains `@moduledoc false`
  </acceptance_criteria>
  <done>Modules are sealed.</done>
</task>

<task type="auto">
  <name>Task 2: Add test asserting sealed modules</name>
  <files>test/paddle/seam_test.exs</files>
  <read_first>test/paddle/seam_test.exs</read_first>
  <action>
    Add a new `test "sealed modules remain undocumented"` block at the end of `test/paddle/seam_test.exs`.
    Iterate over the 6 sealed modules (`[Paddle, Paddle.Http, Paddle.Http.Telemetry, Paddle.Application, Paddle.Internal.Attrs, Paddle.Internal.Pagination]`).
    For each, assert that `Code.fetch_docs/1` returns a tuple where the 5th element is `:hidden` (e.g., `assert {:docs_v1, _, _, _, :hidden, _, _} = Code.fetch_docs(module)`).
  </action>
  <verify>
    <automated>mix test test/paddle/seam_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `test/paddle/seam_test.exs` contains `test "sealed modules remain undocumented"`
    - The test iterates over the 6 exact sealed modules
    - The test runs `Code.fetch_docs(module)` and matches `{:docs_v1, _, _, _, :hidden, _, _}`
    - `mix test test/paddle/seam_test.exs` passes
  </acceptance_criteria>
  <done>Sealed modules are backed by a regression test.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Final verification of documentation build</name>
  <what-built>All documentation generation</what-built>
  <how-to-verify>
    Run `mix docs --warnings-as-errors` to ensure all cross-links, missing module docs, and syntax errors are resolved across the entire project.
  </how-to-verify>
  <resume-signal>Type "approved" if it succeeds, or fix issues and retry.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation | No runtime trust boundaries affected. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-06 | Information Disclosure | Docs | mitigate | No internal API details leaked because modules are hidden |
</threat_model>

<verification>
`mix docs --warnings-as-errors` passes perfectly.
</verification>

<success_criteria>
Documentation pass is complete and verified.
</success_criteria>

<output>
Create `.planning/phases/12-documentation-pass/12-06-SUMMARY.md` when done
</output>
