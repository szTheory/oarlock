---
phase: 12-documentation-pass
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - guides/getting-started.md
  - guides/telemetry.md
  - mix.exs
autonomous: true
requirements:
  - DOCS-03
  - DOCS-04
  - DOCS-05

must_haves:
  truths:
    - "README is complete and has no TODOs"
    - "Telemetry guide provides specific measurement schemas"
    - "Mix config exposes getting started and telemetry guides"
  artifacts:
    - path: "README.md"
      provides: "Project overview"
    - path: "guides/telemetry.md"
      provides: "Telemetry schema docs"
    - path: "mix.exs"
      provides: "Package documentation config"
  key_links:
    - from: "mix.exs"
      to: "guides/telemetry.md"
      via: "docs extras list"
      pattern: "guides/telemetry\\.md"
---

<objective>
Update and publish the core project narrative documents (README, Getting Started, Telemetry) and wire them into the Hex package.

Purpose: Provide a clear onboarding path (DOCS-03, DOCS-04) and operation metrics reference (DOCS-05).
Output: Finalized README, new Telemetry guide, updated mix.exs.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-documentation-pass/12-CONTEXT.md
@.planning/phases/12-documentation-pass/12-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalize README</name>
  <files>README.md</files>
  <read_first>README.md</read_first>
  <action>
    Remove the "TODO: Add description" stub if it exists (it may have been partially updated in the draft).
    Ensure the README has a clear Installation section, Quick Start section, and links out to `guides/getting-started.md`.
    Retain the existing content explaining the project's narrow scope.
  </action>
  <verify>
    <automated>grep -v '^#' README.md | grep -c "TODO" == 0</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` no longer contains the word "TODO"
    - `README.md` includes an `Installation` section
    - `README.md` includes a `Quick Start` section
  </acceptance_criteria>
  <done>README is polished and production-ready.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize Getting Started and Telemetry guides</name>
  <files>guides/getting-started.md, guides/telemetry.md</files>
  <read_first>guides/getting-started.md, lib/paddle/http/telemetry.ex</read_first>
  <action>
    For `guides/getting-started.md`: Review the file, ensure it walks through the happy path (client -> customer -> address -> transaction -> checkout -> webhook -> subscription) and adheres to the "Hybrid Explicit" approach (D-01).

    For `guides/telemetry.md`: Create a new file documenting the `[:paddle, :request, :start]`, `[:paddle, :request, :stop]`, and `[:paddle, :request, :exception]` events emitted by `lib/paddle/http/telemetry.ex` (read it to extract exact schemas).
    Provide exact measurement and metadata schemas in the guide.
  </action>
  <verify>
    <automated>ls guides/telemetry.md</automated>
  </verify>
  <acceptance_criteria>
    - `guides/telemetry.md` contains sections for `start`, `stop`, and `exception` events
    - `guides/telemetry.md` lists the exact metadata fields (e.g. `request`, `error`) and measurements (e.g. `duration`)
  </acceptance_criteria>
  <done>Narrative guides are written.</done>
</task>

<task type="auto">
  <name>Task 3: Expose new guides in mix.exs</name>
  <files>mix.exs</files>
  <read_first>mix.exs</read_first>
  <action>
    Update `mix.exs`. In the `docs/0` private function, append `"guides/telemetry.md"` to the `:extras` list.
    Ensure `guides/getting-started.md` remains in the `:extras` list.
  </action>
  <verify>
    <automated>grep -c "guides/telemetry.md" mix.exs > /dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `mix.exs` contains `"guides/telemetry.md"` in the `extras:` list
  </acceptance_criteria>
  <done>mix.exs docs configuration includes the telemetry guide.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation | No runtime trust boundaries affected. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Information Disclosure | Docs | mitigate | No real API keys in examples; use placeholders like `sk_test_123` |
</threat_model>

<verification>
`mix docs` doesn't crash on compilation.
</verification>

<success_criteria>
Documentation files are present and wired in.
</success_criteria>

<output>
Create `.planning/phases/12-documentation-pass/12-01-SUMMARY.md` when done
</output>
