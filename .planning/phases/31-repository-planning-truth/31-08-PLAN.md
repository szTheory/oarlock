---
phase: 31-repository-planning-truth
plan: "08"
type: execute
wave: 6
depends_on:
  - 31-05
files_modified:
  - scripts/history_integrity.cjs
  - scripts/history_integrity.test.cjs
  - scripts/prohibitions/planning_identity.test.cjs
  - scripts/fixtures/prohibitions/history_rewrite.json
  - scripts/fixtures/prohibitions/history_additive.json
  - scripts/fixtures/prohibitions/planning_identity_inference.cjs
  - .planning/phases/31-repository-planning-truth/31-03-PLAN.md
autonomous: true
gap_closure: true
gap_ids:
  - G-31-5
  - G-31-6
requirements:
  - REPO-03
  - REPO-04
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "G-31-5 / D-11: a base-to-head guard rejects committed edits or deletions of pre-existing frozen archives and prior correction-ledger entries while accepting new archives and dated appended corrections."
    - "G-31-6 / D-12: independent mutations of planning milestone, tag, peeled source SHA, tagged package version, and publication evidence each fail identity automation without inference between fields; tag and peeled SHA cannot substitute for or mask one another."
    - "The REPO-03 preservation and identity prohibitions are resolved with exact content-dependent test descriptors."
  artifacts:
    - path: scripts/history_integrity.cjs
      provides: "Base-to-head frozen archive and append-only correction guard"
    - path: scripts/history_integrity.test.cjs
      provides: "Synthetic Git history matrix and preservation prohibition target"
    - path: scripts/prohibitions/planning_identity.test.cjs
      provides: "Subject-driven independent identity prohibition target"
    - path: .planning/phases/31-repository-planning-truth/31-03-PLAN.md
      provides: "Resolved REPO-03 preservation and identity descriptors"
  key_links:
    - from: scripts/history_integrity.cjs
      to: .planning/milestones
      via: "Git base/head object reads compare frozen files that already existed at base"
      pattern: "git.*(?:show|diff)|\\.planning/milestones"
    - from: scripts/history_integrity.cjs
      to: .planning/EVIDENCE.md
      via: "Prior ledger rows are an ordered prefix/set retained byte-for-byte while validated dated additions are allowed"
      pattern: "EVIDENCE\\.md"
    - from: scripts/prohibitions/planning_identity.test.cjs
      to: scripts/lib/repository_truth.cjs
      via: "Production identity collector/evaluator is the clean subject"
      pattern: "GSD_PROHIB_SUBJECT"
---

<objective>
Add cross-revision history integrity and fail-first independent-identity proof.

Purpose: Close G-31-5 and G-31-6 by detecting changes already committed in the candidate SHA, which worktree-relative checks cannot see, and by proving every milestone identity dimension fails independently when inferred or overclaimed.
Output: A base/head CLI guard, synthetic Git tests, identity mutation subjects, and resolved Plan 03 prohibition metadata.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-repository-planning-truth/31-CONTEXT.md
@.planning/phases/31-repository-planning-truth/31-UAT.md
@.planning/debug/phase-31-frozen-history-additive-corrections-automation.md
@.planning/debug/phase-31-independent-milestone-identity-automation.md
@.planning/phases/31-repository-planning-truth/31-03-PLAN.md
@.planning/phases/31-repository-planning-truth/31-03-SUMMARY.md
@scripts/planning_health.test.cjs
@scripts/lib/repository_truth.cjs
@.planning/EVIDENCE.md
@.planning/MILESTONES.md

<interfaces>
From scripts/lib/repository_truth.cjs:
- collectTagIdentities, readPackageVersion, and validateMilestoneHistory keep planning milestone, tag, peeled source SHA, tagged package declaration, and publication evidence separate.

New history guard CLI contract:
- `node scripts/history_integrity.cjs --base &lt;sha&gt; --head &lt;sha&gt; [--json]` uses argument-array Git calls, emits deterministic changed-artifact evidence, exits 0 when frozen/append-only invariants hold, 1 on prohibited history changes, and 2 when either revision cannot be observed completely.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Guard frozen history from the comparison base to candidate head</name>
  <files>scripts/history_integrity.cjs, scripts/history_integrity.test.cjs, scripts/fixtures/prohibitions/history_rewrite.json, scripts/fixtures/prohibitions/history_additive.json, .planning/phases/31-repository-planning-truth/31-03-PLAN.md</files>
  <behavior>
    - Editing, deleting, renaming, or replacing any `.planning/milestones/**` file already present at base exits 1 even when the candidate worktree is clean.
    - A new milestone snapshot absent at base is allowed when it is additive; existing archives remain unchanged.
    - Removing or modifying a prior `.planning/EVIDENCE.md` correction row exits 1; appending a structurally valid dated correction row is allowed.
    - Missing/unresolvable/shallow base or head evidence exits 2 rather than silently passing.
    - The same subject-driven test is red for the rewrite manifest and green for the additive manifest.
  </behavior>
  <action>Build a dependency-free Node CLI that compares explicit base and head Git objects, never the current worktree diff. Use bounded `spawnSync` argument arrays and Git object reads/diffs scoped to frozen archives and the correction ledger. Define append-only semantics so all base ledger content/rows remain present in order and only dated additions are accepted; allow genuinely new snapshot paths while rejecting changes to paths already frozen at base. In `history_integrity.test.cjs`, synthesize temporary repositories and committed base/head pairs for every operation above. When `GSD_PROHIB_SUBJECT` is set, read the provided JSON mutation manifest and run the same real Git-history oracle so the known rewrite fixture is non-vacuously red and the additive control is green. Resolve PROHIB-REPO-03-PRESERVATION in 31-03-PLAN.md with this test target and the two JSON subjects per G-31-5 and D-11.</action>
  <verify>
    <automated>node --test scripts/history_integrity.test.cjs &amp;&amp; GSD_PROHIB_SUBJECT=scripts/fixtures/prohibitions/history_additive.json node --test scripts/history_integrity.test.cjs &amp;&amp; if GSD_PROHIB_SUBJECT=scripts/fixtures/prohibitions/history_rewrite.json node --test scripts/history_integrity.test.cjs; then exit 1; else exit 0; fi</automated>
  </verify>
  <done>Committed archive/ledger rewrites are caught from base to head, valid additions pass, incomplete revision evidence fails closed, and the preservation prohibition is bound to the content-dependent test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove every milestone identity field remains independently sourced</name>
  <files>scripts/prohibitions/planning_identity.test.cjs, scripts/fixtures/prohibitions/planning_identity_inference.cjs, .planning/phases/31-repository-planning-truth/31-03-PLAN.md</files>
  <behavior>
    - Planning milestone mismatch fails without changing tag, SHA, package, or publication observations.
    - Tag mismatch/failure remains unknown or incomplete while the peeled SHA stays valid; the SHA cannot substitute for or mask the bad tag.
    - Peeled-SHA mismatch/failure remains unknown or incomplete while the tag stays valid; the tag cannot substitute for or mask the bad peeled SHA.
    - Tagged `mix.exs` package mismatch/failure remains separate and cannot be derived from the milestone/tag text.
    - Positive publication status fails without independent publication evidence; local tag/source/test evidence is insufficient.
    - v1.5 remains an explicit missing-local-tag caveat and the production library passes the complete matrix.
  </behavior>
  <action>Create a subject-driven node:test that loads the production truth library by default and a compatible deliberately inferential evaluator when `GSD_PROHIB_SUBJECT` is set. Construct five isolated cases that change exactly one identity dimension at a time—planning milestone, tag, peeled source SHA, tagged package declaration, or publication claim—and assert the stable PIDENT/collection result without cross-field substitution per G-31-6 and D-12. In the tag-only case hold the peeled SHA and other three observations valid; in the peeled-SHA-only case hold the tag and other three observations valid. Assert both directions explicitly so a valid peeled SHA cannot conceal a tag failure and a valid tag cannot conceal a peeled-SHA failure. Preserve the documented missing-tag and pre-archive exceptions. Resolve PROHIB-REPO-03-IDENTITY in 31-03-PLAN.md with the new target, bad subject, and `scripts/lib/repository_truth.cjs` clean subject.</action>
  <verify>
    <automated>node --test scripts/prohibitions/planning_identity.test.cjs &amp;&amp; if GSD_PROHIB_SUBJECT=scripts/fixtures/prohibitions/planning_identity_inference.cjs node --test scripts/prohibitions/planning_identity.test.cjs; then exit 1; else exit 0; fi</automated>
  </verify>
  <done>Five one-variable identity counterexamples are rejected, including separate tag-only and peeled-SHA-only failures that prove neither can substitute for or mask the other; explicit unknown/caveat behavior is retained, the bad subject is red, production is green, and the identity descriptor is canonical-verifier-ready.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Git revisions → history guard | Base/head identifiers and object contents are untrusted inputs to comparison. |
| Planning/tag/package/publication evidence → identity model | Distinct authorities must not impersonate one another. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-08-01 | Tampering | frozen archives and correction ledger | high | mitigate | Compare explicit Git objects and reject modification/deletion/rename of base-frozen paths and prior ledger rows. |
| T-31-08-02 | Spoofing | milestone identity fields | high | mitigate | Test five single-field mutations, including separate tag and peeled-SHA cases, and require authority-specific evidence or explicit unknown for each field. |
| T-31-08-03 | Denial of Service | Git subprocess/object reads | medium | mitigate | Use argument arrays, timeouts, bounded buffers, and exit 2 on incomplete revision observation. |
</threat_model>

<verification>
- `node --test scripts/history_integrity.test.cjs scripts/prohibitions/planning_identity.test.cjs` passes for clean/current subjects.
- History rewrite and identity inference subjects each produce a non-vacuous named assertion failure; the additive history control passes.
- 31-03-PLAN.md retains both stable prohibition IDs with resolved/test descriptors and exact fixture paths.
</verification>

<success_criteria>
- G-31-5 detects committed frozen-history and prior-ledger changes relative to a real base revision.
- G-31-6 independently proves all five identity sources without deriving publication or package identity from local planning/Git facts.
- Canonical verification can dispose both REPO-03 prohibitions automatically.
</success_criteria>

<output>
Create `.planning/phases/31-repository-planning-truth/31-08-SUMMARY.md` when done.
</output>
