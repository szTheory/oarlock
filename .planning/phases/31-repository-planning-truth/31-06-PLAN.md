---
phase: 31-repository-planning-truth
plan: "06"
type: execute
wave: 6
depends_on:
  - 31-05
files_modified:
  - scripts/repository_inventory.test.cjs
  - scripts/prohibitions/repository_inventory_report_only.test.cjs
  - scripts/prohibitions/repository_inventory_transparency.test.cjs
  - scripts/fixtures/prohibitions/repository_inventory_mutating.cjs
  - scripts/fixtures/prohibitions/repository_inventory_inference.cjs
  - .planning/phases/31-repository-planning-truth/31-01-PLAN.md
autonomous: true
gap_closure: true
gap_ids:
  - G-31-1
  - G-31-2
requirements:
  - REPO-01
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "G-31-1 / D-02 / D-05: a subject-driven check fails against an inventory implementation that authorizes or performs mutation and passes against the production report-only CLI."
    - "G-31-2 / D-03 / D-04 / D-15: CLI tests prove inferred, stale, unsupported, and partial ownership facts remain unknown or incomplete in both human and JSON conclusions."
    - "The two REPO-01 prohibitions are resolved at the test tier with locatable bad/clean descriptors that canonical verification can prove fail-first."
  artifacts:
    - path: scripts/prohibitions/repository_inventory_report_only.test.cjs
      provides: "Subject-driven report-only prohibition check"
    - path: scripts/prohibitions/repository_inventory_transparency.test.cjs
      provides: "Subject-driven ownership and incomplete-observation prohibition check"
    - path: scripts/repository_inventory.test.cjs
      provides: "CLI human/JSON behavior matrix for stale, unsupported, inferred, and partial facts"
    - path: .planning/phases/31-repository-planning-truth/31-01-PLAN.md
      provides: "Resolved test-tier REPO-01 prohibition descriptors"
  key_links:
    - from: .planning/phases/31-repository-planning-truth/31-01-PLAN.md
      to: scripts/prohibitions/repository_inventory_report_only.test.cjs
      via: "PROHIB-REPO-01-SAFETY check_target with bad and clean subjects"
      pattern: "check_target: scripts/prohibitions/repository_inventory_report_only\\.test\\.cjs"
    - from: scripts/prohibitions/repository_inventory_transparency.test.cjs
      to: scripts/lib/repository_truth.cjs
      via: "GSD_PROHIB_SUBJECT loads the production evaluator as the clean control"
      pattern: "GSD_PROHIB_SUBJECT"
---

<objective>
Replace human review of the REPO-01 safety and transparency invariants with authoritative fail-first automation.

Purpose: Close G-31-1 and G-31-2 by proving the existing read-only inventory contract can detect deliberately mutating, inferential, stale, and incomplete behavior rather than merely recording a green implementation run.
Output: Two subject-driven prohibition checks, explicit CLI output coverage, known-bad subjects, and resolved canonical prohibition metadata.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-repository-planning-truth/31-CONTEXT.md
@.planning/phases/31-repository-planning-truth/31-UAT.md
@.planning/debug/phase-31-inventory-report-only-automation.md
@.planning/debug/phase-31-ownership-incompleteness-automation.md
@.planning/phases/31-repository-planning-truth/31-01-PLAN.md
@.planning/phases/31-repository-planning-truth/31-01-SUMMARY.md
@scripts/repository_inventory.cjs
@scripts/repository_inventory.test.cjs
@scripts/lib/repository_truth.cjs

<interfaces>
From scripts/repository_inventory.cjs:
- main(argv = process.argv.slice(2), options = {}) returns the CLI exit code and accepts injected cwd/stdout/stderr/collection options.

From scripts/lib/repository_truth.cjs:
- evaluateRepositoryInventory(snapshot, registry) returns shared facts, dispositions, diagnostics, and conclusion.
- renderHuman(result), renderJson(result), and exitCodeFor(result) are the shared output/exit contract.

GSD prohibition contract:
- A node-test target reads GSD_PROHIB_SUBJECT; canonical proof requires non-vacuous RED for check_violation_fixture and GREEN for check_clean_fixture.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove report-only inventory behavior with a fail-first filesystem subject</name>
  <files>scripts/prohibitions/repository_inventory_report_only.test.cjs, scripts/fixtures/prohibitions/repository_inventory_mutating.cjs, .planning/phases/31-repository-planning-truth/31-01-PLAN.md</files>
  <behavior>
    - Bad subject: running the inventory against a temporary Git repository changes a tracked byte or repository/ref state, so the named prohibition assertion fails non-vacuously.
    - Clean subject: running scripts/repository_inventory.cjs in human and JSON modes leaves tracked bytes, refs, index, worktree registration, and lock metadata unchanged and the same assertion passes.
    - Descriptor: PROHIB-REPO-01-SAFETY is status resolved, verification test, flagged_unverified false, and names the target, bad subject, and production clean subject.
  </behavior>
  <action>Create a dependency-free node:test check that resolves `GSD_PROHIB_SUBJECT`, requires a subject exposing the existing `main(argv, options)` seam, constructs a temporary Git repository with tracked and dirty state, snapshots bytes plus Git/ref/worktree metadata, invokes both render modes, and asserts exact preservation per G-31-1 and D-02/D-05. Add a deliberately mutating subject with the same interface so the check fails because observed state changes, not because the fixture crashes or is missing. Update only PROHIB-REPO-01-SAFETY in 31-01-PLAN.md to resolved/test and add `check_kind: node-test`, `check_target`, `check_violation_fixture`, and `check_clean_fixture: scripts/repository_inventory.cjs`; retain its ID, requirement, category, and statement.</action>
  <verify>
    <automated>node --test scripts/prohibitions/repository_inventory_report_only.test.cjs &amp;&amp; if GSD_PROHIB_SUBJECT=scripts/fixtures/prohibitions/repository_inventory_mutating.cjs node --test scripts/prohibitions/repository_inventory_report_only.test.cjs; then exit 1; else exit 0; fi</automated>
  </verify>
  <done>The same content-dependent test is green for the production CLI and demonstrably red for a mutating implementation, and the safety descriptor points exactly to those artifacts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove ownership transparency across human and JSON CLI paths</name>
  <files>scripts/repository_inventory.test.cjs, scripts/prohibitions/repository_inventory_transparency.test.cjs, scripts/fixtures/prohibitions/repository_inventory_inference.cjs, .planning/phases/31-repository-planning-truth/31-01-PLAN.md</files>
  <behavior>
    - Inferred or unsupported owner evidence never becomes an established owner and produces a nonzero conclusion.
    - A stale otherwise-valid claim remains visible with RINV_STALE_CLAIM in both human and JSON output and exits nonzero.
    - Missing/null/bounded collection remains explicitly incomplete in both formats and exits 2 without being rendered as successful absence.
    - The known-bad evaluator that infers ownership or hides incomplete state makes the subject-driven test fail; the production truth library passes.
  </behavior>
  <action>Extend the CLI suite with direct human and JSON assertions for the complete G-31-2 matrix, reusing `invokeInventory`, temporary repositories, and the shared diagnostic/conclusion model per D-01/D-03/D-04/D-15. Create a subject-driven transparency test and a behaviorally compatible bad evaluator that turns unsupported evidence into ownership and/or suppresses stale/partial diagnostics; ensure the failure is an assertion about those conclusions. Resolve only PROHIB-REPO-01-TRANSPARENCY in 31-01-PLAN.md at the test tier with the new target and bad fixture and `scripts/lib/repository_truth.cjs` as the clean control.</action>
  <verify>
    <automated>node --test scripts/repository_inventory.test.cjs scripts/prohibitions/repository_inventory_transparency.test.cjs &amp;&amp; if GSD_PROHIB_SUBJECT=scripts/fixtures/prohibitions/repository_inventory_inference.cjs node --test scripts/prohibitions/repository_inventory_transparency.test.cjs; then exit 1; else exit 0; fi</automated>
  </verify>
  <done>Every inferred, stale, unsupported, and partial branch has CLI-level human/JSON proof, the bad subject is red, the production library is green, and the transparency prohibition carries an exact automated descriptor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fixture subject → test process | A deliberately hostile subject is loaded only inside a bounded temporary-repository test. |
| Git/filesystem → inventory CLI | Untrusted paths and repository state cross into collection and rendering. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-06-01 | Tampering | report-only prohibition fixture | high | mitigate | Snapshot tracked bytes, refs, index, worktree registrations, and lock metadata before and after both CLI modes; fail on any delta. |
| T-31-06-02 | Information Disclosure | transparency fixture/rendering | medium | mitigate | Assert rejected ownership payloads do not become facts and both renderers expose only bounded causal diagnostics. |
| T-31-06-03 | Repudiation | prohibition descriptor | medium | mitigate | Preserve stable prohibition IDs and bind each to exact target, violation fixture, and clean fixture paths. |
</threat_model>

<verification>
- `node --test scripts/repository_inventory.test.cjs scripts/prohibitions/repository_inventory_report_only.test.cjs scripts/prohibitions/repository_inventory_transparency.test.cjs` passes with production subjects.
- Each bad subject produces a non-vacuous failing named test while its paired production clean subject passes.
- 31-01-PLAN.md retains both stable prohibition IDs and each now has `status: resolved`, `verification: test`, and all three node-test descriptor paths.
</verification>

<success_criteria>
- G-31-1 and G-31-2 have authoritative, content-dependent automated proof instead of adjacent green tests.
- D-01 through D-05 and D-13 through D-15 remain intact; no inventory or test path authorizes cleanup or conceals incomplete evidence.
- Canonical verification can consume both REPO-01 descriptors without human-needed fallback.
</success_criteria>

<output>
Create `.planning/phases/31-repository-planning-truth/31-06-SUMMARY.md` when done.
</output>
