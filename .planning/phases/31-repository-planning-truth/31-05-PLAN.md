---
phase: 31-repository-planning-truth
plan: "05"
type: execute
wave: 5
depends_on:
  - 31-04
files_modified:
  - scripts/lib/repository_truth.cjs
  - scripts/planning_health.test.cjs
autonomous: true
gap_closure: true
requirements:
  - REPO-02
  - REPO-03
  - REPO-04
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-06/D-08: Completion proof is collected only from one unambiguous canonical active-phase directory and only leading YAML frontmatter status fields; same-basename decoys and Markdown body text remain inert."
    - "D-11/D-12: Milestone phase ranges compare normalized complete endpoints exactly, and tag/package observations distinguish successful absence/unknown from failed collection without changing frozen archives."
    - "D-13/D-14/D-15: Failed `for-each-ref` or tagged `mix.exs` observation preserves command/status/stderr cause as shared actionable incomplete diagnostics and planning-health exit 2."
    - "D-09: The planning-health path remains non-mutating, and unused Phase-31-specific corroboration is removed rather than adding a misleading process dependency."
  artifacts:
    - path: scripts/lib/repository_truth.cjs
      provides: "Canonical phase-directory resolver, leading-frontmatter proof parser, exact phase-range normalizer, and failure-aware Git identity collection"
      exports: ["resolveCanonicalPhaseDirectory", "parsePhaseRange", "collectTagIdentities", "readPackageVersion", "validateCompletionProof", "validateMilestoneHistory"]
    - path: scripts/planning_health.test.cjs
      provides: "Same-basename/body-status, exact-range, failed-ref, failed-tagged-file, parity, and exit-2 regressions"
  key_links:
    - from: scripts/lib/repository_truth.cjs
      to: .planning/ROADMAP.md
      via: "Active ROADMAP phase number resolves one canonical phase directory before proof lookup"
      pattern: "resolveCanonicalPhaseDirectory|validateCompletionProof"
    - from: scripts/lib/repository_truth.cjs
      to: .planning/MILESTONES.md
      via: "Normalized phase-range endpoints compare the complete ROADMAP and current-index values"
      pattern: "parsePhaseRange|PHIST_PHASE_RANGE_MISMATCH"
    - from: scripts/lib/repository_truth.cjs
      to: scripts/planning_health.cjs
      via: "Git identity collection errors enter snapshot.collectionErrors and drive shared exit 2"
      pattern: "collectionErrors|collectTagIdentities|readPackageVersion"
  assumptions:
    - id: EDGE-REPO-02-UNCLASSIFIED
      status: flagged-unverified
      statement: "For completion proof, zero or multiple canonical phase directories are incomplete/ambiguous; unrelated same-basename files and body-only status text have no authority."
    - id: EDGE-REPO-03-UNCLASSIFIED
      status: flagged-unverified
      statement: "History range equality means both normalized endpoints and the entire accepted field shape agree; substring adjacency such as 8-13 inside 18-130 is a contradiction."
    - id: EDGE-REPO-04-CONCURRENCY
      status: flagged-unverified
      statement: "A failed or interrupted Git observation produces a coherent incomplete snapshot with exit 2; it cannot be interpreted as a successful empty tag set or ordinary unknown package identity."
  prohibitions:
    - "A plan, summary, or verification file outside the one canonical active-phase directory must not contribute routing or completion proof."
    - "Markdown body prose must not impersonate leading YAML frontmatter status, result, or verdict fields."
    - "A substring, prefixed range, suffixed range, malformed endpoint, or reversed endpoint must not satisfy exact milestone phase-range equality."
    - "A failed Git command must not be reported as a successfully absent tag, an ordinary unknown package version, or a policy mismatch."
    - "The repair must not edit `.planning/milestones/**`, infer publication, fetch/change refs, or execute proposed repairs."
---

<objective>
Close false completion/history proof and failed-observation gaps by binding evidence to one canonical phase directory, parsing only leading frontmatter, comparing exact range endpoints, and propagating Git failures as incomplete collection.

Purpose: Ensure REPO-02/REPO-03/REPO-04 health cannot become healthy from decoys, prose, substring matches, or swallowed Git errors, while preserving D-11 archive immutability and D-12 identity separation.
Output: Canonical proof lookup, strict proof metadata, exact range validation, structured Git collection errors, removal/generalization of unused phase-specific corroboration, and adversarial tests.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/MILESTONES.md
@.planning/EVIDENCE.md
@.planning/phases/31-repository-planning-truth/31-CONTEXT.md
@.planning/phases/31-repository-planning-truth/31-RESEARCH.md
@.planning/phases/31-repository-planning-truth/31-VERIFICATION.md
@.planning/phases/31-repository-planning-truth/31-REVIEW.md
@.planning/phases/31-repository-planning-truth/31-02-SUMMARY.md
@.planning/phases/31-repository-planning-truth/31-03-SUMMARY.md

<interfaces>
Consume Plan 31-04's repository-bounded planning snapshot and preserve the shared diagnostic/rendering/exit contracts.

`resolveCanonicalPhaseDirectory(snapshot, phaseNumber)` returns exactly one bounded `.planning/phases/{phaseNumber}-*` directory or an incomplete ambiguity/missing result. `validateCompletionProof` uses only exact paths beneath that directory. Status evidence comes only from `parseFrontmatter` over a leading `---` YAML block.

`parsePhaseRange(value)` accepts the documented two-endpoint phase-range field, normalizes both endpoints, and returns invalid for extra prefix/suffix material, malformed values, or reversed endpoints. `collectTagIdentities` returns identities and structured collection errors; `readPackageVersion` distinguishes `known`, successfully proven `absent`/`unknown`, and `collection-error` with command/status/stderr evidence.
</interfaces>
</context>

## Artifacts this phase produces

- New exports `resolveCanonicalPhaseDirectory` and `parsePhaseRange` from `scripts/lib/repository_truth.cjs`.
- Refined structured contracts for `collectTagIdentities` and `readPackageVersion`, with errors appended to `snapshot.collectionErrors` and `incomplete: true`.
- Stable or extended diagnostics for canonical-directory ambiguity, strict completion metadata, exact `PHIST_PHASE_RANGE_MISMATCH`, and Git identity collection failure.
- New tests named with `canonical completion`, `exact phase range`, and `git identity collection` prefixes.
- Removal of the unused hard-coded Phase 31 corroboration query, or generalization to the resolved active phase only if the result is surfaced explicitly as non-authoritative evidence.
- No new package, external API, schema, planning-history edit, or `.planning/milestones/**` mutation.

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | One accurate, non-destructive repository/planning/history view | 31-04..05 | COVERED | The complete blocker set is partitioned by source trust then proof/evaluation trust. |
| REQ | REPO-01 | Bounded evidence-backed repository inventory | 31-04 | COVERED | Ownership registry and planning read primitive are repaired in the prerequisite plan. |
| REQ | REPO-02 | One authority chain immune to phantom evidence | 31-04, 31-05 | COVERED | Containment plus canonical phase-directory/frontmatter proof. |
| REQ | REPO-03 | Exact and honestly observed milestone history | 31-05 | COVERED | Exact endpoints and failure-aware Git identity observation. |
| REQ | REPO-04 | Actionable read-only health diagnostics | 31-04, 31-05 | COVERED | All six reproduced blockers receive CLI or pure-boundary regression proof. |
| RESEARCH | R-STACK / R-SNAPSHOT | CommonJS, node:test, collect/evaluate/render once | 31-04..05 | COVERED | No dependency and one shared snapshot/result. |
| RESEARCH | R-AUTHORITY / R-COMPLETION | Datum authority and explicit completion proof chain | 31-05 | COVERED | Canonical directory and leading structured metadata close both gaps. |
| RESEARCH | R-HISTORY / R-IDENTITY | Exact archive navigation and separately sourced identities | 31-05 | COVERED | Exact range and failure/absence distinction are explicit. |
| RESEARCH | R-SECURITY | Argument arrays, bounded roots/reads, no mutation | 31-04..05 | COVERED | Source reader, structured Git failures, no ref mutation, and archive diff gates. |
| CONTEXT | D-01..D-05 | Inventory truth and non-mutation | 31-04 | COVERED | Plan 04 directly repairs and regresses these decisions. |
| CONTEXT | D-06..D-10 | Authority, disagreement, inert artifacts, repair, mirror | 31-04, 31-05 | COVERED | Decoys/body proof are inert; existing disagreement/mirror behavior is retained. |
| CONTEXT | D-11..D-15 | Immutable history, identity distinction, actionable parity | 31-05 | COVERED | Exact history and causal Git failure semantics implement the remaining gaps. |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Bind completion proof to one canonical phase directory and leading frontmatter</name>
  <reversibility rating="reversible">Proof lookup and metadata parsing are internal validators over existing artifacts and require no artifact migration.</reversibility>
  <files>scripts/lib/repository_truth.cjs, scripts/planning_health.test.cjs</files>
  <read_first>
    - scripts/lib/repository_truth.cjs
    - scripts/planning_health.test.cjs
    - scripts/planning_health.cjs
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - .planning/EVIDENCE.md
    - .planning/phases/31-repository-planning-truth/31-CONTEXT.md
    - .planning/phases/31-repository-planning-truth/31-VERIFICATION.md
    - .planning/phases/31-repository-planning-truth/31-REVIEW.md
  </read_first>
  <behavior>
    - A fixture with canonical Phase 31 plans but proof files only under `99-decoy` receives missing/unproven completion diagnostics; exact same basenames outside the resolved canonical directory are inert per D-06/D-08.
    - Zero or multiple bounded directories matching the active phase number produce an incomplete canonical-directory diagnostic and exit 2 rather than an arbitrary winner per D-07/D-14.
    - Summary `status: complete` and verification `status|result|verdict: passed` count only when normalized from the leading YAML frontmatter block; identical body-only lines never count.
    - Exact canonical structured proof preserves existing requirement/evidence linkage, human/JSON parity, no-write behavior, and exit 0/1/2 meanings per D-09/D-13/D-15.
  </behavior>
  <action>Write failing `canonical completion: same-basename decoys and body-only status remain inert` and canonical-directory ambiguity fixtures, then add `resolveCanonicalPhaseDirectory` and replace global basename searches in `artifactForPlan`, `validateCompletionProof`, `activeArtifactDiagnostics`, and verification lookup with exact repository-relative paths beneath that directory. Reuse `parseFrontmatter`, but require a leading delimited block and normalize only its `status`, `result`, or `verdict` scalar; do not search Markdown body content. Preserve distinct missing versus unproven diagnostic codes, causal authority/evidence/repair fields, and D-06 through D-09 plus D-13 through D-15.</action>
  <acceptance_criteria>
    - Same-basename plan/summary/verification decoys under any unrelated phase directory cannot satisfy any proof link.
    - Body-only status/result/verdict text without leading YAML frontmatter cannot satisfy completion.
    - Zero/multiple canonical directories are incomplete with exit 2; one exact directory plus valid frontmatter follows the existing policy result.
    - Human/JSON diagnostic codes and conclusions match, and before/after planning manifests remain unchanged.
  </acceptance_criteria>
  <verify>
    <automated>node --test --test-name-pattern="canonical completion|phantom scope|completion|authority diagnostic parity|read-only interrupted" scripts/planning_health.test.cjs</automated>
    <fails_when>non-zero test exit, a decoy/body line supplies proof, an ambiguous directory is selected, valid canonical proof regresses, formats diverge, or files change</fails_when>
  </verify>
  <done>Completion can be proven only by the exact active phase's structured artifacts; unrelated presence and body prose remain inert end to end.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compare normalized milestone phase ranges exactly</name>
  <reversibility rating="reversible">Range normalization is a pure validator helper over the existing current index.</reversibility>
  <files>scripts/lib/repository_truth.cjs, scripts/planning_health.test.cjs</files>
  <read_first>
    - scripts/lib/repository_truth.cjs
    - scripts/planning_health.test.cjs
    - .planning/ROADMAP.md
    - .planning/MILESTONES.md
    - .planning/EVIDENCE.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/31-repository-planning-truth/31-CONTEXT.md
    - .planning/phases/31-repository-planning-truth/31-VERIFICATION.md
    - .planning/phases/31-repository-planning-truth/31-REVIEW.md
  </read_first>
  <behavior>
    - Expected `8-13` versus actual `18-130` produces exactly one `PHIST_PHASE_RANGE_MISMATCH` with expected/actual endpoints and D-14 authority/evidence/repair fields.
    - Equal normalized endpoints pass; prefix/suffix text, missing/malformed/reversed endpoints, and extra range material fail rather than matching by substring.
    - Decimal phase endpoints retain their full identity instead of numeric truncation.
    - Range repair proposals target only mutable `.planning/MILESTONES.md`; D-11 frozen archives remain byte-identical.
  </behavior>
  <action>Write a failing `exact phase range: 8-13 does not match 18-130` matrix, then implement/export `parsePhaseRange` and make `validateMilestoneHistory` compare both normalized complete endpoints and accepted field shape. Keep current ROADMAP parsing semantics, preserve decimal endpoint text, and emit one stable mismatch record for invalid or unequal actual fields. Do not change MILESTONES, EVIDENCE, ROADMAP, or archived files. Implement D-11, D-13, D-14, and D-15.</action>
  <acceptance_criteria>
    - The specified 8-13 versus 18-130 adversarial fixture fails with `PHIST_PHASE_RANGE_MISMATCH`.
    - Exact equal integer and decimal ranges pass; malformed, extra-text, partial, and reversed ranges fail deterministically.
    - Diagnostic parity remains exact between renderers.
    - `.planning/milestones/**` and current planning ledgers have no diff from this task.
  </acceptance_criteria>
  <verify>
    <automated>git diff --exit-code -- .planning/milestones .planning/MILESTONES.md .planning/EVIDENCE.md && node --test --test-name-pattern="exact phase range|milestone archive|milestone diagnostics" scripts/planning_health.test.cjs</automated>
    <fails_when>non-zero command exit, a containing/prefixed/suffixed/malformed/reversed range passes, a valid exact range fails, diagnostics diverge, or any planning-history file changes</fails_when>
  </verify>
  <done>Milestone history accepts only complete normalized endpoint equality and detects the reproduced substring contradiction without rewriting history.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Preserve Git identity collection failures as incomplete evidence</name>
  <reversibility rating="reversible">The collection result shape and diagnostics are internal CLI contracts with fixture coverage and no stored-state change.</reversibility>
  <files>scripts/lib/repository_truth.cjs, scripts/planning_health.test.cjs</files>
  <read_first>
    - scripts/lib/repository_truth.cjs
    - scripts/planning_health.test.cjs
    - scripts/planning_health.cjs
    - .planning/ROADMAP.md
    - .planning/MILESTONES.md
    - .planning/EVIDENCE.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/31-repository-planning-truth/31-CONTEXT.md
    - .planning/phases/31-repository-planning-truth/31-VERIFICATION.md
    - .planning/phases/31-repository-planning-truth/31-REVIEW.md
  </read_first>
  <behavior>
    - Failed `git for-each-ref` returns no successful empty observation; it appends a structured incomplete collection error with command, status, bounded stderr/cause, and planning-health exit 2.
    - Failed tagged `git show` is distinct from a successfully proven absent `mix.exs`/`@version`; failure appends incomplete evidence, while successful absence stays explicit without inventing a version per D-12.
    - Collection failure suppresses downstream tag/SHA/package policy conclusions whose premises were not observed, preserving cause rather than emitting misleading mismatch/unknown diagnostics per D-13/D-14.
    - Human and JSON retain the same ordered error codes/conclusion; no Git fetch, ref write, working-tree write, or proposed repair execution occurs per D-09/D-15.
  </behavior>
  <action>Write failing `git identity collection: for-each-ref and git-show failures exit 2 with cause` tests using the existing injectable runner. Refine `collectTagIdentities`, `readPackageVersion`, and `collectPlanningSnapshot` so identity results carry successful values/absence separately from structured command failures and append failures to `snapshot.collectionErrors` with `incomplete: true`. Update `validateMilestoneHistory` to avoid tag/SHA/package mismatch conclusions when their source observation failed. Bound stderr evidence and retain command/status/cause without secrets. Remove the unused hard-coded `drift-guard.phase-status 31` collection; if corroboration is retained, derive its phase only after canonical scope resolution and surface it explicitly as non-authoritative evidence. Preserve D-09 and D-12 through D-15.</action>
  <acceptance_criteria>
    - Injected `for-each-ref` failure and injected tagged-file read failure each produce a causal incomplete diagnostic and overall exit 2.
    - Successful zero-tag enumeration remains distinguishable from enumeration failure; proven missing version declaration remains distinguishable from failed Git execution.
    - No downstream tag/SHA/package mismatch is emitted when its required observation failed.
    - Both renderers preserve codes/conclusions, existing successful identity tests pass, and no Git/planning state changes.
  </acceptance_criteria>
  <verify>
    <automated>node --test --test-name-pattern="git identity collection|milestone identity|milestone diagnostics|current repository" scripts/planning_health.test.cjs</automated>
    <fails_when>non-zero test exit, a failed command becomes absence/unknown/policy mismatch, cause evidence disappears, overall status is not incomplete exit 2, successful identity behavior regresses, or state changes</fails_when>
  </verify>
  <done>Planning health differentiates failed Git observation from successful absence, preserves the true cause, and fails closed with exit 2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Phase artifact inventory → completion validator | Unrelated files and Markdown prose may attempt to impersonate authoritative structured proof. |
| Current history index → phase-range validator | Ambiguous or adversarial text may exploit substring comparisons. |
| Git subprocess → release identity model | Process errors, bounded output, missing refs/files, and successful absence have materially different meanings. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-31-20 | Spoofing / Elevation of Privilege | Completion artifact selection | high | mitigate | Resolve one bounded canonical phase directory and accept proof only from exact paths plus leading frontmatter. |
| T-31-21 | Tampering / Repudiation | Milestone phase-range validation | high | mitigate | Parse complete normalized endpoints and regression-test the 8-13 versus 18-130 adversarial pair. |
| T-31-22 | Repudiation | Git identity collection | high | mitigate | Preserve command/status/stderr cause in incomplete errors and suppress conclusions whose observation premise failed. |
| T-31-23 | Information Disclosure | Git error diagnostics | medium | mitigate | Bound and sanitize subprocess evidence while retaining enough command/status/cause for repair. |
| T-31-24 | Denial of Service | Artifact/frontmatter parsing | low | accept | Plan 31-04 bounded reads constrain input size; strict leading-block parsing is linear over already bounded repository files. |
</threat_model>

<verification>
- `node --test scripts/repository_inventory.test.cjs scripts/planning_health.test.cjs` passes in under 60 seconds; any failure blocks completion.
- Focused tests prove same-basename decoys/body-only status are inert, 8-13 differs from 18-130, and both Git failure classes produce exit 2 with causal evidence.
- `node scripts/planning_health.cjs --json` preserves the established shared diagnostic schema and deterministic conclusion.
- `git diff --exit-code -- .planning/milestones .planning/MILESTONES.md .planning/EVIDENCE.md` proves D-11 immutable history and current ledgers were not touched by gap repair.
</verification>

<success_criteria>
- All four verification gap entries and all six reproduced blockers are closed across Plans 31-04 and 31-05.
- REPO-02 false proof, REPO-03 exact-range/identity observation, and REPO-04 causal exit-2 behavior are covered by executable adversarial tests.
- D-06/D-08/D-09/D-11 through D-15 are implemented without archive, ledger, ref, or worktree mutation.
- All unresolved probe assumptions remain explicit and receive focused or full-suite execution evidence.
</success_criteria>

<output>
Create `.planning/phases/31-repository-planning-truth/31-05-SUMMARY.md` when done.
</output>
