---
phase: 31-repository-planning-truth
plan: "04"
type: execute
wave: 4
depends_on:
  - 31-03
files_modified:
  - scripts/lib/repository_truth.cjs
  - scripts/repository_inventory.cjs
  - scripts/repository_inventory.test.cjs
  - scripts/planning_health.test.cjs
autonomous: true
gap_closure: true
requirements:
  - REPO-01
  - REPO-02
  - REPO-04
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-03/D-04/D-06: Ownership and planning authority are accepted only from regular files whose resolved identity remains inside the resolved repository root; unsafe or incomplete collection produces causal diagnostics and exit 2."
    - "D-01/D-14/D-15: Human and JSON views derive from the same result and preserve the same source-boundary diagnostic codes and conclusions without echoing external registry content."
    - "D-05/D-09: Source containment checks remain report-only and never repair, replace, delete, unlock, prune, or otherwise mutate repository or planning state."
  artifacts:
    - path: scripts/lib/repository_truth.cjs
      provides: "Reusable same-descriptor, no-follow, realpath-bounded repository file reader for planning authorities"
      exports: ["readBoundedRepositoryFile"]
    - path: scripts/repository_inventory.cjs
      provides: "Repository-root-anchored ownership registry loading with redacted unsafe-source diagnostics"
    - path: scripts/repository_inventory.test.cjs
      provides: "Ownership registry symlink, non-regular-file, renderer parity, and no-mutation regressions"
    - path: scripts/planning_health.test.cjs
      provides: "Intermediate planning-directory symlink and bounded traversal regressions"
  key_links:
    - from: scripts/repository_inventory.cjs
      to: scripts/lib/repository_truth.cjs
      via: "Registry loading uses the same repository-bounded descriptor reader as planning authorities"
      pattern: "readBoundedRepositoryFile"
    - from: scripts/lib/repository_truth.cjs
      to: .planning/ROADMAP.md
      via: "Canonical planning collection resolves every path beneath the resolved repository root before trusting contents"
      pattern: "readPlanningFile|readBoundedRepositoryFile"
    - from: scripts/planning_health.test.cjs
      to: scripts/planning_health.cjs
      via: "A real CLI fixture proves an intermediate symlink produces incomplete health and exit 2"
      pattern: "source boundary|intermediate symlink"
  assumptions:
    - id: EDGE-REPO-01-ADJACENCY
      status: flagged-unverified
      statement: "Equal or overlapping ownership selectors do not merge or win by order; they remain ambiguous and blocking, while an exact single claim is separate. The full inventory regression suite must continue proving this boundary after registry hardening."
    - id: EDGE-REPO-01-EMPTY
      status: flagged-unverified
      statement: "An empty repository/registry is a successful empty observation, null or unreadable input is incomplete, and an unsafe registry source is not treated as empty."
    - id: EDGE-REPO-01-ORDERING
      status: flagged-unverified
      statement: "Equal diagnostics retain the established deterministic tie-break ordering in both renderers after source-boundary errors are added."
    - id: EDGE-REPO-02-UNCLASSIFIED
      status: flagged-unverified
      statement: "The repaired REPO-02 boundary classifies any intermediate-symlink authority as unreadable external input rather than canonical planning truth."
    - id: EDGE-REPO-04-CONCURRENCY
      status: flagged-unverified
      statement: "The same open descriptor supplies size, type, identity, and bytes; replacement or identity change before the final snapshot check yields incomplete exit 2 without writes."
  prohibitions:
    - "Repository-owned symlinks or intermediate symlink components must not authorize reads from outside the resolved repository root."
    - "Unsafe, partial, stale, or unsupported ownership input must not be reported as an observed repository fact or accepted ownership claim."
    - "External registry payloads must not be copied into diagnostic actual/evidence fields or either renderer."
    - "Containment failure must not trigger cleanup, repair, deletion, unlocking, pruning, or any other mutation."
---

<objective>
Close the source-trust gaps in repository inventory and planning health by making every authoritative read repository-bounded, same-descriptor, regular-file-only, and fail-closed.

Purpose: Prevent symlinked external planning documents or ownership registries from becoming repository truth or leaking their contents, while preserving the established non-mutating D-01 through D-05 and D-06/D-09/D-14/D-15 contracts.
Output: One reusable bounded-file primitive, both CLI integrations, causal incomplete-collection diagnostics, and adversarial source-boundary tests.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-repository-planning-truth/31-CONTEXT.md
@.planning/phases/31-repository-planning-truth/31-RESEARCH.md
@.planning/phases/31-repository-planning-truth/31-VERIFICATION.md
@.planning/phases/31-repository-planning-truth/31-REVIEW.md
@.planning/phases/31-repository-planning-truth/31-01-SUMMARY.md
@.planning/phases/31-repository-planning-truth/31-02-SUMMARY.md

<interfaces>
Preserve `collectPlanningSnapshot(root, options)`, `evaluatePlanningHealth(snapshot)`, `collectRepositorySnapshot(options)`, the shared diagnostic schema, renderer parity, and exit taxonomy: 0 healthy, 1 complete observation with policy error, 2 incomplete/unsafe observation.

Add `readBoundedRepositoryFile(root, relativeOrAbsolutePath, options)` as the single primitive for trusted repository data. It returns content plus descriptor-derived identity after verifying the resolved root/candidate containment, rejecting symlinks and non-regular files, opening with no-follow semantics where supported, bounding size before reading, and obtaining identity/type/bytes from one descriptor. It never returns external file contents in an error.
</interfaces>
</context>

## Artifacts this phase produces

- New export `readBoundedRepositoryFile` from `scripts/lib/repository_truth.cjs`.
- Stable incomplete-collection coverage for `PAUTH_SOURCE_UNREADABLE` and `RINV_REGISTRY_UNREADABLE`, including repository-boundary cause evidence and exit 2.
- New tests named with `source boundary` and `ownership registry` prefixes for reliable focused execution.
- No new package, external API, schema, CLI mutation mode, planning-ledger edit, or `.planning/milestones/**` change.

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | One accurate, non-destructive repository/planning/history view | 31-04..05 | COVERED | Unsafe input, false proof, inexact history, and failed observations all become causal fail-closed results. |
| REQ | REPO-01 | Complete read-only inventory with evidence-backed ownership | 31-04 | COVERED | Registry source is repository-bounded and unsafe input exits 2 without disclosure. |
| REQ | REPO-02 | One authority chain immune to phantom/external artifacts | 31-04, 31-05 | COVERED | Source containment closes external authorities; canonical-directory proof closes decoys. |
| REQ | REPO-03 | Exact, honestly sourced milestone history | 31-05 | COVERED | Exact endpoint comparison and failure-aware Git identity collection. |
| REQ | REPO-04 | Non-mutating actionable planning-health failures | 31-04, 31-05 | COVERED | Every reproduced blocker gets an automated causal diagnostic and exit assertion. |
| RESEARCH | R-STACK | Dependency-free CommonJS and node:test | 31-04..05 | COVERED | Uses Node built-ins and the existing fixture stack; no install. |
| RESEARCH | R-SNAPSHOT | Collect once, evaluate once, render twice | 31-04..05 | COVERED | Collection errors join the shared snapshot/result rather than renderer-specific paths. |
| RESEARCH | R-OWNERSHIP | Exact evidence-backed claims; unknown stays explicit | 31-04 | COVERED | External registry bytes cannot become claim evidence. |
| RESEARCH | R-AUTHORITY | Repository documents own datum-specific truth | 31-04, 31-05 | COVERED | Real containment plus one canonical phase directory enforce ownership. |
| RESEARCH | R-COMPLETION | Explicit structured proof chain | 31-05 | COVERED | Only canonical leading frontmatter can satisfy proof. |
| RESEARCH | R-HISTORY / R-IDENTITY | Exact ranges and separately sourced identities | 31-05 | COVERED | Endpoint normalization and observation-error propagation preserve distinctions. |
| RESEARCH | R-SECURITY | Root-bounded, bounded, no-follow, no mutation | 31-04 | COVERED | Same-descriptor reader and adversarial fixtures are the direct mitigation. |
| CONTEXT | D-01..D-05 | Shared output, fact/disposition split, evidence, failure, report-only | 31-04 | COVERED | Task actions and regression suite preserve all inventory decisions. |
| CONTEXT | D-06..D-10 | Authority, conflict, inert presence, report-only repair, mirror | 31-04, 31-05 | COVERED | D-06/D-08 source and proof gaps close; existing D-07/D-09/D-10 remain regression-covered. |
| CONTEXT | D-11..D-15 | Archive immutability, identity, diagnostic actionability/parity | 31-05 | COVERED | Plan 05 closes exact history and failed-observation semantics without archive edits. |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Reject an intermediate-symlink planning authority end to end</name>
  <reversibility rating="reversible">The bounded reader is an internal CommonJS interface and can evolve with its fixture contract without stored-data migration.</reversibility>
  <files>scripts/lib/repository_truth.cjs, scripts/planning_health.test.cjs</files>
  <read_first>
    - scripts/lib/repository_truth.cjs
    - scripts/planning_health.test.cjs
    - scripts/planning_health.cjs
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - .planning/phases/31-repository-planning-truth/31-CONTEXT.md
    - .planning/phases/31-repository-planning-truth/31-VERIFICATION.md
    - .planning/phases/31-repository-planning-truth/31-REVIEW.md
  </read_first>
  <behavior>
    - A fixture whose `.planning`, `.planning/phases`, or another intermediate authority component resolves outside the repository produces `PAUTH_SOURCE_UNREADABLE`, conclusion `incomplete`, and CLI exit 2; external contents never influence active scope per D-06/D-08.
    - A normal in-repository regular authority still participates in the established pre/post identity consistency checks and produces the same nominal result.
    - Symlink, non-regular, oversized, replaced, and escaped sources retain causal artifact/field/evidence while omitting source payloads per D-14.
    - Human and JSON conclusions/codes match and both invocations preserve repository bytes per D-05/D-09/D-15.
  </behavior>
  <action>Create a failing `source boundary: intermediate planning symlink exits 2 without reading external content` CLI fixture, then implement `readBoundedRepositoryFile` in `scripts/lib/repository_truth.cjs` and route `readPlanningFile`, phase traversal, milestone traversal, mirror reads, and final consistency checks through its containment/identity rules. Resolve the repository root and candidate filesystem identities, require candidate containment beneath the root, reject every symlink/non-regular hop, open with no-follow behavior where the platform exposes it, and use fstat plus the same descriptor for size, identity, and bytes so check/read races cannot substitute a different file. Convert failures into the existing incomplete collection records without embedding external bytes. Preserve D-05, D-06, D-08, D-09, and D-13 through D-15.</action>
  <acceptance_criteria>
    - The intermediate-symlink fixture returns process status 2 and includes `PAUTH_SOURCE_UNREADABLE` with the rejected repository-relative authority path and causal boundary evidence.
    - No external sentinel or file payload appears in snapshot documents, diagnostics, stdout, or stderr.
    - Ordinary regular-file fixtures and the prior concurrent snapshot test still pass.
    - Before/after manifests for both output modes are identical.
  </acceptance_criteria>
  <verify>
    <automated>node --test --test-name-pattern="source boundary|concurrent snapshot|read-only interrupted" scripts/planning_health.test.cjs</automated>
    <fails_when>non-zero test exit, an intermediate symlink is read, the CLI returns 0 or 1 instead of 2, causal evidence is lost, or a manifest changes</fails_when>
  </verify>
  <done>Planning health rejects external or unstable authority sources as incomplete and preserves D-06 authority plus D-05/D-09 non-mutation end to end.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bound ownership registry reads without disclosing rejected content</name>
  <reversibility rating="reversible">Registry loading remains an internal CLI seam and retains its existing JSON policy schema.</reversibility>
  <files>scripts/repository_inventory.cjs, scripts/repository_inventory.test.cjs</files>
  <read_first>
    - scripts/repository_inventory.cjs
    - scripts/repository_inventory.test.cjs
    - scripts/lib/repository_truth.cjs
    - .planning/repository-ownership.json
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/phases/31-repository-planning-truth/31-CONTEXT.md
    - .planning/phases/31-repository-planning-truth/31-VERIFICATION.md
    - .planning/phases/31-repository-planning-truth/31-REVIEW.md
  </read_first>
  <behavior>
    - `readRegistry` anchors its candidate to the collected repository root and uses `readBoundedRepositoryFile`; direct/intermediate symlinks, non-regular files, escapes, oversized input, and replacement races are incomplete collection.
    - Unsafe registry input emits `RINV_REGISTRY_UNREADABLE`, returns exit 2, leaves ownership unknown, and never copies rejected JSON fields or payload text into diagnostics.
    - Empty valid registry input remains a successful empty observation; null/unreadable input remains incomplete, preserving the flagged empty-input boundary.
    - Existing exact-match, overlap/adjacency, deterministic ordering, fact/disposition separation, renderer parity, and no-mutation tests remain green per D-01 through D-05 and D-13 through D-15.
  </behavior>
  <action>Write failing `ownership registry: symlinked external JSON exits 2 without disclosure` and non-regular/replacement cases in `scripts/repository_inventory.test.cjs`. Change `readRegistry` and `main` in `scripts/repository_inventory.cjs` to pass the collected `snapshot.root` into the shared bounded reader, parse only accepted in-repository bytes, and reduce unsafe diagnostics to path, reason class, and causal filesystem evidence rather than registry payload. Keep observed Git facts separate from the absent claim and preserve the CLI's single evaluate/render path. Implement D-01, D-02, D-03, D-04, D-05, D-13, D-14, and D-15.</action>
  <acceptance_criteria>
    - Direct and intermediate registry symlinks, directories/devices, oversized sources, and injected replacement races yield `RINV_REGISTRY_UNREADABLE` and process status 2.
    - External sentinel keys/values are absent from human and JSON output.
    - Valid exact claims, empty registry, ambiguous selectors, stable ordering, and both renderer paths retain their prior expected results.
    - Repository refs, indexes, worktree administration, tracked bytes, and fixture sentinels remain unchanged.
  </acceptance_criteria>
  <verify>
    <automated>node --test --test-name-pattern="ownership registry|same result|edge policy|deterministic|read-only" scripts/repository_inventory.test.cjs</automated>
    <fails_when>non-zero test exit, external registry data is accepted or rendered, unsafe input returns another exit class, established ambiguity/empty/ordering behavior regresses, or any mutation sentinel changes</fails_when>
  </verify>
  <done>Inventory ownership is evidence-backed only by bounded repository data; unsafe registry observation is causal, non-disclosing, deterministic, and incomplete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Repository path → filesystem object | A lexical in-repository name may traverse intermediate symlinks or be swapped between checks. |
| Ownership registry → inventory classifier/renderers | Registry JSON is untrusted policy input and rejected external content may contain secrets. |
| Planning files → authority resolver | Externally resolved content must not acquire D-06 authority or yield healthy routing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-31-15 | Tampering / Elevation of Privilege | `readBoundedRepositoryFile` and planning traversal | high | mitigate | Resolve root/candidate containment, reject symlink/non-regular sources, read/fstat one no-follow descriptor, and assert external authority yields exit 2. |
| T-31-16 | Information Disclosure | `readRegistry` diagnostics and renderers | high | mitigate | Never include rejected payload bytes; test external secret sentinels are absent from human/JSON output. |
| T-31-17 | Tampering / Repudiation | Registry-to-ownership classification | high | mitigate | Accept claims only from the bounded versioned registry and leave ownership unknown on any source error. |
| T-31-18 | Denial of Service | Bounded file reads | medium | mitigate | Enforce size bounds before allocation and preserve incomplete diagnostics for oversized/non-regular sources. |
| T-31-19 | Repudiation | Report-only execution | low | accept | Existing and expanded before/after manifests prove the local diagnostic tool performs no mutation; no state-changing command path exists. |
</threat_model>

<verification>
- `node --test scripts/repository_inventory.test.cjs scripts/planning_health.test.cjs` passes in under 60 seconds; any failing test blocks completion.
- `node scripts/planning_health.cjs --json` remains runnable on the real repository and any incomplete source is reported with exit 2 rather than a healthy/policy conclusion.
- `node scripts/repository_inventory.cjs --json` retains observed facts and deterministic diagnostics without exposing rejected registry bytes.
- `git diff --exit-code -- .planning/milestones` proves D-11 frozen archives remain untouched.
</verification>

<success_criteria>
- Both reproduced source-boundary blockers are closed by executable CLI fixtures.
- REPO-01/REPO-02/REPO-04 unsafe-source behavior implements D-03/D-04/D-06/D-08/D-14 and preserves D-01/D-02/D-05/D-09/D-15.
- All five relevant unresolved probe assumptions remain explicit and receive focused or full-suite execution evidence.
- No external content is trusted or disclosed, and no repository/planning state is changed.
</success_criteria>

<output>
Create `.planning/phases/31-repository-planning-truth/31-04-SUMMARY.md` when done.
</output>
