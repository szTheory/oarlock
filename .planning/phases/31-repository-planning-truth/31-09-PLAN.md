---
phase: 31-repository-planning-truth
plan: "09"
type: execute
wave: 7
depends_on:
  - 31-06
  - 31-07
  - 31-08
files_modified:
  - .github/workflows/ci.yml
  - scripts/ci_monitor.cjs
  - scripts/ci_monitor.test.cjs
  - scripts/prohibitions/enforce_phase31.cjs
  - scripts/prohibitions/enforce_phase31.test.cjs
autonomous: true
gap_closure: true
gap_ids:
  - G-31-1
  - G-31-2
  - G-31-3
  - G-31-4
  - G-31-5
  - G-31-6
requirements:
  - REPO-01
  - REPO-02
  - REPO-03
  - REPO-04
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Every push to main and pull request runs one required planning-truth lane containing all Phase 31 suites, non-vacuous bad/clean enforcement for all six prohibition descriptors, the base-to-head history guard, and a live JSON planning-health smoke."
    - "The required lane fails closed when any Phase 31 prohibition descriptor is absent or malformed, any declared target/fixture path is missing, a bad fixture does not produce a named assertion failure, or a clean fixture does not pass non-vacuously."
    - "Historical tags are explicitly provisioned before the live smoke; expected missing-tag exceptions remain explicit rather than fabricated."
    - "The always-running aggregate and exact-SHA monitor both require the named planning-truth job, so success for another SHA or an incomplete job set cannot satisfy the contract."
    - "Canonical re-verification can pass all six G-31 gaps without human-needed prohibition status."
  artifacts:
    - path: .github/workflows/ci.yml
      provides: "Required planning-truth CI job and aggregate dependency"
    - path: scripts/ci_monitor.cjs
      provides: "Exact-SHA hosted CI contract including planning truth"
    - path: scripts/ci_monitor.test.cjs
      provides: "Workflow and monitor regression proof for the new required lane"
    - path: scripts/prohibitions/enforce_phase31.cjs
      provides: "Repository-owned descriptor validator and non-vacuous bad/clean prohibition runner for all six stable Phase 31 IDs"
    - path: scripts/prohibitions/enforce_phase31.test.cjs
      provides: "Fail-closed descriptor, fixture, and TAP-result tests for recurring prohibition enforcement"
  key_links:
    - from: .github/workflows/ci.yml
      to: scripts/history_integrity.cjs
      via: "Event-derived base and github.sha head are passed to the cross-revision guard"
      pattern: "history_integrity\\.cjs.*--base.*--head"
    - from: .github/workflows/ci.yml
      to: scripts/planning_health.cjs
      via: "Live JSON smoke after historical tag provisioning"
      pattern: "planning_health\\.cjs --json"
    - from: scripts/ci_monitor.cjs
      to: .github/workflows/ci.yml
      via: "Required hosted job name matches planning-truth and CI contract names exactly"
      pattern: "planning truth"
    - from: .github/workflows/ci.yml
      to: scripts/prohibitions/enforce_phase31.cjs
      via: "Required planning-truth step validates and proves all six descriptors against their declared bad and clean subjects"
      pattern: "node scripts/prohibitions/enforce_phase31\\.cjs"
---

<objective>
Promote the Phase 31 automation into a recurring exact-SHA CI contract and remove the final human-UAT dependency.

Purpose: Make all six gap proofs durable on every proposed change while limiting CI expansion to the repository/planning truth checks that have recurring regression value.
Output: A required planning-truth job, historical tag and base-revision provisioning, live smoke, aggregate dependency, exact-SHA monitor update, and regression tests for the contract.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-repository-planning-truth/31-CONTEXT.md
@.planning/phases/31-repository-planning-truth/31-UAT.md
@.planning/phases/31-repository-planning-truth/31-06-SUMMARY.md
@.planning/phases/31-repository-planning-truth/31-07-SUMMARY.md
@.planning/phases/31-repository-planning-truth/31-08-SUMMARY.md
@.github/workflows/ci.yml
@scripts/ci_monitor.cjs
@scripts/ci_monitor.test.cjs
@scripts/history_integrity.cjs

<interfaces>
From scripts/ci_monitor.cjs:
- DEFAULT_REQUIRED_JOBS is the default exact-SHA hosted proof contract.
- assertCi(args, options) filters workflow runs by requested head SHA and requires every configured job conclusion.

From .github/workflows/ci.yml:
- `ci-contract` uses `needs`, `always()`, `github.sha`, workflow/run identity, and required job IDs to emit `ci-contract-proof.json`.

Required job naming contract:
- Workflow job id `planning-truth`, display name `planning truth`, and monitor string `planning truth` must remain aligned.

Phase 31 prohibition enforcement contract:
- The stable descriptor set is PROHIB-REPO-01-SAFETY, PROHIB-REPO-01-TRANSPARENCY, PROHIB-REPO-02-TRANSPARENCY, PROHIB-REPO-04-SAFETY, PROHIB-REPO-03-PRESERVATION, and PROHIB-REPO-03-IDENTITY.
- Plans 31-01 through 31-03 are the descriptor sources after Plans 31-06 through 31-08 resolve them; every node-test descriptor declares check_target, check_violation_fixture, and check_clean_fixture.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Run Phase 31 proof and live smoke in a required planning-truth lane</name>
  <files>.github/workflows/ci.yml, scripts/ci_monitor.test.cjs, scripts/prohibitions/enforce_phase31.cjs, scripts/prohibitions/enforce_phase31.test.cjs</files>
  <behavior>
    - Pull requests compare the candidate exact SHA to the fetched target-branch merge base; main pushes compare the event's prior SHA to github.sha, with explicit fail-closed handling when the base is unavailable or all-zero.
    - Checkout/history setup provides full history and explicitly fetches local historical tag refs before planning-health smoke.
    - The job retains the complete clean production suite and additionally runs descriptor-driven bad/clean enforcement for all six stable Phase 31 prohibition IDs before history-integrity base/head proof and `node scripts/planning_health.cjs --json`.
    - Enforcement validates each descriptor's resolved/test status, node-test kind, target, bad fixture, and clean fixture; it rejects missing paths, a vacuous/crash-only red, a bad subject that stays green, and a clean subject that is not a non-vacuous green.
    - No live smoke creates or guesses the documented absent v1.5 tag.
  </behavior>
  <action>Add a `planning-truth` job to push/main and pull_request CI using the existing SHA-pinned checkout action with full history. Add an explicit argument-array-safe tag fetch step for remote historical tags and a read-only assertion of the documented tag set while preserving the documented absent-tag identity as unknown per D-11/D-12. Create a dependency-free repository runner that parses the prohibition blocks in 31-01-PLAN.md through 31-03-PLAN.md, requires exactly the six stable IDs, validates every descriptor as resolved/test with `check_kind: node-test` plus existing target, violation-fixture, and clean-fixture paths, then invokes each target through bounded argument-array child processes: bad subject must yield non-vacuous TAP red from a named assertion and clean subject must yield non-vacuous TAP green. Its own node:test suite must cover the happy path and fail-closed malformed/missing descriptor, missing fixture, crash-only red, toothless bad fixture, and failing clean-control cases. Derive and validate an event-appropriate base SHA, then run the complete clean production command `node --test scripts/*.test.cjs scripts/prohibitions/*.test.cjs`, `node scripts/prohibitions/enforce_phase31.cjs`, the explicit base/head history guard, and the live planning-health JSON command. Keep this lane scoped to recurring Phase 31 truth proof; the broader Phase 33 CI redesign remains outside this gap closure. Extend the workflow-structure test to require every command, descriptor enforcement, tag/base provisioning, `contents: read`, and the exact job id/display name.</action>
  <verify>
    <automated>node --test scripts/ci_monitor.test.cjs scripts/prohibitions/enforce_phase31.test.cjs &amp;&amp; node --test scripts/*.test.cjs scripts/prohibitions/*.test.cjs &amp;&amp; node scripts/prohibitions/enforce_phase31.cjs &amp;&amp; node scripts/planning_health.cjs --json</automated>
  </verify>
  <done>A repository-defined planning-truth job retains the clean production suite, proves non-vacuous bad/clean behavior for all six validated prohibition descriptors, and reproduces the history guard plus live smoke against fully provisioned history/tags for the exact candidate SHA.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind planning truth into aggregate and exact-SHA hosted proof</name>
  <files>.github/workflows/ci.yml, scripts/ci_monitor.cjs, scripts/ci_monitor.test.cjs</files>
  <behavior>
    - `ci-contract.needs` and its internal required ID list include `planning-truth`; failure or skip makes the aggregate fail.
    - DEFAULT_REQUIRED_JOBS includes display name `planning truth` and monitor success requires that exact job for the requested SHA.
    - Fake hosted runs missing or failing planning truth are rejected; a fully green exact-SHA run is accepted.
    - CI contract proof records github.sha and the planning-truth result in required_jobs.
  </behavior>
  <action>Add `planning-truth` to both the aggregate `needs` graph and its in-script required job IDs, preserving `if: ${{ always() }}` and exact `github.sha` evidence. Add `planning truth` to `DEFAULT_REQUIRED_JOBS` and the fake-gh job matrix. Extend tests so missing/failed planning truth rejects the requested SHA and a wrong-SHA green run remains unusable. This is the recurring-value CI promotion requested for G-31-1 through G-31-6; do not claim hosted proof until an actual run for the candidate SHA exists.</action>
  <verify>
    <automated>node --test scripts/ci_monitor.test.cjs &amp;&amp; node -e 'const {DEFAULT_REQUIRED_JOBS}=require("./scripts/ci_monitor.cjs"); if(!DEFAULT_REQUIRED_JOBS.includes("planning truth")) process.exit(1)'</automated>
  </verify>
  <done>The required aggregate and monitor reject any exact-SHA run lacking successful planning truth, and the emitted aggregate proof names that lane for github.sha.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub event/ref metadata → CI base/head selection | Event values determine which committed history is compared. |
| Hosted job results → exact-SHA monitor | Remote workflow/job evidence must match the requested immutable SHA and complete job set. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-09-01 | Spoofing | CI run selection | high | mitigate | Retain exact headSha filtering and add tests that a green wrong-SHA run cannot satisfy planning truth. |
| T-31-09-02 | Tampering | history/tag provisioning | high | mitigate | Use read-only checkout/fetch with full history, validate base/head existence, and never create the known absent v1.5 tag. |
| T-31-09-03 | Elevation of Privilege | workflow permissions | medium | mitigate | Keep workflow permissions at contents: read and add no secrets or write scopes to planning truth. |
| T-31-09-04 | Repudiation | aggregate proof | medium | mitigate | Record exact github.sha, run identity, and every required job conclusion including planning-truth. |
| T-31-09-05 | Tampering | prohibition descriptors and fixtures | high | mitigate | Require the exact six stable IDs, validate every descriptor and referenced path, and prove named non-vacuous red on its bad subject plus green on its clean subject in required CI. |
</threat_model>

<source_coverage>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Accurate non-destructive repository, scope, and history truth | 31-06..31-09 | COVERED | Fail-first local proof plus recurring exact-SHA CI.
REQ | REPO-01 | Read-only complete inventory and disposition truth | 31-06, 31-09 | COVERED | G-31-1/G-31-2.
REQ | REPO-02 | One authority chain; decoys remain inert | 31-07, 31-09 | COVERED | G-31-3.
REQ | REPO-03 | Immutable, identity-honest milestone history | 31-08, 31-09 | COVERED | G-31-5/G-31-6.
REQ | REPO-04 | Non-mutating actionable planning health | 31-07..31-09 | COVERED | G-31-4 plus smoke/history diagnostics.
RESEARCH | — | Node/CommonJS, node:test, real temp Git repositories, collect/evaluate/render parity, no new dependencies | 31-06..31-09 | COVERED | Existing project stack retained.
RESEARCH | — | Stable Git plumbing, bounded subprocesses, report-only operation, real fixtures rather than mocks alone | 31-06..31-09 | COVERED | All new checks exercise behavior and fail closed.
CONTEXT | D-01..D-05 | Shared inventory truth, evidence-backed ownership, visible incomplete state, report-only safety | 31-06, 31-09 | COVERED | Inventory proof and CI.
CONTEXT | D-06..D-10 | Datum authority, conflict blocking, presence inertness, proposal-only repairs/mirror | 31-07, 31-09 | COVERED | Filesystem CLI proof and CI.
CONTEXT | D-11..D-12 | Frozen/additive history and independent identities | 31-08, 31-09 | COVERED | Cross-revision and identity proof.
CONTEXT | D-13 / D-14 / D-15 | Stable severity, actionable diagnostic records, and human/JSON parity | 31-06..31-09 | COVERED | Behavior matrices and live JSON smoke.
</source_coverage>

<verification>
- `node --test scripts/*.test.cjs scripts/prohibitions/*.test.cjs` passes locally as the retained clean production suite.
- `node scripts/prohibitions/enforce_phase31.cjs` validates exactly six descriptors and proves a non-vacuous bad/clean pair for each; its tests reject malformed descriptors, missing fixtures, crash-only reds, toothless bad fixtures, and failing clean controls.
- `node scripts/planning_health.cjs --json` exits 0 against the current repository with historical tags available and preserves the explicit v1.5 caveat.
- Workflow contract tests prove planning-truth is required by both `ci-contract` and `DEFAULT_REQUIRED_JOBS`, and exact-SHA filtering remains enforced.
- Re-running canonical Phase 31 verification after execution reports no human-needed prohibition and produces no UAT tests for G-31-1 through G-31-6.
</verification>

<success_criteria>
- All six stable UAT gaps have recurring automated proof on push/main and pull requests, including descriptor validation and non-vacuous bad/clean enforcement rather than clean-only prohibition runs.
- CI promotion is limited to Phase 31 checks with clear regression value; no broader Phase 33 implementation is claimed.
- Hosted green status remains tied to the exact candidate SHA and complete aggregate job set.
- Canonical verification can close Phase 31 with zero recurring human verification/UAT requirements.
</success_criteria>

<output>
Create `.planning/phases/31-repository-planning-truth/31-09-SUMMARY.md` when done.
</output>
