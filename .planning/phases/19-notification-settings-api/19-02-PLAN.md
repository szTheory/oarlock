---
phase: "19"
plan: "02"
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - lib/paddle/notification_settings.ex
  - test/paddle/notification_settings_test.exs
autonomous: true
requirements:
  - NOTIF-03
  - NOTIF-04
  - NOTIF-05
must_haves:
  truths:
    - User can create, update, and delete notification settings.
    - Create requires api_version as an explicit parameter.
  artifacts:
    - path: lib/paddle/notification_settings.ex
      provides: CRUD mutation operations
  key_links:
    - from: lib/paddle/notification_settings.ex
      to: HTTP POST /notification-settings
      via: request/4
    - from: lib/paddle/notification_settings.ex
      to: HTTP PATCH /notification-settings
      via: request/4
    - from: lib/paddle/notification_settings.ex
      to: HTTP DELETE /notification-settings
      via: request/4
---

<objective>
Implement the mutation operations (create, update, delete) for the Notification Settings API, maintaining strict, pure CRUD constraints.

Purpose: Allow developers to manage webhook destinations via code.
Output: create/3, update/3, and delete/2 functions added to NotificationSettings with robust testing.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-notification-settings-api/19-CONTEXT.md
</context>

<interfaces>
From lib/paddle/notification_setting.ex (Created in Wave 1):
```elixir
defmodule Paddle.NotificationSetting do
  defstruct [
    :id, :description, :type, :destination, :active, :api_version,
    :include_sensitive_fields, :subscribed_events, :endpoint_secret_key, :raw_data
  ]
end
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Create Operation</name>
  <files>lib/paddle/notification_settings.ex, test/paddle/notification_settings_test.exs</files>
  <read_first>lib/paddle/customers.ex, lib/paddle/notification_settings.ex, test/paddle/notification_settings_test.exs</read_first>
  <behavior>
    - Test 1: returns {:error, :missing_api_version} if api_version is omitted.
    - Test 2: create/3 sends POST request to /notification-settings with filtered JSON payload and returns %NotificationSetting{}.
    - Test 3: non-2xx API error maps to %Error{}.
  </behavior>
  <action>Add `create(%Client{} = client, attrs, opts \\ [])` following the analog pattern established in `lib/paddle/customers.ex` (e.g., `Paddle.Customers.create/3` payload filtering and `Http.request` usage). Define `@create_allowlist ~w(description destination type subscribed_events api_version include_sensitive_fields active)`. Validate `"api_version"` or `:api_version` is present in the normalized `attrs` (or `body`) before executing the HTTP request (return `{:error, :missing_api_version}` if missing). As per D-01 and D-03, DO NOT validate `destination` URL or `subscribed_events` values. Ensure the struct returned uses `Http.build_struct`.</action>
  <verify>
    <automated>mix test test/paddle/notification_settings_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - mix test test/paddle/notification_settings_test.exs exits 0
    - lib/paddle/notification_settings.ex contains def create(client, attrs, opts \\ [])
    - lib/paddle/notification_settings.ex handles missing api_version explicitly
  </acceptance_criteria>
  <done>create/3 is fully implemented and tested, specifically checking for api_version.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Update and Delete Operations</name>
  <files>lib/paddle/notification_settings.ex, test/paddle/notification_settings_test.exs</files>
  <read_first>lib/paddle/customers.ex, lib/paddle/notification_settings.ex, test/paddle/notification_settings_test.exs</read_first>
  <behavior>
    - Test 1: update/3 sends PATCH request to /notification-settings/:id with filtered JSON payload.
    - Test 2: delete/2 sends DELETE request to /notification-settings/:id and returns :ok.
  </behavior>
  <action>Add `update(%Client{} = client, id, attrs)` and `delete(%Client{} = client, id)` following the analog pattern established in `lib/paddle/customers.ex` (e.g., `Paddle.Customers.update/3` payload handling and `Http.request` usage). Define `@update_allowlist ~w(description destination active subscribed_events include_sensitive_fields)`. Use `encode_path_segment` for IDs if it was created in Task 1, or implement it. As per D-05, stick to pure CRUD (no `enable/2` or `disable/2` helpers). The delete operation should return `:ok` upon a successful HTTP response. Write comprehensive tests for both.</action>
  <verify>
    <automated>mix test test/paddle/notification_settings_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - mix test test/paddle/notification_settings_test.exs exits 0
    - lib/paddle/notification_settings.ex contains def update(client, id, attrs)
    - lib/paddle/notification_settings.ex contains def delete(client, id)
  </acceptance_criteria>
  <done>update/3 and delete/2 are fully implemented and tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Paddle API | Data traverses to external REST API, secured by TLS and bearer token. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-02 | Tampering | Paddle.NotificationSettings | mitigate | Ensure ID is URI encoded using `encode_path_segment` before injecting into URL on update and delete. |
| T-19-03 | Information Disclosure | Paddle.NotificationSettings | accept | We do not filter what developers pass in webhook configs; API enforces authorization. |
</threat_model>

<verification>
mix test test/paddle/notification_settings_test.exs
</verification>

<success_criteria>
- User can create, update, and delete notification settings via the API.
- Create request safely drops unknown keys and requires `api_version`.
- Tests mock and verify HTTP requests for create, update, and delete.
</success_criteria>

<output>
Create .planning/phases/19-notification-settings-api/19-02-SUMMARY.md when done
</output>
