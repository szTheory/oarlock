---
phase: "19"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/paddle/notification_setting.ex
  - test/paddle/notification_setting_test.exs
  - lib/paddle/notification_settings.ex
  - test/paddle/notification_settings_test.exs
autonomous: true
requirements:
  - NOTIF-01
  - NOTIF-02
must_haves:
  truths:
    - User can list and stream notification settings.
    - User can fetch a notification setting by ID.
    - User is warned that endpoint_secret_key is only returned on create.
    - User receives %Paddle.NotificationSetting{} structs.
  artifacts:
    - path: lib/paddle/notification_setting.ex
      provides: Struct definition
    - path: lib/paddle/notification_settings.ex
      provides: CRUD read operations
  key_links:
    - from: lib/paddle/notification_settings.ex
      to: HTTP GET /notification-settings
      via: request/4
---

<objective>
Implement the Paddle.NotificationSetting struct and the read-only operations for the Notification Settings API.

Purpose: Provide foundational typed representations and retrieval capabilities (list, stream, all, get).
Output: A complete struct module with tests, and a service module implementing pagination and fetch.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-notification-settings-api/19-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define NotificationSetting Struct</name>
  <files>lib/paddle/notification_setting.ex, test/paddle/notification_setting_test.exs</files>
  <read_first>lib/paddle/customer.ex, test/paddle/customer_test.exs, lib/paddle/notification_setting.ex, test/paddle/notification_setting_test.exs</read_first>
  <behavior>
    - Test 1: %NotificationSetting{} exposes expected promoted fields and raw_data.
    - Test 2: Paddle.Http.build_struct/2 correctly maps standard fields and places full payload in raw_data.
  </behavior>
  <action>Create `Paddle.NotificationSetting`. Define `@type t` and `defstruct` with fields: `:id, :description, :type, :destination, :active, :api_version, :include_sensitive_fields, :subscribed_events, :endpoint_secret_key, :raw_data`. Add a `@moduledoc` warning developers that `endpoint_secret_key` is only returned immediately upon creation and will be nil otherwise. Implement test coverage matching `customer_test.exs` patterns.</action>
  <verify>
    <automated>mix test test/paddle/notification_setting_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - mix test test/paddle/notification_setting_test.exs exits 0
    - lib/paddle/notification_setting.ex contains defstruct [:id, :description,
  </acceptance_criteria>
  <done>Struct is complete, tested, and documented regarding endpoint_secret_key.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Read Operations</name>
  <files>lib/paddle/notification_settings.ex, test/paddle/notification_settings_test.exs</files>
  <read_first>lib/paddle/products.ex, test/paddle/customers_test.exs, lib/paddle/notification_settings.ex, test/paddle/notification_settings_test.exs</read_first>
  <behavior>
    - Test 1: get/2 sends GET request to /notification-settings/:id and returns %NotificationSetting{}.
    - Test 2: list/2 sends GET request to /notification-settings with query params, returning %Page{}.
    - Test 3: stream/2 auto-paginates.
  </behavior>
  <action>Create `Paddle.NotificationSettings`. Define `@list_allowlist ~w(after id order_by per_page)`. Implement `get(%Client{} = client, id)`, `list(client, params \\ [])`, `stream(client, params \\ [])`, `all(client, params \\ [])`. Use `Paddle.Internal.Pagination` for pagination logic. Do not implement regex validation for IDs. Add standard test coverage using `client_with_adapter` (referencing `customers_test.exs` patterns).</action>
  <verify>
    <automated>mix test test/paddle/notification_settings_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - mix test test/paddle/notification_settings_test.exs exits 0
    - lib/paddle/notification_settings.ex contains def get(
    - lib/paddle/notification_settings.ex contains def list(
    - lib/paddle/notification_settings.ex contains def stream(
    - lib/paddle/notification_settings.ex contains def all(
  </acceptance_criteria>
  <done>Read operations are complete and returning typed structs and pages.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Paddle API | Data traverses to external REST API, secured by TLS and bearer token. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering | Paddle.NotificationSettings | mitigate | Ensure ID is URI encoded using `encode_path_segment` before injecting into URL. |
</threat_model>

<verification>
mix test test/paddle/notification_setting_test.exs
mix test test/paddle/notification_settings_test.exs
</verification>

<success_criteria>
- User can list, stream, and get notification settings.
- Tests pass for struct definition and read operations.
- `api_version` and `endpoint_secret_key` fields exist in the struct.
</success_criteria>

<output>
Create .planning/phases/19-notification-settings-api/19-01-SUMMARY.md when done
</output>
