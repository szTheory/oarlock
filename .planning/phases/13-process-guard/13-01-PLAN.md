---
phase: 13-process-guard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/check_summary_drift.sh
  - bin/install_hooks.sh
  - mix.exs
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - PROC-01
  - PROC-02
must_haves:
  truths:
    - "A local Git commit is aborted if a SUMMARY.md file is staged while untracked or unstaged files exist."
    - "Developers receive the pre-commit hook seamlessly when running `mix setup`."
    - "The CI pipeline fails if the working tree is dirty after the build completes."
  artifacts:
    - path: "bin/check_summary_drift.sh"
      provides: "Bash script to check working tree state against staged SUMMARY files locally and clean state in CI"
    - path: "bin/install_hooks.sh"
      provides: "Bash script to symlink the drift check script to .git/hooks/pre-commit"
  key_links:
    - from: "mix.exs"
      to: "bin/install_hooks.sh"
      via: "aliases / setup task"
    - from: ".github/workflows/ci.yml"
      to: "bin/check_summary_drift.sh"
      via: "CI job step"
---

<objective>
Implement a process guard to prevent drift between phase SUMMARY.md claims and actual git state by adding a fast bash-based pre-commit hook, an automatic installer via `mix setup`, and a parallel CI gate.

Purpose: Prevent the v1.1 audit-trail recurrence vector where summary files were committed without the corresponding codebase changes.
Output: Two bash scripts, a `setup` alias in `mix.exs`, and an added step in `.github/workflows/ci.yml`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-process-guard/13-CONTEXT.md
@.planning/phases/13-process-guard/13-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create git hook and installer scripts</name>
  <files>bin/check_summary_drift.sh, bin/install_hooks.sh</files>
  <action>
    Create `bin/check_summary_drift.sh` as a bash script. It must check if running in CI (via `"$CI" = "true"` or `"$GITHUB_ACTIONS" = "true"`). If in CI, it must fail if `git status --porcelain` is not empty. If local, it must check if `git diff --cached --name-only | grep -qE '.*-SUMMARY\.md$'`. If a summary is staged, it must check for dirty files using `git status --porcelain | grep -E '^(.[^ ])'`. If any exist, print a loud, colorful error message and exit 1 (per D-01 and user discretion).
    Create `bin/install_hooks.sh` to `mkdir -p .git/hooks` and symlink `../../bin/check_summary_drift.sh` to `.git/hooks/pre-commit` (per D-02).
    Both files must be made executable (`chmod +x`).
  </action>
  <verify>
    <automated>bash -c "[ -x bin/check_summary_drift.sh ] && [ -x bin/install_hooks.sh ]"</automated>
  </verify>
  <done>Executable bash scripts created with correct logic</done>
</task>

<task type="auto">
  <name>Task 2: Wire hook distribution into mix.exs</name>
  <files>mix.exs</files>
  <action>
    Modify `mix.exs` to include an `aliases` block. Add `aliases: aliases()` to `def project do` and define the private `aliases/0` function (e.g. near `deps/0`). The alias must be: `setup: ["deps.get", "cmd ./bin/install_hooks.sh"]` (per D-03).
  </action>
  <verify>
    <automated>mix setup</automated>
  </verify>
  <done>Hook installed automatically when running mix setup</done>
</task>

<task type="auto">
  <name>Task 3: Integrate process guard into CI</name>
  <files>.github/workflows/ci.yml</files>
  <action>
    Modify `.github/workflows/ci.yml`. Add a new step named `Check for SUMMARY/git-state drift` in the `test` job (after `Run tests`). The step must execute `./bin/check_summary_drift.sh` (per D-04). This ensures any files generated during tests or compilation fail the pipeline if they remain uncommitted.
  </action>
  <verify>
    <automated>grep "Check for SUMMARY/git-state drift" .github/workflows/ci.yml</automated>
  </verify>
  <done>CI pipeline configured to fail on working tree drift</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer → Local Env | Local git hook enforces clean commits but can be bypassed. |
| GitHub Actions → Production | CI acts as the immutable gate preventing bypassed hooks from landing in the main branch. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01   | Repudiation | Local Git Hook | mitigate | Run identical script in CI pipeline to prevent bypassed hooks from persisting. |
| T-13-02   | Tampering | bin/*.sh | mitigate | Scripts are tracked in Git, PRs are reviewed, CI guarantees immutable verification. |
</threat_model>

<verification>
- Create a dummy untracked file (`touch dummy.txt`).
- Attempt to `git add .planning/ROADMAP.md` (or another file), then mock staging a summary file.
- Run `bin/check_summary_drift.sh` manually and verify it exits 1 with an error.
- Remove `dummy.txt` and verify it exits 0.
</verification>

<success_criteria>
- Drift check script detects untracked/unstaged files locally when a summary is staged.
- Installer correctly symlinks to `.git/hooks/pre-commit`.
- `mix setup` installs the hook.
- `ci.yml` invokes the script to gate dirty CI pipelines.
</success_criteria>

<output>
Create `.planning/phases/13-process-guard/13-01-SUMMARY.md` when done
</output>
