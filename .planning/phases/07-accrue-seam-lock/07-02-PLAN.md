---
phase: 07-accrue-seam-lock
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/accrue-seam.md
  - lib/paddle/http.ex
  - lib/paddle/http/telemetry.ex
  - lib/paddle.ex
autonomous: true
requirements:
  - SEAM-02
must_haves:
  truths:
    - "The Accrue seam guide is the canonical published contract and uses the locked vocabulary `locked`, `additive`, and `opaque`."
    - "The guide explicitly enumerates the closed public seam: entry modules, support types, locked structs, and exclusion buckets."
    - "Generated docs publish the seam guide while excluding internal modules and the placeholder root `Paddle` module from the consumer API surface."
    - "The guide documents `:raw_data` as a locked escape hatch whose contents are opaque, not as an additive field."
  artifacts:
    - path: "guides/accrue-seam.md"
      provides: "Canonical consumer contract guide"
    - path: "lib/paddle/http.ex"
      provides: "Internal transport module hidden from published docs"
    - path: "lib/paddle/http/telemetry.ex"
      provides: "Internal telemetry hook module hidden from published docs"
    - path: "lib/paddle.ex"
      provides: "Placeholder root module hidden from published docs"
  key_links:
    - from: "guides/accrue-seam.md"
      to: "doc/accrue-seam.html"
      via: "ExDoc extras publication already wired through `mix.exs`"
      pattern: "Accrue Seam Contract"
    - from: "lib/paddle/http.ex"
      to: "doc/api-reference.md"
      via: "`@moduledoc false` suppresses internal modules from generated API reference"
      pattern: "Paddle\\.Http"
---

<objective>
Publish the Accrue seam contract as the canonical guide and seal the generated docs surface so only the enumerated consumer contract is visible.

Purpose: satisfy SEAM-02 by making the guide authoritative, vocabulary-accurate, and aligned with a closed published seam.
Output: rewritten `guides/accrue-seam.md`, hidden internal/placeholder modules, and regenerated docs output that includes the guide and excludes leaked internals.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-accrue-seam-lock/07-CONTEXT.md
@.planning/phases/07-accrue-seam-lock/07-RESEARCH.md
@.planning/phases/07-accrue-seam-lock/07-PATTERNS.md
@guides/accrue-seam.md
@mix.exs
@lib/paddle/http.ex
@lib/paddle/http/telemetry.ex
@lib/paddle.ex
@lib/paddle/client.ex
@lib/paddle/page.ex
@lib/paddle/error.ex
@doc/api-reference.md
@README.md

<interfaces>
Current ExDoc wiring that must remain intact:
```elixir
docs: [
  extras: ["guides/accrue-seam.md"],
  groups_for_extras: [
    Guides: ~r/guides\\//
  ]
]
```

Current leaked API reference entries that must disappear after `mix docs`:
```markdown
- [Paddle](Paddle.md): Documentation for `Paddle`.
- [Paddle.Http](Paddle.Http.md)
- [Paddle.Http.Telemetry](Paddle.Http.Telemetry.md)
```

Current hidden-module pattern to copy:
```elixir
defmodule Paddle.Internal.Attrs do
  @moduledoc false
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite `guides/accrue-seam.md` so it exactly matches D-06 through D-19</name>
  <read_first>
    guides/accrue-seam.md
    .planning/phases/07-accrue-seam-lock/07-CONTEXT.md
    .planning/phases/07-accrue-seam-lock/07-RESEARCH.md
    .planning/phases/07-accrue-seam-lock/07-PATTERNS.md
    .planning/PROJECT.md
    README.md
    mix.exs
  </read_first>
  <files>guides/accrue-seam.md</files>
  <action>Rewrite the existing guide in place; do not create a second guide file. Preserve the current high-level shape (vocabulary -> public modules -> locked structs -> support/error contract -> exclusions), but make the text and tables match the locked decisions exactly.

Make these concrete content changes:
- replace every public `raw` field-tier label with `opaque` (D-14)
- remove `not-planned` as a field tier entirely (D-16)
- keep only the public vocabulary `locked`, `additive`, `opaque` in the stability section (D-11 through D-14)
- add an explicit boundary statement that only documented modules, functions, structs, and support types are supported and undocumented internals may change without notice in 0.x (D-06, D-09, D-10)
- explicitly enumerate the public entry modules already in use: `Paddle.Customers`, `Paddle.Customers.Addresses`, `Paddle.Transactions`, `Paddle.Subscriptions`, `Paddle.Webhooks` (D-07)
- add a dedicated support-types section that documents `Paddle.Client.new!/1`, `%Paddle.Page{}`, `Paddle.Page.next_cursor/1`, and `%Paddle.Error{}` as part of the seam (D-08)
- rewrite every `:raw_data` row so the field tier is `locked` and the notes say the field is the forward-compat escape hatch whose contents are `opaque` (D-15)
- keep `additive` only where the guide intentionally allows growth without changing meaning; do not use it as a synonym for forwarded provider maps (D-13)
- rename the exclusions section into exactly two buckets: `Out of scope for the current 0.x seam` and `Intentionally excluded from core` (D-17)
- remove public-facing `deferred`, `not planned`, or roadmap-promise wording from unsupported-surface sections (D-18, D-19)

Do not broaden the seam to internal modules, `%Paddle.Client{}` internals such as `req`, undocumented helpers, or additional product/API surfaces. Keep README as a lightweight pointer only unless the current guide title changes enough to require adjusting the link label; if the existing README pointer still works, leave README unchanged.
  </action>
  <acceptance_criteria>
    - `rg -n '`"`'opaque'`"`' guides/accrue-seam.md` returns at least 3 matches.
    - `! rg -n '`"`'raw'`"`'|`"`'not-planned'`"`'' guides/accrue-seam.md` exits 0.
    - `rg -n 'Paddle\\.Client\\.new!/1|%Paddle\\.Page\\{|Paddle\\.Page\\.next_cursor/1|%Paddle\\.Error\\{' guides/accrue-seam.md` returns at least 4 matches total.
    - `rg -n 'Out of scope for the current 0\\.x seam|Intentionally excluded from core' guides/accrue-seam.md` returns exactly 2 matches.
    - `rg -n ':raw_data.*`"`'locked'`"`'' guides/accrue-seam.md` returns at least 6 matches across the field tables.
    - `rg -n 'Only explicitly documented modules, functions, structs, and support types are supported|undocumented .* may change without notice' guides/accrue-seam.md` returns at least 1 match for each policy sentence.
  </acceptance_criteria>
  <verify>
    <automated>mix docs</automated>
  </verify>
  <done>The guide is now the canonical public contract and its vocabulary, field tiers, support types, and exclusions all match the locked Phase 07 decisions exactly.</done>
</task>

<task type="auto">
  <name>Task 2: Hide leaked internal modules from the generated docs surface without changing runtime behavior</name>
  <read_first>
    lib/paddle/http.ex
    lib/paddle/http/telemetry.ex
    lib/paddle.ex
    lib/paddle/internal/attrs.ex
    mix.exs
    doc/api-reference.md
    .planning/phases/07-accrue-seam-lock/07-RESEARCH.md
  </read_first>
  <files>lib/paddle/http.ex, lib/paddle/http/telemetry.ex, lib/paddle.ex</files>
  <action>Add `@moduledoc false` at the top of `lib/paddle/http.ex`, `lib/paddle/http/telemetry.ex`, and `lib/paddle.ex`, copying the exact hidden-module pattern already used by `Paddle.Internal.Attrs`.

Preserve runtime behavior in those files:
- do not rename `Paddle.Http.request/4` or `build_struct/2`
- do not change the telemetry step names or attachment pipeline in `Paddle.Http.Telemetry.attach/1`
- do not introduce new public docs surface for the placeholder `Paddle` root module

After the source edits, run `mix docs`. The ExDoc extras wiring in `mix.exs` already points at `guides/accrue-seam.md`; leave that block unchanged unless it has drifted from the exact snippet in context. Verify both sides of the closed seam boundary:
- the regenerated API reference no longer lists `Paddle`, `Paddle.Http`, or `Paddle.Http.Telemetry`
- the regenerated API reference still lists exactly the supported public modules: `Paddle.Address`, `Paddle.Client`, `Paddle.Customer`, `Paddle.Customers`, `Paddle.Customers.Addresses`, `Paddle.Event`, `Paddle.Page`, `Paddle.Subscription`, `Paddle.Subscription.ManagementUrls`, `Paddle.Subscription.ScheduledChange`, `Paddle.Subscriptions`, `Paddle.Transaction`, `Paddle.Transaction.Checkout`, `Paddle.Transactions`, `Paddle.Webhooks`, plus exception entry `Paddle.Error`
- the guide page still renders as `doc/accrue-seam.html`
  </action>
  <acceptance_criteria>
    - `rg -n '@moduledoc false' lib/paddle/http.ex lib/paddle/http/telemetry.ex lib/paddle.ex` returns exactly 3 matches.
    - `mix docs` exits 0.
    - `test -f doc/accrue-seam.html` exits 0.
    - `! rg -n 'Paddle\\.Http|Paddle\\.Http\\.Telemetry|^- \\[Paddle\\]' doc/api-reference.md` exits 0.
    - `rg -n '^- \\[Paddle\\.Address\\]|^- \\[Paddle\\.Client\\]|^- \\[Paddle\\.Customer\\]|^- \\[Paddle\\.Customers\\]|^- \\[Paddle\\.Customers\\.Addresses\\]|^- \\[Paddle\\.Event\\]|^- \\[Paddle\\.Page\\]|^- \\[Paddle\\.Subscription\\]|^- \\[Paddle\\.Subscription\\.ManagementUrls\\]|^- \\[Paddle\\.Subscription\\.ScheduledChange\\]|^- \\[Paddle\\.Subscriptions\\]|^- \\[Paddle\\.Transaction\\]|^- \\[Paddle\\.Transaction\\.Checkout\\]|^- \\[Paddle\\.Transactions\\]|^- \\[Paddle\\.Webhooks\\]|^  - \\[Paddle\\.Error\\]' doc/api-reference.md` returns exactly 16 matches.
    - `test \"$(rg -c '^- \\[Paddle\\.' doc/api-reference.md)\" = \"15\"` exits 0.
    - `rg -n 'Accrue Seam Contract' doc/accrue-seam.md doc/index.html` returns at least 1 match.
    - `rg -n 'extras: \\[\"guides/accrue-seam.md\"\\]' mix.exs` returns exactly 1 match.
  </acceptance_criteria>
  <verify>
    <automated>mix docs</automated>
    <automated>rg -n '^- \\[Paddle\\.Address\\]|^- \\[Paddle\\.Client\\]|^- \\[Paddle\\.Customer\\]|^- \\[Paddle\\.Customers\\]|^- \\[Paddle\\.Customers\\.Addresses\\]|^- \\[Paddle\\.Event\\]|^- \\[Paddle\\.Page\\]|^- \\[Paddle\\.Subscription\\]|^- \\[Paddle\\.Subscription\\.ManagementUrls\\]|^- \\[Paddle\\.Subscription\\.ScheduledChange\\]|^- \\[Paddle\\.Subscriptions\\]|^- \\[Paddle\\.Transaction\\]|^- \\[Paddle\\.Transaction\\.Checkout\\]|^- \\[Paddle\\.Transactions\\]|^- \\[Paddle\\.Webhooks\\]|^  - \\[Paddle\\.Error\\]' doc/api-reference.md</automated>
    <automated>test \"$(rg -c '^- \\[Paddle\\.' doc/api-reference.md)\" = \"15\"</automated>
  </verify>
  <done>The published docs surface now shows the seam guide and consumer-facing modules only; internal transport modules and the placeholder root module no longer appear in `doc/api-reference.md`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source modules -> generated docs | ExDoc turns source metadata into the published public contract, so leaked modules become accidental surface area |
| guide text -> consumer expectations | The seam guide is the canonical contract; inaccurate tier vocabulary or omissions mislead consumers into depending on the wrong behavior |
| support types -> end-user API surface | Support types must be documented explicitly without promoting internal `%Paddle.Client{}` internals or helper modules |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06 | I | generated ExDoc API reference | mitigate | Add `@moduledoc false` to `Paddle.Http`, `Paddle.Http.Telemetry`, and `Paddle` so `mix docs` no longer publishes those modules in `doc/api-reference.md`. |
| T-07-07 | T | `guides/accrue-seam.md` field tiers | mitigate | Replace `raw`/`not-planned` terminology with the locked `locked`/`additive`/`opaque` vocabulary and make every `:raw_data` row explicitly `locked` with opaque contents. |
| T-07-08 | E | public seam boundary | mitigate | Add explicit guide language that only documented modules, functions, structs, and support types are supported; exclude `%Paddle.Client{}` internals and undocumented helpers from the seam. |
| T-07-09 | R | docs publication fidelity | mitigate | Verify both the presence of `doc/accrue-seam.html` and the absence of leaked internal modules in `doc/api-reference.md` after `mix docs`. |
| T-07-10 | D | contract drift between guide and tests | mitigate | Publish the guide first in Wave 1 so the Wave 2 seam-test plan has a stable source of truth and cannot freeze unsupported fields by accident. |
</threat_model>

<verification>
- `mix docs`
- `rg -n 'Paddle\\.Client\\.new!/1|%Paddle\\.Page\\{|Paddle\\.Page\\.next_cursor/1|%Paddle\\.Error\\{' guides/accrue-seam.md`
- `! rg -n 'Paddle\\.Http|Paddle\\.Http\\.Telemetry|^- \\[Paddle\\]' doc/api-reference.md`
</verification>

<success_criteria>
- `guides/accrue-seam.md` uses the locked public vocabulary and documents the closed seam explicitly.
- The guide includes D-08 support types and the two required exclusion buckets.
- `lib/paddle/http.ex`, `lib/paddle/http/telemetry.ex`, and `lib/paddle.ex` are hidden from published docs with no runtime behavior changes.
- `mix docs` produces `doc/accrue-seam.html` and an API reference that no longer leaks internal modules or the placeholder root module.
</success_criteria>

<output>
After completion, create `.planning/phases/07-accrue-seam-lock/07-02-SUMMARY.md`
</output>
