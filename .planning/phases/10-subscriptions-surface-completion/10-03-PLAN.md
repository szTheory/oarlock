---
phase: 10-subscriptions-surface-completion
plan: 03
type: execute
wave: 2
depends_on: ["10-01", "10-02"]
files_modified:
  - lib/paddle/subscriptions.ex
  - test/paddle/subscriptions_test.exs
  - test/paddle/subscription_test.exs
  - test/paddle/seam_test.exs
  - guides/accrue-seam.md
autonomous: true
requirements: [SUB-04, SUB-05, SUB-06]
requirements_addressed: [SUB-04, SUB-05, SUB-06]
tags: [elixir, paddle, subscriptions, resume, seam, docs]

must_haves:
  truths:
    - "`Paddle.Subscriptions.resume/3` exists, defaults omitted `effective_from` to immediate behavior, and validates `effective_from:` / `on_resume:` at the SDK boundary per D-13 through D-17."
    - "Resume supports only `effective_from:`, `on_resume:`, and `retry:` on the public opts list; `idempotency_key:` is rejected loudly and unsupported keys are not silently dropped per D-20 through D-23."
    - "The `%Paddle.Subscription{}` locked field set is unchanged; seam regressions fail if a locked field is added, removed, or renamed, and extra provider payload remains available through `raw_data` per D-24 and D-26."
    - "The public seam guide lists `pause/3`, `pause_immediately/3`, and `resume/3`, removes stale 'pause/resume out of scope' language, and keeps direct subscription creation out of `Paddle.Subscriptions` per D-27."
    - "The end-to-end seam journey remains transaction-driven for recurring start, adds pause/resume additively, and preserves typed subscription hydration at the boundary."
  artifacts:
    - path: "lib/paddle/subscriptions.ex"
      provides: "`resume/3`, resume-path construction, resume opt normalization, and any private helper reuse needed to share lifecycle/request opt splitting with pause."
      contains: "def resume(%Client{} = client, subscription_id, opts \\\\ [])"
    - path: "test/paddle/subscriptions_test.exs"
      provides: "Adapter-backed resume coverage for request body, default immediate semantics, retry forwarding, idempotency rejection, and provider error passthrough."
      contains: "describe \"resume/3\""
    - path: "test/paddle/subscription_test.exs"
      provides: "Strict `%Paddle.Subscription{}` key-set regression to protect the locked seam."
      contains: "Map.keys(%Subscription{})"
    - path: "test/paddle/seam_test.exs"
      provides: "Expanded public seam journey that exercises transaction-driven start plus pause/resume without introducing a direct-create surface."
      contains: "Paddle.Subscriptions.resume"
    - path: "guides/accrue-seam.md"
      provides: "Updated public seam contract for transaction-driven recurring start and the completed pause/resume surface."
      contains: "pause_immediately"
  key_links:
    - from: "lib/paddle/subscriptions.ex"
      to: "test/paddle/subscriptions_test.exs"
      via: "Resume request/response tests lock `effective_from`, `on_resume`, `retry:`, and `idempotency_key:` handling."
      pattern: "resume"
    - from: "test/paddle/subscription_test.exs"
      to: "guides/accrue-seam.md"
      via: "The locked key-set regression backs the guide's `%Paddle.Subscription{}` field contract."
      pattern: "raw_data"
    - from: "test/paddle/seam_test.exs"
      to: "guides/accrue-seam.md"
      via: "The seam journey and guide enumerate the same public `Paddle.Subscriptions` surface."
      pattern: "Paddle.Subscriptions"
---

<objective>
Finish Phase 10 by adding resume semantics and hardening the public seam around the completed subscription lifecycle surface.

Purpose: Close SUB-06 and the remaining seam/documentation delta without breaking the locked v1.1 subscription contract. This plan makes resume truthful, keeps transaction-driven recurring start explicit, and upgrades the seam tests so future additions cannot quietly reintroduce a false direct-create surface or drift the `%Paddle.Subscription{}` shape.

Output: `resume/3` in `lib/paddle/subscriptions.ex`, resume/seam regression coverage across the three subscription test files, and an updated `guides/accrue-seam.md`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-subscriptions-surface-completion/10-CONTEXT.md
@.planning/phases/10-subscriptions-surface-completion/10-RESEARCH.md
@.planning/phases/10-subscriptions-surface-completion/10-PATTERNS.md
@.planning/milestones/archived-phases/05-subscriptions-management/05-01-SUMMARY.md
@.planning/milestones/archived-phases/07-accrue-seam-lock/07-01-SUMMARY.md
@.planning/milestones/archived-phases/07-accrue-seam-lock/07-02-SUMMARY.md
@.planning/phases/10-subscriptions-surface-completion/10-02-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add resume, struct-shape, and seam-regression tests before implementation</name>
  <files>test/paddle/subscriptions_test.exs, test/paddle/subscription_test.exs, test/paddle/seam_test.exs</files>
  <read_first>
    - test/paddle/subscriptions_test.exs
    - test/paddle/subscription_test.exs
    - test/paddle/seam_test.exs
    - lib/paddle/subscriptions.ex
    - guides/accrue-seam.md
    - .planning/phases/10-subscriptions-surface-completion/10-CONTEXT.md
    - .planning/phases/10-subscriptions-surface-completion/10-RESEARCH.md
    - .planning/phases/10-subscriptions-surface-completion/10-PATTERNS.md
  </read_first>
  <behavior>
    - `resume/3` posts to `/subscriptions/{id}/resume` and defaults missing `effective_from:` to `"immediately"`.
    - `effective_from:` accepts `:immediately`, RFC3339 binaries, and `DateTime.t()` values; invalid values return a local validation error.
    - `on_resume:` accepts the two allowed atoms or provider strings; invalid values return a local validation error.
    - `idempotency_key:` raises `ArgumentError` before the adapter runs.
    - `retry: false` disables retries on resume when exercised through a retry-enabled client helper.
    - Provider state errors such as missing payment method or invalid continue-existing-billing-period behavior pass through unchanged as `%Paddle.Error{}`.
    - `%Paddle.Subscription{}` key-set regression fails if any locked field is added, removed, or renamed.
    - The seam journey uses `pause/3` and `resume/3` additively while still starting recurring billing through transactions and canonical subscription fetches.
  </behavior>
  <action>
    Add `describe "resume/3"` to `test/paddle/subscriptions_test.exs` with adapter-backed success, validation, retry, idempotency rejection, and provider-error passthrough cases per D-13 through D-17 and D-20 through D-25.

    In `test/paddle/subscription_test.exs`, replace the weak empty-struct pattern-only regression with an exact key-set assertion built from `Map.keys(%Subscription{}) |> Enum.reject(&(&1 == :__struct__)) |> Enum.sort()`. Compare it against the locked field list from `guides/accrue-seam.md` so the test fails on added, removed, or renamed locked keys per D-24.

    Extend `test/paddle/seam_test.exs` so the public seam journey remains transaction-driven (per SUB-04) but also exercises `Paddle.Subscriptions.pause/3` and `Paddle.Subscriptions.resume/3` before the existing cancellation step. Keep the assertions at the public typed boundary only: selected fields, nested typed structs, and `raw_data` presence per D-26.
  </action>
  <verify>
    <automated>mix test test/paddle/subscriptions_test.exs test/paddle/subscription_test.exs test/paddle/seam_test.exs --color</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'describe "resume/3"' test/paddle/subscriptions_test.exs` returns a hit.
    - `grep -F 'Map.keys(%Subscription{})' test/paddle/subscription_test.exs` returns a hit.
    - `grep -F 'Paddle.Subscriptions.resume' test/paddle/seam_test.exs` returns a hit.
    - `grep -F 'idempotency_key' test/paddle/subscriptions_test.exs` returns new resume rejection coverage.
    - The initial targeted test run fails because `resume/3` and/or the strengthened seam assertions are not implemented yet.
  </acceptance_criteria>
  <done>
    Resume semantics and the final seam shape are pinned in failing tests before production code or guide edits begin.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement resume/3 without widening the lifecycle mutation seam</name>
  <files>lib/paddle/subscriptions.ex, test/paddle/subscriptions_test.exs</files>
  <read_first>
    - lib/paddle/subscriptions.ex
    - test/paddle/subscriptions_test.exs
    - .planning/phases/10-subscriptions-surface-completion/10-CONTEXT.md
    - .planning/phases/10-subscriptions-surface-completion/10-RESEARCH.md
    - .planning/phases/10-subscriptions-surface-completion/10-PATTERNS.md
    - .planning/phases/10-subscriptions-surface-completion/10-02-PLAN.md
  </read_first>
  <behavior>
    - `resume/3` returns `{:ok, %Paddle.Subscription{}}`, `{:error, %Paddle.Error{}}`, or local validation tuples.
    - Resume reuses the same typed hydration path as get/cancel/pause.
    - Resume keeps lifecycle opts out of Req and keeps unsupported request opts out of the public surface.
  </behavior>
  <action>
    In `lib/paddle/subscriptions.ex`, add `def resume(%Client{} = client, subscription_id, opts \\\\ [])` per D-13. Keep this as the only new public resume entry point and do not add `resume_immediately/*`, `resume_at/*`, or command-struct variants per D-16. Route it through a private helper such as `do_resume/3` and add `resume_path/1` for `/subscriptions/{id}/resume`.

    Default omitted `effective_from:` to `"immediately"` per D-14. Accept `:immediately`, `DateTime.t()`, and RFC3339 binaries for `effective_from:`; return `{:error, :invalid_effective_from}` otherwise. Reuse the same `on_resume` normalization rules as pause and return `{:error, :invalid_on_resume}` for invalid values.

    Keep the public mutation opts vocabulary narrow per D-20 through D-23. Forward only `retry:` to `Http.request/4`, reject `idempotency_key:` with `ArgumentError`, and raise `ArgumentError` on any other unsupported opt key rather than silently dropping it. Success responses must pass through `build_subscription/1` unchanged so `status`, `paused_at`, `scheduled_change`, `management_urls`, and `raw_data` remain typed and hydrated.

    If private helper reuse with pause is cleaner, refactor only within `lib/paddle/subscriptions.ex` and keep all helpers private. Do not widen the public module surface beyond the single new `resume/3` function.
  </action>
  <verify>
    <automated>mix test test/paddle/subscriptions_test.exs test/paddle/subscription_test.exs test/paddle/seam_test.exs --color</automated>
    <automated>mix test --color</automated>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'def resume(%Client{} = client, subscription_id, opts \\\\ [])' lib/paddle/subscriptions.ex` returns a hit.
    - `grep -F 'resume_path' lib/paddle/subscriptions.ex` returns a hit.
    - `grep -F 'invalid_effective_from' lib/paddle/subscriptions.ex` returns a hit.
    - `grep -F 'invalid_on_resume' lib/paddle/subscriptions.ex` returns a hit in the shared or resume-specific validator.
    - `grep -F 'idempotency_key' lib/paddle/subscriptions.ex` returns a hit in the resume rejection path.
    - `mix test test/paddle/subscriptions_test.exs test/paddle/subscription_test.exs test/paddle/seam_test.exs --color` exits 0.
    - `mix test --color` exits 0.
    - `mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>
    SUB-06 is implemented additively with explicit immediate-default resume semantics, strict opt validation, and no false idempotency surface.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update the canonical Accrue seam guide for the completed lifecycle surface</name>
  <files>guides/accrue-seam.md</files>
  <read_first>
    - guides/accrue-seam.md
    - test/paddle/subscription_test.exs
    - test/paddle/seam_test.exs
    - .planning/phases/10-subscriptions-surface-completion/10-CONTEXT.md
    - .planning/phases/10-subscriptions-surface-completion/10-RESEARCH.md
    - .planning/milestones/archived-phases/07-accrue-seam-lock/07-02-SUMMARY.md
  </read_first>
  <action>
    Update `guides/accrue-seam.md` per D-24 through D-27. Under `Paddle.Transactions`, correct the public entry to `create(client, attrs, opts \\ [])` and note that recurring subscriptions start through the transaction seam plus checkout/manual collection and canonical fetches. Under `Paddle.Subscriptions`, add `pause(client, subscription_id, opts \\ [])`, `pause_immediately(client, subscription_id, opts \\ [])`, and `resume(client, subscription_id, opts \\ [])` with their tagged-tuple contracts.

    Remove the stale out-of-scope line that still lists pause/resume as unsupported. Keep direct subscription creation out of the public seam explicitly, and add the resume warning from D-18 that immediate resume can charge immediately and should be reconciled via webhook and canonical fetches rather than trusted blindly from the local response.
  </action>
  <verify>
    <automated>mix docs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'create(client, attrs, opts \\\\ [])' guides/accrue-seam.md` returns a hit under `Paddle.Transactions`.
    - `grep -F 'pause(client, subscription_id, opts \\\\ [])' guides/accrue-seam.md` returns a hit.
    - `grep -F 'pause_immediately(client, subscription_id, opts \\\\ [])' guides/accrue-seam.md` returns a hit.
    - `grep -F 'resume(client, subscription_id, opts \\\\ [])' guides/accrue-seam.md` returns a hit.
    - `rg -n 'Subscription mutations beyond cancel: `update`, `pause`, `resume`|Subscriptions\\.create' guides/accrue-seam.md` returns no stale direct-create or out-of-scope pause/resume matches.
    - `grep -F 'charge immediately' guides/accrue-seam.md` returns a hit in the resume guidance.
    - `mix docs --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>
    The canonical Accrue seam guide matches the finished Phase 10 public surface and continues to exclude a false direct-create API.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller opts -> resume mutation | Resume mixes lifecycle behavior and request opts and must not imply unsupported idempotency or silently pass unknown keys. |
| Provider payload -> locked seam | New provider fields can appear in responses, but the documented `%Paddle.Subscription{}` seam must stay stable. |
| Public documentation -> consumer integrations | The seam guide is the contract Accrue and other adopters rely on; stale direct-create or out-of-scope text would create an integration bug. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-08 | Tampering | Resume lifecycle opts | mitigate | Validate `effective_from:` and `on_resume:` locally and keep lifecycle/request opt splitting explicit per D-15, D-17, and D-22. |
| T-10-09 | Tampering | Unsupported `idempotency_key:` on resume | mitigate | Raise `ArgumentError` before dispatch per D-21 so callers do not infer false retry safety on lifecycle mutations. |
| T-10-10 | Information Disclosure / Integrity | Provider state errors on resume | mitigate | Preserve Paddle `%Paddle.Error{}` passthrough unchanged so callers can distinguish missing payment method / billing-period failures per D-17. |
| T-10-11 | Tampering | Locked `%Paddle.Subscription{}` seam | mitigate | Add exact key-set regression tests and keep extra provider data in `raw_data` per D-24 and D-26. |
| T-10-12 | Spoofing | Public seam documentation | mitigate | Update `guides/accrue-seam.md` to enumerate only the truthful public surface and to exclude direct `Paddle.Subscriptions.create/2` per D-27. |
| T-10-SC | Tampering | Dependency/install surface | accept | No new packages or installs in this plan. |
</threat_model>

<verification>
- `mix test test/paddle/subscriptions_test.exs test/paddle/subscription_test.exs test/paddle/seam_test.exs --color`
- `mix test --color`
- `mix docs --warnings-as-errors`
- `mix compile --warnings-as-errors`
- `mix format --check-formatted`
</verification>

<success_criteria>
- SUB-06 is implemented with explicit immediate-default resume semantics and strict opt validation.
- The seam guide enumerates the completed pause/resume public surface and keeps direct subscription creation out of contract.
- `%Paddle.Subscription{}` shape drift now fails fast in tests.
- The full seam journey remains green while expanding additively from cancel-only to pause/resume support.
</success_criteria>

<output>
Create `.planning/phases/10-subscriptions-surface-completion/10-03-SUMMARY.md` when done.
</output>
