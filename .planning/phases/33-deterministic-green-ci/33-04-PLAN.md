---
phase: 33-deterministic-green-ci
plan: 04
type: execute
wave: 4
depends_on: [33-03]
files_modified:
  - scripts/ci_remote_gate.cjs
  - scripts/ci_remote_gate.test.cjs
  - .planning/phases/33-deterministic-green-ci/33-CI-HOSTED.md
  - .planning/phases/33-deterministic-green-ci/33-CI-BASELINE.md
autonomous: true
requirements: [CI-03, CI-04, CI-05]
estimate:
  tokens: 15000
  raw_tokens: 15000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A proposed change has an observed successful hosted `CI contract` for its exact tested SHA, with a matching downloadable proof artifact."
    - "The effective remote main branch rule requires the stable `CI contract` check."
    - "The current remote main head SHA has an observed successful hosted contract and retained proof for that exact SHA."
    - "Post-change duration is compared with the recorded baseline and target without dropping a required lane."
  artifacts:
    - path: scripts/ci_remote_gate.cjs
      provides: "Automated exact-SHA run, artifact, and effective branch-rule inspection"
    - path: .planning/phases/33-deterministic-green-ci/33-CI-HOSTED.md
      provides: "Dated candidate and current-main hosted evidence"
    - path: .planning/phases/33-deterministic-green-ci/33-CI-BASELINE.md
      provides: "Observed post-change comparison to the baseline-derived target"
  key_links:
    - from: scripts/ci_remote_gate.cjs
      to: scripts/ci_monitor.cjs
      via: "Exact-SHA aggregate and proof validation"
    - from: scripts/ci_remote_gate.cjs
      to: .planning/phases/33-deterministic-green-ci/33-CI-HOSTED.md
      via: "Observed effective rule and current-main result become dated evidence"
---

<objective>
Prove the completed contract on hosted GitHub Actions and make `CI contract` an effective remote-main requirement.

Purpose: CI-04 cannot be closed by local workflow inspection; CI-03 and CI-05 also need hosted observations of timing and proof retention.
Output: Automated hosted acceptance, branch-rule verification, and exact-SHA evidence record.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/GSD-PREFERENCES.md
@.planning/phases/33-deterministic-green-ci/33-RESEARCH.md
@.planning/phases/33-deterministic-green-ci/33-CI-BASELINE.md
@scripts/ci_monitor.cjs
@scripts/ci_timing.cjs
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Automate hosted candidate, artifact, and timing acceptance</name>
  <files>scripts/ci_remote_gate.cjs, scripts/ci_remote_gate.test.cjs, .planning/phases/33-deterministic-green-ci/33-CI-HOSTED.md, .planning/phases/33-deterministic-green-ci/33-CI-BASELINE.md</files>
  <behavior>
    - Synthetic hosted data verifies only when run head, tested SHA, workflow, aggregate, every required job, artifact run/attempt, and proof identity agree.
    - Wrong SHA, missing artifact, failed/skipped lane, or absent aggregate rejects acceptance.
    - DNS, authentication, permission, and rate-limit failures are reported as unobserved, never as missing protection or absent green run.
  </behavior>
  <action>Build a read-only hosted acceptance CLI that composes `ci_monitor` and `ci_timing` with bounded GitHub API calls, verifies the downloadable proof's run/attempt and lane results, and emits a compact machine-readable verdict. Fixture-test success and failure classifications. Exercise one actual hosted pull-request run for the final candidate commit after the workflow changes are committed and pushed through a reviewable path; if a run is absent, trigger the already configured PR path rather than substituting local success. Record exact candidate head SHA, tested merge SHA where applicable, run URL/attempt, artifact identity/digest, aggregate and lane conclusions, and observed post-change timing in `33-CI-HOSTED.md`; compare measured duration with the Plan 03 target in `33-CI-BASELINE.md`. Treat the run artifact as the durable proof and keep any subsequently written evidence record off the tested candidate ref, since committing it there creates a new SHA requiring another run. Keep all personal/customer information out of retained files. If GitHub cannot be reached or the run has not completed, record unobserved/blocking status and resume observation when access returns.</action>
  <verify><automated>node --test scripts/ci_remote_gate.test.cjs &amp;&amp; node scripts/ci_remote_gate.cjs candidate --sha "$CI_CANDIDATE_SHA" --json</automated></verify>
  <done>A hosted candidate run and matching proof are recorded for exact identities, with all required lanes and post-change timing measured.</done>
</task>

<task type="auto">
  <name>Task 2: Require the stable check on remote main and observe current-main green</name>
  <files>scripts/ci_remote_gate.cjs, scripts/ci_remote_gate.test.cjs, .planning/phases/33-deterministic-green-ci/33-CI-HOSTED.md</files>
  <action>Read the repository's effective main branch protection/rulesets through GitHub API, preserving the existing required checks and review settings. If `CI contract` is not required, add that exact stable check through the repository's supported protection/ruleset API using authorized maintainer access, then re-read the effective rule; do not treat a failed API read as evidence that a rule is missing. Move the reviewed candidate to main through the repository's normal merge path without bypassing checks. Read remote main's exact HEAD SHA after merge, require a completed hosted push run for that SHA, run the same aggregate/artifact validator, and record rule ID/source, SHA, run URL/attempt, artifact identity, all lane conclusions, timestamp, and target comparison in `33-CI-HOSTED.md`. Keep the post-run evidence record on a separate evidence ref or local handoff until final observation so recording it does not move main; the run artifact remains durable proof on the authoritative SHA. Re-read main HEAD immediately before final acceptance and repeat observation if a later commit, including a documentation or GSD summary commit, advances main. If auth or DNS blocks observation, leave CI-04 explicitly unverified with the failed query and retry condition; never promote local or historical green results to current-main proof.</action>
  <verify><automated>node --test scripts/ci_remote_gate.test.cjs &amp;&amp; node scripts/ci_remote_gate.cjs main --json</automated></verify>
  <done>Effective remote main protection requires `CI contract`, and the latest remote main SHA has a successful matching hosted run and durable proof artifact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub API → acceptance record | Remote results and branch rules must match the exact observed SHA. |
| Maintainer credential → branch protection | Rule update must be limited to the stable check while preserving other protections. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-12 | Spoofing | Hosted run lookup | high | mitigate | Compare requested SHA, run, attempt, tested SHA, jobs, and artifact identity. |
| T-33-13 | Tampering | Main branch rule | high | mitigate | Read effective rule before/after scoped update and preserve other required checks. |
| T-33-14 | Repudiation | Hosted evidence | medium | mitigate | Retain dated run URL, SHA, artifact digest, lane outcomes, and rule source. |
| T-33-15 | Information disclosure | Hosted evidence file | medium | mitigate | Store only public run identifiers, hashes, versions, and timings. |
</threat_model>

<verification>
Run local fixture tests, then obtain one real candidate and one current-main exact-SHA hosted acceptance through the CLI. Check remote main protection through the effective rules API and compare measured post-change duration with the recorded target.
</verification>

<success_criteria>
The effective remote main rule requires the stable aggregate, current main is observed hosted-green for its exact head SHA, and its durable proof and timing are recorded without substituting local evidence.
</success_criteria>

<output>
Create `.planning/phases/33-deterministic-green-ci/33-04-SUMMARY.md` when done.
</output>
