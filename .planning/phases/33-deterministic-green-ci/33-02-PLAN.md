---
phase: 33-deterministic-green-ci
plan: 02
type: execute
wave: 2
depends_on: [33-01]
files_modified:
  - mix.exs
  - mix.lock
  - .github/workflows/ci.yml
  - scripts/ci_workflow_contract.test.cjs
  - scripts/ci_proof.cjs
  - scripts/ci_proof.test.cjs
  - scripts/ci_monitor.cjs
  - scripts/ci_monitor.test.cjs
autonomous: true
requirements: [CI-01, CI-05]
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Every proposed change runs formatting, dependency, warnings, tests, public specs, Dialyzer, Credo, ExDoc, Hex audit, demo/PostgreSQL, package smoke, optional dependencies, and planning truth under the required aggregate."
    - "A missing or unsuccessful required job makes `CI contract` fail and remains visible in proof JSON."
    - "The existing six Phase 31 diagnostic areas continue to run through the planning-truth job and aggregate."
  artifacts:
    - path: .github/workflows/ci.yml
      provides: "Complete required quality contract"
    - path: scripts/ci_workflow_contract.test.cjs
      provides: "Static contract for every named required check"
    - path: mix.lock
      provides: "Reviewed Credo dependency resolution"
  key_links:
    - from: .github/workflows/ci.yml
      to: scripts/ci_proof.cjs
      via: "New required job ID and result included in proof"
    - from: .github/workflows/ci.yml
      to: scripts/ci_monitor.cjs
      via: "New display name required by hosted monitor"
---

<objective>
Complete CI-01's proof matrix while preserving the Phase 31 planning guards and exact aggregate semantics.

Purpose: A green aggregate must mean every named requirement actually ran.
Output: Locked Credo tooling, complete workflow checks, and static contract tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/GSD-PREFERENCES.md
@.planning/phases/33-deterministic-green-ci/33-RESEARCH.md
@.planning/phases/31-repository-planning-truth/31-UAT.md
@.planning/phases/31-repository-planning-truth/31-09-SUMMARY.md
@.github/workflows/ci.yml
@mix.exs
@scripts/ci_monitor.cjs
@scripts/ci_proof.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install reviewed Credo and make lint, docs, and audit required</name>
  <files>mix.exs, mix.lock, .github/workflows/ci.yml, scripts/ci_proof.cjs, scripts/ci_workflow_contract.test.cjs</files>
  <behavior>
    - Static contract rejects absence of `mix credo --strict`, `mix docs`, or `mix hex.audit` from a required job.
    - Static contract rejects removal of any existing required Mix, demo, package, optional-dependency, or planning guard check.
    - Registry or audit network failure fails the audit step distinctly from a clean advisory result.
  </behavior>
  <action>Confirm `credo` package identity, source repository, and selected compatible Hex release through current official/registry metadata before adding the project Mix development/test dependency with `runtime: false`; update `mix.lock` and retain the resolved version. A Hex DNS or authentication failure is unobserved registry state, so stop dependency selection until it is actually verified. Add one required quality job with `mix credo --strict`, `mix docs`, and `mix hex.audit`, using the existing pinned BEAM toolchain; keep all three checks visible by step name and run the audit even if an earlier check fails, then fail the job if any check fails. Add its ID to `ci-contract.needs` and the proof writer's required-job map in the same task so quality is mandatory from its first run. Preserve all existing workflow jobs, especially the Node/prohibition/history/live-smoke planning-truth job whose current implementation follows the six historical Phase 31 diagnoses. Add a workflow contract test that maps every CI-01 named proof to an actual step in a required aggregate dependency; assert job IDs, not only prose or comments. Do not edit the historical UAT or debug files.</action>
  <verify><automated>node --test scripts/ci_workflow_contract.test.cjs</automated></verify>
  <done>Credo resolves to a reviewed locked Hex package, and the required workflow contains the complete named CI-01 check matrix with fail-closed outcomes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind the completed quality job into proof and hosted monitoring</name>
  <files>scripts/ci_proof.test.cjs, scripts/ci_monitor.cjs, scripts/ci_monitor.test.cjs, scripts/ci_workflow_contract.test.cjs</files>
  <behavior>
    - The quality job is listed in `needs`, proof required jobs, and monitor required display names.
    - Missing, skipped, cancelled, or failed quality results make the aggregate fail and cannot yield verified proof.
    - All Phase 31 planning-truth checks remain required after the quality job is added.
  </behavior>
  <action>Require the quality job's stable display name in the exact-SHA monitor. Extend fixture tests to assert the quality job ID and all three named checks appear in the proof when available; record skipped or unavailable explicitly and do not infer a passing step from a green sibling. Exercise missing/failed quality results and preserve the six existing planning-truth test assertions. Keep the workflow aggregate name `CI contract` for remote protection.</action>
  <verify><automated>node --test scripts/ci_workflow_contract.test.cjs scripts/ci_proof.test.cjs scripts/ci_monitor.test.cjs</automated></verify>
  <done>One aggregate requires the new quality job and every existing lane; proof and monitor reject absent or unsuccessful quality evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hex registry → Mix lockfile | New development dependency enters the CI execution graph. |
| External advisory registry → audit result | Online vulnerability state enters the quality verdict. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-05 | Tampering | Credo dependency | high | mitigate | Verify Hex identity/source/version, lock resolution, and review the lockfile delta. |
| T-33-06 | Tampering | Workflow aggregate | high | mitigate | Static test names every CI-01 step and required aggregate job ID. |
| T-33-07 | Repudiation | Audit step | medium | mitigate | Preserve explicit audit outcome in proof and distinguish network error from clean result. |
</threat_model>

<verification>
Run the three focused Node contract suites, `mix credo --strict`, `mix docs`, and `mix hex.audit` with the project toolchain. The hosted complete-contract baseline follows in Plan 03.
</verification>

<success_criteria>
Every CI-01 proof is both executed and covered by a required aggregate dependency, with missing and failing results represented honestly.
</success_criteria>

<output>
Create `.planning/phases/33-deterministic-green-ci/33-02-SUMMARY.md` when done.
</output>
