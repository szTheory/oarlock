---
context: phase
phase: 33-deterministic-green-ci
task: 3
total_tasks: 4
status: in_progress
last_updated: 2026-09-24T20:58:29Z
---

# Phase 33 Continuation

## Current position

- Phase 33 Plans 33-01 and 33-02 have summaries. Plans 33-03 and 33-04 remain incomplete.
- User authorized a PR if direct push to `main` is not safe. Draft PR #6 is at `https://github.com/szTheory/oarlock/pull/6`; leave it draft and do not merge before the full diff and exact-SHA evidence are reviewable.
- The branch is a broad historical sync (roughly 400 commits beyond remote `main`), not a narrow Phase 33 patch. Remote `main` is `fb3d9a185f104194e85987541519a6168e0b568c`; its June CI run failed and the effective rule does not require `CI contract`.
- Published historical tags v1.3, v1.4, v2.0, and v2.1 at their canonical SHAs.

## Hosted CI findings

- CI run `36056214272` on `79f8803` passed mix test, quality checks, optional dependencies, Dialyzer, package smoke, and PostgreSQL. Planning truth failed because the history guard rejected the first introduction of `.planning/EVIDENCE.md`; the exact-SHA proof correctly remained `verified: false`.
- Fixed the history guard to accept only a structurally canonical initial evidence ledger with valid dated correction rows, while preserving append-only checks for an existing ledger and fail-closed malformed-ledger behavior. `scripts/history_integrity.test.cjs` passes 21 assertions; local base-to-head history check from remote `main` is healthy.
- CI run `36057918086` on `8ced58e` passed the history guard and clean production Node suite, but planning-health smoke failed because the PR did not yet contain the four Phase 33 plan files declared by `ROADMAP.md`. The plans, summaries/context, automation scripts, preferences, prompt, and ignore rule are now staged for inclusion in the next candidate commit; verify planning health after publishing that exact commit.
- The Rebar probe issue was actually Hex 2.5.1's unpacked-directory install layout. The workflow now detects both directory and `.ez` installs. Do not rewrite that diagnosis as a Rebar issue.
- No successful full-contract hosted run exists yet. `ci_timing.cjs` correctly refuses failed runs; baseline samples remain zero and no target is asserted.

## Next actions

1. Review the explicit candidate diff and exclude `.hex/`, `.mix/`, other build output, personal config, and unrelated Phase 32 dirty files.
2. Publish the prepared Phase 33 planning artifacts, GSD preference, ratchet prompt, `.gitignore` exception, and timing/remote-gate scripts/tests to PR #6.
3. Run `node scripts/planning_health.cjs --json` on the candidate tree and inspect the exact-SHA GitHub run. Require planning truth and CI contract green before measuring.
4. Run `node scripts/ci_timing.cjs --sha <successful-exact-sha> --run-id <run-id> --json`; record only observed timings and set a provisional target only if the full required job set passed.
5. Continue Plan 33-03's measured CI control work, then Plan 33-04's candidate/main gate. Preserve the current `main` CI failure until the exact current main SHA has durable hosted-green evidence.

## Verification debt and state

- GSD `planning_health.cjs --json` is healthy on the current local tree after Phase 33 artifacts are present.
- Cross-phase UAT audit still reports 12 Phase 31 issue/unknown results. Do not mark them passed until each expected outcome has current automated or irreducible human evidence.
- Phase 32 verification is marked stale because Plan 32-16 has a newer summary. Re-run/reconcile it from its reported automated evidence before routing past Phase 32.
- Preserve other pre-existing dirty/untracked files in the original workspace. Only stage the explicitly reviewed candidate files.
