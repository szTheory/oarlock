---
phase: 33-deterministic-green-ci
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - scripts/ci_proof.cjs
  - scripts/ci_proof.test.cjs
  - scripts/ci_monitor.cjs
  - scripts/ci_monitor.test.cjs
autonomous: true
requirements: [CI-05, CI-04]
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "An aggregate attempt leaves a downloadable proof JSON even when a required job fails."
    - "The proof binds the tested SHA, event head SHA, run ID and attempt, actual toolchain identities, root and demo lockfile digests, and every required job result."
    - "The exact-SHA monitor accepts a run only when its matching proof artifact and required jobs agree with the hosted run."
  artifacts:
    - path: scripts/ci_proof.cjs
      provides: "Strict proof creation and validation contract"
    - path: scripts/ci_monitor.cjs
      provides: "Hosted exact-SHA and artifact verifier"
    - path: .github/workflows/ci.yml
      provides: "Always-run aggregate and retained proof artifact"
  key_links:
    - from: .github/workflows/ci.yml
      to: scripts/ci_proof.cjs
      via: "Aggregate invokes proof writer with needs and event identity"
    - from: scripts/ci_monitor.cjs
      to: scripts/ci_proof.cjs
      via: "Monitor validates downloaded artifact schema and identity"
---

<objective>
Make the existing aggregate produce and retain one exact-SHA CI proof, including failed attempts, and make the hosted monitor verify the same artifact.

Purpose: CI-05 needs durable evidence and CI-04 needs a monitor that refuses a wrong-SHA or incomplete run.
Output: Proof writer, fixture-driven contract checks, artifact upload, and exact-SHA monitor integration.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/GSD-PREFERENCES.md
@.planning/phases/33-deterministic-green-ci/33-RESEARCH.md
@.github/workflows/ci.yml
@scripts/ci_monitor.cjs
@scripts/ci_monitor.test.cjs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Carry one existing CI lane through the aggregate into a retained exact-SHA proof</name>
  <files>scripts/ci_proof.cjs, scripts/ci_proof.test.cjs, .github/workflows/ci.yml</files>
  <behavior>
    - A synthetic successful `test` lane plus the other current required lane results yields verified proof for the supplied event SHA and run attempt.
    - One absent, skipped, cancelled, or failed lane yields verified=false and a failing aggregate while still writing valid proof.
    - Malformed SHA, missing lockfile, incomplete toolchain data, or a mismatched checked-out HEAD fails closed without inventing identity.
  </behavior>
  <action>Extract the current `ci-contract` inline assertion into a Node CLI in scripts/ci_proof.cjs that consumes `needs` JSON and explicit GitHub event/run fields. Start with the existing `test` path as the tracer and retain all six current dependencies in the final required-job map. In the aggregate, check out the event SHA, read `.tool-versions`, capture the actual BEAM/Node/Hex/Rebar and runner identities emitted by setup steps or job outputs, and calculate SHA-256 digests of root and demo `mix.lock`. Distinguish the tested event SHA from a pull request's source head SHA; on main they coincide. Produce bounded JSON with only allowlisted identifiers, versions, hashes, job IDs/results, and URLs; exclude actor, secret, environment dump, and customer data. Write proof before exiting nonzero on a bad dependency. Pin the reviewed `actions/upload-artifact` action by full SHA, upload on `always()` with `if-no-files-found: error`, use a run-attempt-specific artifact name and retention period, and append a short run-summary link. Keep `CI contract` as the stable display name and every existing required job in `needs`. The script/test pair must exercise the same entry point used by the workflow.</action>
  <verify><automated>node --test scripts/ci_proof.test.cjs</automated></verify>
  <done>The aggregate's tested-SHA path produces a retained, schema-valid artifact and a failed dependency still fails `CI contract` after proof upload.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Require the matching artifact in exact-SHA hosted monitoring</name>
  <files>scripts/ci_monitor.cjs, scripts/ci_monitor.test.cjs, scripts/ci_proof.test.cjs</files>
  <behavior>
    - A successful run with the matching artifact, attempt, tested SHA, source head SHA, and required lanes verifies.
    - A missing artifact, mismatched run or attempt, wrong SHA, missing required lane, or verified=false proof fails closed.
    - GitHub API, download, authentication, and DNS failures return an unobserved/blocked result, never a claim that the run or artifact does not exist.
  </behavior>
  <action>Extend `assert-ci` to download the artifact associated with the selected exact run and attempt, parse it through the exported strict proof validator, and compare its identity and required lane results with the run jobs. Keep `--sha` bound to the requested hosted run head; compare the proof's tested event SHA separately for pull requests. Preserve bounded subprocess deadlines and machine-readable failure reasons. Extend the existing fake-gh fixture for artifact download and prove the success, mismatch, missing, failed-lane, and transport-error paths. Do not mark local fixture success as hosted-green evidence.</action>
  <verify><automated>node --test scripts/ci_monitor.test.cjs scripts/ci_proof.test.cjs</automated></verify>
  <done>The monitor prints verified=true only for one exact hosted run and its matching durable proof; unavailable GitHub transport remains explicitly unobserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub event and `needs` → proof artifact | Untrusted event metadata and job outcomes become durable evidence. |
| Hosted run/artifact → local monitor | Downloaded metadata must match the requested run and attempt. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-01 | Tampering | `ci_proof.cjs` | high | mitigate | Validate SHA/run/attempt/required IDs and derive lock digests from checked-out files. |
| T-33-02 | Spoofing | `ci_monitor.cjs` | high | mitigate | Match artifact identity, attempt and required results to exact hosted run. |
| T-33-03 | Information disclosure | Proof artifact | medium | mitigate | Emit only allowlisted non-secret fields and synthetic fixtures. |
| T-33-04 | Denial of service | Artifact download | medium | mitigate | Keep bounded GitHub subprocess deadlines and artifact parse size. |
</threat_model>

<verification>
Run the two focused Node suites and inspect the aggregate's required IDs, `always()` upload, stable display name, and failure path. Hosted artifact acceptance is scheduled in Plan 04.
</verification>

<success_criteria>
One checked-out event SHA is represented by a durable run-attempt artifact, and local contract fixtures prove both green and failure evidence paths without exposing personal data.
</success_criteria>

<output>
Create `.planning/phases/33-deterministic-green-ci/33-01-SUMMARY.md` when done.
</output>
