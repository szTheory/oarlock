---
phase: 33-deterministic-green-ci
plan: 03
type: execute
wave: 3
depends_on: [33-02]
files_modified:
  - scripts/ci_timing.cjs
  - scripts/ci_timing.test.cjs
  - .planning/phases/33-deterministic-green-ci/33-CI-BASELINE.md
  - .github/workflows/ci.yml
  - scripts/ci_workflow_contract.test.cjs
autonomous: true
requirements: [CI-02, CI-03]
estimate:
  tokens: 17000
  raw_tokens: 17000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Maintainers can inspect observed queue, per-job, aggregate, and critical-path durations for an exact hosted SHA."
    - "A feedback target and explicit timeouts derive from recorded hosted timings, with sample count and cache state disclosed."
    - "Every required proof remains present after inputs and cache behavior are tightened."
    - "Actions, runners, service image, toolchains, and dependency caches have reviewed controlled identities and least-privilege access."
  artifacts:
    - path: scripts/ci_timing.cjs
      provides: "Exact-run timing and critical-path summarizer"
    - path: .planning/phases/33-deterministic-green-ci/33-CI-BASELINE.md
      provides: "Observed baseline, target derivation, and Actions-minute accounting"
    - path: .github/workflows/ci.yml
      provides: "Controlled runner, tools, cache, permissions, and timeouts"
  key_links:
    - from: scripts/ci_timing.cjs
      to: .planning/phases/33-deterministic-green-ci/33-CI-BASELINE.md
      via: "Only observed hosted timestamps establish baseline rows"
    - from: .planning/phases/33-deterministic-green-ci/33-CI-BASELINE.md
      to: .github/workflows/ci.yml
      via: "Measured lane durations justify timeouts and performance changes"
---

<objective>
Measure the complete hosted CI contract before changing its performance inputs, then set a recorded feedback target and tighten reproducibility without dropping checks.

Purpose: CI-03 calls for evidence-driven speed, and CI-02 calls for controlled execution and caches.
Output: Timing script and baseline, controlled workflow inputs, and static policy coverage.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/GSD-PREFERENCES.md
@.planning/phases/33-deterministic-green-ci/33-RESEARCH.md
@.github/workflows/ci.yml
@scripts/ci_monitor.cjs
@scripts/ci_workflow_contract.test.cjs
@.tool-versions
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Measure a hosted complete-contract baseline before performance edits</name>
  <files>scripts/ci_timing.cjs, scripts/ci_timing.test.cjs, .planning/phases/33-deterministic-green-ci/33-CI-BASELINE.md</files>
  <behavior>
    - Fixed synthetic job timestamps yield deterministic queue, job runtime, aggregate runtime, critical path, and summed runner-minute values.
    - Missing, reversed, or incomplete timestamps are reported as unmeasured instead of zero duration.
    - Wrong-SHA runs, GitHub DNS/auth errors, and absent runs do not generate baseline or target numbers.
  </behavior>
  <action>Implement a read-only `scripts/ci_timing.cjs` that queries `gh` for an exact run ID/SHA and required job timestamps, checks the same job list as the proof contract, and reports queue separately from execution, per-job elapsed time, aggregate elapsed time, critical path, and sum of runner minutes. Use fixture tests with exact expected calculations and missing-data/transport failures. After Plan 02's full contract is committed and exercised in a hosted pull request, observe the actual run and write only observed SHA, run URL/attempt, UTC timestamps, toolchain/cache classification, durations, and sample count to `33-CI-BASELINE.md`. Use available organic full-contract runs; do not burn Actions minutes solely to inflate the sample. If only one run exists, label the target provisional and derive it from that observation with documented headroom; distinguish cold and warm caches when observable. A GitHub or Hex DNS failure is lack of observation and must leave baseline/target unasserted until access returns. This measurement must precede Task 2's cache/performance edits.</action>
  <verify><automated>node --test scripts/ci_timing.test.cjs &amp;&amp; node scripts/ci_timing.cjs --sha "$CI_BASELINE_SHA" --json</automated></verify>
  <done>A real hosted run of the full required job set is recorded with exact SHA and critical-path timing before workflow optimization; synthetic tests prove timing math and unavailable-state behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Control inputs, cache boundaries, permissions, and observed timeouts</name>
  <files>.github/workflows/ci.yml, scripts/ci_workflow_contract.test.cjs, .planning/phases/33-deterministic-green-ci/33-CI-BASELINE.md</files>
  <behavior>
    - Static policy rejects floating action references, unversioned runner labels, mutable PostgreSQL tag without digest, missing job timeouts, overly broad token permissions, and cache keys without runtime/environment/lock identity.
    - A pull request cannot save executable caches that a privileged main run restores.
    - Every CI-01 check and aggregate dependency survives the performance change.
  </behavior>
  <action>Use Task 1's observed durations to choose each job's explicit timeout with documented headroom and a baseline-derived feedback target. Replace `ubuntu-latest` with a reviewed versioned runner label and record the resolved runner image in proof. Pin the PostgreSQL image by reviewed digest. Keep all third-party actions on full reviewed commit SHAs and make the project-pinned Node version active for Node jobs, checked against `.tool-versions`; record actual BEAM, Node, Hex, and Rebar versions. Replace moving Hex/Rebar installs with reviewed exact versions or verified archive digests selected from official installer/registry data at execution. Scope token permissions to `contents: read` unless a named job genuinely needs more; keep PR CI without release secrets. Separate read-only cache restore from trusted-main cache save; key compiled root, demo, and Dialyzer state by OS, architecture, OTP, Elixir, Mix environment, and relevant lock digest, with no cross-version restore prefix. Retain caches only where measured savings justify their Actions-minute cost. Extend the workflow contract test to assert these controls and the unchanged complete proof matrix; record the before/after timing and target methodology without inventing a performance gain before a post-change hosted run is observed.</action>
  <verify><automated>node --test scripts/ci_workflow_contract.test.cjs scripts/ci_timing.test.cjs</automated></verify>
  <done>Every job has an evidence-based timeout, required checks remain wired, cache writes respect trust, and the documented target follows actual hosted baseline measurements.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Runner/tool/container source → CI execution | External images and installers execute code during proof. |
| Low-trust PR → cache → trusted main | Reused executable cache files can cross a privilege boundary. |
| Hosted timestamps → target | Incomplete metadata could create false performance claims. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-08 | Tampering | Actions and service image | high | mitigate | Reviewed SHA/digest pins and static policy checks. |
| T-33-09 | Elevation of privilege | Cache restore | high | mitigate | PR restore-only policy and main-only save with runtime-scoped keys. |
| T-33-10 | Denial of service | CI jobs | medium | mitigate | Per-job timeouts derived from observed durations plus headroom. |
| T-33-11 | Repudiation | Timing report | medium | mitigate | Exact run/SHA/timestamps and explicit missing-data classification. |
</threat_model>

<verification>
Run local policy/timing tests and obtain the hosted baseline before editing cache policy. Plan 04 observes the post-change hosted run and compares against the target.
</verification>

<success_criteria>
CI remains complete while its inputs and caches are controlled and its time target has an inspectable observed basis.
</success_criteria>

<output>
Create `.planning/phases/33-deterministic-green-ci/33-03-SUMMARY.md` when done.
</output>
