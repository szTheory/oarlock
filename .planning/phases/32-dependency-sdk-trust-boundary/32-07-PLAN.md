---
phase: 32-dependency-sdk-trust-boundary
plan: "07"
type: execute
wave: 5
depends_on: [32-04]
files_modified:
  - lib/paddle/notification_setting.ex
  - lib/paddle/portal_session.ex
  - test/paddle/notification_setting_test.exs
  - test/paddle/portal_session_test.exs
  - lib/paddle/subscription/management_urls.ex
  - lib/paddle/transaction/checkout.ex
  - test/paddle/subscription_test.exs
  - test/paddle/transaction_test.exs
  - test/paddle/inspection_safety_test.exs
autonomous: true
requirements: [SAFE-03]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Inspecting every classified secret-bearing public value replaces promoted capabilities and whole raw_data containers with one stable [REDACTED] marker while leaving stored data unchanged."
    - "Realistic values hydrated through Paddle.Http.build_struct/2 contain unique promoted/nested canaries in memory, but none are reachable from inspect/1."
  assumptions:
    - requirement: SAFE-03
      statement: "The spec-less edge probe classified SAFE-03 as unclassified; the public raw_data inventory and hydrated cross-struct canary suite remain explicitly flagged until execution proves the inventory exhaustive."
      status: unresolved
      flagged: true
  artifacts:
    - path: test/paddle/inspection_safety_test.exs
      provides: "Exhaustive raw_data inventory plus hydrated recursive canary proof"
    - path: lib/paddle/notification_setting.ex
      provides: "Redacted destination, endpoint secret, and raw_data inspection"
    - path: lib/paddle/subscription/management_urls.ex
      provides: "Redacted authenticated management URL inspection"
  key_links:
    - from: test/paddle/inspection_safety_test.exs
      to: lib/paddle/http.ex
      via: "fixtures use Paddle.Http.build_struct/2 so promoted and raw copies both exist"
      pattern: 'build_struct'
    - from: "secret-bearing structs"
      to: Inspect protocol
      via: "explicit total Map.from_struct projection and wholesale container replacement"
      pattern: '\[REDACTED\]'
---

<objective>
Close every public inspection path capable of rendering credentials or authenticated URLs from promoted fields or raw provider payloads, per D-07 through D-09.

Purpose: Keep forward-compatible stored provider data while making ordinary IEx/log inspection deny-by-default for capabilities.
Output: Four additional Inspect implementations, a complete public raw_data classification inventory, and cross-struct canary proof including Client/Error.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-CONTEXT.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-RESEARCH.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-PATTERNS.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-03-SUMMARY.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-04-SUMMARY.md
@lib/paddle/portal_session.ex
@test/paddle/portal_session_test.exs

<interfaces>
Use the exact mapped PortalSession custom Inspect shape: `Map.from_struct`, explicit `Map.replace` projections, stable `"[REDACTED]"`, sorted fields, and total `Inspect.Algebra.to_doc` rendering.
The classified secret-bearing set is Client, NotificationSetting, PortalSession, Subscription.ManagementUrls, Transaction.Checkout, and Error; the test inventory must also enumerate every public struct containing `raw_data` so additions fail closed until classified.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Redact provider-built notification and portal values</name>
  <reversibility rating="costly">Inspect output is observable public behavior, but the stable marker and unchanged stored structs keep rollback limited to coordinated representation/tests/docs.</reversibility>
  <read_first>lib/paddle/notification_setting.ex, lib/paddle/portal_session.ex, test/paddle/notification_setting_test.exs, test/paddle/portal_session_test.exs, lib/paddle/http.ex, .planning/phases/32-dependency-sdk-trust-boundary/32-PATTERNS.md</read_first>
  <files>lib/paddle/notification_setting.ex, lib/paddle/portal_session.ex, test/paddle/notification_setting_test.exs, test/paddle/portal_session_test.exs</files>
  <behavior>
    - NotificationSetting inspection redacts destination, endpoint_secret_key, and raw_data.
    - PortalSession inspection redacts urls and raw_data.
    - Provider-built structs retain original promoted/raw values when accessed directly, but their inspect strings contain no canary.
  </behavior>
  <action>Use the mapped PortalSession projection pattern in both modules. Write provider-shaped fixtures through `Paddle.Http.build_struct/2` before implementation, place distinct canaries in each promoted field and nested raw_data, then replace the listed fields wholesale with the stable marker during Inspect only. Keep implementations total and do not recursively filter arbitrary provider keys; stored data must remain intact per D-08.</action>
  <verify>
    <automated>mix test test/paddle/notification_setting_test.exs test/paddle/portal_session_test.exs --warnings-as-errors</automated>
    <fails_when>A promoted/nested canary appears in inspection, raw_data is only partially filtered, stored data changes, Inspect raises/falls back, the marker differs, or a focused test fails.</fails_when>
  </verify>
  <acceptance_criteria>Real hydrated notification/portal capabilities remain usable in memory while their complete inspection projections are redacted.</acceptance_criteria>
  <done>The first provider-built secret-bearing values are safe to inspect end to end.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete the public inventory and cross-struct canary proof</name>
  <read_first>lib/paddle/subscription/management_urls.ex, lib/paddle/transaction/checkout.ex, test/paddle/subscription_test.exs, test/paddle/transaction_test.exs, lib/paddle/client.ex, lib/paddle/error.ex, .planning/phases/32-dependency-sdk-trust-boundary/32-RESEARCH.md, .planning/phases/32-dependency-sdk-trust-boundary/32-PATTERNS.md</read_first>
  <files>lib/paddle/subscription/management_urls.ex, lib/paddle/transaction/checkout.ex, test/paddle/subscription_test.exs, test/paddle/transaction_test.exs, test/paddle/inspection_safety_test.exs</files>
  <behavior>
    - ManagementUrls redacts update_payment_method, cancel, and raw_data; Checkout redacts url and raw_data.
    - Cross-struct tests cover Client, Error, NotificationSetting, PortalSession, ManagementUrls, and Checkout through realistic construction/hydration.
    - A module inventory names every current public `raw_data` struct and fails if a future one is unclassified.
  </behavior>
  <action>Add total explicit Inspect projections for the remaining two mapped capability models. Create the new async composite test using the PortalSession/telemetry analogs and research-specified recursive walker for structs, maps, tuples, lists, and binary leaves. Hydrate provider values via `Paddle.Http.build_struct/2`, include Client and transport/response-built Error values, and assert exact redacted field markers plus unique canary absence. Encode the complete research inventory of public raw_data structs and require each entry to be explicitly classified secret-bearing or ordinary-data so D-07 cannot silently miss a new module.</action>
  <verify>
    <automated>mix test test/paddle/inspection_safety_test.exs test/paddle/subscription_test.exs test/paddle/transaction_test.exs --warnings-as-errors</automated>
    <fails_when>Any classified capability/raw_data canary renders, a current public raw_data struct is absent/unclassified, stored values are destroyed, a custom Inspect is partial/non-total, or focused tests fail.</fails_when>
  </verify>
  <acceptance_criteria>All six secret-bearing public values pass provider-realistic promoted/nested canary tests, and the complete raw_data inventory fails closed on future additions.</acceptance_criteria>
  <done>SAFE-03 is enforced across every current public inspection boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Provider JSON/transport → public structs | Capability values may exist in promoted fields and duplicated nested raw_data. |
| Public structs → Inspect/log output | Ordinary debugging can disclose stored credentials or authenticated links. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-32-03 | Information Disclosure | Inspect implementations/raw_data | high | mitigate | Whole-container redaction, explicit promoted-field projections, hydrated unique canaries, and exhaustive module inventory. |
| T-32-02 | Information Disclosure | Client/Error transport state | high | mitigate | Cross-struct suite verifies the Client/Error redactions created in Plans 32-03/04. |
| T-32-04 | Repudiation | ambiguity Error inspection | medium | mitigate | Preserve safe operation/request ID/guidance while redacting raw transport/provider payload. |
| T-32-05 | Information Disclosure | credentialized client URL | high | mitigate | Client Inspect redaction and Plan 32-03 validation prevent rendering/accepting credentialized URLs. |
| T-32-SC | Tampering | packages | low | accept | No package installation occurs. |
</threat_model>

## Artifacts this phase produces

- Inspect implementations for `Paddle.NotificationSetting`, `Paddle.PortalSession`, `Paddle.Subscription.ManagementUrls`, and `Paddle.Transaction.Checkout`.
- Stable `[REDACTED]` projections for all promoted capability fields and each secret-capable `raw_data` container.
- `test/paddle/inspection_safety_test.exs`: exhaustive public raw_data inventory and recursive provider-built canary proof across six protected types.

<verification>
Run both task commands plus `mix test`; confirm canaries remain directly readable from stored structs but absent from every inspect string.
</verification>

<success_criteria>
No known or inventory-discoverable public secret-bearing struct discloses promoted or nested capability data through Inspect.
</success_criteria>

<output>
Create `.planning/phases/32-dependency-sdk-trust-boundary/32-07-SUMMARY.md` when done.
</output>
