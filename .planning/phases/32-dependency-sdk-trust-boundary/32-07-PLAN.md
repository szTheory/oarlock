---
phase: 32-dependency-sdk-trust-boundary
plan: "07"
type: execute
wave: 7
depends_on: ["32-02", "32-04", "32-05", "32-06"]
files_modified:
  - lib/paddle/subscriptions.ex
  - lib/paddle/transactions.ex
  - lib/paddle/internal/pagination.ex
  - test/paddle/subscriptions_test.exs
  - test/paddle/transactions_test.exs
autonomous: true
requirements: [SAFE-04]
estimate:
  tokens: 31000
  raw_tokens: 31000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Subscription/transaction reads use bounded retry and static route context; every mutation variant is exactly one attempt with reconciliation guidance (D-10, D-11, D-14)."
    - "All legacy positive and negative `idempotency_key` handling is removed because no current operation has provider replay proof (D-13)."
    - "Pagination dispatch preserves provider cursor URLs while its telemetry operation/route remains the static context supplied by the originating resource (D-06)."
  artifacts:
    - path: lib/paddle/subscriptions.ex
      provides: "Hardened subscription lifecycle request paths"
    - path: lib/paddle/transactions.ex
      provides: "Hardened transaction read/create paths"
    - path: lib/paddle/internal/pagination.ex
      provides: "Static context propagated separately from dynamic cursor dispatch"
  key_links:
    - from: lib/paddle/subscriptions.ex
      to: lib/paddle/internal/pagination.ex
      via: "originating operation/route travels in pagination state"
      pattern: "Pagination"
    - from: lib/paddle/internal/pagination.ex
      to: lib/paddle/http.ex
      via: "dynamic next URL for dispatch plus static context options"
      pattern: "Http\\.request"
---

<objective>
Complete resource-wide request hardening for subscriptions, transactions, and shared pagination (D-06, D-10 through D-14).

Purpose: Close the highest-impact lifecycle mutations and the shared continuation path before telemetry observes attempts.
Output: Safe lifecycle operations, honest option/types, and cursor-safe context propagation.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-dependency-sdk-trust-boundary/32-04-SUMMARY.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-CONTEXT.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-PATTERNS.md
@.planning/phases/32-dependency-sdk-trust-boundary/COVERAGE.md
@lib/paddle/http.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden every subscription lifecycle operation</name>
  <read_first>
    - lib/paddle/subscriptions.ex
    - test/paddle/subscriptions_test.exs
    - lib/paddle/http.ex
    - lib/paddle/internal/pagination.ex
  </read_first>
  <files>lib/paddle/subscriptions.ex, test/paddle/subscriptions_test.exs</files>
  <behavior>
    - get/list/stream/all are bounded reads with static context.
    - update/cancel/pause/resume variants each dispatch once and normalize uncertainty.
    - `idempotency_key` is neither accepted nor specially rejected; option types name only supported options.
  </behavior>
  <action>Follow the mapped `lib/paddle/customers.ex` CRUD/validation analog and explicit boundary-ownership pattern in 32-PATTERNS.md. Update every subscription call with literal operation/route context, remove all idempotency branches/types/docs/tests per D-13, and replace them with method-safety/ambiguity assertions. Per D-18, update Subscriptions @moduledoc/@doc, option types, and specs to state bounded reads, one-attempt lifecycle mutations, ordinary unknown-option rejection, and the exact ambiguity return contract.</action>
  <verify>
    <automated>mix test test/paddle/subscriptions_test.exs</automated>
    <fails_when>Any lifecycle mutation retries, idempotency-specific code remains, a runtime ID enters route metadata, or an existing lifecycle result changes outside the new error fields.</fails_when>
  </verify>
  <acceptance_criteria>All subscription operations are method-safe, statically labeled, and honestly typed.</acceptance_criteria>
  <done>The full subscription lifecycle implements SAFE-04.</done>
  <reversibility rating="costly">Removing a published pre-1.0 option requires migration guidance but can be reversed by a deliberate later contract change.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden transactions and make pagination metadata cursor-safe</name>
  <read_first>
    - lib/paddle/transactions.ex
    - test/paddle/transactions_test.exs
    - lib/paddle/internal/pagination.ex
    - lib/paddle/http.ex
  </read_first>
  <files>lib/paddle/transactions.ex, lib/paddle/internal/pagination.ex, test/paddle/transactions_test.exs</files>
  <behavior>
    - Transaction get retries only as a bounded read; create executes once and may return actionable ambiguity.
    - Transaction option types and recurring-start tests contain no idempotency key.
    - Pagination next-page requests retain the original operation/route and never use cursor path/query as telemetry metadata.
  </behavior>
  <action>Follow the mapped Customers CRUD and `lib/paddle/internal/pagination.ex` cursor-dispatch/static-context analogs in 32-PATTERNS.md. Remove transaction idempotency, wire get/create to static context/ambiguity, complete `next_page/4` migration, remove `/3`, and prove cursor canaries stay out of metadata. Per D-18, update Transactions and Pagination @moduledoc/@doc, option types, and specs to state bounded get, single-attempt create, ambiguity fields, and cursor-safe static context exactly.</action>
  <verify>
    <automated>mix test test/paddle/transactions_test.exs test/paddle/adjustments_test.exs test/paddle/customers/addresses_test.exs test/paddle/events_test.exs test/paddle/notification_settings_test.exs test/paddle/prices_test.exs test/paddle/products_test.exs test/paddle/subscriptions_test.exs</automated>
    <fails_when>Transaction create retries, idempotency remains, pagination loses the cursor, or cursor material appears in operation/route context.</fails_when>
  </verify>
  <acceptance_criteria>Transaction and all pageable surfaces use one cursor-safe continuation contract, and Subscriptions/Transactions/Pagination docs, types, and specs match the tested runtime behavior (D-18).</acceptance_criteria>
  <done>Every outbound request row in COVERAGE.md now uses the central trust boundary.</done>
  <reversibility rating="costly">The removed public option affects callers but remains recoverable through a later explicit API revision.</reversibility>
</task>

</tasks>

## Artifacts this phase produces

- Static operation atoms and normalized route templates for subscription and transaction operations.
- Internal pagination contract finalizes `next_page/4` with `operation` and `route` context and removes the temporary `next_page/3`; dynamic `path` remains dispatch-only.
- Subscription/transaction public option types remove `idempotency_key`; no new public function, struct field, CLI flag, or path.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Lifecycle mutation → Paddle provider | Financial/subscription state may be accepted before a transport failure is observed. |
| Provider cursor URL → shared pagination | Provider-controlled path/query crosses back into SDK dispatch. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-32-18 | Tampering | subscription/transaction mutations | critical | mitigate | Assert one attempt for every mutation variant and return explicit non-retryable ambiguity. |
| T-32-19 | Information Disclosure | pagination telemetry context | high | mitigate | Keep cursor URL dispatch-only and propagate literal originating labels separately. |
</threat_model>

<verification>
Run both task commands, then `mix test` to prove shared pagination changes preserve every resource family.
</verification>

<success_criteria>
All lifecycle mutations are single-attempt, unsupported idempotency is absent, reads remain bounded, and provider cursors cannot contaminate telemetry context.
</success_criteria>

<output>
Create `.planning/phases/32-dependency-sdk-trust-boundary/32-07-SUMMARY.md` when done.
</output>
