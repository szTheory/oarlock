---
phase: 32-dependency-sdk-trust-boundary
plan: "15"
type: execute
wave: 12
depends_on: ["32-13"]
files_modified:
  - lib/paddle/subscriptions.ex
  - test/paddle/subscriptions_test.exs
autonomous: true
requirements: [SAFE-04]
gap_closure: true
estimate:
  tokens: 14000
  raw_tokens: 14000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Pause and resume reject more than one `:retry` key before body normalization or `Paddle.Http` dispatch, for both conflicting value orders (D-10, D-12)."
    - "Every duplicate-retry rejection is an `ArgumentError` built from static text and the key name, and the adapter observes zero physical attempts (D-12, D-17)."
    - "Legal lifecycle body options and one `retry: false` entry retain their existing request body, static operation/route context, and one-attempt ambiguity behavior (D-10, D-14)."
    - "[FLAGGED ASSUMPTION SAFE-04 idempotency] Repeating pause or resume remains an explicit caller decision; each valid invocation executes at most once and the SDK does not deduplicate or reconcile automatically."
    - "[FLAGGED ASSUMPTION SAFE-04 concurrency] Concurrent lifecycle calls validate their own immutable option list without shared retry state or order-dependent acceptance."
  artifacts:
    - path: lib/paddle/subscriptions.ex
      provides: "Order-independent duplicate retry rejection at pause/resume public normalization boundaries"
    - path: test/paddle/subscriptions_test.exs
      provides: "Both-order pause/resume rejection matrix with zero adapter dispatch"
  key_links:
    - from: lib/paddle/subscriptions.ex
      to: Paddle.Http.request/4
      via: "retry uniqueness is established before lifecycle body normalization and request option merge"
      pattern: "normalize_pause_opts|normalize_resume_opts"
    - from: test/paddle/subscriptions_test.exs
      to: lib/paddle/subscriptions.ex
      via: "fail-on-dispatch adapter covers both duplicate value orders for pause and resume"
      pattern: "pause|resume|retry"
  prohibitions:
    - id: PROHIB-SAFE-04-AUTO-RECONCILE
      requirement_id: SAFE-04
      category: autonomy
      status: unresolved
      verification: null
      flagged_unverified: true
      statement: "MUST NOT turn lifecycle reconciliation guidance into a second mutation attempt, background replay, SDK deduplication, or provider-state repair."
    - id: PROHIB-SAFE-04-FALSE-CERTAINTY
      requirement_id: SAFE-04
      category: transparency
      status: unresolved
      verification: null
      flagged_unverified: true
      statement: "MUST NOT report an uncertain lifecycle mutation as successful or definitively failed, or bypass the established Paddle.Error seam."
---

<objective>
Close the remaining public mutation-option hole by rejecting duplicate `:retry` entries for subscription pause and resume before order can change normalization or dispatch behavior (D-10, D-12, D-14, D-17).

Purpose: Make lifecycle mutation validation unique, deterministic, secret-safe, and incapable of enabling ambiguous replay.
Output: One resource-local uniqueness seam and a complete both-order zero-dispatch regression matrix.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-CONTEXT.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-RESEARCH.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-PATTERNS.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-VERIFICATION.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-07-SUMMARY.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-12-SUMMARY.md
@lib/paddle/subscriptions.ex
@test/paddle/subscriptions_test.exs
</context>

<assumption_delta_decision>
Primary noun: `Paddle.Client`. Decision: `no-change` — lifecycle calls continue to use one explicit client; this gap removes contradictory repeated configuration rather than adding another transport identity.
</assumption_delta_decision>

<dependency_rationale>
This plan is vertically executable at the public subscription boundary and shares no modified file with Plan 32-14, so both can run in Wave 12 after executed Plan 32-13. Task 1 proves pause end to end; Task 2 applies the same invariant to resume and all lifecycle regression paths.
</dependency_rationale>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Reject both duplicate retry orders through public pause</name>
  <read_first>
    - lib/paddle/subscriptions.ex
    - test/paddle/subscriptions_test.exs
    - lib/paddle/http.ex
    - .planning/phases/32-dependency-sdk-trust-boundary/32-VERIFICATION.md
    - .planning/phases/32-dependency-sdk-trust-boundary/32-12-SUMMARY.md
  </read_first>
  <files>lib/paddle/subscriptions.ex, test/paddle/subscriptions_test.exs</files>
  <behavior>
    - `Paddle.Subscriptions.pause/3` raises before normalization and dispatch for `[retry: false, retry: true]` and `[retry: true, retry: false]`.
    - The same duplicate invariant reaches `pause_immediately/3` through its shared pause normalizer.
    - A fail-on-dispatch adapter and attempt counter both prove zero physical requests for every rejected list.
    - One boolean retry entry plus valid `resume_at`/`on_resume` options preserves the existing body and one-attempt mutation policy.
  </behavior>
  <action>Write the public pause regression first using the exact two conflicting orders reproduced by verification, valid subscription/body values, and an adapter that records attempts then fails if reached. In `Paddle.Subscriptions`, add private `ensure_unique_retry_opt!/1` to count `:retry` occurrences before `Keyword.pop/2`, `build_pause_body/1`, DateTime normalization, or request option merging. Reject count greater than one with `ArgumentError` text containing only static wording and `retry`; preserve non-keyword/unknown/body validation, one legal boolean retry restriction, static operation/route/resource context, D-10 one-attempt mutation behavior, and D-14 ambiguity guidance. Exercise `pause_immediately/3` through the same shared path so no public pause variant bypasses the check.</action>
  <verify>
    <automated>mix test test/paddle/subscriptions_test.exs --trace</automated>
    <fails_when>Either duplicate order dispatches, pause and pause_immediately differ in uniqueness behavior, error text renders values, or a legal lifecycle request changes body/context/attempt count.</fails_when>
  </verify>
  <acceptance_criteria>Both conflicting duplicate orders raise the same secret-safe error through public pause paths with an adapter attempt count of zero, while valid pause behavior remains unchanged.</acceptance_criteria>
  <done>Pause option order can no longer collapse contradictory retry authority into a dispatched mutation.</done>
  <reversibility rating="costly">This intentionally tightens a public pre-1.0 option contract, but can be revised locally with coordinated types/docs/tests and no data migration.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply order-independent uniqueness to resume and lifecycle safety</name>
  <read_first>
    - lib/paddle/subscriptions.ex
    - test/paddle/subscriptions_test.exs
    - .planning/phases/32-dependency-sdk-trust-boundary/32-07-SUMMARY.md
    - .planning/phases/32-dependency-sdk-trust-boundary/32-12-SUMMARY.md
  </read_first>
  <files>lib/paddle/subscriptions.ex, test/paddle/subscriptions_test.exs</files>
  <behavior>
    - `Paddle.Subscriptions.resume/3` rejects both conflicting duplicate retry orders before `effective_from`/`on_resume` normalization and with zero adapter dispatch.
    - Pause, pause-immediately, and resume share one uniqueness rule rather than order-sensitive `Keyword.pop/2` behavior.
    - Existing invalid body values retain their established tagged errors; unknown option and non-keyword behavior remains deterministic.
    - Every valid lifecycle mutation still performs one attempt and returns the established ambiguous `%Paddle.Error{}` reconciliation contract on uncertain transport outcomes.
  </behavior>
  <action>Extend the RED matrix to `Paddle.Subscriptions.resume/3` for both conflicting duplicate orders and prove zero adapter calls. Reuse the Task 1 private uniqueness seam from `normalize_resume_opts/1` before body normalization or dispatch; do not create a parallel rule. Add a compact table-driven lifecycle test across pause, pause-immediately, and resume that checks duplicate rejection, legal `retry: false`, and the existing one-attempt ambiguous transport result. Preserve current option types, body validators, operation/resource IDs, lookup/webhook/provider-dashboard actions, and static error wording. Do not reintroduce idempotency keys or allow `retry: true` to opt a mutation into replay.</action>
  <verify>
    <automated>mix test test/paddle/subscriptions_test.exs --trace</automated>
    <fails_when>Resume accepts either order, any invalid call reaches transport, an existing tagged body-validation error changes, or lifecycle ambiguity/reconciliation no longer returns after exactly one attempt.</fails_when>
    <automated>mix test test/paddle/http_test.exs test/paddle/subscriptions_test.exs test/paddle/seam_test.exs</automated>
    <fails_when>The central retry contract, public subscription seam, or migration/documentation guard regresses after the local normalization change.</fails_when>
  </verify>
  <acceptance_criteria>All public pause/resume variants reject duplicate retry configuration before any normalization/dispatch independent of order, and the focused central/resource/seam suite remains green.</acceptance_criteria>
  <done>Every subscription lifecycle mutation option list has deterministic uniqueness and preserves the established non-replay contract.</done>
</task>

</tasks>

## Artifacts this phase produces

- No new tracked file, public SDK function, struct field, endpoint, dependency, script flag, schema, or runtime artifact is introduced.
- New private symbol: `Paddle.Subscriptions.ensure_unique_retry_opt!/1` checks `:retry` uniqueness before pause/resume normalization.
- New table-driven ExUnit cases/helpers in `test/paddle/subscriptions_test.exs` cover both conflicting orders for pause, pause-immediately, and resume with zero dispatch.

<threat_model>
ASVS level: 1. Blocking threshold: high.

## Trust Boundaries

| Boundary | Description |
|---|---|
| caller keyword list → subscription lifecycle normalization | Repeated untrusted configuration can be silently collapsed before policy sees it. |
| normalized lifecycle request → Paddle.Http/Req | A mutation crossing this boundary may affect provider state and must execute at most once. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-32-43 | Tampering | pause/resume option normalization | high | mitigate | Count/reject duplicate retry keys before pop, body normalization, merge, or dispatch in every lifecycle variant. |
| T-32-44 | Repudiation | order-dependent validation | high | mitigate | Assert both conflicting value orders yield the same pre-dispatch error and zero attempts. |
| T-32-45 | Elevation of Privilege | mutation retry option | critical | mitigate | Preserve method-aware rejection of retry enabling and the one-attempt ambiguity contract. |
| T-32-46 | Information Disclosure | validation errors | medium | mitigate | Construct errors from static text/key names and assert no option values are rendered. |
</threat_model>

<verification>
Run the two task commands, then include `test/paddle/subscriptions_test.exs` in the bounded compatibility selection before final Phase 32 verification so the exact duplicate-order regression participates in fresh SAFE-04 evidence.
</verification>

<success_criteria>
- Both conflicting duplicate retry orders fail for pause and resume before body normalization or dispatch.
- Pause-immediately shares the same invariant and no lifecycle variant bypasses it.
- Rejected calls produce zero adapter attempts and secret-safe deterministic errors.
- Valid body options, restrictive retry, static request context, one-attempt ambiguity, and reconciliation remain unchanged.
</success_criteria>

<output>
Create `.planning/phases/32-dependency-sdk-trust-boundary/32-15-SUMMARY.md` when done.
</output>
