---
phase: 32-dependency-sdk-trust-boundary
plan: "08"
type: execute
wave: 7
depends_on: [32-02, 32-03, 32-04, 32-05, 32-06, 32-07]
files_modified:
  - README.md
  - guides/getting-started.md
  - guides/telemetry.md
  - test/paddle/seam_test.exs
  - guides/accrue-seam.md
  - demo/README.md
  - CHANGELOG.md
  - lib/paddle/client.ex
  - lib/paddle/http.ex
  - lib/paddle/error.ex
  - bin/phase32_compatibility.sh
  - .planning/phases/32-dependency-sdk-trust-boundary/32-VALIDATION.md
autonomous: true
requirements: [SAFE-01, SAFE-06]
estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Public docs, examples, types, and module docs state the same tested constructor, safe-read retry, mutation ambiguity, telemetry, inspection, Req, and BEAM contracts."
    - "Running documentation generation or the contract suite repeatedly over unchanged input is deterministic and never mutates SDK/provider state."
    - "Concurrent readers receive one coherent evidence ladder distinguishing local/unit, MockServer, package/downstream, sandbox, hosted CI, and live-provider proof."
    - "No public text treats a header-presence/mock result as provider idempotency, replay, compatibility, hosted-CI, or live-provider evidence."
    - "The final complete compatibility matrix and online audit pass against the finished behavior, and 32-VALIDATION.md is signable as Nyquist-compliant."
  artifacts:
    - path: test/paddle/seam_test.exs
      provides: "Mechanical positive/negative public-contract guard"
    - path: guides/telemetry.md
      provides: "Exact safe telemetry schema and migration guidance"
    - path: CHANGELOG.md
      provides: "Pre-1.0 Req/retry/idempotency/telemetry/inspection/client migration notice"
    - path: .planning/phases/32-dependency-sdk-trust-boundary/32-VALIDATION.md
      provides: "Completed Wave 0 map, matrix evidence, measured feedback baseline, and sign-off"
  key_links:
    - from: test/paddle/seam_test.exs
      to: "README.md and guides/*.md"
      via: "positive required claims plus negative stale/unsupported claim patterns"
      pattern: 'File.read!'
    - from: bin/phase32_compatibility.sh
      to: .planning/phases/32-dependency-sdk-trust-boundary/32-VALIDATION.md
      via: "observed final matrix results and timings, never inferred evidence"
      pattern: 'phase32_compatibility'
---

<objective>
Publish one mechanically guarded, evidence-honest SDK contract and re-run the complete compatibility/advisory proof against the finished phase, per D-03, D-04, D-18, and D-19.

Purpose: Consumers receive accurate migration and operating guidance instead of stale retry/idempotency/telemetry claims or inflated proof language.
Output: Updated public/module docs and types, contract tests, migration changelog, final matrix evidence, and signed validation strategy.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-CONTEXT.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-RESEARCH.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-PATTERNS.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-VALIDATION.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-02-SUMMARY.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-03-SUMMARY.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-04-SUMMARY.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-05-SUMMARY.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-06-SUMMARY.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-07-SUMMARY.md
@guides/accrue-seam.md
@test/paddle/seam_test.exs

<interfaces>
Use the mapped `guides/accrue-seam.md` proof-boundary vocabulary and `test/paddle/seam_test.exs` positive/negative file-contract pattern.
Document Req `~> 0.7.4`, Elixir `~> 1.19` / OTP 28-tested toolchain without overstating older/newer BEAM proof, four total eligible read attempts, 60000 ms read-side 429 cap, single-attempt mutations, the Paddle.Error reconciliation fields from Plan 32-04, exact telemetry schema from Plan 32-06, and six-type inspection inventory from Plan 32-07.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Guard one consumer journey from construction through reconciliation and observability</name>
  <reversibility rating="costly">The documented public contract and negative claim guard coordinate multiple guides and downstream expectations, but remain a pre-1.0 documented migration.</reversibility>
  <read_first>README.md, guides/getting-started.md, guides/telemetry.md, test/paddle/seam_test.exs, lib/paddle/client.ex, lib/paddle/http.ex, lib/paddle/error.ex, .planning/phases/32-dependency-sdk-trust-boundary/32-PATTERNS.md</read_first>
  <files>README.md, guides/getting-started.md, guides/telemetry.md, test/paddle/seam_test.exs</files>
  <behavior>
    - A reader can construct sandbox/live/custom clients, understand invalid cases, and handle an ambiguous mutation without retrying it.
    - Telemetry docs list exact safe keys/measurements and contain no transport-object/raw payload promise.
    - Mechanical tests require the secure Req/BEAM, retry, ambiguity, telemetry, inspection, and migration claims and reject stale contradictory claims.
  </behavior>
  <action>Start with failing contract assertions in the mapped seam test. Update README and Getting Started from the tested runtime contracts, replace the obsolete idempotency example with retrieval/list, webhook, and provider-dashboard reconciliation, and rewrite telemetry guidance around the exact Plan 32-06 allowlist per D-18. Guard required version/constructor/retry/ambiguity/inspection claims and forbidden stale/unsafe claims mechanically. Phrase negative regex checks so task prose does not get copied into production docs; scope checks to public documentation files.</action>
  <verify>
    <automated>mix test test/paddle/seam_test.exs test/paddle/client_test.exs test/paddle/http_test.exs test/paddle/http/telemetry_test.exs --warnings-as-errors</automated>
    <fails_when>A required claim is absent, a stale contradictory claim matches, examples fail the runtime contract, telemetry promises forbidden objects/data, or any focused test fails/warns.</fails_when>
  </verify>
  <acceptance_criteria>One public journey accurately covers validated construction, bounded reads, single-attempt mutation reconciliation, safe telemetry, and inspection, with positive and negative mechanical guards.</acceptance_criteria>
  <done>The primary consumer contract is truthful and executable end to end.</done>
</task>

<task type="auto">
  <name>Task 2: Align module docs, downstream proof boundaries, demo, and migration history</name>
  <read_first>guides/accrue-seam.md, demo/README.md, CHANGELOG.md, lib/paddle/client.ex, lib/paddle/http.ex, lib/paddle/error.ex, test/paddle/seam_test.exs, .planning/phases/32-dependency-sdk-trust-boundary/32-PATTERNS.md</read_first>
  <files>guides/accrue-seam.md, demo/README.md, CHANGELOG.md, lib/paddle/client.ex, lib/paddle/http.ex</files>
  <action>Using the mapped Accrue proof-boundary vocabulary, distinguish local/unit, MockServer, package/downstream, sandbox, hosted CI, and live-provider evidence everywhere per D-19. Align Client and Http module docs/types with actual valid options, method-aware policy, operation context, and the absence of arbitrary idempotency. Update demo guidance without claiming Paddle provider state. Add a single coherent Unreleased/pre-1.0 migration entry naming the Req floor and observable constructor/retry/idempotency/telemetry/inspection changes, while explicitly leaving hosted CI authority and publication to later phases.</action>
  <verify>
    <automated>mix docs &amp;&amp; mix test test/paddle/seam_test.exs &amp;&amp; (cd demo &amp;&amp; mix precommit)</automated>
    <fails_when>Docs fail/warn, a proof tier is conflated/omitted, module types disagree with runtime options, demo implies sandbox/live evidence, migration impact is incomplete, or the demo/contract suite fails.</fails_when>
  </verify>
  <acceptance_criteria>All secondary guides, demo instructions, Client/Http docs/types, and changelog use one tested contract and evidence ladder without hosted/release overclaim.</acceptance_criteria>
  <done>Downstream and module-level contract guidance agrees with the primary journey.</done>
</task>

<task type="auto">
  <name>Task 3: Close error docs, re-run all proof, and sign validation</name>
  <precondition>`../accrue/CLAUDE.md`, its Paddle projection test, Hex registry access, and demo PostgreSQL are available for the final D-03 matrix.</precondition>
  <read_first>lib/paddle/error.ex, test/paddle/seam_test.exs, bin/phase32_compatibility.sh, .planning/phases/32-dependency-sdk-trust-boundary/32-VALIDATION.md, ../accrue/CLAUDE.md</read_first>
  <files>lib/paddle/error.ex, test/paddle/seam_test.exs, bin/phase32_compatibility.sh, .planning/phases/32-dependency-sdk-trust-boundary/32-VALIDATION.md</files>
  <action>Align Paddle.Error docs/types with the exact ambiguity/reconciliation fields and safe provider request-ID semantics, and complete seam assertions for all public docs and module contracts. Re-run the fail-closed D-03 runner against the finished behavior and update 32-VALIDATION.md with every row's observed result, measured focused/full runtimes, resolved Wave 0 items, `wave_0_complete: true`, `nyquist_compliant: true`, and approval only when all commands—including online `mix hex.audit` and the read-only Accrue seam—actually pass. Preserve pre-existing sibling and oarlock dirty paths; do not claim hosted CI or publish anything.</action>
  <verify>
    <automated>mix test test/paddle/seam_test.exs test/paddle/inspection_safety_test.exs &amp;&amp; ACCRUE_CHECKOUT=../accrue bin/phase32_compatibility.sh &amp;&amp; mix format --check-formatted</automated>
    <fails_when>A contract/canary/matrix/format command fails, any required lane is skipped or masked, `mix hex.audit` is nonzero, the sibling checkout or preserved dirty paths change, validation is signed without complete evidence, or hosted/release proof is claimed.</fails_when>
  </verify>
  <acceptance_criteria>Error docs/types and all public text match runtime; every D-03 and validation row has attributable green evidence; both validation booleans are true only after proof; preserved work remains untouched.</acceptance_criteria>
  <done>SAFE-01 and SAFE-06 close on the finished SDK contract with a complete, signable local proof record.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Runtime contract → public guidance | Stale examples/types can cause unsafe consumer behavior despite safe code. |
| Local/mock/package/hosted/live evidence → claims | Lower evidence tiers must not be represented as provider or hosted proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-32-07 | Repudiation | documentation/evidence claims | high | mitigate | Positive/negative contract tests and exact evidence ladder mechanically reject stale or inflated claims. |
| T-32-02 | Information Disclosure | telemetry/Inspect docs | high | mitigate | Docs expose only allowlisted schema and stable redaction behavior; safety tests run in final matrix. |
| T-32-04 | Tampering | retry/idempotency guidance | high | mitigate | Document read-only bounds and reconciliation; mechanically reject arbitrary replay/idempotency claims. |
| T-32-05 | Spoofing | client examples/base URLs | high | mitigate | Examples cover coherent canonical/custom cases and forbid credentialized/conflicting URLs. |
| T-32-06 | Denial of Service | Retry-After guidance | high | mitigate | Require the exact four-attempt and 60000 ms read-side cap in docs/tests. |
| T-32-SC | Tampering | dependency proof | high | mitigate | Final runner repeats official Hex audit and all compatibility lanes; no new package or duplicate scanner. |
</threat_model>

## Artifacts this phase produces

- Updated `README.md`, Getting Started, telemetry guide, Accrue seam guide, demo README, and CHANGELOG.
- Runtime-aligned module docs/types in `Paddle.Client`, `Paddle.Http`, and `Paddle.Error`.
- Mechanical positive/negative contract assertions in `test/paddle/seam_test.exs`.
- Final `bin/phase32_compatibility.sh` proof across root, adapters, telemetry, MockServer, package, demo, downstream Accrue, docs/Dialyzer, and online Hex audit.
- `32-VALIDATION.md` with all Wave 0 rows assigned/completed, measured sampling baselines, `wave_0_complete: true`, `nyquist_compliant: true`, and approval evidence.

<verification>
Execute the Task 3 command, compare documentation assertions with the exported runtime/types, and confirm the matrix records local evidence only while preserving all named dirty paths.
</verification>

<success_criteria>
Consumers receive one truthful tested contract, advisory and compatibility gates pass, and no false replay, disclosure, invalid-state, evidence-tier, hosted-CI, or release claim remains.
</success_criteria>

<output>
Create `.planning/phases/32-dependency-sdk-trust-boundary/32-08-SUMMARY.md` when done.
</output>
