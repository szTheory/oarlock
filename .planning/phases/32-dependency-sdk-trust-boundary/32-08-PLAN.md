---
phase: 32-dependency-sdk-trust-boundary
plan: "08"
type: execute
wave: 8
depends_on: ["32-05", "32-06", "32-07"]
files_modified:
  - lib/paddle/http/telemetry.ex
  - test/paddle/http/telemetry_test.exs
autonomous: true
requirements: [SAFE-02]
estimate:
  tokens: 27000
  raw_tokens: 27000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Concurrent or parallel requests emit independent paired start→stop/exception events per physical attempt, with request-local attempt numbers and durations (D-05, D-09)."
    - "The three public event names remain unchanged while measurements/metadata expose only exact allowlisted low-cardinality keys (D-05, D-06)."
    - "Every retry attempt emits exactly one start and one terminal event before Req consumes the transient outcome; subscriber lifecycle cleanup is deterministic (D-09)."
    - "Unique credential, URL/query, ID, body, response, exception, raw_data, and customer canaries are recursively absent from every telemetry outcome (D-06, D-09)."
  artifacts:
    - path: lib/paddle/http/telemetry.ex
      provides: "Attempt-scoped three-event topology and strict allowlisted projection"
    - path: test/paddle/http/telemetry_test.exs
      provides: "Exact-key, event-pairing, retry, concurrency, and recursive canary proofs"
  key_links:
    - from: lib/paddle/http/telemetry.ex
      to: Req request/response/error steps
      via: "terminal instrumentation is ordered before retry consumption"
      pattern: "append_(request|response|error)_steps"
    - from: lib/paddle/http/telemetry.ex
      to: resource operation/route context
      via: "projection reads only static context established in Plans 05-07"
      pattern: "operation|route"
  prohibitions:
    - id: PROHIB-SAFE-02-DEBUG-BYPASS
      requirement_id: SAFE-02
      category: privacy
      status: unresolved
      verification: null
      flagged_unverified: true
      statement: "MUST NOT add a debug, verbose, subscriber opt-in, or exception-only path that restores forbidden request, response, URL, header, body, credential, customer, or raw_data payloads."
    - id: PROHIB-SAFE-02-CARDINALITY
      requirement_id: SAFE-02
      category: safety
      status: unresolved
      verification: null
      flagged_unverified: true
      statement: "MUST NOT derive operation or route labels from runtime identifiers, cursor URLs, query values, signed capabilities, or provider payload text."
---

<objective>
Replace unsafe transport-object telemetry with paired per-attempt allowlisted events while preserving the three public names (D-05, D-06, D-09).

Purpose: Let subscribers observe stable operational facts without receiving any secret-bearing or high-cardinality transport state.
Output: Attempt-scoped instrumentation and exhaustive exact-key/canary/concurrency tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-dependency-sdk-trust-boundary/32-07-SUMMARY.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-CONTEXT.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-PATTERNS.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-RESEARCH.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-VALIDATION.md
@lib/paddle/http/telemetry.ex
@test/paddle/http/telemetry_test.exs
@lib/paddle/http.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Emit one allowlisted span pair for every physical attempt</name>
  <read_first>
    - lib/paddle/http/telemetry.ex
    - test/paddle/http/telemetry_test.exs
    - lib/paddle/http.ex
    - lib/paddle/client.ex
  </read_first>
  <files>lib/paddle/http/telemetry.ex, test/paddle/http/telemetry_test.exs</files>
  <behavior>
    - Event names remain `[:paddle, :request, :start|stop|exception]`.
    - Start measurements are exactly `system_time`; terminal measurements are exactly `duration` in native monotonic units.
    - Metadata keys are exact subsets of method, operation, route, host, status/error_class, result, and attempt for the applicable outcome.
    - Two-attempt and four-attempt reads produce equal start and terminal counts with ordered attempt values.
  </behavior>
  <action>Follow the exact/self `lib/paddle/http/telemetry.ex` Req-step attachment and subscriber-lifecycle analogs mapped in 32-PATTERNS.md. Retain `attach/1` and the three D-05 names; order terminal hooks before retry, store request-local attempt state privately, and project only the D-06 measurement/metadata allowlists. Per D-18, update Telemetry @moduledoc/@doc/specs to enumerate the exact event names, per-outcome keys, per-physical-attempt pairing, duration units, and forbidden transport objects.</action>
  <verify>
    <automated>mix test test/paddle/http/telemetry_test.exs --trace</automated>
    <fails_when>Event names change, exact key sets differ, any attempt lacks/duplicates a terminal pair, terminal instrumentation occurs after retry, or duration is not monotonic/native.</fails_when>
  </verify>
  <acceptance_criteria>Success, provider error, transport exception, two-attempt retry, and four-attempt terminal cases all have exact names/keys and paired counts.</acceptance_criteria>
  <done>The telemetry lifecycle is stable and attempt-scoped.</done>
  <reversibility rating="costly">The metadata schema intentionally changes for safety while event names remain compatible.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove recursive absence and concurrent subscriber isolation</name>
  <read_first>
    - test/paddle/http/telemetry_test.exs
    - lib/paddle/http/telemetry.ex
    - test/paddle/http_test.exs
  </read_first>
  <files>test/paddle/http/telemetry_test.exs</files>
  <behavior>
    - Unique canaries in auth, URL/path/query, IDs, headers, request/response bodies, exceptions, customer data, and raw_data are absent from measurements and metadata.
    - `%Req.Request{}`, `%Req.Response{}`, and exception structs never occur at any depth.
    - Parallel requests and uniquely attached subscribers receive only their own paired attempt sequences.
    - Handler IDs are unique and detached in `on_exit/1`.
  </behavior>
  <action>Follow the mapped telemetry test-isolation pattern in 32-PATTERNS.md: async ExUnit, unique handler IDs, deterministic adapters/counters, and `on_exit/1` cleanup. Build the recursive walker, seed distinct D-06 canaries, assert forbidden structs/leaves are absent, and exercise parallel retried/non-retried requests with independent correlation. This resolves the SAFE-02 concurrency probe and mechanically cross-checks the module-doc allowlist added by Task 1.</action>
  <verify>
    <automated>mix test test/paddle/http/telemetry_test.exs</automated>
    <fails_when>Any canary or forbidden struct is reachable, concurrent event sequences cross, handler cleanup leaks, or assertions accept additional keys.</fails_when>
  </verify>
  <acceptance_criteria>Exact allowlists and recursive absence hold across every outcome/retry/concurrent request, and Telemetry module docs/specs state those exact tested sets (D-18).</acceptance_criteria>
  <done>SAFE-02 is exhaustively proven at the subscriber boundary.</done>
</task>

</tasks>

## Artifacts this phase produces

- Retained public events: `[:paddle, :request, :start]`, `[:paddle, :request, :stop]`, `[:paddle, :request, :exception]`.
- Telemetry measurement keys: `system_time` (start), `duration` (terminal).
- Telemetry metadata keys: `method`, `operation`, `route`, `host`, `status` or `error_class`, `result`, `attempt` as outcome-appropriate exact sets.
- Private telemetry helpers for attempt increment, start state, projection, and terminal emission; no new public function, struct/type field, CLI flag, or path.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Req transport state → telemetry subscriber | Full requests/responses/exceptions currently cross into arbitrary subscriber code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-32-20 | Information Disclosure | telemetry metadata/measurements | critical | mitigate | Exact allowlist projection plus recursive canaries and forbidden-struct assertions for every outcome. |
| T-32-21 | Repudiation | retry attempt topology | medium | mitigate | Pair start/terminal events before retry with explicit attempt counts and concurrency tests. |
| T-32-22 | Denial of Service | telemetry cardinality | medium | mitigate | Use only static operation/routes and sanitized host, never runtime identifiers/query/body text. |
</threat_model>

<verification>
Run `mix test test/paddle/http/telemetry_test.exs` and then `mix test` to prove subscriber instrumentation does not alter transport behavior.
</verification>

<success_criteria>
Three event names remain stable, every physical attempt is paired, exact allowlists hold, and no forbidden payload/canary is reachable under retries or concurrency.
</success_criteria>

<output>
Create `.planning/phases/32-dependency-sdk-trust-boundary/32-08-SUMMARY.md` when done.
</output>
