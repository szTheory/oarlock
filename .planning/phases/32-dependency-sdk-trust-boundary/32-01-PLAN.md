---
phase: 32-dependency-sdk-trust-boundary
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - mix.exs
  - mix.lock
  - demo/mix.lock
  - test/paddle/req_compatibility_test.exs
  - .planning/phases/32-dependency-sdk-trust-boundary/32-VALIDATION.md
autonomous: true
requirements: [SAFE-01]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A root consumer resolves Req at the secure ~> 0.7.4 floor and can execute one real Paddle.Client -> Paddle.Http -> Req adapter request."
    - "The demo resolves the same secure Req line through its path dependency without changing the demo's dependency ownership."
  assumptions:
    - requirement: SAFE-01
      statement: "The spec-less edge probe classified SAFE-01 as unclassified; advisory closure therefore remains explicitly flagged until the online audit and complete compatibility matrix produce execution evidence."
      status: unresolved
      flagged: true
  artifacts:
    - path: mix.exs
      provides: "Req ~> 0.7.4 dependency floor"
      contains: '"~> 0.7.4"'
    - path: mix.lock
      provides: "Root secure Req resolution"
    - path: demo/mix.lock
      provides: "Demo secure Req resolution"
    - path: test/paddle/req_compatibility_test.exs
      provides: "Req 0.7 end-to-end compatibility tracer"
  key_links:
    - from: mix.exs
      to: mix.lock
      via: "Mix resolution, never a hand-edited lock entry"
      pattern: 'req.*0\\.7'
    - from: demo/mix.lock
      to: mix.exs
      via: "demo path dependency on the root :paddle package"
      pattern: 'req.*0\\.7'
---

<objective>
Move the root package and demo consumer onto the secure Req 0.7.4 compatibility floor in an isolated dependency-only plan, per D-01 and D-02.

Purpose: Prove the hardest prerequisite—the dependency can resolve and carry one real SDK request—before any retry, telemetry, inspection, or validation semantics change.
Output: Regenerated root/demo locks, a focused compatibility tracer, online advisory evidence, and measured Wave 0 baselines.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-CONTEXT.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-RESEARCH.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-PATTERNS.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-VALIDATION.md
@mix.exs
@demo/mix.exs
@lib/paddle/client.ex
@lib/paddle/http.ex

<interfaces>
Current public seam: Paddle.Client.new!/1 returns %Paddle.Client{} and Paddle.Http.request/4 returns {:ok, body} or {:error, %Paddle.Error{}} for provider/transport outcomes. Preserve that seam in this dependency-only plan.
The dependency declaration/lock pattern is the exact `mix.exs`/generated `mix.lock` analog mapped in 32-PATTERNS.md; the new focused test follows `test/paddle/http_test.exs`.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Carry one SDK request across the secure Req floor</name>
  <precondition>`mix hex.info req 0.7.4` can reach the Hex registry without printing credentials.</precondition>
  <reversibility rating="costly">Changing the public dependency floor affects every package consumer and both lock graphs, but remains reversible through a coordinated manifest and lock change.</reversibility>
  <read_first>mix.exs, mix.lock, lib/paddle/client.ex, lib/paddle/http.ex, test/paddle/http_test.exs, .planning/phases/32-dependency-sdk-trust-boundary/32-PATTERNS.md</read_first>
  <files>mix.exs, mix.lock, test/paddle/req_compatibility_test.exs</files>
  <behavior>
    - The resolved Req version satisfies the `~> 0.7.4` floor from D-01.
    - A valid client sends one adapter-backed GET through Paddle.Http and preserves the existing success tuple.
    - The test uses Req 0.7's supported adapter shape and emits no adapter deprecation warning.
  </behavior>
  <action>Following the exact manifest analog in 32-PATTERNS.md, change only the root Req constraint to `~> 0.7.4`, use Mix to update the root lock, and add the focused end-to-end compatibility test. Start with the failing version/adapter assertion, then make it green. Do not change client retry policy, telemetry payloads, inspection, request options, or public runtime behavior in this plan, per D-02. Confirm the selected 0.7 patch remains outside the documented advisory ranges before accepting it, per D-01 and D-04.</action>
  <verify>
    <automated>mix deps.get &amp;&amp; mix test test/paddle/req_compatibility_test.exs --warnings-as-errors &amp;&amp; mix hex.audit</automated>
    <fails_when>Any command exits nonzero, Req resolves below 0.7.4, the adapter-backed request changes the public result shape, a warning is emitted, or Hex reports an active advisory/retirement.</fails_when>
  </verify>
  <acceptance_criteria>The root manifest contains `{:req, "~> 0.7.4"}`, the generated lock resolves an advisory-clean 0.7 patch, the focused request passes without warnings, and `mix hex.audit` exits 0 per D-04.</acceptance_criteria>
  <done>The secure root dependency slice is executable end to end and no non-dependency SDK semantics changed.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate the demo resolution and record dependency baselines</name>
  <precondition>The root Req 0.7 resolution from Task 1 exists and the local PostgreSQL required by `demo/mix precommit` is reachable.</precondition>
  <read_first>demo/AGENTS.md, demo/mix.exs, demo/mix.lock, .planning/phases/32-dependency-sdk-trust-boundary/32-VALIDATION.md</read_first>
  <files>demo/mix.lock, .planning/phases/32-dependency-sdk-trust-boundary/32-VALIDATION.md</files>
  <action>Regenerate the demo lock through Mix so its `{:paddle, path: "../"}` dependency resolves the same Req 0.7 line; do not add a direct Req dependency to the demo manifest. Run the focused and full root suites plus demo precommit, measure their actual runtimes, and record those measurements and the exact selected root/demo Req versions in 32-VALIDATION.md. Keep the remaining Wave 0 rows pending and do not mark Nyquist sign-off complete yet.</action>
  <verify>
    <automated>mix test test/paddle/req_compatibility_test.exs &amp;&amp; mix test &amp;&amp; (cd demo &amp;&amp; mix deps.get &amp;&amp; mix precommit)</automated>
    <fails_when>A command exits nonzero, the demo lock remains on Req 0.5.x/0.6.x, root or demo compilation warns/fails, tests fail, or the measured baseline is absent from 32-VALIDATION.md.</fails_when>
  </verify>
  <acceptance_criteria>Both generated lockfiles resolve the secure Req line, the root and demo suites pass, and 32-VALIDATION.md records measured focused/full runtimes without inventing a budget.</acceptance_criteria>
  <done>The isolated root/demo dependency migration is complete and ready for the full adapter/matrix expansion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Hex registry → lockfiles | External package metadata and archive bytes become executable SDK dependencies. |
| Demo/root manifests → consumers | Two resolution graphs must agree on the secure compatibility floor. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-32-01 | Tampering | Req dependency resolution | high | mitigate | Pin the reviewed `~> 0.7.4` floor, regenerate locks via Mix, execute the compatibility tracer, and require online `mix hex.audit`. |
| T-32-SC | Tampering | Hex package install | high | mitigate | Req is an existing dependency verified through the official Hex package/source and audit; no npm/pip/cargo package or unaudited new dependency is installed. |
| T-32-02 | Information Disclosure | telemetry/Inspect/raw_data | high | mitigate | Owned by Plans 32-06 and 32-07; this isolated plan forbids modifying those payload contracts. |
| T-32-04 | Tampering | mutation replay/false idempotency | high | mitigate | Owned by Plans 32-04 and 32-05; the compatibility tracer is a GET and introduces no replay claim. |
| T-32-05 | Spoofing | client base URL | high | mitigate | Owned by Plan 32-03; the tracer uses a fixed noncredentialized test URL. |
| T-32-06 | Denial of Service | Retry-After handling | high | mitigate | Owned by Plan 32-04; this plan preserves behavior only long enough to prove dependency compatibility. |
</threat_model>

## Artifacts this phase produces

- `mix.exs`: Req `~> 0.7.4` compatibility floor (D-01).
- `mix.lock` and `demo/mix.lock`: Mix-generated secure Req resolutions.
- `test/paddle/req_compatibility_test.exs`: focused Req 0.7 end-to-end compatibility tracer.
- `32-VALIDATION.md`: selected-version and measured-runtime evidence fields populated for Wave 0.

<verification>
Run the two task commands, confirm `git diff -- mix.exs mix.lock demo/mix.lock` contains only dependency-resolution changes, and confirm `mix hex.audit` reports no active Req advisory.
</verification>

<success_criteria>
SAFE-01 has a secure resolved floor, real request proof, coherent root/demo locks, online audit evidence, and measured baselines without behavior work leaking into this plan.
</success_criteria>

<output>
Create `.planning/phases/32-dependency-sdk-trust-boundary/32-01-SUMMARY.md` when done.
</output>
