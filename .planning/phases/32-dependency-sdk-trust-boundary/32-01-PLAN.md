---
phase: 32-dependency-sdk-trust-boundary
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - mix.exs
  - mix.lock
  - demo/mix.lock
  - test/paddle/http_test.exs
autonomous: true
requirements: [SAFE-01]
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A root consumer resolves Req from the secure `~> 0.7.4` line and completes one real custom-adapter request through Paddle.Http (D-01, D-02)."
    - "The demo independently resolves the same secure Req line and `mix hex.audit` reports no active advisory (D-01, D-04)."
    - "The dependency-only slice changes no retry, telemetry, inspection, constructor-validation, hosted-CI, or release behavior (D-02, D-04)."
  artifacts:
    - path: mix.exs
      provides: "Req `~> 0.7.4` package constraint"
      contains: "{:req, \"~> 0.7.4\"}"
    - path: mix.lock
      provides: "Generated root Req 0.7-compatible resolution"
    - path: demo/mix.lock
      provides: "Generated demo resolution through the root path dependency"
    - path: test/paddle/http_test.exs
      provides: "Req 0.7-supported custom-adapter tracer"
  key_links:
    - from: mix.exs
      to: mix.lock
      via: "Mix dependency resolution, never manual lock editing"
      pattern: "req.*0\\.7"
    - from: test/paddle/http_test.exs
      to: lib/paddle/http.ex
      via: "one custom adapter request using the resolved Req runtime"
      pattern: "Http\\.request"
  assumptions:
    - id: EDGE-SAFE-01-UNCLASSIFIED
      status: flagged-unverified
      statement: "SAFE-01's deterministic edge probe was unclassified; execution must treat successful resolution, the complete compatibility matrix, and a live clean audit as separate evidence rather than infer one from another."
  prohibitions:
    - id: PROHIB-SAFE-01-EVIDENCE
      requirement_id: SAFE-01
      category: transparency
      status: unresolved
      verification: null
      flagged_unverified: true
      statement: "MUST NOT call the advisory closed from a manifest constraint or local compile alone; the resolved locks, compatibility matrix, and online audit remain distinct evidence."
    - id: PROHIB-SAFE-01-AUTHORITY
      requirement_id: SAFE-01
      category: safety
      status: unresolved
      verification: null
      flagged_unverified: true
      statement: "MUST NOT turn local dependency success into hosted exact-SHA, publication, or release authority, or overwrite the user's existing `.tool-versions` change to obtain a pass."
---

<objective>
Deliver the production-quality dependency tracer: a consumer resolves Req `~> 0.7.4`, executes one real Paddle HTTP adapter path, and the demo resolves coherently, without mixing any SDK behavior change into the migration (D-01, D-02, D-04).

Purpose: Prove the new dependency line itself before every later safety change builds on it.
Output: Root/demo generated locks, the root manifest floor, and one Req 0.7-compatible adapter tracer.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-CONTEXT.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-PATTERNS.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-RESEARCH.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-VALIDATION.md
@mix.exs
@mix.lock
@demo/mix.exs
@demo/mix.lock
@.tool-versions
@lib/paddle/http.ex
@test/paddle/http_test.exs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Resolve Req 0.7.4 and drive one custom-adapter request end to end</name>
  <read_first>
    - mix.exs
    - mix.lock
    - .tool-versions
    - lib/paddle/http.ex
    - test/paddle/http_test.exs
  </read_first>
  <files>mix.exs, mix.lock, test/paddle/http_test.exs</files>
  <behavior>
    - The root lock resolves a Req version satisfying `~> 0.7.4`.
    - A Req 0.7-supported adapter returns a 2xx provider body through `Paddle.Http.request/4`.
    - Existing error normalization remains green before behavior work begins.
  </behavior>
  <action>Follow the `mix.exs` dependency-manifest/lock analog and `test/paddle/http_test.exs` deterministic-adapter analog mapped in 32-PATTERNS.md. Per D-01 and D-02, change only the root Req constraint to `~> 0.7.4`, regenerate `mix.lock` with Mix, and migrate the adapter construction in `test/paddle/http_test.exs` to Req 0.7's supported adapter shape. Begin with the focused adapter assertion failing against the old fixture form, then make the compatibility-only update. Preserve dependency ordering and optional flags. Treat `.tool-versions` as read-only user state; do not edit it and do not use broad dependency cleanup.</action>
  <verify>
    <automated>mix deps.get &amp;&amp; MIX_ENV=test mix do compile --warnings-as-errors + test --warnings-as-errors test/paddle/http_test.exs</automated>
    <fails_when>Dependency resolution fails, Req does not resolve from 0.7.x, the custom adapter emits compatibility warnings, or the focused HTTP suite fails.</fails_when>
  </verify>
  <acceptance_criteria>The root manifest and generated lock select the secure line, and one real adapter-backed request completes through the existing public transport result shape.</acceptance_criteria>
  <done>The root dependency tracer passes with no SDK retry, telemetry, inspection, or validation behavior change.</done>
  <reversibility rating="reversible">The manifest and generated lock can be reverted together; compatibility evidence makes the direction explicit.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Resolve the demo lock and obtain the advisory verdict</name>
  <precondition>The configured Hex registry is reachable for dependency resolution and `mix hex.audit`; if it is not, halt before claiming SAFE-01 evidence.</precondition>
  <read_first>
    - demo/mix.exs
    - demo/mix.lock
    - mix.exs
    - .tool-versions
  </read_first>
  <files>demo/mix.lock</files>
  <action>Follow the mapped `mix.exs` ordered-dependency and generated-lock pattern in 32-PATTERNS.md. Per D-01, D-02, and D-04, regenerate the demo lock through its tracked `{:paddle, path: "../"}` dependency so its transitive Req resolution agrees with the root floor. Run the built-in Hex audit; add no scanner and make no workflow, publication, or release change. Record resolved Req versions and measured focused/demo command durations in the summary for later matrix comparison.</action>
  <verify>
    <automated>(cd demo &amp;&amp; mix deps.get &amp;&amp; mix precommit) &amp;&amp; mix hex.audit</automated>
    <fails_when>The demo lock retains Req 0.5.x/0.6.x, demo precommit fails, the audit is unavailable, or the audit reports an active advisory/retirement.</fails_when>
  </verify>
  <acceptance_criteria>Both generated locks resolve the secure Req line and the authoritative online audit exits zero.</acceptance_criteria>
  <done>The isolated dependency delta is ready for the complete compatibility expansion in Plan 02.</done>
  <reversibility rating="reversible">The generated demo lock can be restored with the manifest/lock set if compatibility fails.</reversibility>
</task>

</tasks>

## Artifacts this phase produces

- Updated dependency constraint: root `{:req, "~> 0.7.4"}`.
- Generated paths: `mix.lock`, `demo/mix.lock` with Req 0.7-compatible resolutions.
- Updated test adapter fixture(s) in `test/paddle/http_test.exs`; no new public symbol, CLI flag, struct field, or function is introduced by this plan.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Hex registry → generated locks | External package metadata becomes executable dependency state. |
| Req adapter → Paddle.Http | Third-party transport behavior crosses the SDK normalization seam. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-32-01 | Tampering | `mix.exs`, root/demo locks | high | mitigate | Pin the D-01 floor, generate locks only through Mix, run the tracer/demo gates, and require built-in `mix hex.audit`. |
| T-32-02 | Repudiation | dependency acceptance evidence | medium | mitigate | Record exact resolved versions and separate compile, compatibility, and audit results. |
| T-32-03 | Denial of Service | registry-dependent verification | low | accept | A registry outage blocks acceptance explicitly; it cannot be mistaken for a clean audit. |
</threat_model>

<verification>
Run the two task commands. Confirm `git diff -- .tool-versions` still contains only the pre-existing user modification and that no SDK behavior or CI/release file entered the plan diff.
</verification>

<success_criteria>
Req resolves from `~> 0.7.4` in root and demo, the adapter tracer and demo precommit pass, `mix hex.audit` exits zero, and D-01/D-02/D-04 remain isolated from later behavior.
</success_criteria>

<output>
Create `.planning/phases/32-dependency-sdk-trust-boundary/32-01-SUMMARY.md` when done.
</output>
