---
phase: 32-dependency-sdk-trust-boundary
plan: "14"
type: execute
wave: 12
depends_on: ["32-13"]
files_modified:
  - bin/phase32_compatibility.sh
  - bin/phase32_contract_proof.sh
  - test/paddle/http_test.exs
  - test/paddle/seam_test.exs
autonomous: true
requirements: [SAFE-01, SAFE-06]
gap_closure: true
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A `--full` compatibility attempt invalidates its destination receipt before any preflight can fail, and the real self-test proves a seeded stale receipt cannot survive a missing or invalid Accrue checkout (D-03, D-04)."
    - "The bounded contract proof has meaningful cold-run margin inside the documented 30-second wrapper by using deterministic evidence without real retry backoff waits, while independent retry-decision coverage remains explicit (D-10, D-11, D-18)."
    - "The contract verifier publishes acceptance only from its successful process-exit path; timeout, signal, or nonzero exit removes staged and destination acceptance (D-03, D-04, D-19)."
    - "[FLAGGED ASSUMPTION SAFE-01 unclassified] Resolution, compatibility, online audit, and receipt freshness remain distinct facts; a current receipt is evidence only for the exact completed local run."
    - "[FLAGGED ASSUMPTION SAFE-06 idempotency] Repeating a verifier run replaces the destination only after another complete success; repeated failure leaves no passing receipt."
    - "[FLAGGED ASSUMPTION SAFE-06 concurrency] Parallel or interrupted verifier runs cannot promote a partial candidate, overwrite another run through a shared temporary name, or preserve stale acceptance."
  artifacts:
    - path: bin/phase32_compatibility.sh
      provides: "Preflight-safe full receipt invalidation, deterministic bounded evidence, and stale-receipt self-test"
    - path: bin/phase32_contract_proof.sh
      provides: "Cold-margin bounded proof whose acceptance is finalized only by successful process exit"
    - path: test/paddle/seam_test.exs
      provides: "Mechanical contract guards for bounded/full evidence separation and receipt lifecycle"
  key_links:
    - from: bin/phase32_compatibility.sh
      to: phase32-compatibility.receipt
      via: "destination invalidation precedes every full-mode preflight and atomic publication follows all 13 rows"
      pattern: "FULL_RECEIPT_PATH"
    - from: bin/phase32_contract_proof.sh
      to: phase32-contract-verifier.receipt
      via: "successful EXIT finalization promotes a same-directory candidate; all abnormal exits remove acceptance"
      pattern: "VERIFY_RECEIPT_PATH"
    - from: bin/phase32_contract_proof.sh
      to: bin/phase32_compatibility.sh
      via: "bounded deterministic compatibility evidence remains subordinate to complete full acceptance"
      pattern: "phase32_compatibility.sh"
  prohibitions:
    - id: PROHIB-SAFE-01-EVIDENCE
      requirement_id: SAFE-01
      category: transparency
      status: unresolved
      verification: null
      flagged_unverified: true
      statement: "MUST NOT call the advisory closed from a manifest constraint or local compile alone; resolved locks, compatibility, online audit, and receipt freshness remain separate evidence."
    - id: PROHIB-SAFE-01-AUTHORITY
      requirement_id: SAFE-01
      category: safety
      status: unresolved
      verification: null
      flagged_unverified: true
      statement: "MUST NOT turn bounded local evidence into hosted exact-SHA, sandbox, live-provider, release, or publication authority."
    - id: PROHIB-SAFE-06-EVIDENCE-INFLATION
      requirement_id: SAFE-06
      category: transparency
      status: unresolved
      verification: null
      flagged_unverified: true
      statement: "MUST NOT collapse bounded verification, full compatibility, package/downstream, hosted-CI, sandbox, or live-provider evidence into one interchangeable verdict."
    - id: PROHIB-SAFE-06-SPLIT-CONTRACT
      requirement_id: SAFE-06
      category: safety
      status: unresolved
      verification: null
      flagged_unverified: true
      statement: "MUST NOT let a focused timing shortcut remove independent retry-policy proof, any required full-matrix row, or the two-manifest full acceptance contract."
---

<objective>
Make Phase 32 acceptance fail closed across preflight failure, timeout, signal, and nonzero termination while restoring meaningful cold-run margin inside the documented 30-second verifier wrapper (D-03, D-04, D-10, D-11, D-18, D-19).

Purpose: Ensure a receipt means the current local process completed the evidence contract, never that an older or externally terminated run once printed success.
Output: Hardened compatibility and contract proof scripts plus executable receipt-lifecycle and boundedness guards.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-CONTEXT.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-RESEARCH.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-PATTERNS.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-VERIFICATION.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-13-SUMMARY.md
@bin/phase32_compatibility.sh
@bin/phase32_contract_proof.sh
@test/paddle/seam_test.exs
</context>

<assumption_delta_decision>
Primary noun: `Paddle.Client`. Decision: `no-change` — the detected custom-base-URL configurability remains one validated explicit-client identity; receipt lifecycle and verifier timing introduce no alternative client identity.
</assumption_delta_decision>

<dependency_rationale>
This receipt/proof plan and Plan 32-15 modify disjoint tracked files and close independent trust boundaries, so both depend directly on executed Plan 32-13 and can run in Wave 12. This plan leads the appended gap set with the end-to-end stale-receipt tracer; its second task expands that proven lifecycle to bounded contract termination.
</dependency_rationale>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Fail a real full preflight without preserving seeded acceptance</name>
  <read_first>
    - bin/phase32_compatibility.sh
    - test/paddle/seam_test.exs
    - .planning/phases/32-dependency-sdk-trust-boundary/32-VERIFICATION.md
    - .planning/phases/32-dependency-sdk-trust-boundary/32-13-SUMMARY.md
  </read_first>
  <files>bin/phase32_compatibility.sh, test/paddle/seam_test.exs</files>
  <behavior>
    - Seed the exact full receipt destination with a passing-looking stale body, invoke the real full-matrix entry path with an empty and then invalid `ACCRUE_CHECKOUT`, and observe nonzero status with no destination receipt.
    - Full mode selects and invalidates `FULL_RECEIPT_PATH` before either Accrue preflight and before any matrix row can return.
    - A successful full run still publishes one same-directory atomically renamed receipt only after all 13 rows and tracked-diff equality pass.
    - Failure and interruption self-tests remain isolated under temporary evidence directories and never delete evidence outside the selected destination.
  </behavior>
  <action>Write the stale-receipt regression first against `bin/phase32_compatibility.sh --self-test`, using the script's real `run_matrix`/full-mode path rather than a fake row alone. Refactor full receipt selection, evidence-directory creation, and destination invalidation into an ordering that executes before the D-03 downstream checkout preflight. Extend `self_test` with isolated seeded destinations for both missing and structurally invalid Accrue checkout cases; assert the actual full path returns nonzero and removes the planted pass in both cases. Retain the existing failure, interruption, and one-success receipt cases, all 13 named full rows, tracked-diff equality, same-directory atomic rename, online `mix hex.audit`, and D-04/D-19 evidence boundaries. Strengthen the seam test only with stable structural assertions that pin preflight-before-receipt ordering and the seeded-preflight self-test contract; do not duplicate shell behavior in Elixir.</action>
  <verify>
    <automated>bin/phase32_compatibility.sh --self-test</automated>
    <fails_when>A seeded full receipt survives either failing preflight, the real full path unexpectedly reaches a matrix row, an interrupted/failed fake run leaves acceptance, or a successful fake run does not publish exactly one receipt.</fails_when>
    <automated>mix test test/paddle/seam_test.exs --trace</automated>
    <fails_when>The executable usage/evidence split changes, invalidation can occur after preflight, or the full 13-row contract is removed.</fails_when>
  </verify>
  <acceptance_criteria>The exact verifier regression—seed stale success, fail full preflight, inspect destination—now ends nonzero with no acceptance receipt, while complete full publication remains atomic and unchanged in authority.</acceptance_criteria>
  <done>A failed or interrupted compatibility attempt cannot be mistaken for current SAFE-01 acceptance.</done>
  <reversibility rating="costly">Receipt lifecycle is consumed by verification, but it can be revised through coordinated script and seam-test changes without changing the public SDK runtime.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Finalize bounded acceptance only on successful process exit</name>
  <read_first>
    - bin/phase32_contract_proof.sh
    - bin/phase32_compatibility.sh
    - test/paddle/seam_test.exs
    - test/paddle/http_test.exs
    - .planning/phases/32-dependency-sdk-trust-boundary/32-VERIFICATION.md
  </read_first>
  <files>bin/phase32_contract_proof.sh, bin/phase32_compatibility.sh, test/paddle/http_test.exs, test/paddle/seam_test.exs</files>
  <behavior>
    - A cold bounded contract run exits zero with reproducible margin inside 30 seconds and retains explicit SAFE-01 through SAFE-06 verdicts, online audit, tracked-diff equality, and byte-identical concurrent-reader evidence.
    - Bounded compatibility selects deterministic named/line-scoped tests or direct retry-classifier decisions; it does not execute real retry sleeps, while exact retry eligibility, delays, and four-attempt ceilings remain independently tested.
    - Contract verifier output is staged under a unique same-directory candidate and promoted from the successful EXIT path only after every check and cleanup-sensitive step succeeds.
    - INT, TERM, timeout, and any nonzero EXIT remove both the candidate and destination receipt; `--self-test-termination` launches and terminates the verifier after its candidate is ready and proves no passing destination remains.
    - Full mode retains concurrent compile/docs readers, two complete 13-row matrices, and equal canonical manifests; bounded evidence remains explicitly subordinate.
  </behavior>
  <action>Start with shell/seam regressions for the measured timeout failure and receipt left after termination. Separate the lightweight bounded concurrent-read integrity check from the full mode's compile/docs readers so `--verify` proves byte-identical, isolated, concurrent reads without paying the full acceptance build cost; keep the existing compile/docs concurrency in `--full`. Narrow `run_bounded_mix` to deterministic tagged, named, or line-scoped tests that cover each SAFE invariant, and add a direct retry-decision test in `test/paddle/http_test.exs` that checks the D-10/D-11 method/status/delay decisions without sleeping; leave the independent four-attempt retry execution tests in the ordinary/full suite. Give the nested bounded Mix run a unique build directory beneath `PROOF_DIR` so every exact wrapper measures a cold verifier path. Rework contract receipt publication into a unique same-directory candidate finalized by an EXIT handler only for zero status after `run_verify` marks every check complete. INT/TERM and nonzero EXIT must delete the candidate and destination before cleanup. Add `--self-test-termination`, an internal deterministic mode that pauses after candidate readiness, terminates the child, and proves the destination is absent; its output is test evidence and never acceptance. Preserve all SAFE mappings, online audit, tracked-diff check, full D-03 rows, two-manifest comparison, and D-19 authority wording. Require repeated cold wrapper runs to finish with at least five seconds of margin, not merely before the deadline.</action>
  <verify>
    <automated>mix test test/paddle/http_test.exs test/paddle/seam_test.exs --trace &amp;&amp; bin/phase32_contract_proof.sh --self-test-termination</automated>
    <fails_when>Deterministic retry-decision coverage is absent, any retry-policy expectation changes, forced termination leaves acceptance, the receipt termination contract is not mechanically guarded, or full-mode rows/manifests disappear.</fails_when>
    <automated>node $HOME/.codex/gsd-core/bin/gsd-tools.cjs run-with-timeout 30 -- env ACCRUE_CHECKOUT=../accrue bin/phase32_contract_proof.sh --verify</automated>
    <fails_when>The cold verifier reaches 25 seconds, times out, omits a SAFE verdict/audit/concurrent-read/no-drift check, or exits zero without exactly one finalized receipt.</fails_when>
    <automated>node $HOME/.codex/gsd-core/bin/gsd-tools.cjs run-with-timeout 30 -- env ACCRUE_CHECKOUT=../accrue bin/phase32_contract_proof.sh --verify</automated>
    <fails_when>The repeated cold verifier reaches 25 seconds, produces a different contract, or exposes a nondeterministic retry wait.</fails_when>
  </verify>
  <acceptance_criteria>Two cold bounded wrapper runs each exit zero in under 25 seconds with complete atomic receipts; the deterministic termination probe exits nonzero with no receipt; and independent retry-decision plus ordinary/full retry-attempt tests remain green.</acceptance_criteria>
  <done>The documented bounded proof has meaningful deadline margin and no timeout, signal, or nonzero termination can leave passing acceptance.</done>
  <reversibility rating="costly">The verifier receipt finalization and bounded evidence selection are cross-script contracts, but remain locally replaceable without changing SDK APIs or stored data.</reversibility>
</task>

</tasks>

## Artifacts this phase produces

- No new tracked source file, public SDK symbol, struct field, endpoint, dependency, schema, or hosted-CI/release configuration is introduced.
- New private shell receipt-lifecycle helpers/state finalize full and bounded receipts only after the corresponding process contract succeeds.
- New internal compatibility self-test cases cover seeded stale full receipts plus failed preflight.
- New internal script mode: `bin/phase32_contract_proof.sh --self-test-termination` proves TERM/nonzero cleanup after candidate readiness and cannot publish acceptance.
- Updated bounded verifier test selection/labels provide deterministic SAFE and retry-decision evidence without wall-clock backoff.
- New ExUnit seam assertions guard receipt ordering, bounded/full separation, retry-decision retention, and full-row preservation.

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Safe, truthful dependency and SDK trust boundary | 32-14, 32-15 | COVERED | The only current blockers are stale/premature evidence and duplicate mutation options. |
| REQ | SAFE-01 | Secure compatible Req plus clean audit | 32-14 | COVERED | Repairs receipt integrity while retaining resolution, compatibility, and audit as distinct evidence. |
| REQ | SAFE-02 | Allowlisted telemetry | existing 32-08, 32-10, 32-13 | EXCLUDED | Verification marks satisfied; bounded selection retains its existing deterministic proof. |
| REQ | SAFE-03 | Secret-safe inspection | existing 32-09, 32-10, 32-13 | EXCLUDED | Verification marks satisfied; no inspection gap is reopened. |
| REQ | SAFE-04 | Bounded reads and non-replayed mutations | 32-15 | COVERED | Duplicate lifecycle retry options are rejected before normalization/dispatch. |
| REQ | SAFE-05 | Valid explicit client construction | existing 32-03, 32-12 | EXCLUDED | Verification marks satisfied; client identity remains unchanged. |
| REQ | SAFE-06 | Runtime/docs/types/evidence agreement | 32-14 | COVERED | Bounded proof timing and receipt semantics become truthful and repeatable. |
| RESEARCH | — | Full compatibility matrix and `mix hex.audit` stay separate | 32-14 | COVERED | No row or audit gate is removed. |
| RESEARCH | — | Deterministic bounded retry policy and validation before dispatch | 32-14, 32-15 | COVERED | Direct decisions avoid sleeps; lifecycle uniqueness precedes normalization. |
| RESEARCH | — | No stored data, schema, dashboard, or hosted configuration | 32-14, 32-15 | COVERED | Plans touch only tracked SDK/tests/scripts. |
| CONTEXT | D-01–D-04 | Req floor, isolated migration, full matrix, audit | existing 32-01/02/11 plus 32-14 | COVERED | Gap work preserves the implemented floor/matrix and repairs its acceptance integrity. |
| CONTEXT | D-05–D-09 | Telemetry and inspection safety | existing 32-08/09/10/13 | EXCLUDED | Already satisfied and retained in deterministic verifier evidence. |
| CONTEXT | D-10–D-14 | Bounded retries, restrictive options, ambiguity | 32-14, 32-15 | COVERED | Decision evidence remains independent; duplicate lifecycle options fail before dispatch. |
| CONTEXT | D-15–D-17 | Explicit client and secret-safe validation | existing 32-03/12 | EXCLUDED | Already satisfied; no new construction/configuration surface. |
| CONTEXT | D-18–D-19 | One truthful contract and distinct evidence tiers | 32-14 | COVERED | Bounded/full modes and receipts remain mechanically distinct. |
| VERIFICATION | gap 7 | Failed preflight preserves stale compatibility acceptance | 32-14 Task 1 | COVERED | Seeded real-path self-test and preflight-first invalidation. |
| VERIFICATION | gap 8 | Bounded proof lacks margin and can leave post-timeout acceptance | 32-14 Task 2 | COVERED | Deterministic evidence plus successful-exit finalization and termination proof. |
| VERIFICATION | gap 9 | Pause/resume duplicate retry is order-dependent | 32-15 | COVERED | Both conflicting orders and zero dispatch are required. |

Existing `COVERAGE.md` remains authoritative for every outbound Paddle capability. New endpoint breadth is excluded, hosted exact-SHA authority remains Phase 33, and release publication remains Phase 34.

<threat_model>
ASVS level: 1. Blocking threshold: high.

## Trust Boundaries

| Boundary | Description |
|---|---|
| local prerequisites/tests/audit → compatibility receipt | Fallible preflight and execution state becomes acceptance evidence. |
| verifier subprocess/signals → contract receipt | External timeout and process status must control whether staged evidence is promoted. |
| focused local proof → full/hosted/provider authority | A faster verifier must not inflate or replace stronger evidence tiers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-32-38 | Repudiation / Tampering | full compatibility receipt | high | mitigate | Invalidate before preflight and prove seeded stale success is absent after each failing prerequisite. |
| T-32-39 | Repudiation | bounded contract receipt | high | mitigate | Stage uniquely and promote only from zero-status EXIT; remove candidate and destination on signals/nonzero status. |
| T-32-40 | Denial of Service | bounded verifier | high | mitigate | Replace wall-clock retry evidence with deterministic decisions and require repeatable five-second deadline margin. |
| T-32-41 | Elevation of Privilege | focused evidence claims | high | mitigate | Preserve distinct receipt names, full rows, equal manifests, and explicit local-only SAFE wording. |
| T-32-42 | Information Disclosure | shell diagnostics/evidence | medium | mitigate | Use synthetic paths/canaries and never render credentials or provider payloads. |
</threat_model>

<verification>
Run the self-test, focused retry/seam tests, and two exact 30-second contract wrappers from the tasks. Then run `ACCRUE_CHECKOUT=../accrue bin/phase32_compatibility.sh --full` and `ACCRUE_CHECKOUT=../accrue bin/phase32_contract_proof.sh --full` once to prove all 13 rows and two equal canonical manifests remain intact after receipt hardening.
</verification>

<success_criteria>
- Failed full preflight and interrupted/failed matrix paths leave no acceptance receipt.
- Two cold bounded contract runs each finish under 25 seconds and publish only from successful exit.
- Forced termination/nonzero exit after candidate readiness leaves neither candidate nor passing destination.
- Deterministic retry decisions and independent full attempt-count tests remain green.
- Full 13-row compatibility and double-manifest acceptance remain complete and local-only.
</success_criteria>

<output>
Create `.planning/phases/32-dependency-sdk-trust-boundary/32-14-SUMMARY.md` when done.
</output>
