---
phase: 32-dependency-sdk-trust-boundary
plan: "05"
type: execute
wave: 6
depends_on: ["32-11", "32-04"]
files_modified:
  - lib/paddle/adjustments.ex
  - lib/paddle/customers.ex
  - lib/paddle/customers/addresses.ex
  - lib/paddle/customers/portal_sessions.ex
  - lib/paddle/portal_sessions.ex
  - test/paddle/adjustments_test.exs
  - test/paddle/customers_test.exs
  - test/paddle/customers/addresses_test.exs
  - test/paddle/customers/portal_sessions_test.exs
autonomous: true
requirements: [SAFE-04]
estimate:
  tokens: 33000
  raw_tokens: 33000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Every adjustment/customer read supplies a static operation and normalized route while runtime IDs remain dispatch-only (D-06, D-10)."
    - "Every adjustment/customer mutation is single-attempt, has no public idempotency option, and receives safe resource/reconciliation context on ambiguity (D-10, D-13, D-14)."
    - "The legacy portal-session wrapper and canonical customer portal-session path expose the same hardened behavior without adding endpoint breadth."
  artifacts:
    - path: lib/paddle/adjustments.ex
      provides: "Hardened adjustment request calls and types"
    - path: lib/paddle/customers.ex
      provides: "Hardened customer request calls and types"
    - path: lib/paddle/customers/addresses.ex
      provides: "Hardened address request calls and pagination context"
    - path: lib/paddle/customers/portal_sessions.ex
      provides: "Hardened canonical portal-session creation"
    - path: lib/paddle/portal_sessions.ex
      provides: "Compatibility wrapper delegating to the hardened path"
  key_links:
    - from: lib/paddle/customers.ex
      to: lib/paddle/http.ex
      via: "literal operation/route and optional resource_id request options"
      pattern: "operation:|route:"
    - from: lib/paddle/customers/portal_sessions.ex
      to: lib/paddle/portal_sessions.ex
      via: "one canonical hardened implementation"
      pattern: "PortalSessions"
---

<objective>
Expand the central D-10 through D-14 request contract across adjustments, customers, addresses, and both portal-session entry points, with static telemetry context required by D-06.

Purpose: Make the first complete resource family safe for reads, mutations, pagination, and reconciliation.
Output: Hardened request call sites, public option types, and focused resource tests.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-dependency-sdk-trust-boundary/32-04-SUMMARY.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-CONTEXT.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-PATTERNS.md
@.planning/phases/32-dependency-sdk-trust-boundary/COVERAGE.md
@lib/paddle/http.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden adjustment and customer request paths</name>
  <read_first>
    - lib/paddle/adjustments.ex
    - lib/paddle/customers.ex
    - test/paddle/adjustments_test.exs
    - test/paddle/customers_test.exs
    - lib/paddle/http.ex
  </read_first>
  <files>lib/paddle/adjustments.ex, lib/paddle/customers.ex, test/paddle/adjustments_test.exs, test/paddle/customers_test.exs</files>
  <behavior>
    - Reads pass literal operation/route labels and retain bounded retry eligibility.
    - Creates/updates execute once and ambiguous outcomes identify only their safe operation/resource context.
    - `idempotency_key` is absent from types, accepted options, and tests.
  </behavior>
  <action>Follow the mapped `lib/paddle/customers.ex` CRUD/request-response and validation-before-dispatch analogs in 32-PATTERNS.md. Update every request call with literal operation/route context, remove idempotency_key from option types/forwarding per D-13, and replace header tests with D-10/D-14 attempt/ambiguity assertions. Per D-18, update owning Adjustments/Customers @moduledoc/@doc, request option types, and affected specs to state bounded reads, single-attempt mutations, unsupported idempotency, and ambiguity return fields exactly.</action>
  <verify>
    <automated>mix test test/paddle/adjustments_test.exs test/paddle/customers_test.exs</automated>
    <fails_when>A request lacks static context, a mutation retries, an idempotency option/header remains, a dynamic ID enters the route label, or existing hydration/domain validation fails.</fails_when>
  </verify>
  <acceptance_criteria>Every adjustment/customer operation is labeled, method-safe, and covered by positive/error tests.</acceptance_criteria>
  <done>The adjustment/customer slice implements the central trust contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden customer-address paths and continuation context</name>
  <read_first>
    - lib/paddle/customers/addresses.ex
    - test/paddle/customers/addresses_test.exs
    - lib/paddle/http.ex
    - lib/paddle/internal/pagination.ex
  </read_first>
  <files>lib/paddle/customers/addresses.ex, test/paddle/customers/addresses_test.exs</files>
  <behavior>
    - Address create/get/list/update and continuation closures retain literal operation/route context.
    - Customer/address IDs are validated and encoded for dispatch but never copied into route telemetry.
    - Address mutations execute once and expose ambiguity context without idempotency support.
  </behavior>
  <action>Follow the mapped Customers CRUD and static-pagination-context patterns in 32-PATTERNS.md. Apply the same contract to all address entry points and continuation context, remove the unsupported option, and preserve tagged-atom validation. Per D-18, update Addresses module docs, request option types, and specs to distinguish bounded reads from single-attempt mutation ambiguity and describe static route context without dynamic IDs.</action>
  <verify>
    <automated>mix test test/paddle/customers/addresses_test.exs</automated>
    <fails_when>Any address path loses validation/hydration, a runtime identifier becomes telemetry context, pagination drops the static labels, or a mutation can replay.</fails_when>
  </verify>
  <acceptance_criteria>Every address call path carries safe static context and current resource behavior remains green.</acceptance_criteria>
  <done>Customer addresses are ready for the shared pagination implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Harden canonical and compatibility portal-session creation</name>
  <read_first>
    - lib/paddle/customers/portal_sessions.ex
    - lib/paddle/portal_sessions.ex
    - test/paddle/customers/portal_sessions_test.exs
    - lib/paddle/http.ex
  </read_first>
  <files>lib/paddle/customers/portal_sessions.ex, lib/paddle/portal_sessions.ex, test/paddle/customers/portal_sessions_test.exs</files>
  <behavior>
    - Both public entry points reach one single-attempt canonical create path.
    - The normalized route contains no customer ID and ambiguity identifies the safe customer resource only.
    - No idempotency option is accepted or documented by the module types.
  </behavior>
  <action>Follow the mapped `lib/paddle/customers.ex` resource pattern and explicit boundary ownership in 32-PATTERNS.md. Make `Paddle.Customers.PortalSessions` the single hardened implementation and keep the wrapper as delegate; remove unsupported idempotency and assert one-attempt ambiguity. Per D-18, update both owning modules' @moduledoc/@doc, option types, and specs so canonical and compatibility entry points document the identical tested contract.</action>
  <verify>
    <automated>mix test test/paddle/customers/portal_sessions_test.exs</automated>
    <fails_when>The wrapper diverges, a mutation retries, a raw customer ID appears in the route label, or either entry point accepts idempotency_key.</fails_when>
  </verify>
  <acceptance_criteria>Canonical and compatibility portal-session calls are behaviorally identical and safe, and every Plan 05 module doc/type/spec states its tested retry/idempotency/ambiguity behavior (D-18).</acceptance_criteria>
  <done>All COVERAGE.md Plan 05 rows are implemented and tested.</done>
</task>

</tasks>

## Artifacts this phase produces

- Static operation atoms for adjustments, customers, addresses, and customer portal-session operations (for example `:create_customer`, `:get_customer`, `:create_customer_portal_session`).
- Normalized route templates for every listed call; request option types retain only `{:retry, boolean()}` where applicable and remove `{:idempotency_key, String.t()}`.
- No new public function, struct field, CLI flag, or path; existing functions are wired to the new central context contract.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Resource IDs/options → transport | Dynamic identifiers and mutation options cross into shared dispatch and telemetry. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-32-14 | Information Disclosure | resource route metadata | high | mitigate | Supply literal normalized routes and test absence of runtime customer/address IDs. |
| T-32-15 | Tampering | create/update requests | high | mitigate | Remove idempotency options and assert a single transport attempt plus ambiguity result. |
</threat_model>

<verification>
Run all three focused commands, then `mix test test/paddle/adjustments_test.exs test/paddle/customers_test.exs test/paddle/customers/addresses_test.exs test/paddle/customers/portal_sessions_test.exs`.
</verification>

<success_criteria>
Every Plan 05 COVERAGE.md call site carries static context, mutations are single-attempt, idempotency support is removed, and resource behavior stays green.
</success_criteria>

<output>
Create `.planning/phases/32-dependency-sdk-trust-boundary/32-05-SUMMARY.md` when done.
</output>
