---
phase: 32-dependency-sdk-trust-boundary
plan: "03"
type: execute
wave: 3
depends_on: [32-02]
files_modified:
  - lib/paddle/client.ex
  - test/paddle/client_test.exs
autonomous: true
requirements: [SAFE-03, SAFE-05]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A valid base-URL-only MockServer client is classified as :custom and can dispatch a request."
    - "Blank/nonbinary keys, unknown or duplicate options, unsupported environments, malformed or credentialized URLs, custom-without-URL, and canonical-environment conflicts raise secret-safe ArgumentError before Req construction."
    - "Inspecting Paddle.Client cannot reveal its API key, base URL, or bearer-bearing Req state."
  assumptions:
    - requirement: SAFE-05
      statement: "The spec-less edge probe classified SAFE-05 as unclassified; the full constructor decision table remains flagged until its tests and downstream MockServer proof pass."
      status: unresolved
      flagged: true
  artifacts:
    - path: lib/paddle/client.ex
      provides: "Validated explicit client constructor and deny-by-default Inspect implementation"
      exports: ["new!/1"]
    - path: test/paddle/client_test.exs
      provides: "Constructor decision table, no-dispatch failures, and secret canaries"
  key_links:
    - from: lib/paddle/client.ex
      to: Req.new/1
      via: "all option/key/environment/URI validation completes before Req construction"
      pattern: 'def new!'
    - from: test/paddle/client_test.exs
      to: lib/paddle/client.ex
      via: "custom MockServer request and exhaustive rejected-input table"
      pattern: 'Paddle.Client.new!'
---

<objective>
Make explicit client construction incapable of producing invalid or credential-disclosing state while preserving the MockServer custom-URL seam, per D-15 through D-17 and D-07/D-08.

Purpose: Establish a trustworthy client boundary before central request policy consumes it.
Output: Validated `Paddle.Client.new!/1`, safe client inspection, and exhaustive constructor tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-CONTEXT.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-RESEARCH.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-PATTERNS.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-02-SUMMARY.md
@lib/paddle/client.ex
@test/paddle/client_test.exs
@test/paddle/mock_server_test.exs

<interfaces>
Keep `@type t :: %Paddle.Client{api_key: String.t(), environment: :sandbox | :live | :custom, base_url: String.t(), req: struct()}` and `new!(keyword()) :: t()` from the mapped self analog.
Canonical URLs are `https://sandbox-api.paddle.com` and `https://api.paddle.com`; custom URLs must be absolute HTTP(S), include a host, and contain no userinfo.
</interfaces>
</context>

<assumption_delta_decision>
noun: client environment classification
decision: no-change
rationale: The custom choice detected by the hook is already modeled by the locked `:sandbox | :live | :custom` environment union; D-16 makes a supplied noncanonical base URL derive `:custom` rather than adding a competing identity field.
</assumption_delta_decision>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Dispatch through an honestly classified custom client</name>
  <reversibility rating="costly">Constructor classification is a public contract used by MockServer and downstream consumers, so reversal requires coordinated call-site and documentation changes.</reversibility>
  <read_first>lib/paddle/client.ex, test/paddle/client_test.exs, test/paddle/mock_server_test.exs, .planning/phases/32-dependency-sdk-trust-boundary/32-PATTERNS.md</read_first>
  <files>lib/paddle/client.ex, test/paddle/client_test.exs</files>
  <behavior>
    - No environment or base URL yields the canonical sandbox client.
    - A valid noncanonical base URL without `:environment` yields `environment: :custom` and dispatches through its adapter.
    - Explicit sandbox/live select their canonical URLs; explicit custom requires a valid base URL.
  </behavior>
  <action>Using the `lib/paddle/client.ex` mapped self analog, write failing decision-table tests and implement D-15/D-16 before `Req.new/1`. Parse with `URI.new/1`, then require http/https, a nonblank host, no userinfo, and coherent canonical environment/URL pairs. Preserve explicit `%Paddle.Client{}` construction and `new!/1`; do not add application-global configuration or a Paddle credential regex. Keep retry implementation changes out of this task beyond retaining a compilable Req client for the later central-policy plan.</action>
  <verify>
    <automated>mix test test/paddle/client_test.exs test/paddle/mock_server_test.exs --warnings-as-errors</automated>
    <fails_when>A valid canonical/custom case fails, custom inference is wrong, a rejected case reaches the adapter, a warning appears, or MockServer base-URL-only use regresses.</fails_when>
  </verify>
  <acceptance_criteria>The complete valid-input side of the D-15/D-16 truth table passes through real client construction and the custom case makes a successful request.</acceptance_criteria>
  <done>Consumers can construct only coherent canonical/custom clients without losing existing MockServer use.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fail closed on invalid state and redact client inspection</name>
  <read_first>lib/paddle/client.ex, test/paddle/client_test.exs, lib/paddle/portal_session.ex, .planning/phases/32-dependency-sdk-trust-boundary/32-PATTERNS.md</read_first>
  <files>lib/paddle/client.ex, test/paddle/client_test.exs</files>
  <behavior>
    - Every invalid row raises `ArgumentError` before Req construction and names only the invalid option.
    - Unique API-key and credentialized-URL canaries never appear in exception messages.
    - `inspect(client)` renders `[REDACTED]` for `api_key`, `base_url`, and `req` while remaining total.
  </behavior>
  <action>Expand tests across missing/blank/nonbinary API keys, unknown/duplicate keys, unsupported environments, relative/non-HTTP/hostless/userinfo URLs, custom without URL, and sandbox/live conflicts. Compute invalid key names without exposing rejected keyword values; all failures must be consistent `ArgumentError` per D-17. Add the total custom Inspect projection using the mapped PortalSession analog, replacing `api_key`, `base_url`, and `req` with the stable marker per D-07/D-08 while leaving stored fields unchanged.</action>
  <verify>
    <automated>mix test test/paddle/client_test.exs --warnings-as-errors</automated>
    <fails_when>An invalid row constructs Req or dispatches, the exception/inspection contains a canary or option value, the error type differs from ArgumentError, or valid rows regress.</fails_when>
  </verify>
  <acceptance_criteria>All invalid rows fail before dispatch with secret-safe option-specific messages and client inspection exposes no credential-bearing field.</acceptance_criteria>
  <done>The explicit client boundary rejects incoherent state and is safe to inspect.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Caller options → Paddle.Client | Untrusted credentials, environments, and URLs become transport configuration. |
| Paddle.Client → logs/IEx | Inspect can expose both promoted secrets and nested Req bearer state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-32-05 | Spoofing/Information Disclosure | `Paddle.Client.new!/1` | high | mitigate | Validate exact option keys, environment/URL coherence, scheme/host/userinfo, and secret-safe failures before Req construction. |
| T-32-02 | Information Disclosure | `Inspect Paddle.Client` | high | mitigate | Explicit total projection redacts API key, base URL, and Req state with recursive canary tests later in Plan 32-07. |
| T-32-04 | Tampering | client retry state | high | mitigate | Plan 32-04 moves replay policy to Paddle.Http; this plan does not expose mutation-enabling options. |
| T-32-06 | Denial of Service | retry delays | high | mitigate | Plan 32-04 owns the bounded read-only classifier and cap. |
| T-32-SC | Tampering | packages | low | accept | No package installation occurs; Plan 32-01 already fixed the reviewed Req floor. |
</threat_model>

## Artifacts this phase produces

- `Paddle.Client.new!/1`: deterministic constructor decision table for canonical and custom environments.
- Private option/key/URI validation helpers with secret-safe `ArgumentError` messages.
- `Inspect` implementation for `Paddle.Client` redacting `api_key`, `base_url`, and `req`.
- `test/paddle/client_test.exs`: custom dispatch, invalid-state, and secret-canary coverage.
- Assumption-delta outcome: keep `environment` as the primary classification noun; derive `:custom` for noncanonical URLs.

<verification>
Run the focused client/MockServer command and inspect failures to confirm rejected values are absent; then run `mix format --check-formatted`.
</verification>

<success_criteria>
SAFE-05's entire constructor table is executable, MockServer compatibility remains real, and the client itself is no longer an inspection disclosure path.
</success_criteria>

<output>
Create `.planning/phases/32-dependency-sdk-trust-boundary/32-03-SUMMARY.md` when done.
</output>
