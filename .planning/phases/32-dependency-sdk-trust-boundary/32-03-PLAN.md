---
phase: 32-dependency-sdk-trust-boundary
plan: "03"
type: execute
wave: 4
depends_on: ["32-11"]
files_modified:
  - lib/paddle/client.ex
  - test/paddle/client_test.exs
autonomous: true
requirements: [SAFE-03, SAFE-05]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "`Paddle.Client.new!/1` accepts only a nonblank binary key, known unique options, `:sandbox | :live | :custom`, and an absolute HTTP(S) URL with a host (D-15, D-17)."
    - "A valid base-URL-only MockServer client is classified `:custom`, while explicit canonical environments reject conflicting noncanonical URLs (D-16)."
    - "Constructor failures happen before Req construction/dispatch and name only the invalid option, never its secret value (D-17)."
    - "Inspecting a client redacts `api_key`, `base_url`, and `req` with `[REDACTED]` while leaving stored values unchanged (D-07, D-08)."
  artifacts:
    - path: lib/paddle/client.ex
      provides: "Validated explicit constructor and total secret-safe Inspect implementation"
      contains: "def new!"
    - path: test/paddle/client_test.exs
      provides: "Constructor decision table, MockServer dispatch tracer, and exception/Inspect canaries"
  key_links:
    - from: lib/paddle/client.ex
      to: Req
      via: "validation and environment/base URL resolution occur before `Req.new/1`"
      pattern: "Req\\.new"
    - from: test/paddle/client_test.exs
      to: lib/paddle/client.ex
      via: "table-driven new!/1 inputs plus real custom-adapter request"
      pattern: "Client\\.new!"
  assumptions:
    - id: EDGE-SAFE-05-UNCLASSIFIED
      status: flagged-unverified
      statement: "SAFE-05's deterministic edge probe was unclassified; the plan assumes unknown/duplicate options, URL identity, custom inference, and pre-dispatch failure form the complete constructor boundary and requires the decision table to prove it."
    - id: ASSUMPTION-DELTA-SAFE-05
      status: resolved
      statement: "Assumption-delta decision: no-change. The primary noun remains `environment`; `custom` is a derived environment identity for a noncanonical validated base URL, not a second configurable client-state model (D-15, D-16)."
  prohibitions:
    - id: PROHIB-SAFE-05-GLOBALIZATION
      requirement_id: SAFE-05
      category: autonomy
      status: unresolved
      verification: null
      flagged_unverified: true
      statement: "MUST NOT turn explicit client construction into global application configuration, ambient credential lookup, or process-wide environment state."
    - id: PROHIB-SAFE-05-MISLABEL
      requirement_id: SAFE-05
      category: transparency
      status: unresolved
      verification: null
      flagged_unverified: true
      statement: "MUST NOT label a noncanonical custom host as sandbox/live or echo a credentialized rejected URL in an exception or inspection result."
---

<objective>
Make explicit client construction valid, secret-safe, and MockServer-compatible while closing the client inspection boundary (D-07, D-08, D-15, D-16, D-17).

Purpose: Ensure no invalid or misleading client state can reach the central request seam.
Output: A deterministic constructor truth table, custom-host tracer, and total client Inspect projection.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-dependency-sdk-trust-boundary/32-02-SUMMARY.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-CONTEXT.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-PATTERNS.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-RESEARCH.md
@lib/paddle/client.ex
@test/paddle/client_test.exs
@lib/paddle/portal_session.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce the complete explicit-client constructor table</name>
  <read_first>
    - lib/paddle/client.ex
    - test/paddle/client_test.exs
    - test/paddle/mock_server_test.exs
  </read_first>
  <files>lib/paddle/client.ex, test/paddle/client_test.exs</files>
  <behavior>
    - Missing, blank, whitespace, and nonbinary keys raise secret-safe `ArgumentError`.
    - Unknown or duplicate options and unsupported environments fail before Req construction.
    - Default/sandbox/live/custom inputs resolve only to their coherent canonical or validated custom URL.
    - Base-URL-only custom configuration performs one successful adapter-backed request.
  </behavior>
  <action>Follow the exact/self `lib/paddle/client.ex` construction and `test/paddle/client_test.exs` table analogs mapped in 32-PATTERNS.md. Implement private `validate_options!/1`, `validate_api_key!/1`, and `resolve_environment_and_base_url!/1` helpers before `Req.new/1`. Accept only `api_key`, `environment`, and `base_url`; detect duplicate/unknown option names without rendering values. Apply the exact D-15/D-16 table and D-17 secret-safe failures. Per D-18, update `Paddle.Client` @moduledoc/@doc and `t()`/option types in this owning module to state the validated option set, nonblank key, canonical/custom environment inference, absolute HTTP(S) host rule, and pre-dispatch `ArgumentError` behavior exactly as tested.</action>
  <verify>
    <automated>mix test test/paddle/client_test.exs --trace</automated>
    <fails_when>Any invalid case reaches adapter dispatch, a rejected canary appears in an exception, custom inference is wrong, or a valid custom base URL cannot dispatch.</fails_when>
  </verify>
  <acceptance_criteria>The table covers every accepted and rejected input combination, with immediate secret-safe failure and a working MockServer-compatible custom path.</acceptance_criteria>
  <done>SAFE-05 client state is explicit, coherent, and runnable.</done>
  <reversibility rating="costly">Validation intentionally rejects states previously accepted, but can be relaxed in a later pre-1.0 change with explicit compatibility review.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Redact all client capability state during inspection</name>
  <read_first>
    - lib/paddle/client.ex
    - test/paddle/client_test.exs
    - lib/paddle/portal_session.ex
  </read_first>
  <files>lib/paddle/client.ex, test/paddle/client_test.exs</files>
  <behavior>
    - `inspect(client)` contains `[REDACTED]` for `api_key`, `base_url`, and `req`.
    - Unique key, URL-userinfo/query, and nested Req authorization canaries are absent.
    - Client fields remain usable after inspection.
  </behavior>
  <action>Follow the `lib/paddle/portal_session.ex` total Inspect projection analog mapped in 32-PATTERNS.md. Per D-07/D-08, add a total `Inspect` implementation for `Paddle.Client`, replacing `api_key`, `base_url`, and `req` wholesale with the stable marker while keeping `environment` visible. Per D-18, make the owning client docs/type describe this exact redaction and retained runtime fields. Test promoted, credentialized-URL, and nested bearer-state canaries plus post-inspection equality.</action>
  <verify>
    <automated>mix test test/paddle/client_test.exs</automated>
    <fails_when>A canary renders, the Inspect implementation raises/falls back, the marker varies, or inspection mutates stored client data.</fails_when>
  </verify>
  <acceptance_criteria>Every client-held credential/capability representation is absent from rendered inspection, runtime state is preserved, and Client module docs/types state the tested constructor and redaction contract exactly (D-18).</acceptance_criteria>
  <done>The client constructor and inspection trust boundary is complete.</done>
  <reversibility rating="reversible">The representation-only projection can be changed without altering stored client state.</reversibility>
</task>

</tasks>

## Artifacts this phase produces

- Private functions: `validate_options!/1`, `validate_api_key!/1`, `resolve_environment_and_base_url!/1` in `Paddle.Client`.
- Public behavior retained: `Paddle.Client.new!/1`; type identity `:sandbox | :live | :custom`.
- Inspect implementation for `Paddle.Client`; redacted fields `api_key`, `base_url`, `req`.
- No new CLI flag or path.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Caller options → Paddle.Client | Untrusted credentials, environment labels, and URLs become transport configuration. |
| Paddle.Client → Inspect protocol | Stored bearer and endpoint state may be rendered to logs/errors. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-32-07 | Spoofing | environment/base URL resolution | high | mitigate | Enforce canonical URL coherence and explicit custom inference before Req construction. |
| T-32-08 | Information Disclosure | constructor errors and client Inspect | high | mitigate | Never render rejected values; redact key, URL, and Req containers wholesale with canary tests. |
| T-32-09 | Tampering | duplicate/unknown options | medium | mitigate | Reject duplicate and unknown keys before transport state exists. |
</threat_model>

<verification>
Run `mix test test/paddle/client_test.exs --trace`, then confirm the test table includes every D-15/D-16 row and every rejected canary is absent from both exceptions and Inspect output.
</verification>

<success_criteria>
Valid explicit/custom clients work, invalid state never reaches Req, errors disclose no values, and client inspection is deny-by-default.
</success_criteria>

<output>
Create `.planning/phases/32-dependency-sdk-trust-boundary/32-03-SUMMARY.md` when done.
</output>
