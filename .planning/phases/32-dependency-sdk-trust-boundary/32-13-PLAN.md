---
phase: 32-dependency-sdk-trust-boundary
plan: "13"
type: execute
wave: 11
depends_on: ["32-12"]
files_modified:
  - lib/paddle/error.ex
  - lib/paddle/customers/addresses.ex
  - test/paddle/error_test.exs
  - test/paddle/http_test.exs
  - test/paddle/customers/addresses_test.exs
  - test/paddle/inspection_safety_test.exs
  - test/paddle/seam_test.exs
  - bin/phase32_compatibility.sh
  - bin/phase32_contract_proof.sh
autonomous: true
requirements: [SAFE-01, SAFE-02, SAFE-03, SAFE-04, SAFE-05, SAFE-06]
gap_closure: true
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Every non-map nested provider error member normalizes to conservative defaults and an ambiguous mutation still returns `{:error, %Paddle.Error{}}` with reconciliation guidance (D-14)."
    - "`Paddle.Customers.Addresses.stream/3` documentation and contract tests state the runtime truth: successful elements are bare `%Paddle.Address{}` values and enumeration failures raise (D-18)."
    - "The deny-by-default inspection inventory parses every source module independently, so a later public `raw_data` struct in the same file cannot escape classification and redaction proof (D-07, D-08, D-09)."
    - "A bounded verifier mode completes each documented Phase 32 probe within 30 seconds, publishes no partial receipt, and does not replace the full 13-row D-03 acceptance matrix."
    - "[FLAGGED ASSUMPTION SAFE-01 unclassified] A bounded verifier receipt proves fresh dependency/audit and gap-sensitive safety facts only; the separate full matrix remains required acceptance evidence (D-03, D-04)."
    - "[FLAGGED ASSUMPTION SAFE-02 concurrency] Existing process-owned telemetry subscriber isolation remains part of the bounded verifier receipt and is not inferred from serial tests (D-05, D-06, D-09)."
    - "[FLAGGED ASSUMPTION SAFE-03 unclassified] This gap plan changes no Inspect projection; it repairs per-module discovery and bounded verification reruns the strengthened source inventory plus recursive canary proof (D-07, D-08, D-09)."
    - "[FLAGGED ASSUMPTION SAFE-06 idempotency] Two unchanged full acceptance runs retain equal canonical 13-row manifests even though elapsed-time fields differ."
    - "[FLAGGED ASSUMPTION SAFE-06 concurrency] Concurrent proof readers consume byte-identical tracked inputs, and an interrupted reader or matrix cannot publish acceptance."
  artifacts:
    - path: lib/paddle/error.ex
      provides: "Total nested provider-error normalization at the public Paddle.Error seam"
    - path: lib/paddle/customers/addresses.ex
      provides: "Runtime-accurate address stream element and failure documentation"
    - path: bin/phase32_compatibility.sh
      provides: "Bounded verifier mode plus preserved complete 13-row acceptance mode"
    - path: bin/phase32_contract_proof.sh
      provides: "Bounded final verifier proof plus preserved double-full-matrix acceptance"
    - path: test/paddle/inspection_safety_test.exs
      provides: "AST-scoped inventory of every public module whose struct or exception declares `:raw_data`, including multiple modules in one source file"
  key_links:
    - from: lib/paddle/http.ex
      to: lib/paddle/error.ex
      via: "terminal non-2xx mutation responses always normalize through `from_response/2`"
      pattern: "Paddle.Error.from_response"
    - from: test/paddle/seam_test.exs
      to: lib/paddle/customers/addresses.ex
      via: "fetched-doc assertion binds the stream example to tested enumeration behavior"
      pattern: "Customers.Addresses"
    - from: bin/phase32_contract_proof.sh
      to: bin/phase32_compatibility.sh
      via: "bounded verifier mode and full acceptance mode invoke matching receipt contracts"
      pattern: "phase32_compatibility.sh"
    - from: test/paddle/inspection_safety_test.exs
      to: lib/paddle
      via: "per-module AST discovery classifies each public raw_data-bearing struct before recursive Inspect canary assertions"
      pattern: "Code.string_to_quoted!|defmodule|defstruct|defexception|raw_data"
  prohibitions:
    - id: PROHIB-SAFE-01-AUTHORITY
      requirement_id: SAFE-01
      category: transparency
      status: unresolved
      verification: null
      flagged_unverified: true
      statement: "MUST NOT turn a bounded local verifier result into hosted exact-SHA, publication, release, sandbox, or live-provider authority."
    - id: PROHIB-SAFE-02-DISCLOSURE
      requirement_id: SAFE-02
      category: privacy
      status: unresolved
      verification: null
      flagged_unverified: true
      statement: "MUST NOT weaken telemetry canary, allowlist, attempt-pairing, or concurrent-subscriber checks to make the verifier mode faster."
    - id: PROHIB-SAFE-03-DISCLOSURE
      requirement_id: SAFE-03
      category: privacy
      status: unresolved
      verification: null
      flagged_unverified: true
      statement: "MUST NOT omit any module from the public raw-data inventory, collapse a multi-module source file to its first module, or omit recursive promoted and nested secret canaries from fresh verifier evidence."
    - id: PROHIB-SAFE-04-FALSE-CERTAINTY
      requirement_id: SAFE-04
      category: transparency
      status: unresolved
      verification: null
      flagged_unverified: true
      statement: "MUST NOT convert malformed provider data into success, definitive mutation failure, or an exception escaping the Paddle.Error boundary."
    - id: PROHIB-SAFE-06-SPLIT-CONTRACT
      requirement_id: SAFE-06
      category: safety
      status: unresolved
      verification: null
      flagged_unverified: true
      statement: "MUST NOT document tuple-yielding address streams, silently omit enumeration exceptions, or let focused verifier evidence replace the complete compatibility contract."
---

<objective>
Make the mutation error seam total, align the address stream contract with tested behavior, repair the inspection inventory's per-module coverage, and give verification a fresh bounded proof without weakening the complete Phase 32 acceptance contract (D-03, D-04, D-07, D-08, D-09, D-14, D-18, D-19).

Purpose: Close the remaining runtime and documentation blockers and make their re-verification executable under the verifier's 30-second probe budget.
Output: Conservative nested-error handling, mechanically checked stream documentation, AST-scoped inspection discovery, bounded proof modes, and fresh full acceptance receipts.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-CONTEXT.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-VERIFICATION.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-REVIEW.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-PATTERNS.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-04-SUMMARY.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-10-SUMMARY.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-11-SUMMARY.md
@.planning/phases/32-dependency-sdk-trust-boundary/COVERAGE.md
@lib/paddle/error.ex
@lib/paddle/customers/addresses.ex
@bin/phase32_compatibility.sh
@bin/phase32_contract_proof.sh
</context>

<assumption_delta_decision>
Primary noun: `Paddle.Client`. Decision: `no-change` — the custom base URL remains a chosen property of one explicit client, while error normalization, documentation, and evidence runners consume that identity without creating a second identity model.
</assumption_delta_decision>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Return reconciliation guidance for a malformed mutation error envelope</name>
  <read_first>
    - lib/paddle/error.ex
    - lib/paddle/http.ex
    - test/paddle/error_test.exs
    - test/paddle/http_test.exs
    - .planning/phases/32-dependency-sdk-trust-boundary/32-VERIFICATION.md
  </read_first>
  <files>lib/paddle/error.ex, test/paddle/error_test.exs, test/paddle/http_test.exs</files>
  <behavior>
    - Nested `"error"` values of nil, binary, list, or another non-map normalize as an empty error object without raising.
    - Atom-keyed or type-invalid nested maps produce conservative field defaults rather than invalid public field types.
    - A POST terminal 408/5xx with any malformed nested value returns one non-retryable ambiguous `%Paddle.Error{}` with static operation/resource context and the fixed reconciliation actions.
    - The original outer provider body remains stored in `raw_data` and redacted by Inspect.
  </behavior>
  <action>Follow the `Paddle.Error` exact/self constructor and shared error-normalization seam mapped in `32-PATTERNS.md`. Write failing direct-constructor cases and one end-to-end `Paddle.Http.request/4` mutation case first. In `Paddle.Error.from_response/2`, normalize the outer body as today, then accept its nested `"error"` member only when it is a map; otherwise use an empty map. Normalize promoted `type`, `code`, and `detail` values to their documented binary-or-nil/default shapes and `errors` to a list, while retaining the unchanged outer body in `raw_data`. Preserve D-14 request-ID precedence, ambiguity classification, fixed reconciliation atoms, tuple shape, and Inspect redaction. Do not introduce a second error family or infer success/failure certainty from malformed data.</action>
  <verify>
    <automated>mix test test/paddle/error_test.exs test/paddle/http_test.exs --trace</automated>
    <fails_when>Any nested shape raises, public field types escape their documented shapes, mutation ambiguity/reconciliation is lost, raw_data storage changes, or the malformed request is retried.</fails_when>
  </verify>
  <acceptance_criteria>Every specified malformed envelope produces a conservative `%Paddle.Error{}` and the HTTP tracer returns it through the established `{:error, error}` result after one mutation attempt.</acceptance_criteria>
  <done>The public mutation-error seam is total for malformed provider envelopes and retains actionable reconciliation.</done>
  <reversibility rating="reversible">The normalization is an internal defensive projection behind the existing public Paddle.Error contract.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind address stream documentation to runtime enumeration semantics</name>
  <read_first>
    - lib/paddle/customers/addresses.ex
    - lib/paddle/internal/pagination.ex
    - test/paddle/customers/addresses_test.exs
    - test/paddle/seam_test.exs
    - guides/getting-started.md
  </read_first>
  <files>lib/paddle/customers/addresses.ex, test/paddle/customers/addresses_test.exs, test/paddle/seam_test.exs</files>
  <behavior>
    - The `stream/3` example consumes bare `%Paddle.Address{}` elements.
    - Module documentation states that initial validation and later provider failures raise during enumeration, matching `Paddle.Internal.Pagination.stream_next/1`.
    - Behavioral tests prove one successful bare element and a raised later-page `%Paddle.Error{}`.
    - The seam test fetches compiled docs and fails if tuple-yielding/error-element guidance returns.
  </behavior>
  <action>Follow the Customers CRUD role analog for the resource and the `test/paddle/seam_test.exs` exact/self documentation-as-tested-contract pattern mapped in `32-PATTERNS.md`. Keep the established shared pagination runtime unchanged. Rewrite only the `Paddle.Customers.Addresses.stream/3` example and Errors section so consumers match bare address structs and handle enumeration exceptions; retain the existing lazy behavior and `Enumerable.t()` spec. Strengthen the owning behavioral tests where needed and add a `Code.fetch_docs/1` seam assertion that requires the direct-element/raised-error contract and rejects the prior tuple-element guidance within the `stream/3` documentation region. Apply D-18/D-19 evidence language without claiming provider behavior beyond the adapter-backed test.</action>
  <verify>
    <automated>mix test test/paddle/customers/addresses_test.exs test/paddle/seam_test.exs</automated>
    <fails_when>The compiled docs describe tuple elements, omit raised enumeration failures, disagree with the behavioral test, or shared pagination behavior changes.</fails_when>
  </verify>
  <acceptance_criteria>Compiled public documentation, the example, the function spec, and adapter-backed enumeration tests state one consistent address stream contract.</acceptance_criteria>
  <done>Consumers can follow the address stream documentation without mismatching successful values or expecting impossible error tuples.</done>
  <reversibility rating="reversible">The task corrects documentation and contract guards to match an unchanged runtime stream interface.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Make inspection discovery exhaustive and add bounded verifier receipts</name>
  <read_first>
    - bin/phase32_compatibility.sh
    - bin/phase32_contract_proof.sh
    - test/paddle/inspection_safety_test.exs
    - test/paddle/seam_test.exs
    - .planning/phases/32-dependency-sdk-trust-boundary/32-10-SUMMARY.md
    - .planning/phases/32-dependency-sdk-trust-boundary/32-11-SUMMARY.md
  </read_first>
  <files>bin/phase32_compatibility.sh, bin/phase32_contract_proof.sh, test/paddle/inspection_safety_test.exs, test/paddle/seam_test.exs</files>
  <behavior>
    - A private AST inventory helper returns every individual public module whose own `defstruct` or `defexception` declares `:raw_data`, without associating fields across sibling modules.
    - A source-string fixture containing two modules in one file proves both raw-data-bearing modules are discovered and independently require classification.
    - Each script exposes a documented `--verify` mode that completes within 30 seconds on the selected Phase 32 toolchain and publishes only an atomic, complete verifier receipt.
    - Compatibility verifier mode runs one warnings-as-errors compilation/test invocation covering the gap-sensitive runtime, client, telemetry, inspection, resource, and seam tests plus `mix hex.audit`, and maps results explicitly to SAFE-01 through SAFE-06.
    - Contract verifier mode retains byte-identical concurrent readers, interrupted/failed receipt rejection, one compatibility verifier receipt, and tracked-diff equality without invoking the full matrix twice.
    - `--full` retains the complete 13-row D-03 matrix and the final two-equal-manifest acceptance behavior; no required row is removed or inferred from focused proof.
  </behavior>
  <action>First replace the known file-level regex inventory in `test/paddle/inspection_safety_test.exs` with a private source-to-AST helper using `Code.string_to_quoted!/1` and scope-aware traversal of each `defmodule`; classify a module only when that module's own `defstruct` or `defexception` fields contain `:raw_data`, and preserve the existing deny-by-default comparison against explicitly protected modules. Follow the inspection-test composite analog in `32-PATTERNS.md` (`ExUnit.Case, async: true`, real hydration, recursive canaries), and add an inline two-sibling-module source fixture asserting that both module names are returned so one module's fields cannot be attributed to another. Then follow the `bin/package_smoke.sh` shell-safety/isolated-consumer analog and `test/paddle/seam_test.exs` exact/self contract style from `32-PATTERNS.md`: add explicit `--verify` and `--full` dispatch to both scripts. Build the bounded compatibility path from one warnings-as-errors Mix invocation over all gap-sensitive and invariant tests, including the strengthened inspection test, followed by online `mix hex.audit`, six explicit SAFE verdict rows, tracked-diff equality, and atomic rename to a distinct verifier receipt. Build the bounded contract path from the existing concurrent snapshot reader, compatibility receipt self-test, one bounded compatibility run, receipt validation, SAFE mapping, and no-drift check. Keep the original 13 named rows and two canonical full-receipt comparisons intact behind `--full`; after Tasks 1-2 and the inventory repair, run both full modes to create fresh acceptance evidence. Update seam assertions and usage text so bounded verifier evidence is explicitly subordinate to full D-03 acceptance and neither is described as hosted, sandbox, live-provider, release, or publication authority per D-03/D-04/D-19.</action>
  <verify>
    <automated>mix test test/paddle/inspection_safety_test.exs --trace</automated>
    <fails_when>The two-module fixture returns only one module, sibling fields are associated across module boundaries, or a discovered public raw_data-bearing struct can remain outside the protected classification.</fails_when>
    <automated>node /Users/jon/.codex/gsd-core/bin/gsd-tools.cjs run-with-timeout 30 -- env ACCRUE_CHECKOUT=../accrue bin/phase32_compatibility.sh --verify</automated>
    <fails_when>The bounded compatibility probe times out, skips a SAFE mapping, accepts a partial result, changes tracked files, omits the online audit, or conflates focused and full evidence.</fails_when>
    <automated>node /Users/jon/.codex/gsd-core/bin/gsd-tools.cjs run-with-timeout 30 -- env ACCRUE_CHECKOUT=../accrue bin/phase32_contract_proof.sh --verify</automated>
    <fails_when>The bounded contract probe times out, concurrent inputs differ, interruption leaves a receipt, its nested verifier result is incomplete, or tracked files drift.</fails_when>
  </verify>
  <acceptance_criteria>The two-module fixture proves exhaustive per-module raw_data discovery; both bounded commands exit zero inside 30 seconds with complete atomic verifier receipts; and both full modes subsequently exit zero with fresh complete 13-row acceptance receipts.</acceptance_criteria>
  <done>The deny-by-default inspection gate cannot miss a later module in one source file, and the verifier can re-prove every gap-sensitive SAFE invariant within its timeout while the full compatibility and repeatability contracts remain independently executable and complete.</done>
  <reversibility rating="costly">The script-mode and receipt contract is consumed by phase verification, but can be changed through coordinated script and seam-test updates without altering SDK runtime behavior.</reversibility>
</task>

</tasks>

## Artifacts this phase produces

- No new production module, public resource function, struct field, or source path is created.
- New private normalization clauses/helpers in `Paddle.Error` make `from_response/2` total for malformed nested provider errors.
- New ExUnit contract assertions bind address stream docs and malformed mutation results to runtime behavior, and AST-scoped inspection inventory tests classify every module independently.
- New CLI flags: `bin/phase32_compatibility.sh --verify|--full` and `bin/phase32_contract_proof.sh --verify|--full`.
- New ephemeral evidence artifact: an atomically published Phase 32 verifier receipt distinct from the existing full compatibility receipt; it is written only beneath the caller-selected evidence directory and is not committed.

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Dependency and SDK trust boundary is safe and truthful | 32-12, 32-13 | COVERED | New plans close all four authoritative verification gaps. |
| REQ | SAFE-01 | Secure Req compatibility and clean audit | 32-01, 32-02, 32-11, 32-13 | COVERED | Existing full matrix retained; bounded mode reruns audit. |
| REQ | SAFE-02 | Allowlisted isolated telemetry | 32-08, 32-10, 32-13 | COVERED | Existing concurrency/canary proof remains in bounded verification. |
| REQ | SAFE-03 | Secret-safe inspection | 32-03, 32-09, 32-10, 32-13 | COVERED | Plan 13 replaces the lossy file-level regex with per-module AST discovery and reruns the strengthened inventory/canary proof. |
| REQ | SAFE-04 | Bounded reads and conservative mutation ambiguity | 32-04 through 32-07, 32-12, 32-13 | COVERED | Options cannot reconfigure transport; malformed envelopes remain Paddle.Error values. |
| REQ | SAFE-05 | Valid explicit client state and custom URLs | 32-03, 32-12 | COVERED | Per-request options cannot usurp the validated client. |
| REQ | SAFE-06 | Runtime/docs/types/evidence agreement | 32-10, 32-13 | COVERED | Address stream docs corrected and proof commands bounded. |
| RESEARCH | — | Central request policy and validation before dispatch | 32-04, 32-12 | COVERED | Shared validator runs at every public option-bearing mutation. |
| RESEARCH | — | Total Paddle.Error normalization and fixed reconciliation | 32-04, 32-13 | COVERED | Nested non-map provider errors normalize conservatively. |
| RESEARCH | — | Documentation as mechanically tested contract | 32-10, 32-13 | COVERED | Compiled stream docs are tied to behavioral tests. |
| RESEARCH | — | Full compatibility matrix and atomic evidence | 32-11, 32-10, 32-13 | COVERED | Full proof remains intact; bounded verification is additional evidence. |
| CONTEXT | D-01–D-04 | Req floor, isolated migration, full matrix, Hex audit | 32-01, 32-02, 32-11, 32-10, 32-13 | COVERED | No dependency scope is reopened. |
| CONTEXT | D-05–D-09 | Telemetry and inspection safety | 32-08, 32-09, 32-10, 32-13 | COVERED | Existing exact canary/concurrency proofs are retained, and inspection discovery is made exhaustive per source module. |
| CONTEXT | D-10–D-14 | Retry, option, idempotency, and ambiguity rules | 32-04 through 32-07, 32-12, 32-13 | COVERED | Both runtime blockers close at the owning seams. |
| CONTEXT | D-15–D-17 | Explicit client and secret-safe validation | 32-03, 32-12 | COVERED | The primary client identity remains unchanged. |
| CONTEXT | D-18–D-19 | Contract truth and evidence boundaries | 32-10, 32-13 | COVERED | Stream docs and verifier/full proof distinctions are guarded. |

Deferred endpoint breadth, hosted-CI authority, and release/publication remain excluded exactly as assigned to later phases.

<threat_model>
ASVS level: 1. Blocking threshold: high.

## Trust Boundaries

| Boundary | Description |
|---|---|
| provider/intermediary response → Paddle.Error | Untrusted status, headers, and arbitrarily shaped JSON become a public SDK error value. |
| runtime behavior → public documentation | Consumers rely on compiled docs/examples to choose matching and exception handling. |
| repository/toolchain → acceptance receipt | Scripts translate local executions into bounded verifier and full acceptance evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-32-32 | Denial of Service / Repudiation | Paddle.Error.from_response/2 | high | mitigate | Normalize every nested error shape and prove ambiguous mutations always return a conservative public error. |
| T-32-33 | Repudiation | address stream documentation | high | mitigate | Bind compiled docs to direct-element and raised-exception behavioral tests. |
| T-32-34 | Tampering | compatibility/contract receipts | high | mitigate | Atomic complete-only publication, interruption self-test, exact SAFE rows, and tracked-diff equality. |
| T-32-35 | Repudiation | bounded versus full evidence | high | mitigate | Distinct modes/receipts and seam assertions prevent focused proof from replacing the 13-row acceptance matrix. |
| T-32-36 | Information Disclosure | verifier logs and errors | medium | mitigate | Use synthetic inputs and preserve existing telemetry/Inspect canary tests in the bounded suite. |
| T-32-37 | Information Disclosure / Repudiation | inspection source inventory | high | mitigate | Parse module-scoped AST, require every raw_data-bearing public struct to be classified, and prove two sibling modules are both discovered. |
</threat_model>

<verification>
Run `mix test test/paddle/inspection_safety_test.exs --trace`, the two 30-second `--verify` commands from Task 3, then `ACCRUE_CHECKOUT=../accrue bin/phase32_compatibility.sh --full` and `ACCRUE_CHECKOUT=../accrue bin/phase32_contract_proof.sh --full` to publish fresh complete acceptance evidence after all runtime and inventory fixes.
</verification>

<success_criteria>
- Malformed nested provider error members never escape the Paddle.Error seam.
- Address stream docs and compiled contract tests match actual element and failure behavior.
- Inspection inventory discovers and classifies every raw_data-bearing public module, including two modules declared in one source file.
- Both bounded verifier commands finish within 30 seconds with atomic evidence.
- The complete 13-row matrix and double-manifest final proof remain intact and pass after the fixes.
</success_criteria>

<output>
Create `.planning/phases/32-dependency-sdk-trust-boundary/32-13-SUMMARY.md` when done.
</output>
