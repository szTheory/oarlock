---
phase: 32-dependency-sdk-trust-boundary
plan: "06"
type: execute
wave: 6
depends_on: ["32-11", "32-04"]
files_modified:
  - lib/paddle/events.ex
  - lib/paddle/notification_settings.ex
  - lib/paddle/prices.ex
  - lib/paddle/products.ex
  - test/paddle/events_test.exs
  - test/paddle/notification_settings_test.exs
  - test/paddle/prices_test.exs
  - test/paddle/products_test.exs
autonomous: true
requirements: [SAFE-04]
estimate:
  tokens: 29000
  raw_tokens: 29000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Every event/price/product read and pagination seed supplies static low-cardinality operation/route context and remains eligible only for bounded read retries (D-06, D-10, D-11)."
    - "Notification-setting create/update/delete is single-attempt and ambiguity-safe, while read/list/stream/all stays bounded (D-10, D-14)."
    - "No catalog or notification-setting public type/call accepts `idempotency_key` without provider proof (D-13)."
  artifacts:
    - path: lib/paddle/events.ex
      provides: "Static event request context"
    - path: lib/paddle/notification_settings.ex
      provides: "Method-safe notification-setting requests"
    - path: lib/paddle/prices.ex
      provides: "Static price request/pagination context"
    - path: lib/paddle/products.ex
      provides: "Static product request/pagination context"
  key_links:
    - from: lib/paddle/notification_settings.ex
      to: lib/paddle/http.ex
      via: "static context and central mutation ambiguity"
      pattern: "operation:|route:"
    - from: lib/paddle/prices.ex
      to: lib/paddle/internal/pagination.ex
      via: "continuation closure carries labels separately from cursor URL"
      pattern: "Pagination"
---

<objective>
Expand the method-aware/static-context contract across catalog, event, and notification-setting resources (D-06, D-10 through D-14).

Purpose: Cover every existing catalog/event/notification outbound Paddle path without adding API breadth.
Output: Hardened modules and focused tests for the COVERAGE.md Plan 06 rows.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-dependency-sdk-trust-boundary/32-04-SUMMARY.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-CONTEXT.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-PATTERNS.md
@.planning/phases/32-dependency-sdk-trust-boundary/COVERAGE.md
@lib/paddle/http.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden event and product request families</name>
  <read_first>
    - lib/paddle/events.ex
    - lib/paddle/products.ex
    - test/paddle/events_test.exs
    - test/paddle/products_test.exs
    - lib/paddle/internal/pagination.ex
  </read_first>
  <files>lib/paddle/events.ex, lib/paddle/products.ex, test/paddle/events_test.exs, test/paddle/products_test.exs</files>
  <behavior>
    - Get/list/stream/all calls carry literal operation and route labels.
    - Dynamic IDs, filters, and cursors remain only in dispatch URLs/params.
    - Unsupported idempotency typing is absent.
  </behavior>
  <action>Follow the mapped `lib/paddle/customers.ex` CRUD and static-pagination analogs in 32-PATTERNS.md. Add literal operation/route context to every event/product request, remove stale idempotency typing per D-13, and preserve validation/hydration/cursors. Per D-18, update Events/Products @moduledoc/@doc, request option types, and specs to state bounded read behavior, static context, and absence of idempotency support exactly.</action>
  <verify>
    <automated>mix test test/paddle/events_test.exs test/paddle/products_test.exs</automated>
    <fails_when>A path lacks context, identifiers/query/cursors enter route metadata, old types remain, or resource behavior regresses.</fails_when>
  </verify>
  <acceptance_criteria>All event/product reads are statically labeled and compatible with central bounded retries.</acceptance_criteria>
  <done>Events and products satisfy the trust contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden price and notification-setting request families</name>
  <read_first>
    - lib/paddle/prices.ex
    - lib/paddle/notification_settings.ex
    - test/paddle/prices_test.exs
    - test/paddle/notification_settings_test.exs
    - lib/paddle/http.ex
  </read_first>
  <files>lib/paddle/prices.ex, lib/paddle/notification_settings.ex, test/paddle/prices_test.exs, test/paddle/notification_settings_test.exs</files>
  <behavior>
    - Price reads/pagination retain bounded retry semantics and static labels.
    - Notification mutations execute once and uncertain outcomes contain safe operation/resource context.
    - Notification reads remain retry-eligible and every idempotency option is removed.
  </behavior>
  <action>Follow the mapped Customers CRUD, validation-before-dispatch, and error-normalization patterns in 32-PATTERNS.md. Wire every price/notification-setting path to the central contract, remove the notification idempotency option, and replace header tests with exact attempt/ambiguity assertions. Per D-18, update Prices/NotificationSettings module docs, option types, and specs to distinguish bounded reads, one-attempt mutations, and safe ambiguity fields exactly.</action>
  <verify>
    <automated>mix test test/paddle/prices_test.exs test/paddle/notification_settings_test.exs</automated>
    <fails_when>A mutation retries, a dynamic secret/ID enters telemetry labels, idempotency remains accepted, or existing CRUD/pagination behavior fails.</fails_when>
  </verify>
  <acceptance_criteria>Prices and notification settings have complete static context, bounded reads, single-attempt mutations, and all four Plan 06 module docs/types/specs agree with runtime tests (D-18).</acceptance_criteria>
  <done>All COVERAGE.md Plan 06 rows are implemented and tested.</done>
</task>

</tasks>

## Artifacts this phase produces

- Static operation atoms and normalized route templates for events, products, prices, and notification settings.
- Updated notification/product request option types with unsupported idempotency removed.
- No new public function, struct/type field, CLI flag, or path.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Catalog filters/IDs/cursors → Req | High-cardinality provider inputs coexist with safe observability context. |
| Notification mutations → provider | Endpoint-secret-bearing resources can end in uncertain state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-32-16 | Information Disclosure | catalog/notification route context | high | mitigate | Literal labels and tests keep filters, IDs, cursors, and secret destinations out of context. |
| T-32-17 | Tampering | notification mutations | high | mitigate | Central one-attempt policy plus safe ambiguity assertions at each mutating call. |
</threat_model>

<verification>
Run both focused commands and verify every matching COVERAGE.md row has a tested call-site mapping.
</verification>

<success_criteria>
All catalog/event/notification paths are statically labeled, method-safe, idempotency-free, and behaviorally green.
</success_criteria>

<output>
Create `.planning/phases/32-dependency-sdk-trust-boundary/32-06-SUMMARY.md` when done.
</output>
