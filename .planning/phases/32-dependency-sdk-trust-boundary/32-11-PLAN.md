---
phase: 32-dependency-sdk-trust-boundary
plan: "11"
type: execute
wave: 3
depends_on: ["32-02"]
files_modified:
  - test/paddle/subscriptions_test.exs
  - test/paddle/transactions_test.exs
  - test/paddle/seam_test.exs
  - test/paddle/http/telemetry_test.exs
  - bin/phase32_compatibility.sh
autonomous: true
requirements: [SAFE-01]
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Every remaining compatibility-sensitive adapter fixture uses Req 0.7's supported contract without changing its asserted SDK behavior (D-02, D-03)."
    - "Every adapter-focused check and the complete root compatibility row treat project compilation and test-suite warnings as errors, so a deprecated adapter warning is a nonzero result (D-03)."
    - "One fail-fast command proves every root, focused, MockServer, demo, package, optional-dependency, Dialyzer, docs, downstream Accrue, and online-audit row (D-03, D-04)."
    - "Only a fully successful matrix can produce an atomic completion receipt; interrupted or partial runs leave no acceptance artifact (D-03, D-04)."
  artifacts:
    - path: bin/phase32_compatibility.sh
      provides: "Named SAFE-01 compatibility matrix with atomic completion receipt and interruption self-test"
      contains: "set -euo pipefail"
    - path: test/paddle/seam_test.exs
      provides: "Req 0.7-compatible downstream/public seam fixture"
  key_links:
    - from: bin/phase32_compatibility.sh
      to: bin/package_smoke.sh
      via: "mapped isolated-consumer shell pattern and package/optional-dependency row"
      pattern: "bin/package_smoke\\.sh"
    - from: bin/phase32_compatibility.sh
      to: test/paddle/seam_test.exs
      via: "named root/focused rows include the tracked contract seam"
      pattern: "mix test"
---

<objective>
Complete the Req 0.7 fixture migration and accept it only through the full D-03/D-04 compatibility and advisory matrix.

Purpose: Separate the compatibility runner and remaining integration seams from Plan 02's bounded fixture families.
Output: Four migrated compatibility-sensitive fixtures and one repeatable fail-fast matrix runner.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-dependency-sdk-trust-boundary/32-02-SUMMARY.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-CONTEXT.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-PATTERNS.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-VALIDATION.md
@bin/package_smoke.sh
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate the remaining integration and contract fixtures</name>
  <read_first>
    - test/paddle/subscriptions_test.exs
    - test/paddle/transactions_test.exs
    - test/paddle/seam_test.exs
    - test/paddle/http/telemetry_test.exs
    - test/paddle/http_test.exs
    - .planning/phases/32-dependency-sdk-trust-boundary/32-PATTERNS.md
  </read_first>
  <files>test/paddle/subscriptions_test.exs, test/paddle/transactions_test.exs, test/paddle/seam_test.exs, test/paddle/http/telemetry_test.exs</files>
  <behavior>
    - Existing subscription, transaction, seam, and telemetry assertions retain identical meaning under Req 0.7.
    - Every custom/function adapter uses Req 0.7's supported request-return contract without deprecation warnings.
  </behavior>
  <action>Follow the mapped `test/paddle/http_test.exs` deterministic adapter/counter analog and the exact/self seam and telemetry test patterns from 32-PATTERNS.md. Finish only the D-02/D-03 compatibility migration in these four fixtures; preserve request, response, event, and public-contract assertions so later semantic plans own every behavior change.</action>
  <verify>
    <automated>MIX_ENV=test mix do compile --warnings-as-errors + test --warnings-as-errors test/paddle/subscriptions_test.exs test/paddle/transactions_test.exs test/paddle/seam_test.exs test/paddle/http/telemetry_test.exs</automated>
    <fails_when>Any test fails, a deprecated adapter warning remains, or an assertion is weakened or semantically changed.</fails_when>
  </verify>
  <acceptance_criteria>All remaining tracked adapter fixtures run on Req 0.7 with their original behavioral coverage intact.</acceptance_criteria>
  <done>The compatibility-sensitive seams are ready for the complete matrix.</done>
</task>

<task type="auto">
  <name>Task 2: Codify the full compatibility matrix and atomic acceptance contract</name>
  <precondition>`../accrue/accrue` exists with its tracked Mix project and the Hex registry is reachable; otherwise halt before claiming D-03 or SAFE-01.</precondition>
  <read_first>
    - bin/package_smoke.sh
    - .github/workflows/ci.yml
    - .planning/phases/32-dependency-sdk-trust-boundary/32-VALIDATION.md
    - .planning/phases/32-dependency-sdk-trust-boundary/32-PATTERNS.md
  </read_first>
  <files>bin/phase32_compatibility.sh</files>
  <action>Follow the mapped `bin/package_smoke.sh` shell-safety and isolated-consumer analog from 32-PATTERNS.md. Create `bin/phase32_compatibility.sh` with `set -euo pipefail`, repository-relative paths, required `ACCRUE_CHECKOUT`, named rows, per-row durations, and before/after tracked-diff equality. Run without masking failures: the complete root suite as `MIX_ENV=test mix do compile --warnings-as-errors + test --warnings-as-errors`; every focused adapter-bearing suite with the same compiler and test-loader warning flags; telemetry steps; MockServer/subscription flows; `mix dialyzer`; `mix docs`; `bin/package_smoke.sh`; demo precommit; the Accrue seam from its owning Mix project; and online `mix hex.audit`. Ensure the root and focused commands propagate the nonzero exit produced by either warnings-as-errors stage, including Req function-adapter deprecations, instead of relying on log inspection. Write any acceptance receipt to `PHASE32_EVIDENCE_DIR` only by atomic rename after every row and the no-drift check pass. Add `--self-test` using isolated temporary fake rows to prove a killed/nonzero partial run produces no receipt and a complete run produces exactly one receipt. Per D-03/D-04/D-19, this is local compatibility evidence only.</action>
  <verify>
    <automated>bin/phase32_compatibility.sh --self-test &amp;&amp; ACCRUE_CHECKOUT=../accrue bin/phase32_compatibility.sh</automated>
    <fails_when>The self-test accepts a partial/interrupted run, a compiler or test-suite warning exits zero, any required row is absent/skipped/masked/nonzero, Accrue runs from the wrong Mix project, tracked files drift, or the audit is inferred.</fails_when>
  </verify>
  <acceptance_criteria>One fail-fast command executes every D-03 row with compiler and test-loader warnings promoted to nonzero failures, and only a complete non-mutating success atomically records acceptance.</acceptance_criteria>
  <done>SAFE-01 has complete compatibility and advisory evidence before SDK behavior expansion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Test adapters → Req 0.7 | Fixture return shapes can create false compatibility confidence. |
| Package/demo/Accrue/registry → acceptance receipt | External and downstream results determine dependency acceptance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-32-05 | Repudiation | matrix rows and receipt | high | mitigate | Name every D-03 row, preserve exit codes, record durations, and publish the receipt only after total success. |
| T-32-06 | Tampering | partial/interrupted matrix | high | mitigate | Isolated self-test kills/fails rows and proves no partial receipt; tracked-diff equality rejects mutation. |
| T-32-07A | Information Disclosure | downstream/audit commands | low | accept | Record command labels/results only and do not print provider credentials. |
</threat_model>

<verification>
Run both task commands and compare the named matrix rows to 32-VALIDATION.md with no omission.
</verification>

<success_criteria>
All fixtures are warning-free, every D-03 row and the live audit pass, partial runs cannot be accepted, and evidence stays within D-04/D-19 boundaries.
</success_criteria>

<output>
Create `.planning/phases/32-dependency-sdk-trust-boundary/32-11-SUMMARY.md` when done.
</output>
