---
phase: 32-dependency-sdk-trust-boundary
plan: "02"
type: execute
wave: 2
depends_on: [32-01]
files_modified:
  - test/paddle/http_test.exs
  - test/paddle/http/telemetry_test.exs
  - test/paddle/seam_test.exs
  - test/paddle/customers_test.exs
  - test/paddle/customers/addresses_test.exs
  - test/paddle/adjustments_test.exs
  - test/paddle/customers/portal_sessions_test.exs
  - test/paddle/events_test.exs
  - test/paddle/notification_settings_test.exs
  - test/paddle/prices_test.exs
  - test/paddle/products_test.exs
  - test/paddle/subscriptions_test.exs
  - test/paddle/transactions_test.exs
  - bin/phase32_compatibility.sh
  - .planning/phases/32-dependency-sdk-trust-boundary/32-VALIDATION.md
autonomous: true
requirements: [SAFE-01]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Every tracked function/custom adapter fixture uses the supported Req 0.7 adapter contract without deprecation warnings."
    - "One repeatable local command proves the entire D-03 compatibility matrix and fails on the first missing or failing lane."
  artifacts:
    - path: bin/phase32_compatibility.sh
      provides: "Fail-closed SAFE-01 compatibility matrix runner"
    - path: .planning/phases/32-dependency-sdk-trust-boundary/32-VALIDATION.md
      provides: "Recorded matrix commands, downstream seam command, timings, and SAFE-01 status"
  key_links:
    - from: bin/phase32_compatibility.sh
      to: bin/package_smoke.sh
      via: "packaged consumer and optional Plug/Bandit boundary proof"
      pattern: 'package_smoke\\.sh'
    - from: bin/phase32_compatibility.sh
      to: ../accrue/accrue/test/accrue/billing/subscription_projection_provider_test.exs
      via: "owning checkout's documented Mix test seam"
      pattern: 'subscription_projection_provider_test\\.exs'
---

<objective>
Complete the Req-only compatibility migration and make its acceptance matrix repeatable before SDK behavior work begins, per D-02, D-03, and D-04.

Purpose: Remove Req 0.7 adapter deprecations everywhere and make package/demo/downstream/advisory evidence a single fail-closed command.
Output: Migrated adapter fixtures, a tracked matrix runner, and updated validation evidence.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-CONTEXT.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-RESEARCH.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-PATTERNS.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-VALIDATION.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-01-SUMMARY.md
@bin/package_smoke.sh
@demo/AGENTS.md

<interfaces>
Req 0.7 adapter fixtures must return the supported `{request, response_or_exception}` result while preserving each test's existing public SDK assertion.
The matrix runner follows the `bin/package_smoke.sh` shell/isolated-consumer analog mapped in 32-PATTERNS.md: `set -euo pipefail`, repository-relative roots, named commands, no masked failures.
The downstream read-only seam command resolved from the owning checkout is `(cd "$ACCRUE_CHECKOUT/accrue" &amp;&amp; mix test test/accrue/billing/subscription_projection_provider_test.exs)`.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Migrate central, telemetry, seam, and customer adapter fixtures</name>
  <read_first>test/paddle/http_test.exs, test/paddle/http/telemetry_test.exs, test/paddle/seam_test.exs, test/paddle/customers_test.exs, test/paddle/customers/addresses_test.exs, .planning/phases/32-dependency-sdk-trust-boundary/32-PATTERNS.md</read_first>
  <files>test/paddle/http_test.exs, test/paddle/http/telemetry_test.exs, test/paddle/seam_test.exs, test/paddle/customers_test.exs, test/paddle/customers/addresses_test.exs</files>
  <action>Migrate only the custom/function adapter construction needed by Req 0.7 in these tests, preserving the existing Agent counters, unique telemetry handler lifecycle, request assertions, and result contracts from their mapped self/http-test analogs. Do not rewrite retry, telemetry, idempotency, or documentation expectations yet; those intentional failures belong to later behavior plans. Remove adapter deprecation warnings without weakening an assertion.</action>
  <verify>
    <automated>mix test test/paddle/http_test.exs test/paddle/http/telemetry_test.exs test/paddle/seam_test.exs test/paddle/customers_test.exs test/paddle/customers/addresses_test.exs --warnings-as-errors</automated>
    <fails_when>Compilation or tests fail, any Req adapter deprecation is emitted, attempt counters change, telemetry handlers leak, or existing assertions are removed instead of migrated.</fails_when>
  </verify>
  <acceptance_criteria>All five files use supported Req 0.7 adapters, execute warning-free, and preserve their pre-behavior-change assertions.</acceptance_criteria>
  <done>The central and customer compatibility fixtures are green on Req 0.7.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate the remaining resource adapter fixtures</name>
  <read_first>test/paddle/adjustments_test.exs, test/paddle/customers/portal_sessions_test.exs, test/paddle/events_test.exs, test/paddle/notification_settings_test.exs, test/paddle/prices_test.exs, .planning/phases/32-dependency-sdk-trust-boundary/32-PATTERNS.md</read_first>
  <files>test/paddle/adjustments_test.exs, test/paddle/customers/portal_sessions_test.exs, test/paddle/events_test.exs, test/paddle/notification_settings_test.exs, test/paddle/prices_test.exs</files>
  <action>Apply the same Req 0.7 adapter migration to these resource tests, using `test/paddle/http_test.exs` as the mapped request-response analog. Preserve provider-shaped fixtures, request path/body assertions, and public return shapes. Keep this task dependency-compatible only; no new endpoint or behavior breadth is authorized.</action>
  <verify>
    <automated>mix test test/paddle/adjustments_test.exs test/paddle/customers/portal_sessions_test.exs test/paddle/events_test.exs test/paddle/notification_settings_test.exs test/paddle/prices_test.exs --warnings-as-errors</automated>
    <fails_when>Any focused test fails, an adapter warning remains, a provider fixture is bypassed, or an existing request/return assertion disappears.</fails_when>
  </verify>
  <acceptance_criteria>The five resource suites run warning-free with behavior unchanged under Req 0.7.</acceptance_criteria>
  <done>The second compatibility fixture batch is green.</done>
</task>

<task type="auto">
  <name>Task 3: Finish adapter migration and run the complete compatibility matrix</name>
  <precondition>`../accrue/CLAUDE.md` and `../accrue/accrue/test/accrue/billing/subscription_projection_provider_test.exs` exist; Hex registry access and the demo PostgreSQL service are available.</precondition>
  <read_first>test/paddle/products_test.exs, test/paddle/subscriptions_test.exs, test/paddle/transactions_test.exs, bin/package_smoke.sh, demo/AGENTS.md, ../accrue/CLAUDE.md, ../accrue/accrue/mix.exs, .planning/phases/32-dependency-sdk-trust-boundary/32-VALIDATION.md</read_first>
  <files>test/paddle/products_test.exs, test/paddle/subscriptions_test.exs, test/paddle/transactions_test.exs, bin/phase32_compatibility.sh, .planning/phases/32-dependency-sdk-trust-boundary/32-VALIDATION.md</files>
  <action>Finish the supported adapter migration in the three tests. Create `bin/phase32_compatibility.sh` from the mapped `bin/package_smoke.sh` analog. It must fail fast and run D-03's root suite, focused adapters and telemetry, MockServer/subscription flows, Dialyzer, docs, package smoke (including the clean consumer without optional Plug/Bandit), demo precommit, the exact Accrue projection seam command through an explicit `ACCRUE_CHECKOUT`, and online `mix hex.audit`; it must not publish, call hosted CI, or mask a missing lane. Record exact commands/results/timings and mark only completed SAFE-01/Wave-0 entries in 32-VALIDATION.md, per D-03 and D-04.</action>
  <verify>
    <automated>bash -n bin/phase32_compatibility.sh &amp;&amp; ACCRUE_CHECKOUT=../accrue bin/phase32_compatibility.sh</automated>
    <fails_when>Shell syntax is invalid, a required matrix lane is absent/skipped/masked, any command exits nonzero, the sibling checkout is modified, an audit advisory remains, or 32-VALIDATION.md claims evidence not actually produced.</fails_when>
  </verify>
  <acceptance_criteria>The remaining adapters are warning-free; one command executes every D-03 lane, `mix hex.audit` is the only advisory tool, downstream proof is read-only, and validation records attributable results.</acceptance_criteria>
  <done>The dependency migration is fully compatibility-tested and behavior work may begin.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Test adapters → Req transport | Test doubles must exercise rather than bypass Req 0.7 behavior. |
| Oarlock → demo/package/Accrue | Compatibility claims cross package and repository ownership boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-32-01 | Tampering | compatibility evidence | high | mitigate | A fail-fast tracked matrix names every D-03 lane and records only observed results. |
| T-32-02 | Information Disclosure | matrix output | high | mitigate | Tests use canaries and the runner never prints real credentials; detailed containment lands in Plans 32-06/07. |
| T-32-04 | Tampering/Repudiation | replay/idempotency assertions | high | mitigate | Preserve old assertions only for compatibility here, then replace false replay claims in Plans 32-04/05 before final matrix acceptance. |
| T-32-05 | Spoofing | custom base URLs | high | mitigate | Matrix covers MockServer, while Plan 32-03 adds coherent URL/environment validation. |
| T-32-06 | Denial of Service | Retry-After | high | mitigate | Plan 32-04 adds the 60-second read-only cap and exact attempt proof. |
| T-32-SC | Tampering | dependency supply chain | high | mitigate | Official Hex resolution plus `mix hex.audit`; no duplicate scanner or new package. |
</threat_model>

## Artifacts this phase produces

- Supported Req 0.7 adapter forms in all 13 classified adapter-bearing test files.
- `bin/phase32_compatibility.sh`: fail-closed local compatibility runner with `ACCRUE_CHECKOUT` input.
- `32-VALIDATION.md`: exact matrix command, Accrue seam command, observed results, and measured runtimes.

<verification>
Run the complete compatibility runner once, check `git -C ../accrue status --short` is unchanged from its pre-task snapshot, and ensure every D-03 lane has a named result.
</verification>

<success_criteria>
The Req migration has warning-free adapter coverage and a repeatable complete matrix; no SDK behavior, hosted-CI authority, release publication, or API breadth was added.
</success_criteria>

<output>
Create `.planning/phases/32-dependency-sdk-trust-boundary/32-02-SUMMARY.md` when done.
</output>
