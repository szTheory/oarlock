---
phase: 32-dependency-sdk-trust-boundary
plan: "09"
type: execute
wave: 6
depends_on: ["32-03", "32-04"]
files_modified:
  - lib/paddle/notification_setting.ex
  - lib/paddle/portal_session.ex
  - lib/paddle/subscription/management_urls.ex
  - lib/paddle/transaction/checkout.ex
  - test/paddle/portal_session_test.exs
  - test/paddle/inspection_safety_test.exs
autonomous: true
requirements: [SAFE-03]
estimate:
  tokens: 29000
  raw_tokens: 29000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Every capability-bearing public value found by the complete raw_data inventory has an explicit total Inspect projection; the minimum is Client, Error, NotificationSetting, PortalSession, ManagementUrls, and Checkout (D-07)."
    - "Promoted secrets and the entire secret-capable `raw_data` container render as the stable `[REDACTED]` marker while stored values remain unchanged (D-08)."
    - "Provider-shaped hydration plus unique nested canaries proves absence for every inventoried value, not only hand-built structs (D-09)."
  artifacts:
    - path: test/paddle/inspection_safety_test.exs
      provides: "Exhaustive public raw_data inventory and recursive provider-hydration canary proof"
    - path: lib/paddle/notification_setting.ex
      provides: "Notification destination/secret/raw_data Inspect redaction"
    - path: lib/paddle/portal_session.ex
      provides: "Portal URL/raw_data Inspect redaction"
    - path: lib/paddle/subscription/management_urls.ex
      provides: "Management capability URL/raw_data Inspect redaction"
    - path: lib/paddle/transaction/checkout.ex
      provides: "Checkout capability URL/raw_data Inspect redaction"
  key_links:
    - from: test/paddle/inspection_safety_test.exs
      to: lib/paddle/http.ex
      via: "fixtures use Paddle.Http.build_struct/2 to preserve realistic promoted/raw duplication"
      pattern: "build_struct"
    - from: test/paddle/inspection_safety_test.exs
      to: lib/paddle
      via: "source inventory fails when a new public raw_data struct lacks classification"
      pattern: "raw_data"
  assumptions:
    - id: EDGE-SAFE-03-UNCLASSIFIED
      status: flagged-unverified
      statement: "SAFE-03's deterministic edge probe was unclassified; the plan assumes capability-bearing fields plus arbitrary provider payload duplication define the redaction boundary and requires a source-derived inventory to detect additions."
  prohibitions:
    - id: PROHIB-SAFE-03-DATA-DESTRUCTION
      requirement_id: SAFE-03
      category: autonomy
      status: unresolved
      verification: null
      flagged_unverified: true
      statement: "MUST NOT delete, rewrite, or stop hydrating stored `raw_data` merely to make inspection safe; redaction applies only to representation boundaries."
    - id: PROHIB-SAFE-03-FIXTURE-BIAS
      requirement_id: SAFE-03
      category: safety
      status: unresolved
      verification: null
      flagged_unverified: true
      statement: "MUST NOT declare arbitrary provider payloads safe from field-name filtering, a known-key denylist, or fixtures that omit promoted/raw duplication."
---

<objective>
Finish the deny-by-default Inspect boundary for every public value capable of carrying credentials, endpoint secrets, authenticated URLs, or equivalent raw provider values (D-07, D-08, D-09).

Purpose: Prevent ordinary logging/inspection from disclosing capability-bearing SDK state while preserving forward-compatible stored data.
Output: Four additional total Inspect implementations and an exhaustive cross-struct safety test.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-dependency-sdk-trust-boundary/32-03-SUMMARY.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-04-SUMMARY.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-CONTEXT.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-PATTERNS.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-RESEARCH.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-VALIDATION.md
@lib/paddle/http.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Inventory public raw_data values and protect notification/portal inspection</name>
  <read_first>
    - lib/paddle/notification_setting.ex
    - lib/paddle/portal_session.ex
    - test/paddle/portal_session_test.exs
    - lib/paddle/http.ex
  </read_first>
  <files>lib/paddle/notification_setting.ex, lib/paddle/portal_session.ex, test/paddle/portal_session_test.exs, test/paddle/inspection_safety_test.exs</files>
  <behavior>
    - A source inventory names every public struct with raw_data and classifies capability-bearing fields.
    - NotificationSetting redacts destination, endpoint_secret_key, raw_data.
    - PortalSession redacts urls and raw_data.
    - Provider-hydrated promoted and nested canaries are absent; stored terms remain equal after inspect.
  </behavior>
  <action>Follow the mapped `lib/paddle/portal_session.ex` total Inspect projection and composite portal/telemetry test analogs in 32-PATTERNS.md. Create the source inventory/recursive walker and add explicit NotificationSetting/PortalSession projections using `[REDACTED]` with provider-hydrated fixtures. Per D-18, update both owning @moduledoc/@doc and public struct types to name every promoted/container field redacted by Inspect while stating stored runtime data remains unchanged.</action>
  <verify>
    <automated>mix test test/paddle/portal_session_test.exs test/paddle/inspection_safety_test.exs</automated>
    <fails_when>The inventory omits a public raw_data struct, Inspect raises/falls back, a promoted/nested canary renders, or stored data changes.</fails_when>
  </verify>
  <acceptance_criteria>The inventory is exhaustive and both notification/portal public values are safe under realistic hydration.</acceptance_criteria>
  <done>The first provider-value group has deny-by-default inspection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Protect management and checkout capability inspection</name>
  <read_first>
    - lib/paddle/subscription/management_urls.ex
    - lib/paddle/transaction/checkout.ex
    - test/paddle/inspection_safety_test.exs
    - lib/paddle/http.ex
  </read_first>
  <files>lib/paddle/subscription/management_urls.ex, lib/paddle/transaction/checkout.ex, test/paddle/inspection_safety_test.exs</files>
  <behavior>
    - ManagementUrls redacts update_payment_method, cancel, raw_data.
    - Checkout redacts url and raw_data.
    - The cross-struct suite also proves Client and Error protections from Plans 03/04.
    - Adding a capability field or public raw_data struct without classification fails closed.
  </behavior>
  <action>Follow the mapped PortalSession Inspect analog and deny-by-default observability pattern in 32-PATTERNS.md. Add explicit ManagementUrls/Checkout projections and complete the six-type provider-hydrated canary table. Per D-18, update both owning module docs and public struct types to identify the exact promoted/raw fields redacted and the visible non-capability fields; keep stored terms unchanged.</action>
  <verify>
    <automated>mix test test/paddle/inspection_safety_test.exs</automated>
    <fails_when>Any capability/raw canary renders, a required marker/ordinary field disappears, the inventory can silently miss additions, or an Inspect implementation mutates data.</fails_when>
  </verify>
  <acceptance_criteria>All six secret-bearing public values are provider-realistic, total, stable-marker, and mutation-free under Inspect, with owning module docs/types matching the exact projections (D-18).</acceptance_criteria>
  <done>SAFE-03 is exhaustively covered by source inventory and recursive canaries.</done>
</task>

</tasks>

## Artifacts this phase produces

- New path: `test/paddle/inspection_safety_test.exs`.
- Inspect implementations for `Paddle.NotificationSetting`, `Paddle.PortalSession`, `Paddle.Subscription.ManagementUrls`, and `Paddle.Transaction.Checkout` (plus coverage of Client/Error created earlier).
- Redacted fields: `destination`, `endpoint_secret_key`, `urls`, `update_payment_method`, `cancel`, `url`, and each protected `raw_data` container.
- Private test functions for source inventory and recursive canary traversal; no new public function, struct/type field, or CLI flag.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Provider payload → typed public struct | Capability values are duplicated into promoted fields and raw_data. |
| Public struct → Inspect/log consumer | Stored provider values become printable representation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-32-23 | Information Disclosure | public Inspect implementations | critical | mitigate | Explicit whole-field/container projections and unique provider-hydrated canaries across every inventoried type. |
| T-32-24 | Tampering | stored forward-compatible raw_data | medium | mitigate | Assert inspect leaves stored structs byte/term-equal and never changes hydration. |
</threat_model>

<verification>
Run both focused commands, then `rg -l 'raw_data' lib/paddle | sort` and compare every public struct result against the test inventory with no silent omission.
</verification>

<success_criteria>
Every secret-bearing public type redacts promoted and raw copies with `[REDACTED]`, stored data remains intact, and additions fail the inventory test until classified.
</success_criteria>

<output>
Create `.planning/phases/32-dependency-sdk-trust-boundary/32-09-SUMMARY.md` when done.
</output>
