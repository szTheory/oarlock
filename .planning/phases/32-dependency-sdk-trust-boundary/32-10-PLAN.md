---
phase: 32-dependency-sdk-trust-boundary
plan: "10"
type: execute
wave: 9
depends_on: ["32-11", "32-03", "32-04", "32-05", "32-06", "32-07", "32-08", "32-09"]
files_modified:
  - README.md
  - guides/getting-started.md
  - guides/telemetry.md
  - guides/accrue-seam.md
  - demo/README.md
  - CHANGELOG.md
  - test/paddle/seam_test.exs
  - bin/phase32_contract_proof.sh
autonomous: true
requirements: [SAFE-01, SAFE-02, SAFE-03, SAFE-04, SAFE-05, SAFE-06]
estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Public docs/types/examples and migration notes state Req `~> 0.7.4`, the supported BEAM range, constructor table, safe-read ceiling/cap, mutation single-attempt ambiguity, removed idempotency option, telemetry allowlist/schema break, and Inspect redaction exactly as runtime tests prove (D-01, D-04, D-18)."
    - "The evidence ladder distinguishes unit/local, MockServer, package/downstream, sandbox, hosted CI, and live provider proof without deriving idempotency/provider compatibility from headers or mocks (D-03, D-19)."
    - "Running the documentation contract and full compatibility runner twice on unchanged input produces the same verdict and no tracked-file mutation; no success depends on an earlier run (SAFE-06 idempotency probe)."
    - "Concurrent readers/builds consume one consistent tracked contract, and an interrupted or partially completed matrix cannot be recorded as phase acceptance (SAFE-06 concurrency probe)."
    - "The final compatibility matrix re-proves SAFE-01 through SAFE-06 after all behavior and docs changes, while hosted authority and publication remain outside Phase 32 (D-04, D-19)."
  artifacts:
    - path: README.md
      provides: "Primary secure installation and behavior contract"
    - path: guides/getting-started.md
      provides: "Validated constructor/retry/reconciliation examples"
    - path: guides/telemetry.md
      provides: "Exact allowlisted telemetry schema and migration guidance"
    - path: guides/accrue-seam.md
      provides: "Downstream compatibility and evidence ladder"
    - path: demo/README.md
      provides: "MockServer/demo evidence boundary"
    - path: CHANGELOG.md
      provides: "Pre-1.0 compatibility and migration record"
    - path: test/paddle/seam_test.exs
      provides: "Positive and negative mechanical public-contract guards"
    - path: bin/phase32_contract_proof.sh
      provides: "Executable SAFE-06 concurrency, interruption, repeatability, and no-drift proof over the final compatibility contract"
  key_links:
    - from: test/paddle/seam_test.exs
      to: README.md
      via: "required and forbidden contract assertions"
      pattern: "README\\.md"
    - from: test/paddle/seam_test.exs
      to: guides/telemetry.md
      via: "exact event/metadata and forbidden raw-object claims"
      pattern: "guides/telemetry\\.md"
    - from: bin/phase32_compatibility.sh
      to: test/paddle/seam_test.exs
      via: "final complete matrix includes the public contract test"
      pattern: "mix test"
    - from: bin/phase32_contract_proof.sh
      to: bin/phase32_compatibility.sh
      via: "runs the compatibility self-test and two fresh complete matrices, then compares their atomic receipts"
      pattern: "phase32_compatibility\\.sh"
    - from: bin/phase32_contract_proof.sh
      to: test/paddle/seam_test.exs
      via: "isolated concurrent contract readers verify the tracked documentation/type/spec inputs before final matrix acceptance"
      pattern: "seam_test\\.exs"
    - from: bin/phase32_contract_proof.sh
      to: README.md
      via: "byte-identical isolated source snapshots bind repeatability evidence to the primary public contract consumed by the seam assertions"
      pattern: "README\\.md"
  prohibitions:
    - id: PROHIB-SAFE-06-EVIDENCE-INFLATION
      requirement_id: SAFE-06
      category: transparency
      status: unresolved
      verification: null
      flagged_unverified: true
      statement: "MUST NOT collapse local, MockServer, package/downstream, sandbox, hosted-CI, or live-provider evidence into an undifferentiated compatibility or provider-behavior guarantee."
    - id: PROHIB-SAFE-06-SPLIT-CONTRACT
      requirement_id: SAFE-06
      category: safety
      status: unresolved
      verification: null
      flagged_unverified: true
      statement: "MUST NOT leave generated docs, examples, module types, migration notes, or the demo describing a conflicting retry, idempotency, telemetry, validation, Req, or BEAM contract."
---

<objective>
Publish one mechanically guarded, evidence-honest public contract for all Phase 32 behavior and re-run the complete compatibility/advisory matrix (D-01 through D-19, with D-18/D-19 owning the documentation boundary).

Purpose: Ensure SDK consumers learn exactly the runtime behavior that has been tested, including migration impact and proof limits.
Output: Aligned public docs/changelog/examples, contract tests, and final full-matrix evidence for SAFE-01 through SAFE-06.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-dependency-sdk-trust-boundary/32-02-SUMMARY.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-03-SUMMARY.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-04-SUMMARY.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-08-SUMMARY.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-09-SUMMARY.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-CONTEXT.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-PATTERNS.md
@.planning/phases/32-dependency-sdk-trust-boundary/32-VALIDATION.md
@README.md
@guides/getting-started.md
@guides/telemetry.md
@guides/accrue-seam.md
@demo/README.md
@CHANGELOG.md
@test/paddle/seam_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define and guard the secure installation/runtime migration contract</name>
  <read_first>
    - README.md
    - guides/getting-started.md
    - guides/telemetry.md
    - CHANGELOG.md
    - test/paddle/seam_test.exs
    - lib/paddle/client.ex
    - lib/paddle/http.ex
    - lib/paddle/error.ex
    - lib/paddle/internal/pagination.ex
    - lib/paddle/adjustments.ex
    - lib/paddle/customers.ex
    - lib/paddle/customers/addresses.ex
    - lib/paddle/customers/portal_sessions.ex
    - lib/paddle/portal_sessions.ex
    - lib/paddle/events.ex
    - lib/paddle/notification_settings.ex
    - lib/paddle/prices.ex
    - lib/paddle/products.ex
    - lib/paddle/subscriptions.ex
    - lib/paddle/transactions.ex
    - lib/paddle/http/telemetry.ex
    - lib/paddle/notification_setting.ex
    - lib/paddle/portal_session.ex
    - lib/paddle/subscription/management_urls.ex
    - lib/paddle/transaction/checkout.ex
    - .planning/phases/32-dependency-sdk-trust-boundary/32-PATTERNS.md
  </read_first>
  <files>README.md, guides/getting-started.md, guides/telemetry.md, CHANGELOG.md, test/paddle/seam_test.exs</files>
  <behavior>
    - Positive assertions require the exact Req floor, BEAM support, constructor table, read retry ceiling/429 cap, mutation ambiguity/reconciliation, telemetry keys, and redaction marker.
    - Negative assertions reject idempotency_key support, mutation retry enabling, raw transport telemetry, credentialized validation examples, and conflicting version ranges.
    - Changelog/migration text explicitly calls out pre-1.0 source/observable behavior changes.
    - Compiled module docs and fetched types/specs encode the exact constructor, retry, ambiguity, telemetry, idempotency-removal, static-context, and Inspect contracts owned by Plans 03-09.
  </behavior>
  <action>Follow the composite `guides/accrue-seam.md` proof-boundary plus `test/paddle/seam_test.exs` documentation-as-tested-contract analog mapped in 32-PATTERNS.md. Write failing assertions first, then align the four public files per D-01/D-04/D-18. In addition to tracked-file assertions, use `Code.fetch_docs/1`, `Code.Typespec.fetch_types/1`, and `Code.Typespec.fetch_specs/1` over Client, Http, Error, Pagination, every resource module, Telemetry, and all inspected public structs to require the owning plans' exact constructor, retry, idempotency removal, ambiguity fields/atoms, telemetry keys, static-route, cursor, and redaction semantics; explicitly compare extracted type and function-spec shapes to the runtime decision tables rather than treating `mix docs` compilation as agreement. Keep region-scoped negative gates.</action>
  <verify>
    <automated>mix test test/paddle/seam_test.exs &amp;&amp; mix docs</automated>
    <fails_when>A required claim is missing, stale/unsafe wording matches, docs fail to build, negative gates scan unrelated regions, or any claim exceeds tested runtime behavior.</fails_when>
  </verify>
  <acceptance_criteria>Primary docs plus every affected runtime module @doc/@moduledoc/type/spec agree mechanically with the Phase 32 decision tables; semantic drift fails even when `mix docs` compiles (D-18).</acceptance_criteria>
  <done>The secure contract and migration impact are explicit and generated-doc compatible.</done>
  <reversibility rating="costly">Public guidance records intentional pre-1.0 behavior changes; reversal requires another explicit migration notice.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete the evidence ladder and certify the final matrix</name>
  <precondition>`../accrue/accrue` remains available and the Hex registry is reachable; otherwise halt without marking the final contract accepted.</precondition>
  <read_first>
    - guides/accrue-seam.md
    - demo/README.md
    - test/paddle/seam_test.exs
    - bin/phase32_compatibility.sh
    - .planning/phases/32-dependency-sdk-trust-boundary/COVERAGE.md
    - .planning/phases/32-dependency-sdk-trust-boundary/32-PATTERNS.md
  </read_first>
  <files>guides/accrue-seam.md, demo/README.md, test/paddle/seam_test.exs, bin/phase32_contract_proof.sh</files>
  <behavior>
    - The evidence ladder names local/unit, MockServer, package/downstream, sandbox, hosted CI, and live provider as distinct tiers.
    - Negative assertions reject provider replay/compatibility guarantees based on mock or header evidence.
    - Two consecutive seam/matrix runs are green and leave tracked files unchanged.
    - Every COVERAGE.md row and SAFE requirement has a named final proof result.
    - Two isolated concurrent readers/builds consume byte-identical tracked inputs and both complete successfully.
    - A killed or nonzero partial matrix produces no acceptance receipt; only all-row success creates one atomic receipt.
  </behavior>
  <action>Per D-19, follow the mapped Accrue proof-boundary vocabulary and package-smoke isolation pattern in 32-PATTERNS.md. Extend the evidence ladder and negative seam assertions. Create `bin/phase32_contract_proof.sh` to make the SAFE-06 probes executable: copy the same tracked contract into two isolated temporary workspaces, run a seam-contract reader and `mix docs` build concurrently with separate `MIX_BUILD_PATH` values, require byte-identical source inputs and both zero exits, then run `bin/phase32_compatibility.sh --self-test` to kill/fail a synthetic partial matrix and require no receipt. Finally run the complete D-03 matrix twice into fresh evidence directories, require one atomic complete receipt per run, equal row/verdict manifests, and no tracked-file drift. Map results to SAFE-01..06/COVERAGE.md.</action>
  <verify>
    <automated>mix test test/paddle/seam_test.exs &amp;&amp; ACCRUE_CHECKOUT=../accrue bin/phase32_contract_proof.sh</automated>
    <fails_when>Isolated concurrent readers/builds observe different tracked inputs or either fails, the interruption self-test leaves a receipt, a partial matrix is accepted, either full matrix differs/fails/skips a row, tracked files drift, or any SAFE/COVERAGE row lacks proof.</fails_when>
  </verify>
  <acceptance_criteria>The public contract is evidence-honest; isolated concurrent readers/builds agree; interruption/partial runs cannot be accepted; and two identical complete matrix receipts certify the unchanged Phase 32 state.</acceptance_criteria>
  <done>SAFE-01 through SAFE-06 are aligned across runtime, types, docs, examples, migration guidance, package/downstream proof, and audit.</done>
</task>

</tasks>

## Artifacts this phase produces

- Documentation contracts for Req `~> 0.7.4`, supported BEAM versions, `Paddle.Client.new!/1`, retry/ambiguity fields, telemetry event/key sets, and `[REDACTED]` inspection behavior.
- Migration guidance for removed `idempotency_key`, mutation single-attempt semantics, telemetry metadata break, constructor rejection, and pre-1.0 impact.
- Mechanical test data/helpers in `test/paddle/seam_test.exs` for required claims, region-scoped forbidden claims, evidence tiers, and repeat-run/no-drift checks.
- New path: `bin/phase32_contract_proof.sh` for isolated concurrent reader/build proof, interrupted/partial receipt rejection, and identical complete matrix receipts.
- No new public symbol, CLI flag, struct/type field, or function beyond artifacts introduced and listed in preceding plans.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Tested runtime → public documentation | Claims can drift from actual safe behavior and migration impact. |
| Local/mock/package evidence → consumer confidence | Weaker proof can be overstated as hosted/live/provider-state authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-32-25 | Repudiation | public contract/evidence ladder | high | mitigate | Mechanical positive/negative assertions bind each claim to tested behavior and explicit evidence tiers. |
| T-32-26 | Tampering | concurrent/interrupted/final compatibility evidence | high | mitigate | Require byte-identical isolated concurrent readers/builds, no receipt after killed/nonzero partial runs, and two equal atomic all-row receipts with no tracked-file drift. |
| T-32-27 | Information Disclosure | documentation examples | medium | mitigate | Use synthetic placeholders and negative checks for credentialized URLs/raw telemetry examples. |
</threat_model>

<verification>
Run both task commands, `mix format --check-formatted`, and confirm the final matrix summary maps all SAFE IDs, all D-01..D-19 decisions, and every COVERAGE.md row without claiming Phase 33/34 authority.
</verification>

<success_criteria>
All public surfaces agree with runtime behavior, stale unsafe claims are mechanically rejected, evidence tiers remain distinct, and two complete final matrix runs are green and non-mutating.
</success_criteria>

<output>
Create `.planning/phases/32-dependency-sdk-trust-boundary/32-10-SUMMARY.md` when done.
</output>
