---
phase: 27-public-contract-documentation-truth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/paddle/seam_test.exs
  - guides/accrue-seam.md
autonomous: true
requirements:
  - DOCS-01
  - DOCS-03
  - DOCS-04
must_haves:
  truths:
    - "DOCS-01: `guides/accrue-seam.md` describes the live public `Paddle.*` seam by module/function/arity rather than stale prose."
    - "DOCS-03: The seam guide distinguishes locked SDK contract from app-owned Phoenix/Ecto/provisioning responsibilities."
    - "DOCS-04: The seam guide uses proof ladder language and does not imply MockServer is provider-state proof."
    - "D-01/D-02/D-03/D-04: The human seam guide stays canonical while a lightweight guard checks live inventory drift."
    - "D-09/D-10/D-11: Proof claims separate unit/contract, MockServer-backed local proof, sandbox checks, and operator-owned live readiness."
  artifacts:
    - path: "test/paddle/seam_test.exs"
      provides: "Focused docs-truth guard for live public inventory and proof-boundary terms"
    - path: "guides/accrue-seam.md"
      provides: "Canonical human-readable public seam contract with `Paddle.PortalSessions` classification"
  key_links:
    - from: "test/paddle/seam_test.exs"
      to: "guides/accrue-seam.md"
      via: "File.read! assertion against documented module/function/arity inventory"
      pattern: "guides/accrue-seam.md"
    - from: "guides/accrue-seam.md"
      to: "lib/paddle/customers/portal_sessions.ex"
      via: "Preferred portal session namespace documented as `Paddle.Customers.PortalSessions.create/2..4`"
      pattern: "Paddle.Customers.PortalSessions"
    - from: "guides/accrue-seam.md"
      to: "lib/paddle/portal_sessions.ex"
      via: "`Paddle.PortalSessions.create/2` classified as legacy compatibility surface, not the preferred Accrue seam"
      pattern: "Paddle.PortalSessions"
---

<objective>
Make the Accrue seam guide and seam test the durable source of truth for the shipped public contract.

Purpose: Prevent public contract drift while preserving the human-readable seam guide as the canonical contract per D-01 and D-02.
Output: A focused ExUnit docs-truth guard and an updated seam guide that classifies `Paddle.PortalSessions.create/2` relative to `Paddle.Customers.PortalSessions.create/2..4`.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/27-public-contract-documentation-truth/27-CONTEXT.md
@.planning/phases/27-public-contract-documentation-truth/27-RESEARCH.md
@.planning/phases/27-public-contract-documentation-truth/27-PATTERNS.md
@.planning/phases/27-public-contract-documentation-truth/27-VALIDATION.md
@test/paddle/seam_test.exs
@guides/accrue-seam.md
@lib/paddle/portal_sessions.ex
@lib/paddle/customers/portal_sessions.ex
@lib/paddle/webhooks.ex
</context>

## Artifacts this phase produces

- New or extended ExUnit test in `test/paddle/seam_test.exs` named to indicate the seam guide documents live public inventory.
- New or extended ExUnit test in `test/paddle/seam_test.exs` that rejects unsupported provider-proof wording across the Phase 27 public docs.
- `guides/accrue-seam.md` section or subsection classifying `Paddle.PortalSessions.create/2` as legacy compatibility surface while steering new code to `Paddle.Customers.PortalSessions.create/2`, `create/3`, and `create/4`.
- Updated `guides/accrue-seam.md` public module/function inventory covering `Paddle.Customers`, `Paddle.Customers.Addresses`, `Paddle.Customers.PortalSessions`, `Paddle.Transactions`, `Paddle.Adjustments`, `Paddle.Subscriptions`, `Paddle.Webhooks`, `Paddle.Products`, `Paddle.Prices`, `Paddle.Events`, `Paddle.NotificationSettings`, `Paddle.Page`, `Paddle.Error`, and compatibility `Paddle.PortalSessions`.
- Updated `guides/accrue-seam.md` proof-boundary and app-owned responsibility wording.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add lightweight seam docs-truth guard</name>
  <files>test/paddle/seam_test.exs</files>
  <read_first>
    test/paddle/seam_test.exs
    guides/accrue-seam.md
    .planning/phases/27-public-contract-documentation-truth/27-CONTEXT.md
    .planning/phases/27-public-contract-documentation-truth/27-RESEARCH.md
    .planning/phases/27-public-contract-documentation-truth/27-PATTERNS.md
    lib/paddle/portal_sessions.ex
    lib/paddle/customers/portal_sessions.ex
  </read_first>
  <behavior>
    - Test 1: Loading the live public inventory from `__info__(:functions)` for the documented `Paddle.*` modules finds the same module/function/arity entries named in `guides/accrue-seam.md`.
    - Test 2: `Paddle.Customers.PortalSessions` must be documented with `create/2`, `create/3`, and `create/4`; `Paddle.PortalSessions` must be documented as compatibility `create/2`, not as the preferred new-code seam.
    - Test 3: Public docs do not contain unsupported provider-proof phrases unless paired with explicit real Paddle credential evidence in the same section.
  </behavior>
  <action>
    Extend `Paddle.SeamTest` with a focused docs-truth guard per D-01, D-03, and D-04. Use existing ExUnit assertions, `File.read!("guides/accrue-seam.md")`, `function_exported?/3`, and `module.__info__(:functions)`; do not add a broad docs generator or a new dependency. Keep the expected inventory explicit in the test so a public API addition or removal forces a deliberate seam-guide edit. Include `Paddle.PortalSessions.create/2` in the inventory as legacy compatibility surface and assert that the preferred path remains `Paddle.Customers.PortalSessions.create/2`, `create/3`, and `create/4`. Add a small proof-boundary assertion over `README.md`, `guides/getting-started.md`, `guides/accrue-seam.md`, `demo/README.md`, and `CHANGELOG.md` that rejects unsupported claims equivalent to sandbox/provider-state verification when the docs only describe MockServer-backed local proof, per D-09, D-10, and D-11.
  </action>
  <acceptance_criteria>
    Source assertions: `test/paddle/seam_test.exs` contains an explicit expected inventory for the public modules listed in 27-PATTERNS.md, including `Paddle.PortalSessions` compatibility `create/2`; the test reads `guides/accrue-seam.md` from disk rather than relying on copied strings only inside the test.
    Behavior assertions: the seam test fails if `guides/accrue-seam.md` omits `Paddle.Customers.PortalSessions.create/4`, omits the compatibility classification for `Paddle.PortalSessions.create/2`, or uses unsupported provider-proof wording without evidence.
    Test command: `mix test test/paddle/seam_test.exs --warnings-as-errors`.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/paddle/seam_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The seam test enforces live inventory truth and proof-boundary wording without introducing a docs generator or package dependency.</done>
</task>

<task type="auto">
  <name>Task 2: Update canonical seam contract inventory and boundaries</name>
  <files>guides/accrue-seam.md</files>
  <read_first>
    guides/accrue-seam.md
    test/paddle/seam_test.exs
    .planning/phases/27-public-contract-documentation-truth/27-CONTEXT.md
    .planning/phases/27-public-contract-documentation-truth/27-RESEARCH.md
    .planning/phases/27-public-contract-documentation-truth/27-PATTERNS.md
    prompts/oarlock-brand-book.md
    lib/paddle/client.ex
    lib/paddle/error.ex
    lib/paddle/webhooks.ex
    lib/paddle/customers/portal_sessions.ex
    lib/paddle/portal_sessions.ex
  </read_first>
  <action>
    Update `guides/accrue-seam.md` per D-01, D-02, D-03, D-06, D-09, D-10, D-11, D-17, D-18, and D-19. Preserve the locked/additive/opaque vocabulary. Make the public function inventory match live exported functions and arities from 27-PATTERNS.md, including default-expanded arities. Classify `Paddle.PortalSessions.create/2` as legacy compatibility surface: visible and documented because it ships, but not the preferred Accrue seam for new integrations. State that new code should use `Paddle.Customers.PortalSessions.create(client, customer_id, attrs \\ %{}, opts \\ [])`. Add or tighten app-owned boundary language for Phoenix/Plug raw-body capture, Ecto persistence, provisioning, authorization, idempotency, and environment-specific Paddle credentials. Use proof ladder wording from D-09 through D-12 and avoid provider proof claims unless a real Paddle credential run is documented.
  </action>
  <acceptance_criteria>
    Source assertions: `guides/accrue-seam.md` names `Paddle.Customers.PortalSessions.create(client, customer_id, attrs \\ %{}, opts \\ [])` or equivalent `create/2`, `create/3`, `create/4` arity wording; it also contains a clearly labeled compatibility entry for `Paddle.PortalSessions.create/2`.
    Wording assertions: the seam guide states MockServer-backed proof is deterministic local proof, not live Paddle provider-state verification; it states Phoenix/Plug/Ecto/provisioning/authorization are app-owned responsibilities.
    Test commands: `mix test test/paddle/seam_test.exs --warnings-as-errors` and `mix docs --warnings-as-errors`.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/paddle/seam_test.exs --warnings-as-errors && mix docs --warnings-as-errors</automated>
  </verify>
  <done>`guides/accrue-seam.md` is a truthful canonical contract for the shipped seam and passes the new guard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs reader -> SDK API | Consumers may depend on documented modules, functions, arities, and struct fields. |
| Phoenix/Plug app -> Paddle webhook | Untrusted webhook payload must be verified from the exact raw body before parsing or provisioning. |
| local MockServer -> Paddle provider | Local deterministic fixture behavior must not be represented as live provider-state proof. |
| app authorization -> portal/session handoff | Oarlock can create Paddle portal sessions, but the app owns signed-in authorization and entitlement checks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | Tampering | `guides/accrue-seam.md` public inventory | mitigate | Add `test/paddle/seam_test.exs` guard that compares live module/function/arity inventory to the seam guide per D-03. |
| T-27-02 | Spoofing/Tampering | Webhook documentation | mitigate | Document raw-body preservation and `Paddle.Webhooks.verify_signature/4` before event parsing per D-07 and D-19. |
| T-27-03 | Repudiation | MockServer proof claims | mitigate | Require proof ladder wording and reject unsupported provider-proof claims per D-09, D-10, and D-11. |
| T-27-04 | Elevation of Privilege | Portal session docs | mitigate | Classify portal APIs accurately and state app-owned authorization/provisioning boundaries per D-06 and D-19. |
| T-27-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs are planned; RESEARCH.md Package Legitimacy Audit recommends none. |
</threat_model>

<verification>
Plan checks: `mix test test/paddle/seam_test.exs --warnings-as-errors` after the guard change, then `mix docs --warnings-as-errors` after seam-guide edits.
</verification>

<success_criteria>
DOCS-01, DOCS-03, and DOCS-04 are satisfied for the canonical seam contract: shipped inventory is explicit, compatibility surface is classified, app-owned boundaries are named, and proof claims are bounded.
</success_criteria>

<output>
Create `.planning/phases/27-public-contract-documentation-truth/27-01-SUMMARY.md` when done.
</output>
