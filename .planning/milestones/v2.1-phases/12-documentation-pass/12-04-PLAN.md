---
phase: 12-documentation-pass
plan: 4
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/paddle/customers.ex
  - lib/paddle/customers/addresses.ex
  - lib/paddle/webhooks.ex
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02

must_haves:
  truths:
    - "Core controller modules have hybrid explicit pipeline examples"
    - "Every public function in core controllers has an example showing the explicit match"
  artifacts:
    - path: "lib/paddle/customers.ex"
      provides: "Customers controller docs"
    - path: "lib/paddle/customers/addresses.ex"
      provides: "Addresses controller docs"
    - path: "lib/paddle/webhooks.ex"
      provides: "Webhooks controller docs"
  key_links: []
---

<objective>
Document the core controller modules (Customers, Addresses, Webhooks) following the Hybrid Explicit approach.

Purpose: Provide excellent DX by explicitly showing pattern matching on success and error tuples (DOCS-01, DOCS-02).
Output: Controller modules documented with `@moduledoc` and `@doc`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-documentation-pass/12-CONTEXT.md
@.planning/phases/12-documentation-pass/12-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document Customers and Addresses controllers</name>
  <files>lib/paddle/customers.ex, lib/paddle/customers/addresses.ex</files>
  <read_first>lib/paddle/customers.ex, lib/paddle/customers/addresses.ex</read_first>
  <action>
    Add `@moduledoc` to `Paddle.Customers` and `Paddle.Customers.Addresses` showing a full pipeline example (`Client.new!/1` -> function call -> `case` match) (D-02).
    Add `@doc` to every public function in both modules. Examples must show the `{:ok, struct}` and `{:error, error}` pattern match (D-03). Client setup can be elided.
    Document local validation atoms if any (e.g. `{:error, :invalid_id}`) explicitly (D-11).
    Add `## Related Paddle docs` with a link to the canonical Paddle API docs for each function (D-07).
    Include a `## Provider behavior` section in docs for non-obvious footguns (D-08).
    Ensure typespecs exhaustively list the error union (e.g., `{:error, :invalid_id | Paddle.Error.t()}`) (D-10).
  </action>
  <verify>
    <automated>grep -c "@moduledoc" lib/paddle/customers.ex > /dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `lib/paddle/customers.ex` contains `@moduledoc` showing a full pipeline
    - `lib/paddle/customers/addresses.ex` contains `@moduledoc` showing a full pipeline
    - Every public function in both files has a `@doc` showing pattern matching on `{:ok, _}` and `{:error, _}`
    - Every public function in both files contains a `## Related Paddle docs` section
  </acceptance_criteria>
  <done>Customers and Addresses are documented.</done>
</task>

<task type="auto">
  <name>Task 2: Document Webhooks controller</name>
  <files>lib/paddle/webhooks.ex</files>
  <read_first>lib/paddle/webhooks.ex</read_first>
  <action>
    Add `@moduledoc` to `Paddle.Webhooks` showing a full pipeline example of receiving a webhook, verifying the signature, and parsing the event.
    Add `@doc` to every public function. Examples must show the `{:ok, _}` and `{:error, _}` pattern match (D-03).
    Document local validation atoms explicitly (e.g. `{:error, :invalid_signature}`) (D-11).
    Add `## Related Paddle docs` with a link to the canonical Paddle API docs for webhooks (D-07).
  </action>
  <verify>
    <automated>grep -c "@moduledoc" lib/paddle/webhooks.ex > /dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `lib/paddle/webhooks.ex` contains `@moduledoc` showing a full pipeline
    - Every public function in `lib/paddle/webhooks.ex` has a `@doc` showing pattern matching
    - Every public function in `lib/paddle/webhooks.ex` contains a `## Related Paddle docs` section
  </acceptance_criteria>
  <done>Webhooks module is documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation | No runtime trust boundaries affected. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Information Disclosure | Docs | mitigate | No real API keys in examples; use placeholders like `sk_test_123` |
</threat_model>

<verification>
No warnings in `mix docs`.
</verification>

<success_criteria>
Core controllers are well-documented.
</success_criteria>

<output>
Create `.planning/phases/12-documentation-pass/12-04-SUMMARY.md` when done
</output>
