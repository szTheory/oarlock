---
phase: 16-seam-validation-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/accrue-seam.md
  - test/paddle/seam_test.exs
autonomous: true
requirements:
  - None
must_haves:
  truths:
    - "User can see PortalSession and Adjustment documented in the Accrue Seam Contract"
    - "Test verifies portal session creation after customer creation"
    - "Test verifies adjustment creation after transaction completion"
  artifacts:
    - path: guides/accrue-seam.md
      provides: "Documentation of the Accrue seam contract"
    - path: test/paddle/seam_test.exs
      provides: "Test verification of the Accrue seam contract"
  key_links:
    - from: test/paddle/seam_test.exs
      to: Paddle.Customers.PortalSessions
      via: create
    - from: test/paddle/seam_test.exs
      to: Paddle.Adjustments
      via: create
---

<objective>
Update the Accrue seam contract documentation and validation tests to explicitly cover the newly added Customer Portal Sessions and Adjustments entities.

Purpose: To ensure Accrue consumers can safely rely on the newly added structs and functions, and to prevent accidental regressions.
Output: An updated `guides/accrue-seam.md` and `test/paddle/seam_test.exs`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@guides/accrue-seam.md
@test/paddle/seam_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document PortalSession and Adjustment in Seam Contract</name>
  <files>guides/accrue-seam.md</files>
  <action>
    Add `Paddle.Customers.PortalSessions` and `Paddle.Adjustments` to the "Public Modules" section.
    For `Paddle.Customers.PortalSessions`, document the explicit arity: `create(client, customer_id, attrs \\ %{}, opts \\ [])` and its returns.
    For `Paddle.Adjustments`, document the explicit arity: `create(client, attrs, opts \\ [])` and its returns.

    Add `%Paddle.PortalSession{}` and `%Paddle.Adjustment{}` to the "Locked Structs" section.
    Follow the stability tier vocabulary:
    - For `%Paddle.PortalSession{}`, mark top-level fields `id`, `customer_id`, `created_at`, `custom_data` as `locked`. Mark `urls` as `opaque`. Mark `raw_data` as `locked`.
    - For `%Paddle.Adjustment{}`, mark top-level fields `id`, `action`, `transaction_id`, `subscription_id`, `customer_id`, `reason`, `credit_applied_to_balance`, `currency_code`, `status`, `created_at`, `updated_at` as `locked`. Mark `items`, `totals`, `payouts` as `opaque` since they represent nested data structures directly from the provider. Mark `raw_data` as `locked`.
  </action>
  <verify>
    <automated>grep -v '^#' guides/accrue-seam.md | grep -c "Paddle.Adjustments"</automated>
  </verify>
  <done>Both modules and structs are documented with standard stability tiers.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate into End-to-End Seam Test</name>
  <files>test/paddle/seam_test.exs</files>
  <action>
    Update the end-to-end flow in `test/paddle/seam_test.exs`:
    1. Immediately following the Customer creation assertion, append a call to `Paddle.Customers.PortalSessions.create/3` to generate a portal session for the created customer. Use a distinct `client_with_adapter` that intercepts `POST /customers/ctm_seam01/portal-sessions` and returns a mock portal session response. Assert on the returned `%Paddle.PortalSession{}` and verify `raw_data` is a map.
    2. Immediately following the Webhook transaction.completed assertion, append a call to `Paddle.Adjustments.create/2` to issue an adjustment (e.g., full refund or credit) for the transaction. Use a distinct `client_with_adapter` that intercepts `POST /adjustments` and returns a mock adjustment response. Assert on the returned `%Paddle.Adjustment{}` and verify `raw_data` is a map.

    Do not reuse `customer_client` or `transaction_client`. Use `client_with_adapter` independently for both operations.
    Create `portal_session_payload/0` and `adjustment_payload/0` helpers at the bottom of the file following existing conventions.
  </action>
  <verify>
    <automated>mix test test/paddle/seam_test.exs</automated>
  </verify>
  <done>The seam test runs successfully and includes portal session and adjustment logic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test Environment | Stubbed responses do not cross real network boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Information Disclosure | `guides/accrue-seam.md` | accept | Ensuring opaque nested structures are explicitly documented as untyped to avoid downstream coupling. |
| T-16-02 | Tampering | `test/paddle/seam_test.exs` | accept | Test stubs simulate untrusted network inputs reliably. |
</threat_model>

<verification>
mix test test/paddle/seam_test.exs passes.
Both new modules and structs appear in guides/accrue-seam.md.
</verification>

<success_criteria>
The Accrue consumer contract is fully up-to-date with explicit tiers for the new features.
The integration path successfully executes without regression or unused clients.
</success_criteria>

<output>
Create `.planning/phases/16-seam-validation-documentation/16-01-SUMMARY.md` when done
</output>
