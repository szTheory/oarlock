---
phase: "18-events-api"
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/paddle/events.ex
  - test/paddle/events_test.exs
autonomous: true
requirements:
  - EVT-01
  - EVT-02
  - EVT-03
  - EVT-04
must_haves:
  truths:
    - User can list event history using auto-paginated helpers (list, stream, all).
    - User can fetch a specific event by ID.
    - API events return standard %Paddle.Event{} structs.
    - Developers are guided by module documentation to use events as triggers.
  artifacts:
    - path: lib/paddle/events.ex
      provides: Events API module
      min_lines: 40
    - path: test/paddle/events_test.exs
      provides: Tests for Events API
      min_lines: 60
  key_links:
    - from: lib/paddle/events.ex
      to: lib/paddle/event.ex
      via: HTTP struct mapping
      pattern: "Http.build_struct\\(Event,"
---

<objective>
Implement the Paddle.Events API wrapper for retrieving historical events.

Purpose: Allow developers to fetch events or poll for changes using the same Event struct generated by incoming webhooks. Provide strong documentation ensuring events are used as triggers rather than state.
Output: Working, tested `Paddle.Events` module.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-events-api/18-RESEARCH.md

<interfaces>
From lib/paddle/event.ex:
```elixir
defmodule Paddle.Event do
  @type t :: %__MODULE__{
          event_id: String.t() | nil,
          event_type: String.t() | nil,
          occurred_at: String.t() | nil,
          notification_id: String.t() | nil,
          data: map() | nil,
          raw_data: map() | nil
        }
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Paddle.Events API Implementation</name>
  <files>lib/paddle/events.ex, test/paddle/events_test.exs</files>
  <behavior>
    - `get/2` requests `GET /events/{event_id}` and returns `{:ok, %Paddle.Event{}}`
    - `list/2` requests `GET /events` with an allowlist `~w(after per_page order_by event_type)` and returns `{:ok, %Paddle.Page{}}`
    - `stream/2` and `all/2` delegate to `Paddle.Internal.Pagination`
    - Any returned `data` map is parsed into a `%Paddle.Event{}` via `Http.build_struct(Event, data)`
    - Empty or invalid string IDs passed to `get/2` return `{:error, :invalid_event_id}`
    - The module documentation explicitly warns against using event payload data as canonical state, guiding developers to fetch the resource directly (fetch-before-act pattern).
  </behavior>
  <action>
    Implement `lib/paddle/events.ex` mirroring the pattern in `lib/paddle/products.ex`. 
    Be sure to `alias Paddle.Event` and use it in `Http.build_struct(Event, data)`.
    Write tests in `test/paddle/events_test.exs` utilizing the `Req` adapter mock pattern (similar to `products_test.exs`) verifying query param allowlists and parsing logic.
    This fulfills requirements EVT-01, EVT-02, EVT-03, and EVT-04.
  </action>
  <verify>
    <automated>mix test test/paddle/events_test.exs</automated>
  </verify>
  <done>Tests pass, pagination works, module docs explain fetch-before-act, and EVT-01, EVT-02, EVT-03, EVT-04 are fully implemented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API Client -> Paddle | HTTP requests carry authentication tokens to fetch sensitive event history. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Information Disclosure | `Paddle.Events.get/2` | mitigate | TLS encryption handled automatically by Req adapter in `Paddle.Http`. |
| T-18-02 | Tampering | `Paddle.Events.list/2` | mitigate | Use parameter allowlist `~w(after per_page order_by event_type)` via `Attrs.allowlist/2` to prevent parameter injection. |
</threat_model>

<verification>
- `mix test test/paddle/events_test.exs` passes.
- `mix format` passes with no violations.
</verification>

<success_criteria>
- The full suite passes correctly.
- Module documentation explains fetch-before-act.
- Pagination is wired correctly using `Paddle.Internal.Pagination`.
</success_criteria>

<output>
Create `.planning/phases/18-events-api/18-01-SUMMARY.md` when done
</output>