---
phase: 10-subscriptions-surface-completion
plan: 01
type: execute
wave: 1
depends_on: ["08-03", "09-01"]
files_modified:
  - test/paddle/transactions_test.exs
  - test/paddle/seam_test.exs
  - guides/getting-started.md
autonomous: true
requirements: [SUB-04]
requirements_addressed: [SUB-04]
tags: [elixir, paddle, subscriptions, transactions, seam, docs]

must_haves:
  truths:
    - "SUB-04 is delivered as a truthful recurring-start seam: recurring subscriptions start with `Paddle.Transactions.create/3`, not a new `Paddle.Subscriptions.create/2` public API (per D-01, D-02)."
    - "`idempotency_key:` remains valid on the transaction create call and is documented/tested there, while the recurring-start story continues through checkout or manual collection, webhook correlation, and canonical `Paddle.Subscriptions.get/2` hydration (per D-03, D-19)."
    - "The public seam explicitly rejects direct subscription creation and does not introduce `Paddle.Subscriptions.start_checkout/3` or similar namespace duplication (per D-01, D-04)."
    - "Getting-started guidance tells adopters to reconcile recurring subscription materialization with webhook events and canonical fetches instead of assuming the local create response is the full billing story (per D-03, D-06, D-18)."
  artifacts:
    - path: "test/paddle/transactions_test.exs"
      provides: "Adapter-backed recurring-start tests for `Paddle.Transactions.create/3` + `idempotency_key:` + canonical `subscription_id` bridge."
      contains: "idempotency_key"
    - path: "test/paddle/seam_test.exs"
      provides: "Public seam proof that the end-to-end path starts at `Paddle.Transactions.create/3`, uses `Transactions.get/2` for subscription correlation, and does not export `Paddle.Subscriptions.create/2`."
      contains: "function_exported?"
    - path: "guides/getting-started.md"
      provides: "Updated recurring-start narrative and examples that point users to the transaction-driven seam."
      contains: "subscriptions are normally created indirectly"
  key_links:
    - from: "test/paddle/transactions_test.exs"
      to: "lib/paddle/transactions.ex"
      via: "Adapter-backed create/get tests lock the `Transactions.create/3` -> `Transactions.get/2` -> `subscription_id` bridge."
      pattern: "Transactions\\.(create|get)"
    - from: "test/paddle/seam_test.exs"
      to: "Paddle.Subscriptions"
      via: "The seam journey refutes `create/2` export and fetches the canonical subscription through `get/2` only."
      pattern: "function_exported\\?"
    - from: "guides/getting-started.md"
      to: "Paddle.Transactions.create/3"
      via: "Guide example and prose tie checkout/webhook reconciliation to the existing transaction seam."
      pattern: "Paddle\\.Transactions\\.create"
---

<objective>
Correct the executable SUB-04 contract around recurring subscription start without inventing a false Paddle surface.

Purpose: Make the phase plans, seam tests, and onboarding guide honest about how recurring subscriptions actually materialize in Paddle Billing: `Paddle.Transactions.create/3` starts the flow, checkout or manual collection completes it, webhooks/canonical fetches confirm it, and `Paddle.Subscriptions.get/2` hydrates the typed subscription.

Output: recurring-start coverage in `test/paddle/transactions_test.exs` and `test/paddle/seam_test.exs`, plus updated guide language in `guides/getting-started.md` that directs users to the transaction-driven seam and rejects direct `Paddle.Subscriptions.create/2`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-subscriptions-surface-completion/10-CONTEXT.md
@.planning/phases/10-subscriptions-surface-completion/10-RESEARCH.md
@.planning/phases/10-subscriptions-surface-completion/10-PATTERNS.md
@.planning/phases/08-reliability-primitives/08-03-SUMMARY.md
@.planning/phases/09-pagination-ergonomics-0-plans/09-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the corrected recurring-start seam in adapter-backed tests</name>
  <files>test/paddle/transactions_test.exs, test/paddle/seam_test.exs</files>
  <read_first>
    - test/paddle/transactions_test.exs
    - test/paddle/seam_test.exs
    - lib/paddle/transactions.ex
    - lib/paddle/subscriptions.ex
    - lib/paddle/http.ex
    - .planning/phases/10-subscriptions-surface-completion/10-CONTEXT.md
    - .planning/phases/10-subscriptions-surface-completion/10-RESEARCH.md
    - .planning/phases/10-subscriptions-surface-completion/10-PATTERNS.md
    - .planning/phases/08-reliability-primitives/08-03-SUMMARY.md
  </read_first>
  <behavior>
    - `Paddle.Transactions.create/3` with recurring items and `idempotency_key:` forwards the `Idempotency-Key` header and returns a checkout-bearing `%Paddle.Transaction{}`.
    - `Paddle.Transactions.get/2` remains the canonical bridge from a completed recurring transaction to `subscription_id`.
    - The seam journey continues from `Transactions.get/2` to `Paddle.Subscriptions.get/2`; it does not rely on a direct subscription create function.
    - The public API still does not export `Paddle.Subscriptions.create/2`.
  </behavior>
  <action>
    Extend `test/paddle/transactions_test.exs` with recurring-start-focused coverage per D-01, D-02, D-03, and D-19. Add one adapter-backed create test that calls `Paddle.Transactions.create/3` with a recurring `items` payload plus an Accrue-style `idempotency_key:` literal, asserts `Req.Request.get_header(request, "idempotency-key")` contains that literal, and keeps the success contract on `%Paddle.Transaction{checkout: %Checkout{}}`.

    Add a companion assertion path in `test/paddle/transactions_test.exs` or the adjacent get-path test that treats `subscription_id` on the fetched completed transaction as the canonical handoff into subscription hydration. The test should name that bridge explicitly so SUB-04 is pinned to `Transactions.get/2`, not implied indirectly.

    Update `test/paddle/seam_test.exs` so the transaction-create request in the existing end-to-end seam test asserts the forwarded `Idempotency-Key` header and the journey explicitly refutes a direct `Paddle.Subscriptions.create/2` export with `refute function_exported?(Paddle.Subscriptions, :create, 2)` before fetching the typed subscription through `Paddle.Subscriptions.get/2` per D-01 and D-04.
  </action>
  <verify>
    <automated>mix test test/paddle/transactions_test.exs test/paddle/seam_test.exs --color</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'Req.Request.get_header(request, "idempotency-key")' test/paddle/transactions_test.exs` returns a hit for the new recurring-start create test.
    - `grep -F 'subscription_id' test/paddle/transactions_test.exs` returns new or updated recurring-start assertions tied to `Transactions.get/2`.
    - `grep -F 'refute function_exported?(Paddle.Subscriptions, :create, 2)' test/paddle/seam_test.exs` returns a hit.
    - `grep -F 'idempotency_key:' test/paddle/seam_test.exs` returns a hit on the transaction create call.
    - `mix test test/paddle/transactions_test.exs test/paddle/seam_test.exs --color` exits 0.
  </acceptance_criteria>
  <done>
    The test suite names and proves the corrected SUB-04 seam: recurring start begins with `Transactions.create/3`, keeps `idempotency_key:` there, correlates through `Transactions.get/2`, and never exports `Paddle.Subscriptions.create/2`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Tighten the getting-started guide around transaction-driven recurring start</name>
  <files>guides/getting-started.md</files>
  <read_first>
    - guides/getting-started.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/10-subscriptions-surface-completion/10-CONTEXT.md
    - .planning/phases/10-subscriptions-surface-completion/10-RESEARCH.md
    - .planning/phases/09-pagination-ergonomics-0-plans/09-01-SUMMARY.md
  </read_first>
  <action>
    Update `guides/getting-started.md` so the recurring-billing walkthrough implements D-03, D-04, D-05, D-06, and D-18 explicitly. Keep the guide rooted in `Paddle.Transactions.create/3`, align its wording with the already-corrected `.planning/ROADMAP.md` / `.planning/REQUIREMENTS.md` language from D-05, and treat those planning files as corrected preconditions from the planning commit rather than execution-batch edit targets. Add an example that includes `idempotency_key:` on the transaction create call, describe checkout/manual-collection completion as the provider step that materializes the subscription, and tell readers to correlate `subscription.created` / `transaction.completed` events or `Paddle.Transactions.get/2` before hydrating the typed subscription via `Paddle.Subscriptions.get/2`.

    Do not introduce new public helper names such as `Paddle.Subscriptions.create/2` or `Paddle.Subscriptions.start_checkout/3`. The guide should instead say those functions are intentionally absent because the truthful public seam is transaction-driven in Paddle Billing.
  </action>
  <verify>
    <automated>mix docs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'idempotency_key:' guides/getting-started.md` returns a hit in the recurring transaction example.
    - `grep -F 'subscription.created' guides/getting-started.md` returns a hit.
    - `grep -F 'Paddle.Transactions.get' guides/getting-started.md` returns a hit in the reconciliation narrative.
    - `grep -F 'Paddle.Subscriptions.get' guides/getting-started.md` returns a hit in the canonical hydration step.
    - `rg -n 'Subscriptions\\.create|start_checkout' guides/getting-started.md` returns no matches.
    - `mix docs --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>
    The getting-started guide teaches the corrected SUB-04 job end-to-end and no longer leaves room for a reader to infer a direct subscription-create API.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Guide reader -> public API expectation | Documentation can accidentally create a false contract if it implies a direct subscription-create API. |
| Test suite -> future contributors | The seam and transactions tests are the executable guardrail that prevents a future direct-create API from slipping back in. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Spoofing | Public recurring-start surface | mitigate | Refute `function_exported?(Paddle.Subscriptions, :create, 2)` in `test/paddle/seam_test.exs` and keep docs rooted in `Transactions.create/3` per D-01 and D-04. |
| T-10-02 | Tampering | Idempotency boundary for recurring start | mitigate | Adapter-backed tests assert `Idempotency-Key` only on `Transactions.create/3` per D-19; later pause/resume plans reject it explicitly. |
| T-10-03 | Repudiation | Recurring-start reconciliation story | mitigate | Guide and seam tests require webhook/canonical fetch reconciliation instead of trusting the first local create response alone per D-03 and D-18. |
| T-10-SC | Tampering | Dependency/install surface | accept | No new packages or installs in this plan. |
</threat_model>

<verification>
- `mix test test/paddle/transactions_test.exs test/paddle/seam_test.exs --color`
- `mix docs --warnings-as-errors`
- `mix compile --warnings-as-errors`
</verification>

<success_criteria>
- SUB-04 is represented truthfully in executable tests and public onboarding docs.
- The seam test fails if `Paddle.Subscriptions.create/2` appears.
- `Transactions.create/3` remains the only idempotent recurring-start entrypoint documented for Phase 10.
</success_criteria>

<output>
Create `.planning/phases/10-subscriptions-surface-completion/10-01-SUMMARY.md` when done.
</output>
