---
phase: "26"
plan: "02"
type: execute
wave: 2
depends_on:
  - 26-01
files_modified:
  - test/paddle/subscription_flows_test.exs
autonomous: true
requirements:
  - ADV-02
must_haves:
  truths:
    - E2E tests exercise update/3 for upgrade and downgrade flows, defaulting to Paddle.MockServer (D-01).
    - Tests are structured to run against real sandbox on demand (D-03).
    - Tests verify both immediate and scheduled changes (D-06).
    - Scheduled downgrade test explicitly asserts scheduled_change struct is populated (D-07).
  artifacts:
    - path: test/paddle/subscription_flows_test.exs
      provides: integration test cases
  key_links:
    - from: test/paddle/subscription_flows_test.exs
      to: lib/paddle/subscriptions.ex
      via: test asserts
      pattern: Paddle.Subscriptions.update
---

<objective>
Verify complex subscription upgrade and downgrade scenarios via comprehensive E2E tests.

Purpose: Guarantee the SDK accurately deserializes `scheduled_change` and behaves deterministically during subscription mutations.
Output: Complete E2E flow tests for ADV-02.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-advanced-subscription-flows-e2e/26-CONTEXT.md
@.planning/phases/26-advanced-subscription-flows-e2e/26-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add E2E tests for subscription flows</name>
  <files>test/paddle/subscription_flows_test.exs</files>
  <read_first>
    - test/paddle/subscriptions_test.exs
    - test/paddle/seam_test.exs
    - .planning/phases/26-advanced-subscription-flows-e2e/26-RESEARCH.md
    - .planning/phases/26-advanced-subscription-flows-e2e/26-PATTERNS.md
  </read_first>
  <behavior>
    - `setup_all` toggles cleanly between `Paddle.MockServer` (default for E2E tests, D-01) and sandbox on demand based on `PADDLE_API_KEY` and `INTEGRATION_TESTS` (D-03).
    - Immediate upgrade via `proration_billing_mode: "prorated_immediately"` succeeds (D-06).
    - Scheduled downgrade via `proration_billing_mode: "next_billing_period"` returns a Subscription with a populated `scheduled_change` struct (D-06, D-07).
  </behavior>
  <action>
    Review `test/paddle/seam_test.exs` analog from PATTERNS.md to observe the Client Test Helper Pattern and End-to-End Execution Flow Pattern for chaining operations with the MockServer/Req overrides.
    
    Create `test/paddle/subscription_flows_test.exs`.
    Implement `setup_all` using `Paddle.MockServer.start_link(port: 4448)` as the default test target (D-01). Ensure tests are structured to run against real sandbox on demand if configured (D-03). Use the analog patterns from seam tests.
    
    Add test "immediate upgrade" to verify immediate changes (D-06): calls `Paddle.Subscriptions.update(client, id, items: [...], proration_billing_mode: "prorated_immediately")` and asserts `{:ok, %Paddle.Subscription{}}` returned.
    Add test "scheduled downgrade" to verify scheduled changes (D-06): calls `Paddle.Subscriptions.update(client, id, items: [...], proration_billing_mode: "next_billing_period")` and explicitly assert the returned `subscription.scheduled_change` is parsed as a `%Paddle.Subscription.ScheduledChange{}` struct (D-07).
  </action>
  <acceptance_criteria>
    - `test/paddle/subscription_flows_test.exs` exists.
    - E2E tests pass reliably using MockServer and follow `seam_test.exs` analog patterns.
    - Test asserts correct parsing of `scheduled_change` struct for downgrade.
    - `mix test test/paddle/subscription_flows_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/paddle/subscription_flows_test.exs</automated>
  </verify>
  <done>E2E tests for advanced flows successfully completed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SDK Client -> API | Client sends authenticated request to Paddle API (or Mock Server). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-03 | Information Disclosure | Tests | mitigate | Ensure test setup correctly defaults to MockServer to avoid sandbox API key leakage or unexpected calls in CI. |
| T-26-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies installed in this phase. |
</threat_model>

<verification>
mix test test/paddle/subscription_flows_test.exs
</verification>

<success_criteria>
The complete E2E test suite asserts complex subscription upgrade/downgrade logic properly against Paddle mock state.
</success_criteria>

<output>
Create `.planning/phases/26-advanced-subscription-flows-e2e/26-02-SUMMARY.md` when done
</output>