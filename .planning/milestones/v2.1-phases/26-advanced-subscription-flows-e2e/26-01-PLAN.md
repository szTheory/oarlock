---
phase: "26"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/paddle/subscriptions.ex
  - test/paddle/subscriptions_test.exs
  - lib/paddle/mock_server/fixtures.ex
  - lib/paddle/mock_server.ex
  - test/paddle/mock_server_test.exs
autonomous: true
requirements:
  - ADV-02
must_haves:
  truths:
    - Developers can update subscriptions using pure CRUD mapping (D-04, D-05)
    - Offline mode supports subscription updates via minimal happy path stubs (D-02)
  artifacts:
    - path: lib/paddle/subscriptions.ex
      provides: update/3
    - path: lib/paddle/mock_server.ex
      provides: patch "/subscriptions/:id" route
  key_links:
    - from: lib/paddle/mock_server.ex
      to: lib/paddle/mock_server/fixtures.ex
      via: send_json
      pattern: Fixtures.subscription_updated
---

<objective>
Implement core `update/3` SDK capability and required MockServer test routes.

Purpose: Provide foundational SDK methods and simulation endpoints to allow advanced subscription flow tests.
Output: `update/3` implementation and simulated MockServer behavior.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-advanced-subscription-flows-e2e/26-CONTEXT.md
@.planning/phases/26-advanced-subscription-flows-e2e/26-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement update/3 with unit tests</name>
  <files>lib/paddle/subscriptions.ex, test/paddle/subscriptions_test.exs</files>
  <read_first>
    - lib/paddle/subscriptions.ex
    - test/paddle/subscriptions_test.exs
    - lib/paddle/customers.ex
    - test/paddle/customers_test.exs
    - .planning/phases/26-advanced-subscription-flows-e2e/26-RESEARCH.md
    - .planning/phases/26-advanced-subscription-flows-e2e/26-PATTERNS.md
  </read_first>
  <behavior>
    - Valid id and params successfully updates subscription and returns `{:ok, %Paddle.Subscription{}}`
    - Invalid id returns `{:error, :invalid_subscription_id}`
    - Invalid params returns `{:error, :invalid_params}`
  </behavior>
  <action>
    Review `lib/paddle/customers.ex` and `test/paddle/customers_test.exs` analogs from PATTERNS.md to ensure pattern compliance. Specifically, follow the Core CRUD pattern for `update/3` handling `Attrs.normalize`, `Attrs.allowlist`, and `Http.build_struct(Subscription, data)`.
    
    Add `update(%Client{} = client, subscription_id, params)` to `Paddle.Subscriptions` module to implement pure CRUD (D-04).
    Use pure REST mapping wrapping `Http.request(client, :patch, subscription_path(subscription_id), json: body)` with no framework coupling (D-05).
    Ensure `normalize_params` and `validate_subscription_id` are used properly as per analog.
    
    Update `test/paddle/subscriptions_test.exs` with unit tests for `update/3` mimicking the mock-based unit tests from `test/paddle/customers_test.exs` for HTTP method matching and JSON asserting.
  </action>
  <acceptance_criteria>
    - `lib/paddle/subscriptions.ex` exports `update/3`.
    - `test/paddle/subscriptions_test.exs` contains tests asserting `update` behavior following analog patterns.
    - `mix test test/paddle/subscriptions_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/paddle/subscriptions_test.exs</automated>
  </verify>
  <done>update/3 is implemented and unit tested</done>
</task>

<task type="auto">
  <name>Task 2: Add MockServer fixtures and endpoints</name>
  <files>lib/paddle/mock_server/fixtures.ex, lib/paddle/mock_server.ex, test/paddle/mock_server_test.exs</files>
  <read_first>
    - lib/paddle/mock_server/fixtures.ex
    - lib/paddle/mock_server.ex
    - test/paddle/mock_server_test.exs
    - .planning/phases/26-advanced-subscription-flows-e2e/26-PATTERNS.md
  </read_first>
  <action>
    Review `lib/paddle/mock_server.ex` from PATTERNS.md for the Core Endpoint Pattern as an analog for the new route (`patch "/customers/:id" do send_json(...) end`).
    
    Build minimal happy path stubs for specific upgrade/downgrade payloads (D-02): add `subscription_updated(id \\ "sub_mock123")` and `subscription_scheduled_change(id \\ "sub_mock123")` to `Paddle.MockServer.Fixtures`.
    Both must return valid Paddle Subscription JSON maps. The `subscription_scheduled_change` must include `"scheduled_change" => %{"action" => "pause", "effective_at" => "2026-12-01T12:00:00Z"}`.
    
    Add `patch "/subscriptions/:id"` block to `Paddle.MockServer` following the analog.
    Inspect `conn.body_params["proration_billing_mode"]`. If mode is `"next_billing_period"`, return `Fixtures.subscription_scheduled_change(id)`. Else, return `Fixtures.subscription_updated(id)`.
    
    Add basic route coverage in `test/paddle/mock_server_test.exs`.
  </action>
  <acceptance_criteria>
    - `lib/paddle/mock_server/fixtures.ex` exports `subscription_updated/1` and `subscription_scheduled_change/1`.
    - `lib/paddle/mock_server.ex` resolves `PATCH /subscriptions/:id`.
    - `mix test test/paddle/mock_server_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/paddle/mock_server_test.exs</automated>
  </verify>
  <done>MockServer supports /subscriptions/:id patching</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SDK Client -> API | Client sends authenticated request to Paddle API (or Mock Server). |
| API -> SDK Client | Responses from Paddle API are parsed as JSON and structs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Tampering | `Paddle.Subscriptions.update/3` | accept | Payload validation happens server-side. Local param validation is structural. |
| T-26-02 | Denial of Service | `Http.build_struct` | accept | Trusting Paddle API JSON payloads. Nested structs parsed deterministically. |
| T-26-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies installed in this phase. |
</threat_model>

<verification>
mix compile
</verification>

<success_criteria>
All tests pass locally and the MockServer supports patching subscriptions.
</success_criteria>

<output>
Create `.planning/phases/26-advanced-subscription-flows-e2e/26-01-SUMMARY.md` when done
</output>