---
phase: 11-type-safety-pass
plan: 03
type: execute
wave: 3
depends_on: ["11-01", "11-02"]
files_modified:
  - lib/mix/tasks/typecheck.specs.ex
autonomous: true
requirements: [TYPES-01]
must_haves:
  truths:
    - "`mix typecheck.specs` mechanically fails when a public seam function lands without a directly preceding `@spec`, per D-18 and D-19."
    - "Sealed `@moduledoc false` internals are excluded from the public-spec gate unless explicitly part of the public seam, per resolved D-20 guidance."
    - "The gate reports actionable file/function failures instead of vague grep output, making future seam drift reviewable."
  artifacts:
    - path: "lib/mix/tasks/typecheck.specs.ex"
      provides: "Project-local public-spec coverage gate."
      contains: "defmodule Mix.Tasks.Typecheck.Specs"
  key_links:
    - from: "lib/mix/tasks/typecheck.specs.ex"
      to: "lib/paddle/**/*.ex"
      via: "AST scan over the enforced public seam"
      pattern: "@spec"
---

<objective>
Create the mechanical public-spec coverage gate required for Phase 11.

Purpose: Turn TYPES-01 into an enforceable project rule by implementing `mix typecheck.specs` per D-18, D-19, and the resolved D-20 exclusion policy for sealed internals.

Output: a deterministic Mix task that scans the intended public seam and fails on missing public specs.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-type-safety-pass/11-CONTEXT.md
@.planning/phases/11-type-safety-pass/11-RESEARCH.md
@.planning/phases/11-type-safety-pass/11-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement `mix typecheck.specs` as the public-seam gate</name>
  <files>lib/mix/tasks/typecheck.specs.ex</files>
  <read_first>
    - lib/mix/tasks/typecheck.specs.ex
    - .planning/phases/11-type-safety-pass/11-CONTEXT.md
    - .planning/phases/11-type-safety-pass/11-RESEARCH.md
    - .planning/phases/11-type-safety-pass/11-PATTERNS.md
  </read_first>
  <action>
    Create `Mix.Tasks.Typecheck.Specs` as a formatter-friendly, dependency-free AST scan over the intended public seam. Enforce that every public `def` in non-sealed public modules under `lib/paddle/**` has a directly preceding `@spec`. Implement the resolved D-20 rule explicitly: exclude sealed `@moduledoc false` internals such as root/internal/http/application modules unless they are intentionally brought into the public seam, which this phase must not do.

    Handle default-argument heads correctly so wrapper signatures like `opts \\\\ []` do not produce false positives or false negatives. Emit actionable failures with file path and function name/arity, exit non-zero on failure, and print a concise success message on a clean sweep.
  </action>
  <verify>
    <automated>mix typecheck.specs</automated>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `test -f lib/mix/tasks/typecheck.specs.ex` succeeds.
    - `grep -F 'defmodule Mix.Tasks.Typecheck.Specs' lib/mix/tasks/typecheck.specs.ex` returns a hit.
    - `grep -F '@shortdoc' lib/mix/tasks/typecheck.specs.ex` returns a hit.
    - `mix typecheck.specs` exits 0 on the completed Phase 11 seam.
  </acceptance_criteria>
  <done>
    The repo has a repeatable project gate for missing public specs across the intended `lib/paddle/` seam.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Future contributor changes -> CI/local tooling | The Mix task decides whether an unspecced public function is allowed to merge. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06 | Spoofing | Public-spec coverage gate | mitigate | Scan explicit seam files via AST and emit file/function/arity failures so skipped defs cannot masquerade as covered. |
| T-11-07 | Tampering | Sealed internal exclusions | mitigate | Encode the resolved D-20 allowlist/denylist policy directly in the task instead of inferring exclusions from grep heuristics. |
| T-11-SC | Tampering | Dependency/install surface | accept | No third-party dependency changes occur in this plan. |
</threat_model>

<verification>
- `mix typecheck.specs`
- `mix compile --warnings-as-errors`
</verification>

<success_criteria>
- `mix typecheck.specs` exists and exits non-zero on missing public specs.
- The gate excludes sealed internals unless they are intentionally part of the public seam.
- Public-spec coverage is enforced without shell-only grep logic.
</success_criteria>

<output>
Create `.planning/phases/11-type-safety-pass/11-03-SUMMARY.md` when done
</output>
