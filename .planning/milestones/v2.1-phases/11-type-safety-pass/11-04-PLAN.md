---
phase: 11-type-safety-pass
plan: 04
type: execute
wave: 4
depends_on: ["11-01", "11-02", "11-03"]
files_modified:
  - mix.exs
  - .dialyzer_ignore.exs
  - lib/paddle/client.ex
  - lib/paddle/error.ex
  - lib/paddle/page.ex
  - lib/paddle/http.ex
  - lib/paddle/http/telemetry.ex
  - lib/paddle/internal/attrs.ex
  - lib/paddle/internal/pagination.ex
  - lib/paddle/application.ex
autonomous: false
requirements: [TYPES-02]
must_haves:
  truths:
    - "`mix dialyzer` exits 0 against an empty `.dialyzer_ignore.exs`, per TYPES-02, D-11, D-12, and D-17."
    - "Dialyxir uses stable PLT paths under `priv/plts` keyed to the current project runtime baseline, per D-13 and D-16."
    - "Any refinements added to reach a clean baseline stay additive and prefer sealed/internal modules where Dialyzer reports them, per D-09 and D-20."
  artifacts:
    - path: "mix.exs"
      provides: "Dialyxir dependency and dialyzer project configuration."
      contains: "dialyzer:"
    - path: ".dialyzer_ignore.exs"
      provides: "Empty suppression baseline."
      contains: "[]"
  key_links:
    - from: "mix.exs"
      to: ".dialyzer_ignore.exs"
      via: "Dialyxir reads the empty ignore baseline"
      pattern: "ignore_warnings"
    - from: "mix.exs"
      to: "priv/plts"
      via: "project/core PLT configuration"
      pattern: "plt"
---

<objective>
Wire the local Dialyxir baseline and make the first clean unsuppressed dialyzer run pass.

Purpose: Deliver the local half of TYPES-02 by adding Dialyxir, the empty ignore file, stable PLT configuration, and any additive type refinements needed in shared/sealed modules to satisfy Dialyzer without suppressions.

Output: Dialyxir configured in `mix.exs`, `.dialyzer_ignore.exs` containing `[]`, and a clean local `mix dialyzer` run.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-type-safety-pass/11-CONTEXT.md
@.planning/phases/11-type-safety-pass/11-RESEARCH.md
@.planning/phases/11-type-safety-pass/11-PATTERNS.md
@.tool-versions
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify the approved Dialyxir package identity before installation</name>
  <files>mix.exs</files>
  <read_first>
    - .planning/phases/11-type-safety-pass/11-RESEARCH.md
    - mix.exs
  </read_first>
  <action>
    Perform the required package-legitimacy checkpoint before adding `dialyxir`, because the research audit marks it `[ASSUMED]` for Hex-only verification. Confirm the package identity against the cited Hex metadata and official docs/source lineage in `11-RESEARCH.md`, then proceed only after approval. This task implements D-11 while satisfying the package-legitimacy gate.
  </action>
  <how-to-verify>
    1. Review the package metadata and official docs already cited in `11-RESEARCH.md`.
    2. Confirm the dependency being added is exactly `dialyxir`.
    3. Resume only after explicitly approving the package identity.
  </how-to-verify>
  <resume-signal>Type "approved" after confirming the package identity, or describe the concern.</resume-signal>
  <verify>
    <automated>MISSING — blocking human legitimacy checkpoint before package installation</automated>
  </verify>
  <acceptance_criteria>
    - The user explicitly approves installing `dialyxir`.
    - No dependency is added before this checkpoint passes.
  </acceptance_criteria>
  <done>
    The required package-legitimacy gate is satisfied.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Dialyxir config and clear the local baseline without suppressions</name>
  <files>mix.exs, .dialyzer_ignore.exs, lib/paddle/client.ex, lib/paddle/error.ex, lib/paddle/page.ex, lib/paddle/http.ex, lib/paddle/http/telemetry.ex, lib/paddle/internal/attrs.ex, lib/paddle/internal/pagination.ex, lib/paddle/application.ex</files>
  <read_first>
    - mix.exs
    - lib/paddle/client.ex
    - lib/paddle/error.ex
    - lib/paddle/page.ex
    - lib/paddle/http.ex
    - lib/paddle/http/telemetry.ex
    - lib/paddle/internal/attrs.ex
    - lib/paddle/internal/pagination.ex
    - lib/paddle/application.ex
    - .planning/phases/11-type-safety-pass/11-CONTEXT.md
    - .planning/phases/11-type-safety-pass/11-RESEARCH.md
    - .tool-versions
  </read_first>
  <action>
    After the checkpoint passes, add `{:dialyxir, "~> 1.4.7", only: [:dev, :test], runtime: false}` to `mix.exs` per D-11. Add `dialyzer:` project config that stores PLTs under `priv/plts`, points `ignore_warnings` at `.dialyzer_ignore.exs`, and targets the current `.tool-versions` baseline per D-13 and D-16. Create `.dialyzer_ignore.exs` with exactly `[]`.

    Run the first real Dialyzer cycle and clear the reported warnings with the smallest additive typing changes possible. Prefer shared and sealed/internal modules listed in this plan when Dialyzer points there, per D-09 and D-20. Do not add non-empty ignore entries, `@dialyzer` escapes, or advisory flags.
  </action>
  <verify>
    <automated>mix deps.get</automated>
    <automated>mix typecheck.specs</automated>
    <automated>mix dialyzer</automated>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F '{:dialyxir, "~> 1.4.7", only: [:dev, :test], runtime: false}' mix.exs` returns a hit.
    - `grep -F 'ignore_warnings: ".dialyzer_ignore.exs"' mix.exs` returns a hit.
    - `test -f .dialyzer_ignore.exs` succeeds and `tr -d '[:space:]' < .dialyzer_ignore.exs` outputs `[]`.
    - `mix dialyzer` exits 0.
  </acceptance_criteria>
  <done>
    The repo has a clean local Dialyxir baseline with no suppressions.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Third-party package metadata -> project dependency graph | Installing Dialyxir changes the dev/test toolchain. |
| Local type/config changes -> dialyzer result | Baseline cleanliness depends on exact spec/config choices. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-08 | Tampering | Package install (`dialyxir`) | mitigate | Enforce a blocking human legitimacy checkpoint before dependency installation. |
| T-11-09 | Tampering | Warning suppression baseline | mitigate | Commit `.dialyzer_ignore.exs` as exactly `[]` and forbid non-empty suppressions or advisory flags per D-12 and D-17. |
| T-11-10 | Repudiation | Dialyzer baseline quality | mitigate | Require `mix dialyzer` to exit 0 locally and fix warnings at the source instead of ignoring them. |
| T-11-SC | Tampering | npm/pip/cargo installs | mitigate | No npm, pip, or cargo installs occur; only the human-verified Hex dependency is added. |
</threat_model>

<verification>
- `mix deps.get`
- `mix typecheck.specs`
- `mix dialyzer`
- `mix compile --warnings-as-errors`
</verification>

<success_criteria>
- Dialyxir is installed and configured in `mix.exs`.
- `.dialyzer_ignore.exs` contains exactly `[]`.
- `mix dialyzer` exits 0 locally on the current `.tool-versions` baseline.
</success_criteria>

<output>
Create `.planning/phases/11-type-safety-pass/11-04-SUMMARY.md` when done
</output>
