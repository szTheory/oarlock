---
phase: 11-type-safety-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/paddle/client.ex
  - lib/paddle/error.ex
  - lib/paddle/page.ex
  - lib/paddle/address.ex
  - lib/paddle/customer.ex
  - lib/paddle/event.ex
  - lib/paddle/subscription.ex
  - lib/paddle/subscription/management_urls.ex
  - lib/paddle/subscription/scheduled_change.ex
  - lib/paddle/transaction.ex
  - lib/paddle/transaction/checkout.ex
  - test/paddle/client_test.exs
  - test/paddle/error_test.exs
  - test/paddle/page_test.exs
autonomous: true
requirements: [TYPES-01]
must_haves:
  truths:
    - "Core public struct modules expose explicit `@type t :: %__MODULE__{...}` contracts without becoming opaque, per D-02 and D-23."
    - "Shared seam carriers such as `Paddle.Client`, `Paddle.Error`, and `Paddle.Page` publish honest helper specs that match current runtime behavior, per D-06 and D-21."
    - "Provider-owned payload fields stay broad enough for forward compatibility instead of overfitting Paddle schemas, per D-01 and D-07."
  artifacts:
    - path: "lib/paddle/client.ex"
      provides: "Public `Paddle.Client.t()` contract for explicit client passing."
      contains: "@type t"
    - path: "lib/paddle/error.ex"
      provides: "Public typed error shape and helper specs used across the SDK seam."
      contains: "@spec from_transport"
    - path: "lib/paddle/page.ex"
      provides: "Typed page struct plus cursor accessor contract."
      contains: "@spec next_cursor"
  key_links:
    - from: "lib/paddle/client.ex"
      to: "lib/paddle/customers.ex"
      via: "resource-module specs consume `Paddle.Client.t()` in later plans"
      pattern: "Paddle.Client.t"
    - from: "lib/paddle/error.ex"
      to: "lib/paddle/webhooks.ex"
      via: "public error/result specs reuse the normalized error type"
      pattern: "Paddle.Error.t"
---

<objective>
Create the typed core seam for Phase 11 by adding public struct types and shared helper specs to the non-sealed value modules under `lib/paddle/`.

Purpose: Establish the reusable contracts that later resource-module specs and Dialyzer enforcement will depend on, implementing D-01, D-02, D-06, D-07, D-21, and D-23 for TYPES-01.

Output: typed core structs and helper specs in the public seam carrier modules.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-type-safety-pass/11-CONTEXT.md
@.planning/phases/11-type-safety-pass/11-RESEARCH.md
@.planning/phases/11-type-safety-pass/11-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add public `@type t` contracts to the core seam carriers</name>
  <files>lib/paddle/client.ex, lib/paddle/error.ex, lib/paddle/page.ex, lib/paddle/address.ex, lib/paddle/customer.ex, lib/paddle/event.ex, lib/paddle/subscription.ex, lib/paddle/subscription/management_urls.ex, lib/paddle/subscription/scheduled_change.ex, lib/paddle/transaction.ex, lib/paddle/transaction/checkout.ex</files>
  <read_first>
    - lib/paddle/client.ex
    - lib/paddle/error.ex
    - lib/paddle/page.ex
    - lib/paddle/address.ex
    - lib/paddle/customer.ex
    - lib/paddle/event.ex
    - lib/paddle/subscription.ex
    - lib/paddle/subscription/management_urls.ex
    - lib/paddle/subscription/scheduled_change.ex
    - lib/paddle/transaction.ex
    - lib/paddle/transaction/checkout.ex
    - .planning/phases/11-type-safety-pass/11-CONTEXT.md
    - .planning/phases/11-type-safety-pass/11-RESEARCH.md
  </read_first>
  <action>
    Add explicit public `@type t :: %__MODULE__{...}` declarations to each non-sealed public struct module per D-02. Keep field types useful but pragmatic per D-01 and D-07: type stable local fields precisely, keep provider-owned maps/lists/raw payloads broad, and do not introduce `@opaque`.

    Where a module benefits from local aliases per D-03 and D-04, define them in place with `String.t()`-based IDs rather than branded opaque strings. Do not introduce new public modules, new runtime fields, or new request vocabulary while doing this pass.
  </action>
  <verify>
    <automated>mix test test/paddle/client_test.exs test/paddle/error_test.exs test/paddle/page_test.exs --color</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F '@type t :: %__MODULE__{' lib/paddle/client.ex lib/paddle/error.ex lib/paddle/page.ex lib/paddle/address.ex lib/paddle/customer.ex lib/paddle/event.ex lib/paddle/subscription.ex lib/paddle/subscription/management_urls.ex lib/paddle/subscription/scheduled_change.ex lib/paddle/transaction.ex lib/paddle/transaction/checkout.ex | wc -l` reports `11`.
    - No added type marks any of these public structs `@opaque`.
    - The modules still compile and the targeted tests stay green.
  </acceptance_criteria>
  <done>
    All non-sealed core value modules publish stable public struct contracts without changing runtime behavior.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add shared helper specs and keep the seam behavior unchanged</name>
  <files>lib/paddle/client.ex, lib/paddle/error.ex, lib/paddle/page.ex, test/paddle/client_test.exs, test/paddle/error_test.exs, test/paddle/page_test.exs</files>
  <read_first>
    - lib/paddle/client.ex
    - lib/paddle/error.ex
    - lib/paddle/page.ex
    - test/paddle/client_test.exs
    - test/paddle/error_test.exs
    - test/paddle/page_test.exs
    - .planning/phases/11-type-safety-pass/11-CONTEXT.md
  </read_first>
  <action>
    Add public helper specs for `Paddle.Client.new!/1`, `Paddle.Error.message/1`, `Paddle.Error.from_response/1`, `Paddle.Error.from_transport/1`, and `Paddle.Page.next_cursor/1`, making the tagged contracts explicit per D-06 and D-21. If the existing tests need small assertion updates to reflect the typed seam, keep them narrowly focused on existing behavior only.
  </action>
  <verify>
    <automated>mix test test/paddle/client_test.exs test/paddle/error_test.exs test/paddle/page_test.exs --color</automated>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F '@spec new!(opts \\\\ [])' lib/paddle/client.ex` returns a hit.
    - `grep -F '@spec from_response(' lib/paddle/error.ex` returns a hit.
    - `grep -F '@spec from_transport(' lib/paddle/error.ex` returns a hit.
    - `grep -F '@spec next_cursor(' lib/paddle/page.ex` returns a hit.
    - `mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>
    Shared helper contracts are explicit and still match the current public behavior.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public struct source -> SDK consumers | Types here become callable contracts for Accrue and other downstream users. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | Public struct contracts | mitigate | Keep non-opaque `@type t` definitions aligned with the existing runtime field set per D-02 and D-23. |
| T-11-02 | Repudiation | Shared helper result contracts | mitigate | Add explicit helper specs for the existing public functions so result/error shapes are review-visible per D-06. |
| T-11-SC | Tampering | Dependency/install surface | accept | No third-party dependency changes occur in this plan. |
</threat_model>

<verification>
- `mix test test/paddle/client_test.exs test/paddle/error_test.exs test/paddle/page_test.exs --color`
- `mix compile --warnings-as-errors`
</verification>

<success_criteria>
- The core seam carriers under `lib/paddle/` expose public `@type t` contracts.
- Shared helper functions on `Paddle.Client`, `Paddle.Error`, and `Paddle.Page` have explicit specs.
- No public behavior or field set changes are introduced.
</success_criteria>

<output>
Create `.planning/phases/11-type-safety-pass/11-01-SUMMARY.md` when done
</output>
