---
phase: 11-type-safety-pass
plan: 05
type: execute
wave: 5
depends_on: ["11-04"]
files_modified:
  - .github/workflows/ci.yml
  - lib/paddle/customers.ex
autonomous: true
requirements: [TYPES-02]
must_haves:
  truths:
    - "CI has a dedicated blocking job that runs `mix typecheck.specs` and `mix dialyzer` separately from `mix test`, per D-14 and D-17."
    - "PLTs are cached under `priv/plts` with a key that includes OS, Elixir, OTP, and `mix.lock`, per D-13 and D-15."
    - "There is an explicit negative-path validation proving a broken public spec would fail the same gate wired into CI, satisfying the roadmap broken-spec criterion."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Dedicated Dialyzer/spec gate job with PLT restore/build/save."
      contains: "mix dialyzer"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "mix typecheck.specs"
      via: "required CI job runs the same local spec gate"
      pattern: "typecheck.specs"
    - from: ".github/workflows/ci.yml"
      to: "priv/plts"
      via: "restore/build/save cache steps"
      pattern: "priv/plts"
---

<objective>
Add the required CI static-analysis lane and make the broken-spec failure path explicit.

Purpose: Finish TYPES-02 by wiring a dedicated `dialyzer` job in CI, caching PLTs correctly, and defining a concrete negative-path validation that proves the broken-spec gate fails before merge.

Output: updated `.github/workflows/ci.yml` plus a repeatable negative-path verification procedure for the CI gate.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-type-safety-pass/11-CONTEXT.md
@.planning/phases/11-type-safety-pass/11-RESEARCH.md
@.planning/phases/11-type-safety-pass/11-PATTERNS.md
@.tool-versions
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the dedicated CI Dialyzer/spec gate with stable PLT caching</name>
  <files>.github/workflows/ci.yml, lib/paddle/customers.ex</files>
  <read_first>
    - .github/workflows/ci.yml
    - mix.exs
    - .tool-versions
    - .planning/phases/11-type-safety-pass/11-CONTEXT.md
    - .planning/phases/11-type-safety-pass/11-RESEARCH.md
  </read_first>
  <action>
    Extend `.github/workflows/ci.yml` with a dedicated blocking `dialyzer` job per D-14 through D-17. Keep the existing pinned-action style and `erlef/setup-beam` usage. Add restore/build/save steps for the `priv/plts` cache using a key that includes `runner.os`, the resolved OTP version, the resolved Elixir version, and `hashFiles('**/mix.lock')` per D-15. Run `mix deps.get`, `mix typecheck.specs`, and `mix dialyzer` in this job, and keep it separate from the normal `mix test` lane.
  </action>
  <verify>
    <automated>rg -n 'dialyzer|typecheck\\.specs|priv/plts|actions/cache/(restore|save)|setup-beam' .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'dialyzer:' .github/workflows/ci.yml` returns a hit for a dedicated job identifier.
    - `grep -F 'mix typecheck.specs' .github/workflows/ci.yml` returns a hit.
    - `grep -F 'mix dialyzer' .github/workflows/ci.yml` returns a hit.
    - `grep -F 'priv/plts' .github/workflows/ci.yml` returns a hit.
    - `grep -F "hashFiles('**/mix.lock')" .github/workflows/ci.yml` returns a hit in the PLT cache key.
    - `rg -n -- '--ignore-exit-status' .github/workflows/ci.yml` returns no matches.
  </acceptance_criteria>
  <done>
    CI has a dedicated required static-analysis lane with correct PLT caching.
  </done>
</task>

<task type="auto">
  <name>Task 2: Validate the broken-spec failure path with a controlled temporary spec break</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml
    - lib/mix/tasks/typecheck.specs.ex
    - lib/paddle/customers.ex
  </read_first>
  <action>
    Add an explicit negative-path verification procedure to the implementation work for roadmap success criterion 3. Use a controlled temporary local edit that removes or comments one known public `@spec` in a seam file such as `lib/paddle/customers.ex`, run `mix typecheck.specs` to confirm non-zero failure with actionable output, then restore the spec before final verification. Because the CI job runs the same `mix typecheck.specs` command, this local failure is the equivalent proof that a deliberately broken-spec branch would fail the required CI gate. Document that this temporary break must not be committed.
  </action>
  <verify>
    <automated>mix typecheck.specs</automated>
  </verify>
  <acceptance_criteria>
    - The implementation includes a documented temporary-break procedure naming a concrete public spec to remove and restore.
    - The procedure proves `mix typecheck.specs` exits non-zero before restoration and exits 0 after restoration.
    - The final committed state restores the spec and leaves the workflow job intact.
  </acceptance_criteria>
  <done>
    The roadmap's broken-spec failure criterion is backed by an explicit repeatable negative-path check instead of implication.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI workflow config -> merge gate | The workflow decides whether public-spec drift or dialyzer drift can be merged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-11 | Denial of Service | PLT cache/runtime cost | mitigate | Cache `priv/plts` with OS + OTP + Elixir + `mix.lock` per D-15 so the dedicated job stays predictable. |
| T-11-12 | Repudiation | Broken-spec gate confidence | mitigate | Add a concrete temporary-break verification procedure proving the exact CI command fails when a public spec is removed. |
| T-11-13 | Tampering | CI attribution of static-analysis failures | mitigate | Keep `mix typecheck.specs` and `mix dialyzer` in a dedicated required job rather than folding them into tests. |
| T-11-SC | Tampering | Dependency/install surface | accept | No new package installs occur in this plan. |
</threat_model>

<verification>
- `rg -n 'dialyzer|typecheck\\.specs|priv/plts' .github/workflows/ci.yml`
- `mix typecheck.specs`
</verification>

<success_criteria>
- `.github/workflows/ci.yml` contains a dedicated required Dialyzer/spec gate.
- PLT caching is keyed by OS, OTP, Elixir, and `mix.lock`.
- The phase includes an explicit negative-path validation for broken public specs.
</success_criteria>

<output>
Create `.planning/phases/11-type-safety-pass/11-05-SUMMARY.md` when done
</output>
