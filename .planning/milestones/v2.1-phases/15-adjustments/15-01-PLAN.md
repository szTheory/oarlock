---
phase: 15-adjustments
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/paddle/adjustment.ex
  - lib/paddle/adjustments.ex
  - test/paddle/adjustments_test.exs
autonomous: true
requirements:
  - ADJ-01
  - ADJ-02
  - ADJ-03
  - ADJ-04
must_haves:
  truths:
    - User can create a full adjustment for a transaction
    - User can create a partial adjustment targeting specific transaction line items
    - User can retrieve an adjustment by its ID
    - User can list, stream, and get all adjustments
    - D-01: create/2 uses signature `(client, attrs)` with strictly attrs for action/reason
    - D-02: create partial adjustment uses raw `attrs` with strict Typespecs, no builder helpers
  artifacts:
    - path: lib/paddle/adjustment.ex
      provides: Paddle.Adjustment struct
    - path: lib/paddle/adjustments.ex
      provides: Paddle.Adjustments operations
  key_links:
    - from: lib/paddle/adjustments.ex
      to: lib/paddle/http.ex
      via: Http.request/4
---

<objective>
Implement Adjustments for Paddle API v1.

Purpose: Allow users to issue and track full/partial refunds or credits.
Output: `Paddle.Adjustment` struct, `Paddle.Adjustments` operations, and corresponding tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-adjustments/15-CONTEXT.md
@.planning/phases/15-adjustments/15-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Paddle.Adjustment struct</name>
  <files>lib/paddle/adjustment.ex</files>
  <read_first>lib/paddle/adjustment.ex, lib/paddle/customer.ex, lib/paddle/transaction.ex</read_first>
  <behavior>
    - The struct is correctly defined with standard fields for Adjustment.
    - Fields match the Paddle API response format.
    - Contains `raw_data: map() | nil` field.
  </behavior>
  <action>
    Define the `Paddle.Adjustment` struct in `lib/paddle/adjustment.ex`. Include `@moduledoc` indicating it represents a Paddle Adjustment. Define `@type t` with fields mapping to the Paddle API object shape. Use standard Elixir types (e.g. `String.t()`, `map()`, `list(map())`). Ensure `raw_data` is present for forward compatibility. The keys to include are: id, action, transaction_id, subscription_id, customer_id, reason, credit_applied_to_balance, currency_code, status, items, totals, payouts, created_at, updated_at, raw_data.
  </action>
  <verify>
    <automated>mix compile</automated>
  </verify>
  <acceptance_criteria>
    - Code: `lib/paddle/adjustment.ex` contains `defstruct [:id, :action,`
    - Code: `@type t :: %__MODULE__{` is fully defined
  </acceptance_criteria>
  <done>Struct is defined and compiles</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Paddle.Adjustments API</name>
  <files>lib/paddle/adjustments.ex, test/paddle/adjustments_test.exs</files>
  <read_first>lib/paddle/adjustments.ex, test/paddle/adjustments_test.exs, lib/paddle/transactions.ex, lib/paddle/subscriptions.ex</read_first>
  <behavior>
    - `create/2` correctly posts to `/adjustments` with only allowlisted keys.
    - `get/2` correctly gets `/adjustments/{id}` and handles invalid empty string IDs.
    - `list/2`, `stream/2`, and `all/2` correctly fetch pages of adjustments.
  </behavior>
  <action>
    Implement `Paddle.Adjustments` module.

    1. Define `@type create_attrs` including `action`, `reason`, `transaction_id`, and `items` using a detailed Dialyzer typespec (`@type adjustment_item_attr :: %{required(:item_id) => String.t(), required(:type) => String.t(), optional(:amount) => String.t() | integer()}`).
    2. Implement `create(client, attrs, opts \\ [])`. Use `@create_allowlist ~w(action reason transaction_id items)`. Ensure `Attrs.normalize/1` and `Attrs.allowlist/2` are used before making `Http.request(client, :post, "/adjustments")`.
    3. Implement `get(client, id)`. Use a private `validate_adjustment_id(id)` that ensures the ID is a non-empty binary string. The endpoint is `/adjustments/#{encode_path_segment(id)}`.
    4. Implement `list(client, params \\ [])`, `stream(client, params \\ [])`, `all(client, params \\ [])`. Add `@list_allowlist ~w(after id action transaction_id subscription_id customer_id status order_by per_page)`. Refer to `Paddle.Subscriptions` for pagination patterns (`next_page/2`, `build_page/2`).
    5. Write tests in `test/paddle/adjustments_test.exs` covering valid and invalid creation, retrieval, and pagination using standard `Tesla` or mock test patterns found in the project.
  </action>
  <verify>
    <automated>mix test test/paddle/adjustments_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Code: `lib/paddle/adjustments.ex` contains `def create(%Client{} = client, attrs, opts \\ [])`
    - Code: `@create_allowlist ~w(action reason transaction_id items)` is defined
    - Code: `validate_adjustment_id/1` returns `{:error, :invalid_adjustment_id}` for empty strings
    - Test: `mix test test/paddle/adjustments_test.exs` passes cleanly
  </acceptance_criteria>
  <done>Adjustments module provides all core CRUD endpoints with Dialyzer types and passes tests</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | Inputs to creation endpoints pass across boundary and must be validated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | Paddle.Adjustments | mitigate | Strict local validation of adjustment ID to prevent URL injection/tampering |
| T-15-02 | Information Disclosure | Paddle.Adjustments | mitigate | Attrs.allowlist filters all payload requests, preventing leaking local app map data to Paddle API |
</threat_model>
