---
phase: "14"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/paddle/portal_session.ex
  - lib/paddle/customers/portal_sessions.ex
  - test/paddle/portal_session_test.exs
  - test/paddle/customers/portal_sessions_test.exs
autonomous: true
requirements: ["PORTAL-01", "PORTAL-02"]
objective: "Implement Customer Portal Sessions via `Paddle.Customers.PortalSessions.create/3`"
requirements_addressed: "PORTAL-01, PORTAL-02"
must_haves:
  truths:
    - "User can generate short-lived Customer Portal session URL (PORTAL-01)"
    - "User can optionally scope a Portal session to specific subscription IDs (PORTAL-02)"
    - "D-01 is respected: Module namespace is Paddle.Customers.PortalSessions"
    - "D-02 is respected: Function signature is create(client, customer_id, attrs)"
    - "D-03 is respected: Rely on API rejection and use Dialyzer typespecs + guard clauses"
    - "D-04 is respected: Keep the urls property flat"
    - "D-05 is respected: Strictly limit the module to create/3"
  artifacts:
    - path: "lib/paddle/portal_session.ex"
      provides: "Struct representation of a Customer Portal Session"
    - path: "lib/paddle/customers/portal_sessions.ex"
      provides: "Client module for generating portal sessions"
  key_links:
    - from: "lib/paddle/customers/portal_sessions.ex"
      to: "Paddle.Http.request/4"
      via: "POST /customers/:customer_id/portal-sessions"
---

<objective>
Implement Customer Portal Sessions generation by creating a `Paddle.PortalSession` struct and a `Paddle.Customers.PortalSessions` client module. This allows developers to generate temporary authenticated session URLs for customers.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-customer-portal-sessions/14-CONTEXT.md
@.planning/phases/14-customer-portal-sessions/14-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define `Paddle.PortalSession` struct</name>
  <files>lib/paddle/portal_session.ex, test/paddle/portal_session_test.exs</files>
  <behavior>
    - Defines a struct with `id`, `customer_id`, `urls`, `created_at`, `custom_data`
    - `urls` is kept as a flat map for forward compatibility (D-04)
    - Excludes `urls` from `Inspect` protocol
  </behavior>
  <action>
    Create `lib/paddle/portal_session.ex` per D-04. Do not create nested modules for the URLs (e.g., `Urls.General`). Create a test file verifying the struct shape.
  </action>
  <verify>
    <automated>mix test test/paddle/portal_session_test.exs</automated>
  </verify>
  <done>The struct module exists, compiles, and has basic test coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement `Paddle.Customers.PortalSessions.create/3`</name>
  <files>lib/paddle/customers/portal_sessions.ex, test/paddle/customers/portal_sessions_test.exs</files>
  <behavior>
    - Calls `POST /customers/:customer_id/portal-sessions`
    - Sends `attrs` as JSON body
    - Returns `{:ok, %Paddle.PortalSession{}}` on success
    - Fails if `customer_id` is blank (using standard validation)
    - Forwards API validation errors without deep local validation (D-03)
  </behavior>
  <action>
    Create `lib/paddle/customers/portal_sessions.ex` with `create(client, customer_id, attrs \\ %{}, opts \\ [])` per D-01, D-02, D-05.
    Use `Paddle.Http.request/4`.
    Pass the attrs payload containing `subscription_ids` without manual shape validation (letting the API reject bad inputs).
    Create extensive integration tests to mock the Paddle API response for `POST /customers/:customer_id/portal-sessions`.
  </action>
  <verify>
    <automated>mix test test/paddle/customers/portal_sessions_test.exs</automated>
  </verify>
  <done>`create/3` successfully triggers a POST request and correctly parses the returned payload.</done>
</task>

</tasks>

<verification>
mix test test/paddle/portal_session_test.exs test/paddle/customers/portal_sessions_test.exs
</verification>

<success_criteria>
- Users can create a portal session via `Paddle.Customers.PortalSessions.create/3`
- The `Paddle.PortalSession` struct holds a `urls` map
- Only `create/3` is defined for `PortalSessions`
- No deep validation exists for `subscription_ids`
</success_criteria>

<output>
Create `.planning/phases/14-customer-portal-sessions/14-01-SUMMARY.md` when done
</output>
