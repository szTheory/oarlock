---
phase: "01"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - mix.exs
  - lib/paddle.ex
  - lib/paddle/error.ex
  - lib/paddle/page.ex
autonomous: true
requirements:
  - CORE-03
  - CORE-05
must_haves:
  truths:
    - "Elixir Mix project is successfully bootstrapped with dependencies."
    - "Paddle.Error acts as a native Elixir exception and formats responses."
    - "Paddle.Page encapsulates list data and metadata for pagination."
  artifacts:
    - path: mix.exs
      provides: "Package and dependency configuration"
      min_lines: 20
    - path: lib/paddle/error.ex
      provides: "Exception struct and API error decoding"
      contains: "defexception"
    - path: lib/paddle/page.ex
      provides: "Pagination struct"
      contains: "defstruct"
  key_links: []
---

<objective>
Bootstrap the Elixir library application and set up the foundational model structs for API errors and pagination pages.

Purpose: Creates the foundational types (`Paddle.Error` and `Paddle.Page`) that the rest of the HTTP layer will use, and initializes the Mix project.
Output: Valid `mix.exs`, `lib/paddle/error.ex`, and `lib/paddle/page.ex`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-transport-client-setup/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mix Project Initialization</name>
  <files>mix.exs, lib/paddle.ex</files>
  <read_first>mix.exs</read_first>
  <action>
    Initialize the standard Elixir project skeleton if it doesn't exist by running `mix new . --app paddle --module Paddle --sup --force`.
    Then, modify `mix.exs` to add `{:req, "~> 0.5.17"}` and `{:telemetry, "~> 1.4"}` to the `deps/0` list.
    Ensure `application/0` includes `extra_applications: [:logger]`.
    Run `mix deps.get` to install dependencies.
  </action>
  <acceptance_criteria>
    - `mix.exs` contains `{:req, "~> 0.5.17"}`
    - `mix.exs` contains `{:telemetry, "~> 1.4"}`
    - `mix deps.get` returns exit code 0
  </acceptance_criteria>
  <verify>
    <automated>mix deps.get &amp;&amp; mix compile</automated>
  </verify>
  <done>Dependencies are installed and the basic project compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Paddle.Error</name>
  <files>lib/paddle/error.ex, test/paddle/error_test.exs</files>
  <read_first>lib/paddle/error.ex, .planning/phases/01-core-transport-client-setup/01-RESEARCH.md</read_first>
  <behavior>
    - Test 1: Exception.message/1 returns the `message` field.
    - Test 2: from_response/1 safely extracts status, request_id, type, code, detail, and errors from a Req.Response.
  </behavior>
  <action>
    Create `lib/paddle/error.ex`.
    Define the `Paddle.Error` module with `defexception [:type, :code, :message, :errors, :request_id, :status_code, :raw]`.
    Implement `Exception.message/1`:
    ```elixir
    @impl Exception
    def message(%{message: message}), do: message
    ```
    Implement `from_response(%Req.Response{status: status, body: body} = resp)`:
    Fallback `body` to `%{}`, extract `error_body = Map.get(body, "error", %{})`.
    Map `status_code` to `status`.
    Map `request_id` to `Req.Response.get_header(resp, "x-request-id") |> List.first()`.
    Map `type` to `error_body["type"]`.
    Map `code` to `error_body["code"]`.
    Map `message` to `Map.get(error_body, "detail", "Unknown Paddle Error")`.
    Map `errors` to `Map.get(error_body, "errors", [])`.
    Map `raw` to `body`.
  </action>
  <acceptance_criteria>
    - `lib/paddle/error.ex` contains `defexception [:type, :code, :message, :errors, :request_id, :status_code, :raw]`
    - `lib/paddle/error.ex` contains `def message(%{message: message}), do: message`
    - `lib/paddle/error.ex` contains `def from_response(%Req.Response`
    - `test/paddle/error_test.exs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>mix test test/paddle/error_test.exs</automated>
  </verify>
  <done>Error struct behaves as an Exception and correctly maps API error formats.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement Paddle.Page</name>
  <files>lib/paddle/page.ex, test/paddle/page_test.exs</files>
  <read_first>lib/paddle/page.ex</read_first>
  <behavior>
    - Test 1: Struct holds data list and meta map.
    - Test 2: next_cursor/1 correctly extracts pagination URL from meta.
  </behavior>
  <action>
    Create `lib/paddle/page.ex`.
    Define `defstruct [:data, :meta]`.
    Implement `next_cursor/1` helper:
    ```elixir
    def next_cursor(%__MODULE__{meta: %{"pagination" => %{"next" => next}}}) when is_binary(next) do
      next
    end

    def next_cursor(_), do: nil
    ```
  </action>
  <acceptance_criteria>
    - `lib/paddle/page.ex` contains `defstruct [:data, :meta]`
    - `lib/paddle/page.ex` contains `def next_cursor`
    - `test/paddle/page_test.exs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>mix test test/paddle/page_test.exs</automated>
  </verify>
  <done>Page struct encapsulates paginated payload and cursor extraction logic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API Responses | External data from Paddle API must be defensively parsed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Denial of Service | `Paddle.Error.from_response/1` | mitigate | Ensure fallback to `%{\}` when JSON body is empty or non-map to prevent function clause crashes. |
</threat_model>

<verification>
Ensure tests pass and Mix project structure is correct without any dangling or unused deps.
</verification>

<success_criteria>
`mix test` passes with zero warnings, and `mix format` shows standard Elixir style.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-transport-client-setup/01-01-SUMMARY.md`
</output>
