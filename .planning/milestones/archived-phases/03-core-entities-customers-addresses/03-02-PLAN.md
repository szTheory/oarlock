---
phase: 03-core-entities-customers-addresses
plan: "02"
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - lib/paddle/address.ex
  - lib/paddle/customers/addresses.ex
  - test/paddle/address_test.exs
  - test/paddle/customers/addresses_test.exs
autonomous: true
requirements:
  - ADDR-01
must_haves:
  truths:
    - "A caller can create, fetch, and update a customer-owned Paddle address through `Paddle.Customers.Addresses` with explicit client and customer arguments."
    - "Address entity responses are returned as `%Paddle.Address{}` structs with promoted top-level fields and preserved `raw_data`."
    - "Address writes remain customer-scoped in the public signature and keep PATCH nil-clear semantics for nullable fields."
    - "The address CRUD boundary preserves the same API-error and transport exception tuple behavior already established by `Paddle.Http.request/4`."
  artifacts:
    - path: "lib/paddle/address.ex"
      provides: "Paddle.Address struct with declared address fields plus raw_data"
    - path: "lib/paddle/customers/addresses.ex"
      provides: "Paddle.Customers.Addresses.create/3, get/3, update/4 with scoped paths and local `data` unwrapping"
    - path: "test/paddle/address_test.exs"
      provides: "Struct contract coverage for Paddle.Address"
    - path: "test/paddle/customers/addresses_test.exs"
      provides: "Request/response coverage for scoped address CRUD, allowlisted bodies, PATCH nil-clear semantics, and transport passthrough"
  key_links:
    - from: "lib/paddle/customers/addresses.ex"
      to: "lib/paddle/address.ex"
      via: "Paddle.Http.build_struct(Paddle.Address, data)"
      pattern: "Paddle\\.Http\\.build_struct\\(Paddle\\.Address"
    - from: "test/paddle/customers/addresses_test.exs"
      to: "lib/paddle/customers/addresses.ex"
      via: "Req adapter assertions on customer-scoped paths and JSON payloads"
      pattern: "Paddle\\.Customers\\.Addresses\\.(create|get|update)"
---

<objective>
Implement the address entity contract plus customer-scoped address create/get/update operations.

Purpose: establish the nested resource pattern and customer ownership semantics that later transaction flows will rely on for billing addresses.
Output: `%Paddle.Address{}`, `Paddle.Customers.Addresses.create/3`, `get/3`, `update/4`, and focused ExUnit coverage.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-core-entities-customers-addresses/03-CONTEXT.md
@.planning/phases/03-core-entities-customers-addresses/03-RESEARCH.md
@.planning/phases/03-core-entities-customers-addresses/03-PATTERNS.md
@.planning/phases/03-core-entities-customers-addresses/03-01-PLAN.md
@lib/paddle/client.ex
@lib/paddle/http.ex
@lib/paddle/error.ex
@lib/paddle/customer.ex
@lib/paddle/customers.ex
@test/paddle/http_test.exs
@test/paddle/customer_test.exs

<interfaces>
From lib/paddle/http.ex:
```elixir
def request(%Paddle.Client{} = client, method, path, opts \\ []) do
  ...
end

def build_struct(struct_module, data) when is_map(data) do
  ...
end
```

Target contract for this plan:
```elixir
defmodule Paddle.Address do
  defstruct [
    :id,
    :customer_id,
    :description,
    :first_line,
    :second_line,
    :city,
    :postal_code,
    :region,
    :country_code,
    :custom_data,
    :status,
    :created_at,
    :updated_at,
    :import_meta,
    :raw_data
  ]
end

defmodule Paddle.Customers.Addresses do
  def create(%Paddle.Client{} = client, customer_id, attrs)
  def get(%Paddle.Client{} = client, customer_id, address_id)
  def update(%Paddle.Client{} = client, customer_id, address_id, attrs)
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the `%Paddle.Address{}` contract and lock its field surface</name>
  <read_first>
    lib/paddle/event.ex
    lib/paddle/http.ex
    test/paddle/event_test.exs
    .planning/phases/03-core-entities-customers-addresses/03-CONTEXT.md
    .planning/phases/03-core-entities-customers-addresses/03-RESEARCH.md
    .planning/phases/03-core-entities-customers-addresses/03-PATTERNS.md
  </read_first>
  <files>lib/paddle/address.ex, test/paddle/address_test.exs</files>
  <behavior>
    - Test 1: `%Paddle.Address{}` exposes the exact promoted fields chosen in research plus `raw_data`.
    - Test 2: `Paddle.Http.build_struct/2` can hydrate the declared address keys while unmapped keys still survive in `raw_data`.
  </behavior>
  <action>Create `lib/paddle/address.ex` as a minimal struct-only module per D-05, D-06, D-07, D-08, and D-12 using the researched field list: `id`, `customer_id`, `description`, `first_line`, `second_line`, `city`, `postal_code`, `region`, `country_code`, `custom_data`, `status`, `created_at`, `updated_at`, `import_meta`, `raw_data`. Add `test/paddle/address_test.exs` to freeze the public field surface and raw-data behavior before the nested resource module depends on it.</action>
  <verify>
    <automated>mix test test/paddle/address_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `lib/paddle/address.ex` defines only the address struct contract and does not contain HTTP logic.
    - `test/paddle/address_test.exs` asserts the full `%Paddle.Address{}` field list including `raw_data`.
    - The tests demonstrate `build_struct/2` promotes only declared fields while preserving the full response payload in `raw_data`.
  </acceptance_criteria>
  <done>`%Paddle.Address{}` exists as the typed address entity contract required by ADDR-01.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement scoped address create/get/update with separate allowlists</name>
  <read_first>
    lib/paddle/customers/addresses.ex
    lib/paddle/address.ex
    lib/paddle/http.ex
    lib/paddle/error.ex
    test/paddle/http_test.exs
    .planning/phases/03-core-entities-customers-addresses/03-CONTEXT.md
    .planning/phases/03-core-entities-customers-addresses/03-RESEARCH.md
    .planning/phases/03-core-entities-customers-addresses/03-PATTERNS.md
  </read_first>
  <files>lib/paddle/customers/addresses.ex, test/paddle/customers/addresses_test.exs</files>
  <behavior>
    - Test 1: `create/3` posts to `/customers/{customer_id}/addresses` with only the researched create allowlist and returns `{:ok, %Paddle.Address{}}`.
    - Test 2: `get/3` requests `/customers/{customer_id}/addresses/{address_id}` and returns a typed address struct.
    - Test 3: `update/4` patches only the researched update allowlist while keeping explicit `nil` keys so nullable address fields can be cleared per D-16.
    - Test 4: invalid API responses remain `{:error, %Paddle.Error{}}` through the unchanged HTTP boundary.
    - Test 5: transport exceptions still pass through unchanged to the public address functions.
    - Test 6: blank customer ids return exactly `{:error, :invalid_customer_id}`, blank address ids return exactly `{:error, :invalid_address_id}`, and non-map/non-keyword attrs return exactly `{:error, :invalid_attrs}`.
  </behavior>
  <action>Implement `lib/paddle/customers/addresses.ex` as the nested customer-owned address resource module per D-03, D-04, D-10, D-12, D-13, D-14, D-15, and D-16. Keep `%Paddle.Client{}` first and `customer_id` explicit in every public function, perform only lightweight path-argument and attrs-container validation, and make that local contract explicit: return `{:error, :invalid_customer_id}` for nil/empty customer ids, `{:error, :invalid_address_id}` for nil/empty address ids, and `{:error, :invalid_attrs}` for non-map/non-keyword attrs. Unwrap Paddle's `"data"` envelope locally, and map entities with `Paddle.Http.build_struct/2`. Use separate create and update body allowlists so `import_meta` never goes out and create-only/update-only fields stay aligned with research, while keeping explicit `nil` values in PATCH bodies. Add Req-adapter-backed coverage in `test/paddle/customers/addresses_test.exs` for method/path correctness, typed success mapping, customer-scoped ownership, exact local validation tuples, non-2xx API error alignment, and unchanged transport exception passthrough.</action>
  <verify>
    <automated>mix test test/paddle/address_test.exs test/paddle/customers/addresses_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `lib/paddle/customers/addresses.ex` exports `create/3`, `get/3`, and `update/4` only at this stage.
    - Public signatures keep `customer_id` explicit rather than hiding ownership in attrs.
    - Address create/update payloads are allowlisted separately and preserve explicit `nil` keys for PATCH clears.
    - Blank customer/address ids return `{:error, :invalid_customer_id}` or `{:error, :invalid_address_id}` as appropriate, and non-map/non-keyword attrs return `{:error, :invalid_attrs}`.
    - Success paths return `{:ok, %Paddle.Address{}}`; API and transport failures stay aligned with the existing tuple conventions.
    - `mix test test/paddle/address_test.exs test/paddle/customers/addresses_test.exs` exits successfully.
  </acceptance_criteria>
  <done>Customer-scoped address CRUD is available behind the locked nested public API and satisfies the entity half of ADDR-01.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller attrs + path ids -> `Paddle.Customers.Addresses` | Untrusted caller input must be scoped to a customer path and reduced to the researched writable fields before API dispatch. |
| Paddle API response -> `%Paddle.Address{}` | Only known address fields should be promoted while unknown keys remain available in `raw_data`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | T | `Paddle.Customers.Addresses.create/3` and `update/4` | mitigate | Apply explicit per-endpoint allowlists so unsupported or read-only fields never reach Paddle, and keep nil values only for known writable keys. |
| T-03-06 | S | customer-scoped address paths | mitigate | Require explicit `customer_id` and `address_id` path arguments per D-10/D-12 so ownership stays visible and cannot be inferred from body data. |
| T-03-07 | I | `%Paddle.Address{}` mapping | accept | Preserve full response payloads in `raw_data` intentionally while only promoting declared top-level fields through `build_struct/2`. |
| T-03-08 | R | nested resource tuple boundary | mitigate | Lock scoped path construction, typed success mapping, API-error tuples, and transport passthrough in adapter-backed tests. |
</threat_model>

<verification>
- `mix test test/paddle/address_test.exs test/paddle/customers/addresses_test.exs`
- `grep -n 'def create(%Paddle.Client{} = client, customer_id, attrs)' lib/paddle/customers/addresses.ex`
- `grep -n 'def update(%Paddle.Client{} = client, customer_id, address_id, attrs)' lib/paddle/customers/addresses.ex`
</verification>

<success_criteria>
- `Paddle.Customers.Addresses.create/3`, `get/3`, and `update/4` exist with explicit `%Paddle.Client{}` and customer ownership in the public signature.
- `%Paddle.Address{}` preserves `raw_data` while promoting the declared address fields.
- Address CRUD request shaping is allowlisted, PATCH updates preserve explicit `nil`, and tuple behavior remains aligned with the transport boundary.
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-entities-customers-addresses/03-02-SUMMARY.md`
</output>
