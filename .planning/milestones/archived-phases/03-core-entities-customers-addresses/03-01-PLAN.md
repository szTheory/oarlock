---
phase: 03-core-entities-customers-addresses
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/paddle/customer.ex
  - lib/paddle/customers.ex
  - test/paddle/customer_test.exs
  - test/paddle/customers_test.exs
autonomous: true
requirements:
  - CUST-01
must_haves:
  truths:
    - "A caller can create, fetch, and update a Paddle customer through `Paddle.Customers` with explicit `%Paddle.Client{}` passing."
    - "Customer responses are returned as `%Paddle.Customer{}` structs with promoted top-level fields and preserved `raw_data`."
    - "Customer writes keep Paddle PATCH semantics so omitted keys stay untouched and explicit `nil` values can clear nullable fields."
    - "Transport exceptions still pass through unchanged while non-2xx API responses keep the existing `%Paddle.Error{}` tuple behavior."
  artifacts:
    - path: "lib/paddle/customer.ex"
      provides: "Paddle.Customer struct with declared customer fields plus raw_data"
    - path: "lib/paddle/customers.ex"
      provides: "Paddle.Customers.create/2, get/2, update/3 with allowlisted request shaping and local `data` unwrapping"
    - path: "test/paddle/customer_test.exs"
      provides: "Struct contract coverage for Paddle.Customer"
    - path: "test/paddle/customers_test.exs"
      provides: "Request/response coverage for customer CRUD, PATCH nil-clear semantics, and transport passthrough"
  key_links:
    - from: "lib/paddle/customers.ex"
      to: "lib/paddle/http.ex"
      via: "Paddle.Http.request/4 plus `Paddle.Http.build_struct/2` over the response `data` object"
      pattern: "Paddle\\.Http\\.(request|build_struct)"
    - from: "test/paddle/customers_test.exs"
      to: "lib/paddle/customers.ex"
      via: "Req adapter assertions on method, path, and JSON body allowlists"
      pattern: "Paddle\\.Customers\\.(create|get|update)"
---

<objective>
Implement the customer entity contract and customer CRUD resource functions required by Phase 3.

Purpose: establish the first public resource-module slice that later billing flows can rely on without changing the explicit client or tuple conventions.
Output: `%Paddle.Customer{}`, `Paddle.Customers.create/2`, `get/2`, `update/3`, and focused ExUnit coverage.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-core-entities-customers-addresses/03-CONTEXT.md
@.planning/phases/03-core-entities-customers-addresses/03-RESEARCH.md
@.planning/phases/03-core-entities-customers-addresses/03-PATTERNS.md
@.planning/phases/01-core-transport-client-setup/01-CONTEXT.md
@.planning/phases/02-webhook-verification/02-01-PLAN.md
@.planning/phases/02-webhook-verification/02-02-PLAN.md
@lib/paddle/client.ex
@lib/paddle/http.ex
@lib/paddle/error.ex
@test/paddle/http_test.exs
@test/paddle/event_test.exs

<interfaces>
From lib/paddle/client.ex:
```elixir
defmodule Paddle.Client do
  @enforce_keys [:api_key, :environment]
  defstruct [:api_key, :environment, :req]
end
```

From lib/paddle/http.ex:
```elixir
def request(%Paddle.Client{} = client, method, path, opts \\ []) do
  ...
end

def build_struct(struct_module, data) when is_map(data) do
  ...
  struct(struct_module, Map.put(attrs, :raw_data, data))
end
```

Target contract for this plan:
```elixir
defmodule Paddle.Customer do
  defstruct [
    :id,
    :name,
    :email,
    :marketing_consent,
    :status,
    :custom_data,
    :locale,
    :created_at,
    :updated_at,
    :import_meta,
    :raw_data
  ]
end

defmodule Paddle.Customers do
  def create(%Paddle.Client{} = client, attrs)
  def get(%Paddle.Client{} = client, customer_id)
  def update(%Paddle.Client{} = client, customer_id, attrs)
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the `%Paddle.Customer{}` contract and lock its field surface</name>
  <read_first>
    lib/paddle/event.ex
    lib/paddle/http.ex
    test/paddle/event_test.exs
    .planning/phases/03-core-entities-customers-addresses/03-CONTEXT.md
    .planning/phases/03-core-entities-customers-addresses/03-RESEARCH.md
    .planning/phases/03-core-entities-customers-addresses/03-PATTERNS.md
  </read_first>
  <files>lib/paddle/customer.ex, test/paddle/customer_test.exs</files>
  <behavior>
    - Test 1: `%Paddle.Customer{}` exposes the exact promoted fields chosen in research plus `raw_data`.
    - Test 2: `Paddle.Http.build_struct/2` can hydrate the declared customer keys while unmapped keys still survive in `raw_data`.
  </behavior>
  <action>Create `lib/paddle/customer.ex` as a minimal struct-only module per D-05, D-06, D-07, and D-08 using the first-pass field list from research: `id`, `name`, `email`, `marketing_consent`, `status`, `custom_data`, `locale`, `created_at`, `updated_at`, `import_meta`, `raw_data`. Add `test/paddle/customer_test.exs` mirroring the existing entity-struct test style so the declared field surface is fixed before the resource module depends on it, and assert that `raw_data` remains the forward-compatible escape hatch rather than adding alternate raw payload fields.</action>
  <verify>
    <automated>mix test test/paddle/customer_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `lib/paddle/customer.ex` defines only the customer struct contract and does not contain HTTP logic.
    - `test/paddle/customer_test.exs` asserts the full `%Paddle.Customer{}` field list including `raw_data`.
    - The test coverage demonstrates `Paddle.Http.build_struct/2` only promotes declared fields while preserving the full payload in `raw_data`.
  </acceptance_criteria>
  <done>`%Paddle.Customer{}` exists as the Phase 3 customer entity contract required by CUST-01.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement `Paddle.Customers.create/2`, `get/2`, and `update/3` with allowlisted request shaping</name>
  <read_first>
    lib/paddle/customers.ex
    lib/paddle/customer.ex
    lib/paddle/http.ex
    lib/paddle/error.ex
    test/paddle/http_test.exs
    .planning/phases/03-core-entities-customers-addresses/03-CONTEXT.md
    .planning/phases/03-core-entities-customers-addresses/03-RESEARCH.md
    .planning/phases/03-core-entities-customers-addresses/03-PATTERNS.md
  </read_first>
  <files>lib/paddle/customers.ex, test/paddle/customers_test.exs</files>
  <behavior>
    - Test 1: `create/2` posts only the allowlisted create keys and returns `{:ok, %Paddle.Customer{}}` built from the local `"data"` object.
    - Test 2: `get/2` requests `/customers/{customer_id}` with explicit `%Paddle.Client{}` passing and returns a typed customer struct.
    - Test 3: `update/3` patches only the allowlisted update keys while retaining explicit `nil` values for nullable-field clears per D-16.
    - Test 4: non-2xx API responses stay aligned with existing `{:error, %Paddle.Error{}}` handling because `Paddle.Http.request/4` remains the boundary.
    - Test 5: transport exceptions still surface unchanged as `{:error, %Req.TransportError{...}}`.
    - Test 6: blank customer ids return exactly `{:error, :invalid_customer_id}` and non-map/non-keyword attrs return exactly `{:error, :invalid_attrs}`.
  </behavior>
  <action>Implement `lib/paddle/customers.ex` as the public customer resource module per D-01, D-02, D-13, D-14, D-15, and D-16. Keep `%Paddle.Client{}` as the first argument on every public function, unwrap Paddle's `"data"` envelope locally instead of changing `Paddle.Http.request/4`, and map entities through `Paddle.Http.build_struct/2`. Normalize `map | keyword` attrs at the public edge, enforce only lightweight boundary checks for non-empty customer IDs and attrs container shape, and make that local contract explicit: return `{:error, :invalid_customer_id}` for nil/empty customer ids and `{:error, :invalid_attrs}` for non-map/non-keyword attrs. Use separate allowlists for create vs update so read-only fields like `marketing_consent` and `import_meta` never go out on writes, and preserve explicit `nil` values for PATCH bodies. Cover the module with `test/paddle/customers_test.exs` using Req adapter stubs that assert method, path, payload/query shape, typed success mapping, API-error alignment, transport exception passthrough, and the exact local validation tuples.</action>
  <verify>
    <automated>mix test test/paddle/customer_test.exs test/paddle/customers_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `lib/paddle/customers.ex` exports `create/2`, `get/2`, and `update/3` only.
    - Customer create/update request bodies are allowlisted separately and do not send `marketing_consent` or `import_meta`.
    - Customer update keeps explicit `nil` keys in the JSON body for supported clears.
    - Blank customer ids return `{:error, :invalid_customer_id}` and non-map/non-keyword attrs return `{:error, :invalid_attrs}`.
    - Success paths return `{:ok, %Paddle.Customer{}}`; non-2xx API responses and transport failures keep the existing tuple conventions unchanged.
    - `mix test test/paddle/customer_test.exs test/paddle/customers_test.exs` exits successfully.
  </acceptance_criteria>
  <done>Customer CRUD is available behind the locked `Paddle.Customers` API and satisfies CUST-01.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller attrs -> `Paddle.Customers` | Untrusted caller input must be normalized, allowlisted, and validated only for stable boundary rules before reaching the API. |
| Paddle API response -> `%Paddle.Customer{}` | Only known customer fields should be promoted while unknown keys remain preserved in `raw_data`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | T | `Paddle.Customers.create/2` and `update/3` | mitigate | Use per-endpoint allowlists so unsupported or read-only attrs are dropped before request serialization, per D-14 and research guidance. |
| T-03-02 | I | `%Paddle.Customer{}` mapping | accept | Preserve full entity payloads in `raw_data` intentionally for forward compatibility while only promoting declared fields through `build_struct/2`. |
| T-03-03 | D | customer path arguments | mitigate | Reject empty or invalid customer IDs locally before HTTP dispatch per D-15 to avoid meaningless remote calls. |
| T-03-04 | R | customer tuple boundary | mitigate | Lock success, API-error, and transport-exception behavior in ExUnit so later phases cannot silently change the public contract. |
</threat_model>

<verification>
- `mix test test/paddle/customer_test.exs test/paddle/customers_test.exs`
- `grep -n 'def create(%Paddle.Client{} = client, attrs)' lib/paddle/customers.ex`
- `grep -n 'def update(%Paddle.Client{} = client, customer_id, attrs)' lib/paddle/customers.ex`
</verification>

<success_criteria>
- `Paddle.Customers.create/2`, `get/2`, and `update/3` exist and accept explicit `%Paddle.Client{}` as the public boundary.
- `%Paddle.Customer{}` preserves `raw_data` while promoting the declared customer fields.
- Customer request shaping is allowlisted, PATCH updates preserve explicit `nil`, and transport exception passthrough stays aligned with existing code.
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-entities-customers-addresses/03-01-SUMMARY.md`
</output>
