---
phase: 03-core-entities-customers-addresses
plan: "03"
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - lib/paddle/customers/addresses.ex
  - test/paddle/customers/addresses_test.exs
autonomous: true
requirements:
  - ADDR-01
must_haves:
  truths:
    - "A caller can list customer-owned addresses and receive `{:ok, %Paddle.Page{data: [%Paddle.Address{}, ...], meta: meta}}`."
    - "Address list responses preserve Paddle pagination metadata exactly so `%Paddle.Page.next_cursor/1` continues to work."
    - "Address listing only forwards allowlisted query params while keeping the explicit `%Paddle.Client{}` and `customer_id` public boundary."
    - "Address list transport failures still pass through unchanged and the list boundary performs its own local `data`/`meta` unwrapping."
  artifacts:
    - path: "lib/paddle/customers/addresses.ex"
      provides: "Paddle.Customers.Addresses.list/3 plus local list-to-page mapping and query allowlist shaping"
    - path: "test/paddle/customers/addresses_test.exs"
      provides: "Coverage for page mapping, preserved meta, query allowlists, archived-status fetches, and transport passthrough"
  key_links:
    - from: "lib/paddle/customers/addresses.ex"
      to: "lib/paddle/page.ex"
      via: "%Paddle.Page{data: mapped_addresses, meta: meta}"
      pattern: "%Paddle\\.Page\\{"
    - from: "test/paddle/customers/addresses_test.exs"
      to: "lib/paddle/customers/addresses.ex"
      via: "adapter assertions for query params and page-shaped success responses"
      pattern: "Paddle\\.Customers\\.Addresses\\.list"
---

<objective>
Implement customer-scoped address listing and the `%Paddle.Page{}` mapping boundary for Phase 3.

Purpose: finish the address resource surface with the paginated list behavior that later transaction and subscription flows can consume consistently.
Output: `Paddle.Customers.Addresses.list/3` with local envelope unwrapping, allowlisted query shaping, page mapping, and ExUnit coverage.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-core-entities-customers-addresses/03-CONTEXT.md
@.planning/phases/03-core-entities-customers-addresses/03-RESEARCH.md
@.planning/phases/03-core-entities-customers-addresses/03-PATTERNS.md
@.planning/phases/03-core-entities-customers-addresses/03-02-PLAN.md
@lib/paddle/http.ex
@lib/paddle/page.ex
@lib/paddle/address.ex
@lib/paddle/customers/addresses.ex
@test/paddle/http_test.exs
@test/paddle/page_test.exs
@test/paddle/customers/addresses_test.exs

<interfaces>
From lib/paddle/page.ex:
```elixir
defmodule Paddle.Page do
  defstruct [:data, :meta]

  def next_cursor(%__MODULE__{meta: %{"pagination" => %{"next" => next}}}) when is_binary(next) do
    next
  end
end
```

From lib/paddle/address.ex:
```elixir
defmodule Paddle.Address do
  defstruct [...]
end
```

Target contract for this plan:
```elixir
defmodule Paddle.Customers.Addresses do
  def list(%Paddle.Client{} = client, customer_id, params \\ [])
end

{:ok, %Paddle.Page{data: [%Paddle.Address{}, ...], meta: meta}}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add executable tests for paginated address listing and query shaping</name>
  <read_first>
    test/paddle/customers/addresses_test.exs
    test/paddle/page_test.exs
    lib/paddle/page.ex
    lib/paddle/customers/addresses.ex
    .planning/phases/03-core-entities-customers-addresses/03-RESEARCH.md
    .planning/phases/03-core-entities-customers-addresses/03-PATTERNS.md
  </read_first>
  <files>test/paddle/customers/addresses_test.exs</files>
  <behavior>
    - Test 1: `list/3` returns `{:ok, %Paddle.Page{data: [%Paddle.Address{}, ...], meta: meta}}` with each address mapped through the typed struct contract.
    - Test 2: the preserved `meta` keeps `Paddle.Page.next_cursor/1` functional on list results.
    - Test 3: only the researched query allowlist (`id`, `after`, `per_page`, `order_by`, `status`, `search`) is forwarded to the adapter.
    - Test 4: passing `status: "archived"` remains possible so archived-address retrieval is not blocked by local filtering.
    - Test 5: transport exceptions still surface unchanged as `{:error, %Req.TransportError{...}}`.
    - Test 6: blank customer ids return exactly `{:error, :invalid_customer_id}` and non-map/non-keyword params containers return exactly `{:error, :invalid_params}`.
  </behavior>
  <action>Extend `test/paddle/customers/addresses_test.exs` with adapter-backed list coverage that pins the Phase 3 list contract from D-11: the public return shape must be `{:ok, %Paddle.Page{data: [%Paddle.Address{}, ...], meta: meta}}`. Assert that `meta` is preserved exactly, that `Paddle.Page.next_cursor/1` works against the returned page, that only allowlisted query params are sent, that `status: "archived"` can be forwarded per research, that blank customer ids return `{:error, :invalid_customer_id}`, that non-map/non-keyword params return `{:error, :invalid_params}`, and that transport exceptions remain unchanged at the public boundary.</action>
  <verify>
    <automated>mix test test/paddle/customers/addresses_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `test/paddle/customers/addresses_test.exs` contains a list-specific assertion on `%Paddle.Page{data: [%Paddle.Address{} | _], meta: meta}`.
    - The tests assert query allowlisting and preserved `meta` rather than only entity mapping.
    - The tests cover an archived-status query case and a transport exception passthrough case.
  </acceptance_criteria>
  <done>The address list contract is specified in executable tests before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement `Paddle.Customers.Addresses.list/3` with local page mapping</name>
  <read_first>
    lib/paddle/customers/addresses.ex
    lib/paddle/http.ex
    lib/paddle/page.ex
    lib/paddle/address.ex
    test/paddle/customers/addresses_test.exs
    .planning/phases/03-core-entities-customers-addresses/03-CONTEXT.md
    .planning/phases/03-core-entities-customers-addresses/03-RESEARCH.md
    .planning/phases/03-core-entities-customers-addresses/03-PATTERNS.md
  </read_first>
  <files>lib/paddle/customers/addresses.ex</files>
  <behavior>
    - The function accepts `%Paddle.Client{}`, `customer_id`, and `params \\ []`.
    - The function performs only lightweight container/path validation, forwards only allowlisted query params, and does not invent a new generic page helper.
    - The function unwraps `%{"data" => data, "meta" => meta}` locally and returns `{:ok, %Paddle.Page{data: mapped_addresses, meta: meta}}`.
    - The function maps each entity through `Paddle.Http.build_struct(Paddle.Address, item)` and preserves transport exception passthrough unchanged.
  </behavior>
  <action>Extend `lib/paddle/customers/addresses.ex` with `list/3` per D-03, D-10, D-11, D-12, D-13, D-14, and D-15. Keep the existing nested resource module as the only public address API, validate only non-empty `customer_id` and `map | keyword` params container shape, and make that local contract explicit: return `{:error, :invalid_customer_id}` for nil/empty customer ids and `{:error, :invalid_params}` for non-map/non-keyword params containers. Forward only the researched query allowlist, and unwrap the Paddle `"data"`/`"meta"` envelope inside the resource module rather than changing `Paddle.Http.request/4` or adding a generic page-mapping helper in `Paddle.Http`. Build the result as `%Paddle.Page{data: Enum.map(data, &Paddle.Http.build_struct(Paddle.Address, &1)), meta: meta}` so page metadata stays intact for `next_cursor/1`, and preserve the existing API-error and transport-exception tuple behavior unchanged.</action>
  <verify>
    <automated>mix test test/paddle/customers/addresses_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `lib/paddle/customers/addresses.ex` exports `list/3` in addition to the CRUD functions from Plan 03-02.
    - The list implementation constructs `%Paddle.Page{}` locally and does not move list mapping into a new generic transport helper.
    - Query shaping forwards only `id`, `after`, `per_page`, `order_by`, `status`, and `search`.
    - Blank customer ids return `{:error, :invalid_customer_id}` and non-map/non-keyword params return `{:error, :invalid_params}`.
    - Success returns `{:ok, %Paddle.Page{data: [%Paddle.Address{}, ...], meta: meta}}` with preserved metadata and unchanged transport passthrough behavior.
    - `mix test test/paddle/customers/addresses_test.exs` exits successfully.
  </acceptance_criteria>
  <done>Address listing completes the locked Phase 3 public API and satisfies the paginated half of ADDR-01.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller query params -> `Paddle.Customers.Addresses.list/3` | Untrusted query input must be reduced to the supported filter/pagination keys before API dispatch. |
| Paddle list response -> `%Paddle.Page{}` | The SDK must unwrap `data` and `meta` correctly so typed entities and pagination metadata remain aligned. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | T | `Paddle.Customers.Addresses.list/3` query shaping | mitigate | Forward only the researched allowlisted query keys and reject invalid params-container shapes before making the request. |
| T-03-10 | I | `%Paddle.Page{}` metadata preservation | mitigate | Preserve Paddle `meta` unchanged so pagination semantics stay accurate and `next_cursor/1` works on real list responses. |
| T-03-11 | D | list response mapping | mitigate | Unwrap `data` and `meta` locally in the resource module and fail through the existing tuple boundary rather than introducing hidden transport transformations. |
| T-03-12 | R | list contract stability | mitigate | Lock the exact `%Paddle.Page{data: [%Paddle.Address{}, ...], meta: meta}` response shape and transport passthrough behavior in ExUnit. |
</threat_model>

<verification>
- `mix test test/paddle/customers/addresses_test.exs`
- `grep -n 'def list(%Paddle.Client{} = client, customer_id, params \\\\ \\[\\])' lib/paddle/customers/addresses.ex`
- `grep -n '%Paddle.Page{' lib/paddle/customers/addresses.ex`
</verification>

<success_criteria>
- `Paddle.Customers.Addresses.list/3` returns `{:ok, %Paddle.Page{data: [%Paddle.Address{}, ...], meta: meta}}`.
- The returned page preserves `meta` exactly so `Paddle.Page.next_cursor/1` keeps working.
- Query shaping is allowlisted, envelope unwrapping stays local to the resource module, and transport exception passthrough remains aligned with existing code.
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-entities-customers-addresses/03-03-SUMMARY.md`
</output>
